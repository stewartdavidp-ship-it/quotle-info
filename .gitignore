# Local Claude Code config (dev-only; never deployed)
.claude/launch.json
.claude/settings.local.json

# OS / editor cruft
.DS_Store

# harvest transient batch files (regenerated by tools/harvest.js batch)
data/.harvest-batch*.json
