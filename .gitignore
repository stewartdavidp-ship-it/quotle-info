# Local Claude Code config (dev-only; never deployed)
.claude/launch.json
.claude/settings.local.json

# OS / editor cruft
.DS_Store

# harvest transient batch files (regenerated by tools/harvest.js batch)
data/.harvest-batch*.json

# Phase 2 backend wiring (public values, build-time; provide per the worker/ README)
data/harvest-config.json
