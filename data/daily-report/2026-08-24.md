# Daily report — 2026-08-24

## 1. Verdict

**All five due routines ran and the site is healthy — but nothing on the decision list has been
touched for three days, and today the list itself lost two items without them being closed.**

CI green on `main`, 1,752 records, 0 flagged, working tree clean, no tool failures. The wave audited
its own work, failed 3 of 5 records, and fixed everything it found. The weekly discovery audit passed
all 20 sampled records with no findings. The performance canary is green.

The problems are all in the *bookkeeping around* the work, not the work: the report window is still
losing merges (seventh consecutive wave merge invisible), and the decision list has now grown for a
week while zero commits have landed under `tools/`, `workflows/` or `.github/workflows/` since
2026-08-23.

## 2. What ran

| | slot | routine | result |
|---|---|---|---|
| ✓ | 03:00 | `daily-wave` | 1 run: **pr** · 5 records → [#626](https://github.com/stewartdavidp-ship-it/quotle-info/pull/626) |
| ✓ | 04:00 | `daily-reports` | 1 run: no-op — "public `/reports/pending` returned no pending reports" |
| ✓ | 05:00 | `daily-review` | 1 run: no-op — 1,752 scanned, 0 flagged |
| ✓ | 07:00 | `daily-merge` | 4 runs: merged, merged, merged, merged · 7 PRs |
| · | 12:00 | `reports-close` | not yet due (17:00 UTC) — this report runs too early to judge it |
| ✓ | Mon 02:00 | `weekly-discovery` | 1 run: **pr** · 20 records sampled → [#624](https://github.com/stewartdavidp-ship-it/quotle-info/pull/624) |

**Nothing was missing.** Yesterday's `reports-close` (the slot yesterday's report ran too early to
see) did run, no-op, no pending reader reports.

Not in the census, so checked by hand: **`perf-check` ran today at 11:19 UTC and passed** (run #5,
scheduled, on `e8a54a48`). It has passed on every scheduled run since it was added; its only failure
was the pre-fix dress rehearsal on 2026-08-21. See decision 13.

## 3. What shipped

- **7 PRs merged** as the tool windows them: #621, #622, #623, **#624** (weekly discovery audit),
  #625, #627, #628. Five of the seven are routine-log shards.
- **2 PRs open, both `CLEAN`, both 0 days old** — [#626](https://github.com/stewartdavidp-ship-it/quotle-info/pull/626)
  (the wave, 5 records) and [#629](https://github.com/stewartdavidp-ship-it/quotle-info/pull/629)
  (today's merge-pass shards). Both were rebuilt and pushed by the last merge pass and merge at the
  next one. Nothing is rotting.
- **20 records stamped `PASS`** by the weekly discovery audit, with `scan.js` re-run so content
  hashes match.
- **5 new records built** on the wave branch (Thatcher ×1, Galileo ×2, Goldman ×2) — not yet on
  `main`, so the corpus count below is still pre-wave.

**Health:** CI `success` on `e8a54a486`; **1,752 records**; **0 flagged**; report ladder `pr`;
0 deferred items; 0 unjudged runs; working tree clean; **no tool failures** — these numbers are
complete, not partial.

## 4. Findings

### 4.1 The wave failed 3 of its own 5 records and fixed all four issues *(PR #626)*

This is the system working. Quoting the wave's own report:

> **Blocker — `if-i-cant-dance…`, false machine-readable claim.** The JSON-LD asserted the slogan
> `isPartOf` *Living My Life* (1931) — i.e. that the sentence is a line from Goldman's book, which is
> precisely what the whole page refutes. Same defect class as the `beyond-the-sea` case in
> `workflows/README.md`. `isPartOf` removed.

> **High — same page, prose and structured data disagreed.** The page told humans "Yes and no… right
> person, wrong words" while the ClaimReview told machines `1 / "False"`.

Two mediums were fixed by *omitting* fields rather than guessing them: a `rightsHolder` that was a
description rather than a party, and a `dateCreated: "1890"` on the Lacassagne record that is Ellis's
publication date, not a creation date any source supports.

One of three skeptic verdicts was **refuted** — the audit wanted to downgrade the Arago/Galileo record
from `verified` to `disputed`, and the wave declined, on the grounds that Wikiquote files that wording
in its *sourced* section as a variant translation. That is the refusal working in the correct
direction: it prevented an overclaim of doubt.

### 4.2 The wave corrected two queue assumptions against primary sources

- The queue filed the Galileo 1615 letter as **public-domain**; the English wording we show is a
  modern translation, so it shipped **in-copyright**.
- The queue asserted Goldman's text prints the criminals line **in capitals**; the 1911 second revised
  edition sets it in *italics*, and the page does not repeat the queue's claim.

The harvest queue is a source of assumptions that waves are catching one at a time. Nothing on the
decision list addresses queue-metadata accuracy as a class.

### 4.3 Generator gap the wave deliberately did not fix — decision 6

> `prep-wave.js:206` withholds `creditedTo` whenever the record's author equals the batch magnet. That
> guard is right for `verified` and `attributed` records — it prevents a false misattribution claim —
> but it is **backwards for a `disputed` record** […] `README.md` documents the correct rule; the
> guard cannot express it, so **every such record ships the harsher machine verdict until an audit
> catches it, as happened here.**

Seventh day on the list. It has now produced an audit **high** on a live-bound page. The wave was
right not to touch `tools/` from a content PR.

### 4.4 What the wave could not establish

Recorded honestly rather than guessed:

- **Which published translation the Galileo 1615 English wording is** — Drake (1957) is
  lending-restricted on archive.org. No translator named as rightsholder.
- **The name of the printer who composed the "If I can't dance" slogan in 1973** — "Wikipedia says
  only 'a printer friend'; a name in wider circulation was **not** used, since no source I read
  supports it."
- **Lacassagne's original French wording and its date.**
- **The Thatcher primary transcript** — `margaretthatcher.org` and `news.bbc.co.uk` "are outside this
  environment's egress allowlist," so the record rests on Wikiquote plus Wikipedia's verbatim
  passage. **This is the same host that blocked yesterday's Thatcher record.** Decision 5.

### 4.5 The corrupt C. S. Lewis candidate led the draw for the fifth wave running

> The corrupt `friendship-is-born-at-that-moment…` candidate led the draw again (5th wave running).
> Drawn N+1, dropped from the batch per the runbook, and released back to the queue.

The runbook handles it correctly every time, which is why it is not an incident — but it burns a draw
slot every single wave and will keep doing so until decision 25 is taken.

### 4.6 Weekly discovery: clean, and it measured something the list should own

All 20 sampled records PASS, **0 findings, 0 proposals**. Three targeted source spot-checks confirmed
records verbatim. The audit investigated one structural oddity and correctly declined to flag it:

> The Charlotte Brontë record files its containing work (*Jane Eyre*) under `schema.isBasedOn` rather
> than `isPartOf`, which diverges from the other verified records and from the CLAUDE.md schema RULE.
> Measured across the corpus: **1046 of 1752 records (60%)** use `isBasedOn`-only. […] a detector for
> it would hit ~60% (well into the auto-reject range the doc warns about), and editing one record
> against a planned bulk migration would only desync it.

Declining to flag it per-record was right. But 60% of the corpus diverging from a stated RULE, waiting
on a migration nothing is scheduled to perform, is an operator decision, not an audit finding — now
decision 11. `tools/template.js:339` says the blocking prerequisite already landed: *"Land this before
that backfill, not after."* It landed 2026-07-30.

### 4.7 Discrepancy — the report window lost six merges again *(decision 2)*

Yesterday's report merged at 12:13 UTC. These six merged **after** it, still on 2026-08-23, so they
appear in neither yesterday's report nor today's:

| PR | merged (UTC) | what |
|---|---|---|
| **#612** | 15:21 | **wave d20260823: 5 records — 3 Galileo, 2 Thatcher** |
| #617 | 15:21 | merge-pass shard |
| #618 | 18:28 | merge-pass shard |
| #619 | 18:28 | reports-close shard |
| #620 | 21:20 | merge-pass shard |
| #615 | 12:38 | merge-pass shard |

`tools/daily-report.js:30` sets `SINCE` to `new Date().toISOString().slice(0, 10)` — the calendar day.
**This is the seventh consecutive wave merge to be invisible to this report.** The record of five
shipped quote pages exists only in git.

### 4.8 Discrepancy — two decision items vanished from the list without being closed

The 2026-08-22 report carried 27 items; the 2026-08-23 report carried 28, adding three new ones. That
arithmetic only works if two were dropped, and two were:

- *"Fix the `themes` comment at `workflows/generate.js:168`"* (8th day on 08-22)
- *"Correct the '~134 magnet authors harvested' figure — 314 distinct authors have been touched"*
  (4th day on 08-22)

There have been **zero commits under `tools/` or `workflows/` since 08-22**, so neither can have been
done. They were lost in the renumbering. Both are restored below as **31** and **32**, with their day
counts continued. The list is renumbered by urgency every day, which is what makes this possible —
worth knowing when reading "decision 4" across two reports.

### 4.9 Discrepancy — `daily-report.js` reports a number that means nothing

The census printed `weekly-discovery · 1772 record(s)`. The routine audited **20**. `daily-report.js:148`
computes `work` as `built + processed + sampled`, and the weekly-discovery shard carries
`built: 1752` and `sampled: 20`, so it summed the corpus size and the sample size. Nothing is wrong
with the run; the number displayed is an artifact. Decision 12.

### 4.10 The stale local `main` fired again — in every pass

`daily-reports` logged *"preflight 15/15 after documented exception (b) recovery"*; `weekly-discovery`
logged the same; this pass hit it too, at **46 commits behind**. Tenth day. The two-command remedy
worked as documented, and the one-command remedy would have written this report against a 46-commit
stale tree.

## 5. Needs a decision

**Nothing closed today, yesterday, or the day before.** Three consecutive days, and this list is now
32 items — it cannot be read standing up, which was its entire design goal. **The single most useful
thing you can do is 1.** If you have one minute instead, do 2.

1. **Declare which of these you are not going to do.** Eleven of the 32 are on their eighth day or
   later and five more are on their seventh, which means the list is no longer a queue but an archive
   pretending to be one; doing this means replying with the numbers to strike, and the next report
   will carry only what survives.

2. **Fix the report window so merges stop vanishing.** `tools/daily-report.js:30` windows on calendar
   day, so anything merged after this report is written is never reported by anyone — six PRs today
   including the seventh consecutive wave merge; doing it is a one-line change deriving `SINCE` from
   the previous report's timestamp instead of `new Date()`. *(17th day)*

3. **Root-cause the stale local `main`, or ratify exception (b) as permanent.** Fired in all three
   passes again today, at 46 commits here, and the exception's own text says finding the cause "would
   retire this exception"; doing it means finding what leaves the container detached with a stale ref,
   or writing the two-command remedy into `preflight.js` as the normal path. *(10th day)*

4. **Do the deindexing watchlist diff.** The per-URL baseline was observed 2026-08-18 and committed to
   re-inspection "in a few days" — now six days overdue, and it is still the highest-stakes open
   question on the site; doing it needs a human in Google Search Console, because nothing here can
   reach it. *(6th day, clock running)*

5. **Extend the egress allowlist — after one measured sweep.** It blocked `margaretthatcher.org` again
   today, for the second Thatcher record in two days, so both ship `verified` on secondary sources;
   doing it means one sweep of every host the waves have hit (`$HTTPS_PROXY/__agentproxy/status`),
   then a single allowlist request from that list rather than another piecemeal one. *(13th day)*

6. **Make `prep-wave.js` stamp `creditedTo` when `confidence === 'disputed'`, regardless of name
   equality.** The guard is backwards for exactly the right-person-wrong-words case, so those records
   ship a false `False` verdict until an audit catches them — today it produced an audit *high*;
   doing it is one guard relaxed for `disputed`. *(7th day)*

7. **Decide what to do about Google Books' 429.** The API has been quota-exhausted for five straight
   days, disabling the earliest-attestation sweep, and no allowlist change fixes it; doing it means
   either obtaining an API key with quota or dropping the sweep from the wave procedure and saying so
   in the doc. *(2nd day as an item; 5th day as a condition)*

8. **Stop one source's outage from hard-stopping the review pass.** `preflight.js`'s `head()`
   classifies a 12s timeout as `blocked: true`, so a slow-but-alive origin reads as an egress block;
   doing it means treating a timeout as a warning, or requiring 3 of 4 citation hosts, and correcting
   the remedy text that points at the allowlist. *(5th day — did not bite today)*

9. **Fail the build when `Quotation.creator` is suppressed on a record that has an author**, and
   replace `template.js`'s exact hero/`creatorName` string match with a normalised comparison; doing
   it is one build assertion plus one comparison. *(5th day)*

10. **Give `claimReviewed` a lever, or accept the coarse verdict.** The string is generated from
    `confidence` alone, so a `disputed` page whose named author demonstrably wrote the words still
    publishes `ClaimReview` **False**; doing it means an optional in-record `claimReviewed` override,
    or narrowing the generated claim to authorship. *(2nd day)*

11. **Decide the `isBasedOn → isPartOf` bulk migration.** 1,046 of 1,752 records (60%) file the
    containing work under the derivation field, diverging from the CLAUDE.md Schema.org RULE, and the
    prerequisite that made the backfill safe landed on 2026-07-30 — nothing is scheduled to run it;
    doing it means one scripted pass over ~1,046 records plus a rebuild, on its own branch. *(NEW —
    measured today by the weekly audit)*

12. **Fix `daily-report.js`'s `work` figure.** Line 148 sums `built + processed + sampled`, which
    reported weekly-discovery as touching "1772 record(s)" when it audited 20; doing it means picking
    one field per routine rather than adding incommensurable ones. *(NEW)*

13. **Decide whether `perf-check` belongs in the routine census.** It commits nothing and is not in
    `EXPECTED`, so a failing site canary reaches you only by GitHub email — I checked the Actions API
    by hand today and it is green; doing it means teaching the census that one Actions read, or
    consciously accepting email-only. *(3rd day)*

14. **Settle the branch-name conflict between the harness and the routine docs.** The harness
    designates `claude/…`, the docs require the routine prefix, and `merge-gate.js` fails closed on
    the former; doing it means a harness setting, or one line in each doc ratifying that routines
    override the designated branch. *(recurring)*

15. **Fix `template.js`'s canned no-rights copy** so it states the absence instead of asserting a
    reason the record never gave; one central edit — state the absence, or defer to `source.rightsNote`
    when present. *(3rd day)*

16. **Work the disputed records with no `creditedTo`.** Items 6 and 20 would stop the class growing;
    today's wave added another record shipping with rights unasserted, deliberately. *(13th day)*

17. **Do the measured `mis-kind.js` pass.** Four gap families characterised; a blind backfill "would
    soften real debunks," so it needs the measured pass, not a guess. *(12th day)*

18. **Fix `plain()` to preserve unknown HTML entities** — add the ~40 missing ones **and** return
    `&name;` verbatim for the unknown case; `&copy;` is currently dropped from `copyrightNotice` on 11
    records. *(6th day)*

19. **Make `parse-audit.js` union findings across repeated audits of the same slug** — it currently
    keeps one audit's issues and drops the other's. *(6th day)*

20. **Add `verdictNote` to `DOSSIER_SCHEMA`** — 83 records carry one, all hand-set; `SCHEMA_BUDGET` has
    zero headroom, so measure that first. *(12th day)*

21. **Add the mechanical near-duplicate check to the wave pipeline** — run by hand again today (batch ×
    corpus, batch × batch, slug uniqueness), clean, still not in the tool. *(7th day)*

22. **Make the started-vs-result gate count coverage per slug, not journal lines.** *(6th day)*

23. **Decide `verbatimNote()`'s "Verbatim:" label corpus-wide** — ~370 records, one editorial call.
    *(6th day)*

24. **Accept or decline the detector `magnet-quoted-it-rated-as-never-said`** — 10 hits, 0.6%, all
    hand-checked; admission is deliberately serial, so this one blocks the next. *(8th day)*

25. **Repair or retire the corrupt C. S. Lewis record and the Ali duplicate** — the Lewis candidate has
    now led the wave draw five waves running, costing a draw slot each time. *(9th day)*

26. **Decide the house convention for characters with no HTML entity** — ratify the literal-Unicode fix
    as a rule; pairs with 9. *(5th day)*

27. **Decide the jurisdiction wording for `USE['public-domain']`** — it asserts "no permission or
    licence needed" with no jurisdiction, across 54 records. *(7th day)*

28. **Decide the `creatorOk` right-person-wrong-words creator emission corpus-wide** — decide alongside
    9 and 10. *(7th day)*

29. **Make the wave log a cost figure.** Today's `weekly-discovery` shard carries one (~130K context
    tokens); the wave shard carries no cost field at all, so wave cost remains unmeasured. *(9th day)*

30. **Decide the `Unknown`-creator `sameAs` convention**, defining the matching rule as part of it.
    *(10th day)*

31. **Fix the `themes` comment at `workflows/generate.js:168`** — one comment edit. *(10th day —
    dropped from the 08-23 list without being closed; restored)*

32. **Correct the "~134 magnet authors harvested" figure** — 314 distinct authors have been touched.
    *(6th day — dropped from the 08-23 list without being closed; restored)*

## 6. Cost

Only one run reported a figure. `weekly-discovery`:

> Effort: ~130K context tokens this session (harness counter 15.0M→14.87M), no worktree, single pass.

The `daily-wave` shard — the most expensive run of the day by a wide margin, covering a full
select → build → audit → skeptic → fix → rebuild pipeline over five records with primary sources read
in full — carries **no cost field**. Neither do the four `daily-merge` shards, `daily-reports`, or
`daily-review`. Wave cost has never been measured on any day this report has covered. That is
decision 29.
