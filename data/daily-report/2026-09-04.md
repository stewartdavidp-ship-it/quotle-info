# Daily report — 2026-09-04

## 1. Verdict

**All five due routines ran, nothing is flagged, `main` is green — and for the second night running
the wave's records did not ship.** The difference from yesterday is that the merge pass *noticed*:
it rebuilt #762 itself and left the note "merges next pass, once verify is green on the new head",
so item **64** now costs a day rather than a stall. Corpus stands at **2036**.

Two things the operator should read before anything else. First, **yesterday's report was overtaken
four hours after it merged**: #752's five records went in at 15:52 UTC, and because the tool windows
on the UTC calendar day, *neither* yesterday's report nor this one counts them — carried item **4**,
now demonstrated rather than theoretical. Second, the wave hit carried item **6** in production and
it produced a machine-readable false claim about Dorothy Parker on a page that was about to ship;
the wave caught it, fixed it in-record, and correctly refused to touch `tools/`.

**Traffic is unavailable for the eighth consecutive day**, and this report can now name the exact
cause — see § 4.

---

## 2. What ran

| slot | routine | result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run: `pr` · 4 records → **#762, still open** |
| 04:00 | `daily-reports` | ✓ 1 run: `no-op` (empty dispute queue) |
| 05:00 | `daily-review` | ✓ 1 run: `no-op` · 2036 scanned, **0 flagged** |
| 07:00 | `daily-merge` | ✓ 2 runs: `merged`, `merged` (shards 03:08 and ~10:42 UTC) |
| 12:00 | `reports-close` | · not yet — due 17:00 UTC (yesterday's ran `no-op`) |
| Mon 02:00 | `weekly-discovery` | · not scheduled today |

**Nothing failed to run.** No routine is missing a shard.

`daily-report.js` reported **1 tool-call failure** — `traffic: fetch failed`. § 4 is the only
incomplete part of this report; §§ 2, 3, 5 are complete.

---

## 3. What shipped

- **Merged (4):** #764 (review log), #763 (reports log), #761 (merge log 09-04), #760 (merge log
  09-03). **All four are routine-log shards. No content merged inside today's window.**
- **Merged outside every report window (5 records):** #752 merged 2026-09-03T15:52:44Z, taking the
  corpus 2031 → 2036. Yesterday's report said it "did not ship"; that was true when written and
  false four hours later. See finding 5.2.
- **Built but not shipped:** 4 records in #762 — `first-i-brush-my-teeth-and-then-i-sharpen-my-tongue`,
  `hes-a-writer-for-the-ages-for-the-ages-of-four-to-eight`,
  `i-was-the-toast-of-two-continents-greenland-and-australia`,
  `you-cannot-persuade-her-with-gun-or-lariat-to-come-across`. One verified Parker, three disputed
  (two reassigned to Oscar Levant and George Jean Nathan).
- **Audited:** 2036 records scanned by `daily-review`, 6 detectors, **0 flagged**; `vocab-sweep.js`
  found 0 under-used keys. 4 pages audited inside the wave: 2 PASS / 2 FAIL, 2 issues, both fixed.
- **Reader reports closed:** none — the queue was empty (`no pending reader reports`).

> The readable output credits `daily-merge` with "4 record(s)". That is the known `work`-figure
> defect (carried item **18**) summing PRs as records; **no content merged today.**

---

## 4. Traffic

**Traffic: unavailable — eighth consecutive day.** `traffic` is `null` and `tool_failures` carries
`traffic: fetch failed`. **This is an analytics outage, not a quiet day. It is not a zero.**

New this report — the cause, in the tool's own words. Running `node tools/traffic.js` here returns:

> `traffic.js: 403 from GoatCounter — the dashboard is no longer public.`
> `Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.`

So the failure is not transient and not network: the public dashboard endpoint is switched off, and
no figure — spam-adjusted or raw — can be produced from this checkout until someone flips that one
setting. Eight days of GoatCounter data are still being recorded and are not lost; they are simply
unreadable from here. This is item **43**.

---

## 5. Findings

### 5.1 The wave hit carried item 6, and it emitted a false machine-readable claim about Parker

The wave's own words:

> **BLOCKER — `i-was-the-toast-of-two-continents…` contradicted itself between prose and structured
> data.** The ClaimReview emitted `ratingValue 1 / "False"` — machine-readably asserting the Parker
> attribution is false — while the sibling `Quotation.creator` was "Dorothy Parker" and the FAQ
> answer credited her.

And the cause, again in its words:

> Cause is a genuine collision between two committed rules:
> - `workflows/README.md` (§ creditedTo, from r35/r37/r38): *on a disputed record whose author **is**
>   the magnet, **stamp it***
> - `workflows/prep-wave.js:206` declines to stamp `creditedTo` whenever hero == batch author, so
>   `--credited` could never produce it.

It fixed the record (`creditedTo: "Dorothy Parker"` → rating 2 / "Mostly False", FAQ opens "Yes and
no.") and stated plainly: **"I did not touch `prep-wave.js`" — that is one central edit on its own
branch, reported here, not smuggled into a content wave.** That is the correct call and exactly what
the docs ask for.

**This is carried item 6, on its 18th day, and today is the first time it is documented producing a
wrong published claim rather than a merely wrong internal state.** The record is fixed; the
generator that produced it is not, so the next disputed-self-magnet record will do the same thing.

### 5.2 Yesterday's report is wrong about #752, and the window is why

Yesterday's verdict opened: *"today's five records did not ship."* True at 08:27 UTC when it merged.
#752 merged at **15:52 UTC the same day**, along with #755, #757, #758 and #759. Because
`daily-report.js` windows on the UTC calendar day, those merges fall after yesterday's report and
before today's window opens, so **no report in the series records the corpus going 2031 → 2036 or
the five Parker/Becker/Long/Wrigley/anonymous records going live.**

This is carried item **4**, on its 28th day. Its cost was previously theoretical. It is now a
report that states the opposite of what happened, sitting in the archive with nothing to correct it.

### 5.3 A fifth candidate was dropped for corrupt stored text — the second of its class

The wave built 4, not 5. Its words:

> The 5th draw, `the-two-most-beautiful-words-in-the-english-language-are`, is **stored truncated
> mid-quote**: `The Two Most Beautiful Words in the English Language Are "Check Enclosed` — no
> closing quote. `toRecord` takes `displayQuote` straight from the batch text, so it would have
> shipped a visibly broken headline, and the slug already drops the payload ("check-enclosed")
> entirely.

It followed the documented precedent — dropped from the batch, never hand-edited in
`harvest-queue.json`, released back to `queued` by `unselect --wave d20260904`, "0 left `selected`".
It notes the research is already done and cheap to reuse (QI dates the joke to Dec 1903, ~29 years
before Parker; Parker's real 1932 remark was only that the words she *liked to see* were "cheque"
and "enclosed").

**This is the same class as the standing C. S. Lewis truncation (item 32), not the same candidate.**
Two corrupt stored records have now each cost a wave a slot.

### 5.4 A template question the wave raised and deliberately did not act on

> `disputed` maps unconditionally to ClaimReview `1 / "False"` unless the right-person-wrong-words
> path fires. On the Nathan page that reads more absolutely than QI's provisional finding warrants.
> `disputed` is still the honest confidence and the alternative (`attributed`) would understate the
> 1938 evidence, so nothing was changed — flagging it as a possible template question, since a fix
> would be central.

This is a new and specific sighting of carried item **21**. Noted there rather than given its own
number; the list is long enough.

### 5.5 What the wave could not establish — recorded honestly on every page

> **No page's primary text was read by me directly.** The 1940 column is paywalled at
> Newspapers.com; the 1938 *Reader's Digest* and 1948 Cerf are print-only; the 1944 Viking Portable
> is on the Internet Archive but lending-restricted. QI reports verifying each on paper, and every
> page names QI as the reader rather than claiming the primary as ours.

Also unresolved and stated on the pages: renewal status of the 1940/1938/1956 periodical printings
(all conservatively marked in-copyright); Nathan's credit is provisional and says so; the "two
continents" credit is soft even for Parker (Cerf thought Frank Sullivan said it first). All 19
external links across the 4 pages re-fetched, 19/19 HTTP 200. **No record claims public domain.**

### 5.6 The stale-`main` defect fired on three of four routines again

- `daily-wave` — exception **(a)** (`local main sane` + `git on main` failing, `tree clean` passing).
- `daily-review` — exception **(b)**, in its words: *"`git on main: on claude/wizardly-fermat-v8byt6`
  … This is another sighting of the underlying open question that exception (b) documents — something
  leaves the container off `main`."*
- **This report** — exception **(b)**: detached at `HEAD`, local `main` ref three commits behind. Ran
  both printed commands, re-ran preflight, 10/10 green before any work.
- `daily-reports` — passed 15/15 clean, the only one that did.

Carried item **12**, 21st day. Three different failure shapes in one morning.

### 5.7 #670 is still DIRTY on its ninth day

`merge-gate.js` reports it `DIRTY` (8d open). Nothing has touched it since 2026-08-27. Its five
candidates were verified back at `queued` with no wave stamp, so closing it loses research effort
but no content. Item **42**.

---

## 6. Needs a decision

**One new (67). The rest are carried items that MOVED today — 6 caused a published false claim, 4
produced a wrong archived report, 43 now has its exact error string. If you do nothing else, do 6
and 43.**

67. **Rule on the truncated `the-two-most-beautiful-words-in-the-english-language-are` candidate.**
    Its stored text ends mid-quote with no closing quote, so any wave that draws it must drop it —
    the second candidate in the corpus with this defect (item **32** is the first). Doing it is:
    decide whether corrupt stored text may be repaired in `harvest-queue.json` at all, then either
    repair this one and the C. S. Lewis record together or retire both — the research for this one
    is already done in #762's body and costs nothing to reuse.

6.  **Make `prep-wave.js:206` stamp `creditedTo` when `confidence === 'disputed'` regardless of name
    equality.** *(18th day — and today it shipped a ClaimReview rating `1 / "False"` denying Parker
    said her own line, § 5.1.)* Doing it is one condition in `tools/prep-wave.js` on its own branch,
    plus a count of already-shipped disputed-self-magnet records carrying the same contradiction.

43. **Flip GoatCounter → Settings → "Dashboard viewable by" → Anyone.** *(8th day.)* The endpoint
    returns 403 with the message "the dashboard is no longer public" (§ 4), so this report has
    carried no traffic figure for eight days and cannot carry one tomorrow. One setting in the
    GoatCounter UI; nothing here can reach it and no data is lost.

4.  **Fix the report window.** *(28th day — it has now produced an archived report that states the
    opposite of what happened, § 5.2.)* `daily-report.js:30` windows on the UTC calendar day, so
    records merged between two reports appear in neither. One change, on its own branch.

64. **Make the merge pass merge content PRs before routine-log PRs.** *(2nd day — recurred exactly as
    predicted; #762 was demoted behind three zero-content shards and merges tomorrow.)* Today's pass
    did rebuild and push #762 itself, so the cost is one day rather than a stall. Doing it is one
    sort key in `tools/merge-run.js`'s planner (content/derived-output PRs first), plus a test case.

42. **Rebuild and merge #670, or close it.** *(9th day; `merge-gate.js` says DIRTY, 8d open.)*
    Closing loses the research but no content — its five candidates are back at `queued` (§ 5.7).
    Doing it is: merge `main` in, take `main`'s side on generated files, `node tools/build.js`,
    `node tools/scan.js`, push — or close it and let a later wave redraw them.

63. **Ratify or investigate the out-of-band merge of #741.** *(3rd day.)* A 40-record, 165-file
    content PR merged two minutes after opening with no `daily-merge` shard, while the 07:00 pass is
    documented as the single merge authority. Doing it is: confirm you merged it by hand and add one
    line to `workflows/DAILY-MERGE.md` — or, if you did not, find out what did.

65. **Put the verdict before the re-poll chatter in the merge note, or widen the cap.** *(2nd day —
    today's note spent three of its lines on re-poll progress before reaching the verdict.)* Doing it
    is reordering the `notes` join at `tools/merge-run.js:309` so verdicts precede progress lines, or
    raising the 400-char `.slice()`.

66. **Fix `template.js:848`'s unconditional period on `disambiguatingDescription`.** *(2nd day.)* Any
    author name ending in an abbreviation renders `William Wrigley Jr...` in the JSON-LD. Doing it is
    one conditional in `tools/template.js` on its own branch, plus a corpus count of how many shipped
    records already carry the doubled period.

62. **Fix `template.js:1227`'s flat "rights held by X" in the visible copy kit.** *(3rd day.)* It
    asserts a rights holder as settled fact even where the record's own rights state is `uncertain`.
    Doing it is one hedged interpolation plus a count of affected pages.

56. **Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog`, which is live.** *(5th day.)*
    Title, slug, canonical URL and ClaimReview all quote QI's headline "(or Dog)" — nobody said it.
    Now either unpublishing/renaming a live page or ratifying the convention (item **54**).

61. **Fix the `mis-kind.js` comma gap.** *(5th day.)* `/\bdocumented\b[^,]*\bunverified\b/i` cannot
    match `documented, but unverified`, so a row saying no verdict was reached renders the ✕ meaning
    the credit is false. One character class, plus a count of rows the widening would flip.

40. **Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`).** *(9th day — today's runs
    landed at 03:08 and ~10:42 UTC.)* One line in `.github/workflows/merge.yml`.

25. **Do the measured `mis-kind.js` pass.** *(23rd day.)* Narrow literal patterns only, with counts,
    on their own branch — **61** is one line of this.

5.  **Declare which carried items you are not going to do.** *(12th day.)* The table below is 40
    rows. Reply with the numbers to strike and tomorrow's report carries only what survives.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 2 | Stop the daily wave and the backlog waves colliding over the queue | 11th |
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" — **57 is its mirror image** | 11th |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 11th |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 17th, clock running |
| 9 | Fix the middle-initial hub fork at the generator, scoped to `schema.creator` | 10th |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row | 10th |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 10th |
| 12 | Root-cause the stale local `main`, or ratify the exceptions in `preflight.js` — **fired on 3 of 4 routines, in 3 different shapes (§ 5.6)** | 21st |
| 13 | Extend the egress allowlist — `bioguide.congress.gov`, `finance.senate.gov` | 24th |
| 14 | Decide the `isBasedOn → isPartOf` bulk migration (1,046 of 1,752 measured 08-24) | 12th |
| 15 | Decide what to do about Google Books' quota | 13th |
| 16 | Stop one source's outage hard-stopping the review pass | 16th |
| 17 | Fail the build when `Quotation.creator` is suppressed; normalise `template.js`'s hero match — **folded into 58** | 16th |
| 18 | Fix `daily-report.js:148`'s `work` figure — **today it credited `daily-merge` with "4 record(s)" when 0 records merged (§ 3)** | 12th |
| 19 | Decide whether `perf-check` belongs in the routine census | 14th |
| 20 | Settle the branch-name / draft conflict between the harness and the routine docs — **re-flagged by #764, both halves** | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict — **56 is this item live on the site; § 5.4 is a new sighting on the Nathan page** | 13th |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason — **62 is its sibling** | 14th |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — cannot see unmerged PRs | 18th |
| 24 | Work the disputed records with no `creditedTo` — 7 of 10 `magnet` rows still render the false-credit ✕ | 24th |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) | 17th |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 17th |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 17th |
| 30 | Decide `verbatimNote()`'s "Verbatim:" label corpus-wide (~370 records) | 17th |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — **58 is queued behind it** | 19th |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate — **67 is a second instance of the same defect** | 20th |
| 33 | Decide the house convention for characters with no HTML entity | 16th |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 18th |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — **folded into 58** | 18th |
| 36 | Make the **wave** log a cost figure — tonight's shard again carries none (§ 7) | 20th |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 21st |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 21st |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 17th |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 9th |
| 44 | Teach `merge-gate.js` to look at what actually changed (`:98` DIRTY→SKIP; `:123` behind→demote) — **#670 is its case; 64 is its ordering half** | 8th |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 8th |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — **54 is the ruling this needs** | 8th |
| 47 | Decide whether QI's Title Case should be normalised centrally | 8th |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 8th |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 8th |
| 51 | Make the merge pass's rebuild step fetch before it merges | 7th |
| 54 | Rule on Quote Investigator *headlines* reaching `displayQuote` (50+ records shipped this way) | 6th |
| 57 | Give the orphaned `taxes-…` candidate a way back into the queue | 5th |
| 58 | Accept or decline the `disputed-creator-name-mismatch` detector, and rule on its 99-record sweep | 5th |
| 59 | Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it | 5th |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Add `traffic.js`'s specific error to `daily-report.js`'s `tool_failures` — **folded into 43; § 4 had to shell out to get it** | 7th |

**Struck previously:** 28 (superseded by 59), 50, 52 (folded), 53 and 55 (done 08-30/08-31), 60 (done
09-02).

---

## 7. Cost

**No routine reported a cost figure today.** `weekly-discovery` — the only routine that has ever
logged one — is a Monday pass and did not run. Today's `daily-wave` shard again carries no cost
field. Carried item **36** stands, on its 20th day.

The standing datapoints, both from `weekly-discovery`, remain the project's only measured numbers:
**~9.5K new tokens/record** (2026-08-31, 20 records) against **~21K/record** (2026-07-29). Neither
speaks to a pass that re-fetches primary sources, which today's did across 19 external links.

**Wasted work today is smaller than yesterday's and non-zero.** #762 was built, audited and pushed,
then rebuilt by the merge pass after three one-line shards were merged ahead of it — one extra
`build.js` run over the derived output, and 4 records held a day. That is the second consecutive
night item **64** has cost the wave its ship date.

**Nothing reported an interrupted run** — no 529, no rate limit, no capacity ceiling. The only
environmental hits were the GoatCounter 403 (§ 4) and the paywalled/print-only/lending-restricted
primaries the wave documented rather than worked around (§ 5.5).
