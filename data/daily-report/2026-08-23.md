# Daily report — 2026-08-23

**All four due routines ran and all four worked.** The wave built its full 5 (3 Galileo, 2 Thatcher),
its audit caught **a blocker and a high before they shipped** and fixed all 8 findings in-record, and
the merge pass merged 6 and rebuilt the wave PR. Corpus **1,747 · 0 flagged · CI green on `main` ·
tree clean · 0 deferred**. Yesterday's `daily-review` failure did **not** recur.

Three things to read standing up. First, **nothing was closed for a second straight day** — zero
commits under `tools/`, `workflows/` or `.github/workflows/` since yesterday's report, so all 27
standing decisions are open unchanged and one new one joins them. The list has stopped being a queue
anyone is working. Second, **the report-window defect cost a sixth consecutive wave merge**: #599
(*"wave d20260822 — 5 Galileo misattributions"*) merged at 15:20Z yesterday, after yesterday's report
and before today's calendar window, so those 5 records appear in **no** daily report — five other PRs
vanished with it. Third, **the stale-`main` exception fired in all three passes again**, at 29, 29 and
32 commits; this pass's own preflight needed it, and the one-command remedy would have written this
report against a tree 32 commits stale.

The report tool reported **no tool failures**, so the mechanical picture below is complete.

---

## 1. What ran

| Slot | Routine | Result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run — PR **#612**, **5 records**, audit 3 PASS / 2 FAIL, `verify` **green** on the rebuilt head, **not yet merged** |
| 04:00 | `daily-reports` | ✓ 1 run — no-op (queue empty, public `GET /reports/pending` read cleanly) |
| 05:00 | `daily-review` | ✓ 1 run — no-op (1,747 scanned, **0 flagged**) |
| 07:00 | `daily-merge` | ✓ **4 runs** — merged **6 PRs**, rebuilt #612 |
| 12:00 | `reports-close` | · not yet due (17:00 UTC) — ran yesterday 2026-08-22, no-op |
| Mon 02:00 | `weekly-discovery` | · not scheduled today (Sunday) |

**Nothing is missing** — every due routine fired and left a shard. No routine reported an `error`.
`daily-review`, which hard-STOPped yesterday on the Gutenberg reachability check, ran normally today;
the defect behind that stop (decision **2**) is unfixed, it simply did not bite.

Outside the census: **`perf-check`** (11:00 UTC) commits nothing and is not in `daily-report.js`'s
expected roster, so it cannot appear above — see decision **9**.

## 2. What shipped

- **6 PRs merged in the window** (#608, #609, #610, #611, #613, #614). **Every one is a routine-log
  shard or a no-op review.** No content shipped in the window.
- **Corpus: 1,747 records · 0 flagged · CI `success` on `main` (`948144828`) · tree clean · 0 deferred
  · 0 unjudged runs · report ladder `pr`.**
- **Yesterday's projection reconciles exactly.** Yesterday's report said *"Corpus goes to 1,747 when
  #599 merges"*; #599 merged 2026-08-22 15:20Z and the tool reads **1,747** today.
- **Two PRs open, both the 07:00 pass's business, not this one's:** **#612** (`wave-d20260823`, CLEAN,
  `verify` **success** on head `41d44d89`) and **#615** (`merge/2026-08-23`, CLEAN). Corpus goes to
  **1,752** when #612 merges.
- **Today's 5 records are still in #612** — 3 Galileo, 2 Thatcher: *"The intention of the Holy Ghost…"*
  (disputed, actually Cardinal Cesare Baronio), *"All truths are easy to understand…"* (attributed,
  rights unasserted), *"Mathematics is the language with which God has written the universe"*
  (disputed — right person, wrong words), *"In politics, if you want anything said, ask a man…"*
  (verified, 1965 *Evening News*), and *"There is no such thing as society"* (verified, *Woman's Own*
  1987).
- **Vocabulary is healthy.** `vocab-sweep.js`: 6 tables, 1,747 records, **0 under-used keys**.
  CONFIDENCE verified=617 / attributed=188 / disputed=942.

## 3. Findings

Most serious first. Each names the record or file it concerns.

### F1 — Six merged PRs, including a whole wave, are invisible to this report series

`tools/daily-report.js:30` windows on calendar day. Yesterday's report was written at 12:11Z; the
merge passes then merged **#599** (`wave-d20260822`, *"5 Galileo misattributions"*, 15:20:02Z), **#602**,
**#604**, **#605**, **#606** and **#607** later the same day. None of them fall in today's window
either. The five records reached the site and no daily report will ever mention them shipping.

This is the **sixth consecutive wave merge lost** to the same one-line defect, and it is the reason
today's "What shipped" honestly reads *"no content shipped"* while five pages went live yesterday
afternoon. It is decision **1**, on its **16th day**.

### F2 — The wave's audit caught a JSON-LD/prose contradiction that violated the standing Schema.org RULE

On `the-intention-of-the-holy-ghost…`, in the wave's own words:

> The page's prose named Cardinal Cesare Baronio as the true author in four places while the JSON-LD
> emitted **no `Quotation.creator` at all** and declared "The true author is not established; no
> creator is asserted here" — the machine layer returning the opposite verdict from the human layer,
> and a silent miss of the standing Schema.org creator RULE.

The cause was in the record, not the generator: `schema.creatorName` was `Cesare Baronio` against a
hero of `Cardinal Cesare Baronio`, and **`template.js` emits creator on a disputed page only on an
exact match**. The wave fixed the record and correctly did not touch `tools/`. The fragile exact-match
rule and the silence of the miss both remain — it was caught by an audit, not by the build. Decision
**4**.

### F3 — A `high` finding: a record shipped `attributed` against its own evidence

On `in-politics-if-you-want-anything-said…`:

> Shipped as `attributed`, which generated the machine-readable claim "not traced to a primary source"
> — **false on the page's own evidence**, since QI traced it to a named source on a named date and
> concluded she said it.

Promoted to `verified`. Both hard findings were skeptic re-checked defaulting to "this finding is
wrong"; **both confirmed, 0 refuted**. Six further medium/minor findings were fixed in-record,
including a computed-not-sourced duration in the Baronio bio, an unsourced "widely repeated" frequency
claim, and an arithmetic error ("four months after a third election win" — the 1987 election was in
June, so three).

### F4 — `ClaimReview` publishes a blunter verdict than the page's own prose, with no in-record lever

The wave flagged this deliberately as an observation rather than a change:

> on the Holy Ghost page the generated `ClaimReview` rates *'Galileo Galilei said: "…"'* as **False**.
> That is consistent with the page's verdict on *authorship*, but Galileo did literally write the
> sentence down, so the flat "False" is a slightly blunter reading than the prose gives. The
> `claimReviewed` string is template-generated from `confidence`, so there is no in-record lever for it.

This is distinct from the standing `creditedTo` self-credit gap (decision **8**) — here `creditedTo`
**is** stamped correctly and the machine claim is still coarser than the truth. **NEW**, decision **3**.

### F5 — The stale local `main` fired in every pass that ran preflight, and the gap is not shrinking

Three passes, three fires, gaps of **29, 29 and 32** commits. `daily-reports` recorded the history:

> Prior occurrences logged in the doc were 28, 28, 28 and 31. The checkout-alone remedy would again
> have moved the tree backward across all 29; the fast-forward discarded nothing.

And `daily-review`:

> This is now a recurring daily occurrence, not a one-off. The underlying cause (what leaves the
> container detached with a stale `main`) is still open, and finding it would retire the exception.

**This pass hit it too**, at 32 commits — the largest yet. Decision **5**, **9th day**.

### F6 — Verification reach: five hosts refused and Google Books rate-limited all run

The wave could not establish three things, and named the cause each time:

> `margaretthatcher.org` (the full Thatcher transcripts), `sourcebooks.fordham.edu`, `blog.oup.com`,
> `discovermagazine.com` and **`web.archive.org`** are all refused by the egress policy. Nothing on
> those hosts is cited anywhere in this wave, even where a Wayback snapshot exists — a link we cannot
> fetch is a link we cannot check.

Separately, the Google Books API returned **429 throughout**, so the earliest-attestation sweep for
the *"God has written the universe"* paraphrase was unavailable — the **4th day running** Google Books
has been unusable. Note this is a **quota** problem, not an allowlist one, so decision **6** will not
fix it. The Internet Archive item for the 1999 Giovagnoli book is access-restricted (search-inside
403), so `all-truths-are-easy-to-understand…` reports only that Wikiquote points there.

`margaretthatcher.org` being blocked is the sharpest case: it is the authoritative transcript archive
for the two records this wave shipped as `verified`.

### F7 — The harness and the doc disagree on branch names, and the doc is load-bearing

`daily-review` reported:

> The session harness designated `claude/wizardly-fermat-9j64vc`, but DAILY-REVIEW.md is explicit that
> the `review/` prefix is load-bearing — `tools/merge-gate.js` fails closed and would class a
> `claude/` branch as `HUMAN` forever.

It followed the doc and flagged the disagreement, which is correct. It recurs whenever a routine runs
in a cloud session. Decision **10**.

### F8 — No discrepancies between what the routines claimed and what the log shows

Checked and clean: the wave claimed 5 records and #612 carries 5; the review claimed 1,747 scanned and
the tool reads 1,747; the merge pass claimed #608–#611, #613, #614 and the tool's merged list matches
exactly. Yesterday's corpus projection reconciled to the record. **Nothing to report here — stated so
it is on the record that it was checked.**

## 4. Needs a decision

**One new item (3). The other 27 were open yesterday and are open unchanged** — there have been **zero
commits under `tools/`, `workflows/` or `.github/workflows/` since yesterday's report**, so nothing on
this list was touched. That is now two consecutive days of no closures, on a list that has grown every
day this week.

**If you do only one thing, do 1** — it is one line and it is currently erasing the record of every
wave that merges after noon.

1. **Fix the report window so merges stop vanishing** — `tools/daily-report.js:30` windows on calendar
   day; change `SINCE` to run report-to-report. Doing it is a one-line change to how `SINCE` is
   derived. **Sixth consecutive wave merge lost (#599), plus five other PRs.** *(16th day)*

2. **Stop one source's outage from hard-stopping the review pass.** `preflight.js`'s `head()` classifies
   a 12s timeout as `blocked: true`, so a down-but-slow origin reads as an egress block. Doing it means
   treating a timeout as a warning rather than a block, or requiring only 3 of the 4 citation hosts, and
   correcting the remedy text that points at the allowlist. *(4th day — did not bite today, Gutenberg
   was up)*

3. **Give `claimReviewed` a lever, or accept the coarse verdict.** The string is generated from
   `confidence` alone, so a `disputed` page whose named author demonstrably wrote the words down still
   publishes `ClaimReview` **False** on "X said …". Doing it means either an optional in-record
   `claimReviewed` override or narrowing the generated claim to authorship ("X originated…"). *(NEW)*

4. **Fail the build when `Quotation.creator` is suppressed on a record that has an author** — and
   replace `template.js`'s exact hero/`creatorName` string match with a normalised comparison. Doing it
   is one build assertion plus one comparison; today it was the difference between an audit blocker and
   a silent RULE violation on a live page. *(4th day — first day it produced a blocker)*

5. **Root-cause the stale local `main`, or write exception (b) up as permanent.** Fired in all three
   passes today plus this one, at 29, 29 and **32** commits, so the one-command remedy would have
   written this report against a 32-commit-stale tree. Doing it means finding what leaves the container
   detached with a stale ref, or ratifying the two-command remedy. *(9th day)*

6. **Extend the egress allowlist — and get one authoritative list first.** Today it blocked
   `margaretthatcher.org` (the authoritative archive for the two Thatcher records shipped as
   `verified`), `sourcebooks.fordham.edu`, `blog.oup.com`, `discovermagazine.com` and `web.archive.org`.
   Doing it means one measured sweep of every host the waves have hit
   (`$HTTPS_PROXY/__agentproxy/status`), then a single allowlist request from that list. *(12th day)*

7. **Decide what to do about Google Books' 429** — the API has been quota-exhausted for four straight
   days, disabling the earliest-attestation sweep, and **no allowlist change fixes it**. Doing it means
   either an API key with quota, or dropping the sweep from the wave procedure and saying so in the doc.
   *(NEW as a separate item; the condition is on its 4th day)*

8. **Make `prep-wave.js` stamp `creditedTo` when `confidence === 'disputed'`, regardless of name
   equality.** Doing it is one guard relaxed for `disputed`. *(6th day)*

9. **Decide whether `perf-check` belongs in the routine census.** It commits nothing and is not in
   `daily-report.js`'s `EXPECTED` roster, so a failing site canary reaches you only by GitHub email.
   Doing it means teaching the census to read the Actions API for that one workflow, or consciously
   accepting email-only. *(2nd day)*

10. **Settle the branch-name conflict between the harness and the routine docs** — the harness
    designates `claude/…`, the docs require the routine prefix, and `merge-gate.js` fails closed on the
    former. Doing it means either a harness setting or a line in each doc ratifying that routines
    override the designated branch. *(recurring; called out again today)*

11. **Do the deindexing watchlist diff** — the per-URL baseline was observed 2026-08-18 and committed to
    re-inspection *"in a few days"*; now **five days overdue**, needs a human in GSC, nothing is
    scheduled to do it, and it remains the highest-stakes open question on the site. *(5th day, clock
    running)*

12. **Fix `template.js`'s canned no-rights copy** so it states the absence instead of asserting a reason
    the record never gave. Doing it is one central edit: state the absence, or defer to
    `source.rightsNote` when one is present. *(2nd day)*

13. **Add a `mis-kind.js` pattern for "Translator, not the original"** — a strict subset of the measured
    pass in **18**, which could absorb it. *(2nd day)*

14. **Fix `plain()` to preserve unknown HTML entities** — add the ~40 missing entities **and** return
    `&name;` verbatim for the unknown case; `&copy;` is currently dropped from `copyrightNotice` on 11
    records. *(5th day)*

15. **Make `parse-audit.js` union findings across repeated audits of the same slug** — it keeps one
    audit's issues and drops the other's. *(5th day)*

16. **Make the started-vs-result gate count coverage per slug, not journal lines.** *(5th day)*

17. **Decide `verbatimNote()`'s "Verbatim:" label corpus-wide** — ~370 records, one editorial call.
    *(5th day)*

18. **Do the measured `mis-kind.js` pass** — four gap families characterised; a blind backfill *"would
    soften real debunks"*. *(11th day)*

19. **Add the mechanical near-duplicate check to the wave pipeline** — run by hand again today (batch ×
    corpus and batch × batch, plus slug uniqueness), clean, still not in the tool. *(6th day)*

20. **Accept or decline the detector `magnet-quoted-it-rated-as-never-said`** — 10 hits, 0.6%, all
    hand-checked. *(7th day)*

21. **Repair or retire the corrupt C. S. Lewis record and the Ali duplicate** — ranks 27 and 48.
    *(8th day)*

22. **Add `verdictNote` to `DOSSIER_SCHEMA`** — 83 records on `main` carry one, all hand-set;
    `SCHEMA_BUDGET` has zero headroom, so check that first. *(11th day)*

23. **Decide the house convention for characters with no HTML entity** — ratify the literal-Unicode fix
    as a rule. *(4th day — pairs with 4)*

24. **Decide the jurisdiction wording for `USE['public-domain']`** — asserts "no permission or licence
    needed" with no jurisdiction across 54 records. *(6th day)*

25. **Decide the `creatorOk` right-person-wrong-words creator emission corpus-wide** — today's
    `mathematics-is-the-language…` ("right person, wrong words") adds another record to the class.
    *(6th day — decide alongside 4)*

26. **Make the wave log a cost figure** — verified again today: no shard on 2026-08-23 carries any cost
    field. *(8th day)*

27. **Decide the `Unknown`-creator `sameAs` convention**, defining the matching rule as part of it.
    *(9th day)*

28. **Work the disputed records with no `creditedTo`** — items 8 and 22 would stop the class growing;
    today added one more record asserting no rights status (`all-truths-are-easy-to-understand…`,
    shipped with rights unasserted, deliberately). *(12th day)*

**Nothing closed today, and nothing closed yesterday.** Twenty-two of these items are now older than a
week. If the intent is that most of them will not be done, saying so would be worth more than carrying
them — this section is meant to be read standing up, and at 28 items it no longer can be.

## 5. Cost

**No run reported a cost figure.** Checked directly: every routine-log shard written on 2026-08-23
carries only `date`, `routine`, `outcome`, `note` and a work count (`processed` or
`scanned`/`flagged`) — no token or dollar field exists in the schema. This is decision **26**, on its
8th day, and it means no daily report in this series has ever been able to state a cost.
