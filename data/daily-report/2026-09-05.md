# Daily report — 2026-09-05

## 1. Verdict

**All four due routines ran, nothing is flagged, `main` is green, and for the third night running
the wave's records did not ship inside the window — but this time they will ship today.** The merge
pass rebuilt #772 and pushed it, `verify` went green on the new head at **10:02:21Z**, so the next
3-hourly pass (~13:00 UTC) should take it. Corpus stands at **2040**.

Three things to read before anything else.

1. **The wave repaired corrupt stored text without waiting for your ruling.** Carried item **67** —
   opened yesterday precisely to ask whether that is allowed — was re-selected today, and the wave
   restored the lost closing quotation mark in the record's `displayQuote` while leaving
   `data/harvest-queue.json` untouched. Its reasoning is sound and narrowly scoped, but a routine
   has now set the precedent your ruling was supposed to set. See § 5.1 and item **67**.
2. **A wave PR merged in the blind spot again.** #762's 4 records went in at
   **2026-09-04T20:20:38Z** — after yesterday's report and before today's window — so they appear in
   *neither* report's merged list. That is carried item **4**, now demonstrated on two consecutive
   nights. § 5.2.
3. **Traffic is unavailable for the ninth consecutive day**, for the same one-setting reason (§ 4).

Carried item **6** — the one that shipped a false ClaimReview about Dorothy Parker yesterday — **did
not fire tonight**. All four disputed records in #772 carry a `creditedTo`, checked directly on the
branch. The defect stands; the name-equality case simply did not arise.

---

## 2. What ran

| slot | routine | result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run: `pr` · 5 records → **#772, open, `verify` green** |
| 04:00 | `daily-reports` | ✓ 1 run: `no-op` — "no pending reader reports; public /reports/pending healthy" |
| 05:00 | `daily-review` | ✓ 1 run: `no-op` · **2040 scanned, 0 flagged** |
| 07:00 | `daily-merge` | ✓ 2 runs: `merged`, `merged` (shards 03:10 and ~10:01 UTC) |
| 12:00 | `reports-close` | · not yet — due 17:00 UTC (yesterday's ran `no-op`) |
| Mon 02:00 | `weekly-discovery` | · not scheduled today |

**Nothing failed to run.** No routine is missing a shard. `daily-wave`'s shard is still on its
unmerged branch, which `daily-report.js` reads deliberately, so it is a run and not an absence.

`daily-report.js` reported **1 tool-call failure** — `traffic: fetch failed`. **§ 4 is the only
incomplete part of this report; §§ 2, 3, 5 are complete.**

---

## 3. What shipped

- **Merged inside today's window (4):** #774 (review log), #773 (reports log), #771 (merge log
  09-05), #770 (merge log 09-04). **All four are routine-log shards. No content merged inside the
  window.**
- **Merged outside every report window (4 records):** #762 merged **2026-09-04T20:20:38Z**, taking
  the corpus 2036 → 2040. Yesterday's report listed it as "built but not shipped"; it shipped seven
  hours later and no report says so. § 5.2.
- **Built but not yet shipped:** 5 records in #772 — `the-two-most-beautiful-words-in-the-english-language-are`,
  `friendship-is-born-at-that-moment-when-one-man-says-to`,
  `half-of-the-town-councilors-are-not-fools`,
  `he-is-a-self-made-man-yes-and-he-worships-his-creator`,
  `if-you-are-not-a-liberal-at-25-you-have-no-heart-if-you-are`. One `verified` (C. S. Lewis), four
  `disputed` — three of them Disraeli misattributions.
- **Audited:** 2040 records scanned by `daily-review`, **0 flagged**. Inside the wave: **3 PASS /
  2 FAIL, 7 issues (2 high, 0 blockers), all fixed in-record**; 20 source links re-fetched, 19
  resolved.
- **Reader reports closed:** none — the queue was empty.

> The readable output credits `daily-merge` with "4 record(s)". That is the known `work`-figure
> defect (carried item **18**) summing merged **PRs** as records: the shards report `processed: 1`
> and `processed: 3`. **Zero quote records merged inside today's window.**

---

## 4. Traffic

**Traffic: unavailable — ninth consecutive day.** `traffic` is `null` and `tool_failures` carries
`traffic: fetch failed`. **This is an analytics outage, not a quiet day. It is not a zero.**

The cause is unchanged and is not transient. `node tools/traffic.js` here returns, verbatim:

> `traffic.js: 403 from GoatCounter — the dashboard is no longer public.`
> `Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.`

Nine days of GoatCounter data are still being recorded and are not lost — they are unreadable from
this checkout until someone flips that one setting. Nothing here can reach it. Carried item **43**,
ninth day.

Reminder for when the number returns: it is an **upper bound**, not spam-filtered
(`spamFiltered: false` always in cloud), and on 2026-09-01 roughly **37% of attributed referrals
were bots impersonating search engines on `.info` domains**. Say "up to N".

---

## 5. Findings

### 5.1 The wave repaired corrupt stored text ahead of your ruling — narrowly, and it said so

The Parker candidate's stored text was truncated mid-title — `... Are "Check Enclosed` with the
closing quotation mark lost. Yesterday's wave dropped the record for this and released it to the
queue, which is why it was re-selected today. Today's wave, in its own words:

> "I restored the closing quotation mark in the record's `displayQuote` only. This is a
> transcription repair, not an invention: the slug is byte-identical either way (verified against
> `tools/slugify.js`), and no citation, date, rights value or URL changed. Without it the site's most
> prominent element — the H1 — shipped with an unbalanced quote."

And on what it did *not* do:

> "`data/harvest-queue.json` still holds the truncated string, and there is no `harvest.js`
> subcommand to repair stored text (`sync`/`select`/`unselect`/`batch`/`skip`/`unskip`/`votes`
> only). The entry is now marked ingested so it will not be re-selected, but **the same class of
> truncation will recur on any candidate harvested from a title ending in a quoted phrase.** I did
> not hand-edit the queue, per the standing rule."

This is the system working — it refused the queue edit, disclosed the record edit, and bounded it.
But item **67** exists to decide exactly this, and a routine has now answered half of it. Either
ratify the narrow repair as policy or reverse it before #772 merges (~13:00 UTC today).

### 5.2 #762 merged in the report blind spot — second consecutive night

`daily-report.js:263` calls `listMerged(SINCE)` where `SINCE` is the UTC calendar day, so a PR
merged after one report and before the next day's 00:00Z boundary is invisible to both. #752 hit
this on 09-03; #762 hit it on 09-04 at 20:20:38Z. Item **4**, 29th day, no longer theoretical on
either side.

### 5.3 The merge pass demoted the content PR behind three one-line shards — third night

The 10:01 UTC shard, verbatim:

> "merged #771 #773 #774 · re-poll 1/4: 3 PR(s) with mergeability not yet computed — waiting 6s ·
> #772 rebuilt and pushed — merges next pass, once verify is green on the new head"

`verify` completed **success** on #772's new head `331cab55` at 10:02:21Z, so the cost is one pass
rather than a day this time. Item **64**, third night running. Two small credits: the verdict came
*before* the re-poll chatter this time (item **65** did not recur), and the pass rebuilt the PR
itself rather than stalling.

### 5.4 An egress-blocked link was removed rather than kept unread

The wave's second high finding, in its own words:

> "That host is blocked at the host level by this environment's egress policy (CONNECT refused even
> to the site root), so I never read it and could not confirm it supports the claim pinned to it.
> **Link removed**; the fact is kept and re-anchored to QI, which transcribes the exchange and was
> fetched in full. Blocked ≠ dead, but an unread link is not corroboration."

The host is `hansard.parliament.uk`. That is the right call and it is also a third host for the
allowlist question in carried item **13**.

### 5.5 A `verified` record that was never page-verified — the wave flagged its own verdict

> "**No primary text of *The Four Loves* was read directly.** It is a 1960 in-copyright book;
> archive.org's copy is lending-restricted and its full-text search endpoints, HathiTrust, and Google
> Books' API (quota) were all unreachable from here. The attribution rests on Wikiquote's **sourced**
> section, which is the anchor the pipeline names, and the page claims no page number — only ch. IV.
> Flagging this because `verified` is a claim about depth: it is well-founded here, but it is
> Wikiquote-anchored rather than page-verified."

There is no written rule saying whether that clears the `verified` bar. New item **69**.

### 5.6 Nested quotes in `claimReviewed`, reported not fixed

> "`claimReviewed` renders nested quotes (`said: \"…\"Check Enclosed\"\"`) when a quote ends in a
> quoted phrase. This is pre-existing shared-template behaviour across many records, not introduced
> here, so it is reported rather than fixed."

Correct scoping — it is a generator issue and the wave does not touch `tools/`. New item **68**.

### 5.7 Two things the wave could not establish, and did not guess

- **Henry Clapp's birth year.** "There is no standalone English Wikipedia article (the name redirects
  to *The Saturday Press*), and nothing I fetched gives it… No date was guessed."
- **Renewal status of the 1932 AP report and the 1958 *Reader's Digest* printing.** Not researched;
  the Parker page "asserts no rights status at all rather than guessing".

### 5.8 The stale-`main` exception fired on both routines that ran preflight today

The wave: preflight failed on `git on main` alone, exception (b) applied, and "the checkout alone
would have moved the tree **10 commits backward**, exactly as that exception warns". **This report's
own preflight hit the identical state four hours later, 13 commits behind.** Two for two. Item **12**
is the root cause and is on its 22nd day.

### 5.9 #670 is still stuck, tenth day

`mergeable_state` confirmed **`dirty`** directly against GitHub. (The tool's JSON said `UNKNOWN` and
its readable run said `DIRTY` a few seconds later — that is GitHub computing mergeability lazily
between two invocations, the same thing the merge pass logged, not a tool defect.) 9 days open.

---

## 6. Needs a decision

**Two new (68, 69). Three carried items MOVED: 67 has been half-answered by a routine, 4 has now
been demonstrated on two consecutive nights, and 12 fired on both routines today. If you do nothing
else, do 67 and 43.**

67. **Rule on the truncated-queue-text repair — the wave has already made a narrow version of the
    call.** It restored the lost closing quote in the record's `displayQuote` (slug byte-identical,
    nothing else touched) while `data/harvest-queue.json` still holds the corrupt string and no
    `harvest.js` subcommand can fix it. Doing it is: ratify that record-level transcription repair
    as policy and add a queue-repair subcommand, or reverse the edit before #772 merges (~13:00 UTC).

43. **Flip GoatCounter → Settings → "Dashboard viewable by" → Anyone.** *(9th day.)* The endpoint
    returns 403 "the dashboard is no longer public", so this report has carried no traffic figure for
    nine days and cannot carry one tomorrow. One setting in the GoatCounter UI; no data is lost.

4.  **Fix the report window.** *(29th day — second consecutive night it hid a merged wave, this time
    #762's 4 records at 20:20:38Z.)* `daily-report.js:263` windows `listMerged` on the UTC calendar
    day. One change — window from the previous report's timestamp — on its own branch.

69. **Rule on whether a Wikiquote *sourced-section* anchor is enough for `verified` on an
    in-copyright work.** #772's C. S. Lewis record is marked `verified` although no primary text was
    read — every route to the 1960 book was closed — and the wave flagged this itself. Doing it is:
    write the rule into `workflows/` (either "verified requires primary text" plus a new verdict tier
    for this case, or an explicit carve-out), then count how many shipped `verified` records rest on
    the same anchor.

68. **Fix nested quotes in `claimReviewed`.** A quote ending in a quoted phrase renders
    `said: "…"Check Enclosed""` in the JSON-LD, across many already-shipped records. Doing it is one
    quoting change in `tools/template.js` on its own branch, plus a corpus count of affected records.

64. **Make the merge pass merge content PRs before routine-log PRs.** *(3rd day.)* #772 was demoted
    behind three zero-content shards again; `verify` is green on the rebuilt head so the cost is one
    pass, not a day. Doing it is one sort key in `tools/merge-run.js`'s planner, plus a test case.

42. **Rebuild and merge #670, or close it.** *(10th day; confirmed `dirty`, 9d open.)* Closing loses
    the research but no content — its five candidates are back at `queued`. Doing it is: merge `main`
    in, take `main`'s side on generated files, `node tools/build.js`, `node tools/scan.js`, push — or
    close it and let a later wave redraw them.

12. **Root-cause the stale local `main`, or ratify the exceptions in `preflight.js`.** *(22nd day —
    fired on 2 of 2 routines today, 10 and 13 commits behind, § 5.8.)* Doing it is: find what leaves
    the container detached with a stale `main` ref, or accept it and write exceptions (a) and (b)
    into `preflight.js` itself so no routine has to reason about them.

18. **Fix `daily-report.js:194`'s `work` figure.** *(13th day — today it again credited `daily-merge`
    with "4 record(s)" when zero records merged.)* It sums `processed`, which the merge pass uses for
    PRs. Doing it is one field rename or a per-routine label.

6.  **Make `prep-wave.js:206` stamp `creditedTo` when `confidence === 'disputed'` regardless of name
    equality.** *(19th day — **inert tonight**: all four disputed records in #772 carry `creditedTo`,
    checked on the branch.)* Doing it is one condition in `tools/prep-wave.js` on its own branch,
    plus a count of already-shipped disputed-self-magnet records carrying the contradiction.

13. **Extend the egress allowlist — now three hosts.** *(25th day.)* Add `hansard.parliament.uk` to
    `bioguide.congress.gov` and `finance.senate.gov`; today's wave had to delete a Hansard link it
    could not read (§ 5.4). Doing it is an allowlist entry per host.

5.  **Declare which carried items you are not going to do.** *(13th day.)* The table below is 42
    rows. Reply with the numbers to strike and tomorrow's report carries only what survives.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 2 | Stop the daily wave and the backlog waves colliding over the queue | 12th |
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" — **57 is its mirror image** | 12th |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 12th |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 18th, clock running |
| 9 | Fix the middle-initial hub fork at the generator, scoped to `schema.creator` | 11th |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row | 11th |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 11th |
| 14 | Decide the `isBasedOn → isPartOf` bulk migration (1,046 of 1,752 measured 08-24) | 13th |
| 15 | Decide what to do about Google Books' quota — **blocked the Lewis verification tonight, § 5.5** | 14th |
| 16 | Stop one source's outage hard-stopping the review pass | 17th |
| 17 | Fail the build when `Quotation.creator` is suppressed; normalise `template.js`'s hero match — **folded into 58** | 17th |
| 19 | Decide whether `perf-check` belongs in the routine census | 15th |
| 20 | Settle the branch-name / draft conflict between the harness and the routine docs | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict — **56 is this item live on the site; 68 is a separate quoting defect in the same field** | 14th |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason — **62 is its sibling** | 15th |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — cannot see unmerged PRs | 19th |
| 24 | Work the disputed records with no `creditedTo` — 7 of 10 `magnet` rows still render the false-credit ✕ | 25th |
| 25 | Do the measured `mis-kind.js` pass — **61 is one line of this** | 24th |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) | 18th |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 18th |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 18th |
| 30 | Decide `verbatimNote()`'s "Verbatim:" label corpus-wide (~370 records) | 18th |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — **58 is queued behind it** | 20th |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate — **67 is the second instance** | 21st |
| 33 | Decide the house convention for characters with no HTML entity | 17th |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 19th |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — **folded into 58** | 19th |
| 36 | Make the **wave** log a cost figure — tonight's shard again carries none (§ 7) | 21st |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 22nd |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 22nd |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 18th |
| 40 | Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`) — **today's runs landed at 03:10 and 10:01 UTC** | 10th |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 10th |
| 44 | Teach `merge-gate.js` to look at what actually changed (`:98` DIRTY→SKIP; `:123` behind→demote) — **#670 is its case; 64 is its ordering half** | 9th |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 9th |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — **54 is the ruling this needs** | 9th |
| 47 | Decide whether QI's Title Case should be normalised centrally | 9th |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 9th |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 9th |
| 51 | Make the merge pass's rebuild step fetch before it merges | 8th |
| 54 | Rule on Quote Investigator *headlines* reaching `displayQuote` (50+ records shipped this way) | 7th |
| 56 | Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog`, which is live — nobody said "(or Dog)" | 6th |
| 57 | Give the orphaned `taxes-…` candidate a way back into the queue | 6th |
| 58 | Accept or decline the `disputed-creator-name-mismatch` detector, and rule on its 99-record sweep | 6th |
| 59 | Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it | 6th |
| 61 | Fix the `mis-kind.js` comma gap — `/\bdocumented\b[^,]*\bunverified\b/i` cannot match `documented, but unverified` | 6th |
| 62 | Fix `template.js:1227`'s flat "rights held by X" in the visible copy kit | 4th |
| 63 | Ratify or investigate the out-of-band merge of #741 | 4th |
| 65 | Put the verdict before the re-poll chatter in the merge note — **did NOT recur tonight (§ 5.3)** | 3rd |
| 66 | Fix `template.js:848`'s unconditional period on `disambiguatingDescription` | 3rd |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Add `traffic.js`'s specific error to `daily-report.js`'s `tool_failures` — **folded into 43; § 4 had to shell out again** | 8th |

**Struck previously:** 28 (superseded by 59), 50, 52 (folded), 53 and 55 (done 08-30/08-31), 60 (done
09-02).

---

## 7. Cost

**No routine reported a cost figure today.** `weekly-discovery` — the only routine that has ever
logged one — is a Monday pass and did not run. Today's `daily-wave` shard carries `built: 5` and no
cost field. Carried item **36** stands, 21st day.

The standing datapoints, both from `weekly-discovery`, remain the project's only measured numbers:
**~9.5K new tokens/record** (2026-08-31, 20 records) against **~21K/record** (2026-07-29). Neither
speaks to a pass like tonight's, which re-fetched 20 external source links and read primaries in
full.

**Wasted work tonight is small and non-zero.** #772 was built, audited and pushed, then rebuilt by
the merge pass after three one-line shards merged ahead of it — one extra `build.js` run over
derived output. Because `verify` went green immediately on the rebuilt head, the cost is one merge
pass rather than a day. That is the third consecutive night item **64** has cost the wave something.

**Nothing reported an interrupted run** — no 529, no rate limit, no capacity ceiling. The
environmental hits were the GoatCounter 403 (§ 4), the `hansard.parliament.uk` egress block (§ 5.4),
and the closed routes to *The Four Loves* — archive.org lending restriction, HathiTrust 403, Google
Books quota (§ 5.5) — all documented rather than worked around.
