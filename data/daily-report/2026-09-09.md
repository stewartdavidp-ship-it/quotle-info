# Daily report — 2026-09-09

## 1. Verdict

**The night worked. All four due routines ran, `main` is green, 0 flagged, tree clean, corpus 2,157.**
The scheduled wave built 5 and is waiting on tomorrow's pass by the merge pass's own explicit
instruction. Nothing failed.

Three things to read before anything else.

1. **Yesterday's proposal 73 was half-done, and the half that was skipped is the half that loses
   data.** #792 was closed at 12:26Z on 09-08 — but the `daily-wave` shard for **2026-09-07** was
   never written to `main` first. Read from `main` today, the 09-07 daily wave **did not run**. The
   file still exists on the closed PR's branch and is recoverable *today*; it is one `git show` away.
   § 5.1.
2. **The report window hid three entire waves — 47 records.** r50 (18), r51 (19) and r52 (10) all
   merged on 09-08 *after* yesterday's report was written, so neither yesterday's window nor today's
   can see them. The corpus went 2,110 → 2,157 and **no daily report records why**. Carried item
   **4**, 33rd day, and by far its largest instance. § 5.2.
3. **A new harvest-side defect, reported by the wave rather than worked around, with a direct ask:**
   `qi-harvest` derives `magnetAuthor` from QI *article metadata*, so the person who **reported** a
   quote lands in the queue as the person it is falsely credited to. Two of five stamps in today's
   wave were wrong. The wave dropped both and asks you to sanity-check the call. New item **75**.
   § 5.3.

Traffic is unavailable for the thirteenth consecutive day, same one setting (§ 4).

---

## 2. What ran

| slot | routine | result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run: `pr` — 5 records → **#823** (shard 07:45:21Z) |
| 04:00 | `daily-reports` | ✓ 1 run: `no-op` (shard 08:12:55Z) |
| 05:00 | `daily-review` | ✓ 1 run: `no-op` — scanned 2,157, flagged 0 (shard 09:05:31Z) |
| 07:00 | `daily-merge` | ✓ 2 runs: `merged`, `merged` (03:20:21Z and 10:51:00Z) |
| 12:00 | `reports-close` | · not yet — due 17:00 UTC (yesterday's ran `no-op`, shard 18:03Z) |
| Mon 02:00 | `weekly-discovery` | · not scheduled today (last ran Mon 09-07) |

**Every expected shard for today exists. Nothing due today failed to run.**

Worth noting *where* one of them lives: today's `daily-wave` shard
(`2026-09-09T07-45-21-005Z-daily-wave.jsonl`) is **not on `main`** — it rides the unmerged `#823`
branch. `daily-report.js:109–177` reads shards off open PR branches, which is the only reason the wave
reads as ✓ rather than DID NOT RUN. That mechanism is working. **It is also exactly why § 5.1 is a
loss: it reads *open* PRs, and #792 is closed.**

`daily-report.js` reported **1 tool-call failure** — `traffic: fetch failed`. **§ 4 is the only
incomplete part of this report; §§ 2, 3, 5 are complete.**

---

## 3. What shipped

**Merged inside today's tool window (8 PRs) — 0 new records.** A ninth, **#831**, merged at 12:28Z,
after the tool ran:

| PR | what | merged by |
|---|---|---|
| #831 | `fix` — the fallback claimant must be a person, not a work, character or role (11 pages) | out-of-band 12:28Z |
| #830 | `fix` — the author-hub gate at ingest, + `verdictNote`'s context-row misattribution | out-of-band 11:42Z |
| #829 | `reports` — search-log reading, and a correction to the Sep 1 traffic figure | out-of-band 11:22Z |
| #828 | `fix(traffic)` — a flaky referrer call no longer takes down the totals | out-of-band 11:20Z |
| #827 | `docs` — the r47–r52 handoff, eight open items, measured | out-of-band 11:20Z |
| #821 | routine-log shard | merge pass 03:20Z |
| #822 #824 #825 | routine-log shards | merge pass 10:51Z |

- **Built today:** 5 records (#823, open — see below). **Merged today: 0 new records.**
- **Audited today:** 5 by the wave (3 PASS / 2 FAIL, 5 issues, 2/2 skeptic verdicts confirmed).
  `daily-review` scanned all 2,157 and returned `no-op`.
- **Reader reports:** none pending. `daily-reports` ran `no-op`.
- **Corpus 2,110 → 2,157 (+47).** **None of that landed today** — it is the three blind-spot waves of
  § 5.2. Today's merges were net-zero on record count.

**#823 is open by design, not by neglect.** The 10:51Z merge pass says so in its own shard:

> `#823 rebuilt and pushed — merges next pass, once verify is green on the new head`

`verify` came back green on the new head `fc13b6d6` at **11:51:05Z**. #831's merge at 12:28Z then put
it one commit behind, so `merge-gate.js` now reads it `REBUILD` — *"1 commit(s) behind main and
rebuilds 38 derived file(s)"*. **Still no action needed:** rebase-rebuild-push is what the merge pass
does, and it did exactly that for this branch at 10:51Z this morning. Tomorrow's pass takes it.

**#826** (the merge pass's own log shard) is likewise open and `CLEAN` for tomorrow. **#670** the gate
reads `SKIP` — *"merge conflict — needs a human, do not auto-resolve generated files"*, which is item
**42** stated by the tool rather than by me.

**Opened and merged after the tool's snapshot: #831** — `fix: the fallback claimant must be a person,
not a work, a character or a role`, closing item 9 of the handoff. Opened 12:20:58Z, `verify` green on
`92e08d33` at 12:21:36Z, **merged out-of-band at 12:28:53Z**, while this report was being written.
`daily-report.js` ran three minutes before it existed, so it appears in no table the tool produced —
a same-day instance of the § 5.2 blind spot, and the fifth out-of-band merge of the day (§ 5.7).

> The readable output credits `daily-merge` with "4 record(s)". That is carried item **18** counting
> merged **PRs** as records — the two passes processed 1 and 3 PRs. **17th day.**

---

## 4. Traffic

**Traffic: unavailable — thirteenth consecutive day.** `traffic` is `null` and `tool_failures` carries
`traffic: fetch failed`. **This is an analytics outage, not a quiet day. It is not a zero.**

Cause unchanged, not transient, and re-confirmed twice this morning. `node tools/traffic.js` here
returns, verbatim:

> `traffic.js: 403 from GoatCounter — the dashboard is no longer public.`
> `Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.`

**One thing has changed and it sharpens the diagnosis.** #829 read GoatCounter successfully at
**11:21Z today** and published a full daily series from it (see below). The same endpoint returned 403
to this pass at **12:21Z and again at 12:22Z**. So the data is reachable *with* the authenticated
path #829 used, and unreachable on the anonymous path this report is restricted to. The setting flip
in item **43** remains the fix; nothing has been lost in the meantime.

**The last figure the project actually has, from #829 (merged today), is the one to trust** — and it
retracts a number this report previously carried:

> Records 563 visits for the last 7d, and states plainly that the **942 logged on 2026-09-01 must not
> be read as a trend point**.

Its measured series, D..D+1 windows, visits per day:

```
Jul 9-31       55/day
Aug 1-14      147/day
Aug 15-25      63/day
Aug 26-Sep 1  218/day
Sep 2-9        81/day
```

In its words: *"Noise around a ~80/day baseline, not the 33→130 growth curve the Sep 1 reading
claimed. Aug 26–Sep 1 was a **two-day spike** (Aug 28 = 817, Aug 27 = 256) on a flat baseline — 573
the week before it, 563 the week after."* These are **upper bounds** — unfiltered for referrer spam,
which ran ~37% of attributed referrals on 09-01.

**The site is flat at roughly 80 visits/day. It is not growing.** Any earlier reading of growth in
these reports was an artifact of dividing by a moving denominator.

---

## 5. Findings

### 5.1 The 2026-09-07 wave's run-record is now orphaned — proposal 73 was executed half-way

Yesterday's decision item **73** read, in full: *"Close #792, but re-record the 2026-09-07 wave
first."* **#792 was closed at 2026-09-08T12:26:19Z. The shard was not written.**

Confirmed by direct check of `main`: the `2026-09-07` shards present are `daily-merge` ×4,
`weekly-discovery`, `daily-reports`, `daily-review`, `daily-report`, `reports-close`. **There is no
`daily-wave` entry.** A wave that ran, built five records and produced a full audit reads from `main`
as never having fired — which is the precise *"a broken night looks like a quiet one"* failure the
routine log exists to prevent.

**It is recoverable right now, and cheaply.** The branch `wave-d20260907` still exists at
`6e6d0d2f`, and the file is intact on it:

```
data/routine-log/2026-09-07T07-44-35-244Z-daily-wave.jsonl
```

Its content is complete — `built: 5`, the PR link, the audit line (*"3 PASS / 2 FAIL, 4 issues, 0
blockers; 2 of 3 high findings survived skeptic re-check"*), and its could-not-establish notes. One
`git show` from that branch onto a fresh branch restores it exactly, with no reconstruction and no
judgement call. **This stops being free the moment that branch is pruned.**

Note also what this exposes about the census: `daily-report.js` reads shards from **open** PR
branches. A closed PR's shard is invisible to it forever. Nothing today re-derives that; it is stated
so the same hole is not walked into on #670.

### 5.2 Three waves — 47 records — are invisible to every daily report

`daily-report.js:263` windows `listMerged` on the UTC calendar day. Yesterday's report was written at
~12:27Z on 09-08. Everything merged on 09-08 after that falls between the two windows:

| merged (UTC) | PR | records |
|---|---|---|
| 13:40Z | #815 `wave(r50)` — two QI headlines skipped, 72/72 audit issues fixed | **18** |
| 15:11Z | #816 `wave(r51)` — `--credited` omitted, 4 blockers the audit caught | **19** |
| 16:04Z | #810, #817 routine-log / reports-close shards | — |
| 20:46Z | #818 merge pass shard | — |
| 21:04Z | #820 `wave(r52)` — three Senecas merged onto one hub, 28/28 fixed | **10** |
| 23:13Z | #819 merge pass shard | — |

2,110 + 18 + 19 + 10 = **2,157**, which is exactly today's corpus count. So the entire +47 is
accounted for, and **not one record of it appears in any daily report** — yesterday's was already
written, and today's window starts at 00:00Z on 09-09.

Yesterday this item hid a 20-record audit and was called "live again". Today it hid three complete
waves including r52, whose Seneca hub collision is the origin of the fix that merged this morning as
#830. The reports are now systematically blind to the repo's largest content days, because operator
backlog waves run in the afternoon and the report runs in the morning. Carried item **4**, **33rd
day**.

### 5.3 The harvester puts the *reporter* of a quote into `creditedTo` — 2 of 5 in one wave

Today's wave reported this rather than editing `tools/`, and was explicit that it is out of a content
PR's lane:

> `prep-wave.js --credited` stamped `creditedTo` on three records. I removed two before ingest,
> because the research does not support them and the field publishes a **false-credit claim about a
> named real person** on the author pages.

The two it dropped, in its words:

> **Lewis H. Lapham** — QI's earliest citation *is* his 1978 `Harper's` column, but it already credits
> Vidal, and QI "nowhere suggests he coined it". A web check found the saying credited to Vidal
> everywhere and to Lapham nowhere.
>
> **Walter Winchell** — his 3 Jan 1944 column names Parker, names her review, and quotes her. Again no
> evidence of real-world misattribution.

And the root cause it names:

> Both appear to be **harvest artifacts**: `qi-harvest-2026-08-27.json` derives `magnetAuthor` from QI
> article metadata, so the person who *reported* a quote can land in the queue as the person it is
> falsely credited to. Two of five in this wave. Worth a look at the harvester, but that is a
> `tools/`-lane change and does not belong in a content PR.

It kept the third (Depew) because *"Wikiquote does list the line under him"* — so the guard is not
uniformly wrong, it is unvalidated. This is **adjacent to but distinct from** handoff item 5 (QI
*headlines* in the queue): same metadata source, different field, and this one publishes a defamatory-
shaped claim about a real person rather than an unbuildable candidate. New item **75**, and it carries
a direct request for a human ruling on the two drops.

### 5.4 The `creatorOk` suppression bug fired for real — as a RULE violation, caught by hand

Carried item **17** (folded into **58**) has been "fail the build when `Quotation.creator` is
suppressed; normalise `template.js`'s hero match" for twenty days. Today it shipped a blocker:

> The page emitted **no `Quotation.creator` at all** and a `disambiguatingDescription` reading
> *"Commonly misattributed to Antoine de Saint-Exupéry. The true author is not established; no creator
> is asserted here."* — flatly contradicting its own prose, which says the thought **is** his, and
> violating the CLAUDE.md Schema.org RULE.

The trigger was a single missing accent: `schema.creator.name` written *Antoine de Saint-Exupery*
against `answer.authorName` carrying the é, so `creatorOk`'s name match failed and **silently dropped
the whole node**. The wave fixed its own record.

**The point for this report is what caught it.** Nothing did — no gate, no scan, no validator. It was
found because the audit read the *built artifact* rather than the record. A one-character difference
between two fields silently voids the site's single hardest RULE, and today it produced a page that
asserted the opposite of its own prose. Item **17/58** is no longer theoretical.

### 5.5 #830 shipped two fixes and *refused* the third the handoff asked for — measured, not asserted

The author-hub gate is now enforced at ingest, calibrated the way this repo asks for:

> **Calibrated on all 2,157 records, every pair read by hand: 12 hits.** Four were real splits, merged
> here […] The other eight are genuinely different people sharing a name prefix (the two Bushes, the
> two Rockefellers, the two Coveys, Martin Luther and MLK, Jean Paul and Sartre, the two John
> Gardners) and are listed in `DISTINCT_HUBS` with a note on how each pair differs.

It also declined half of what the handoff proposed, and showed the working:

> It also proposed gating on `wordingDrift` "for symmetry with `claimant`". Implemented and reverted
> […] It suppressed **"Actually by John D. Rockefeller"** and **"Actually by Dolly Parton"** — both
> true — on pages whose only drift was punctuation and case.

And it corrected the handoff's own measurement rather than repeating it:

> The handoff reported 59 records. 148 match that description today, but **122 carry a `creditedTo`
> and never reach the line**. Exposed count is **26** […] The old filter measured how often the tag is
> used, not how often the defect can ship.

**This is the system working.** Two handoff items closed, one declined with evidence, one stale figure
retired, and a new item (9) raised from the measurement — now itself addressed by #831, opened at
12:20Z. No decision needed on any of it.

### 5.6 `schema.verdictNote` still cannot be emitted by any wave — second wave in two days

Item **74** was raised yesterday. Today's wave hit the same wall and used the same workaround:

> **High — `a-gift-book-is-an-item-which-you-wouldnt-take-on-any-other`.** […] Fixed with
> `schema.verdictNote`, the sanctioned hand-set override.

Re-confirmed here: `verdictNote` appears **zero** times in `workflows/generate.js`, and
`SCHEMA_BUDGET` is still `4072` at `tools/verify-corpus.js:557`. So every use remains a hand-edit at
the fix stage, and the field cannot be added without spending budget that CLAUDE.md records as having
zero headroom. Two consecutive waves have now needed it. Item **74**, 2nd day, and item **59** is what
gates it.

### 5.7 Five of today's nine merges bypassed the merge pass — all five with `verify` green

The merge pass's shards account for four PRs: **#821** (03:20Z) and **#822 #824 #825** (10:51Z). The
other five — **#827 #828 #829 #830 #831** — merged between 11:19Z and 12:28Z, seconds after their PRs
opened.

**Checked before reporting it as a risk. `verify` completed green on each merged head first:**

| PR | head | verify green | merged |
|---|---|---|---|
| #827 | `3dd40142` | 11:20:08Z | 11:20:30Z |
| #828 | `6eb04da8` | 11:20:38Z | 11:20:56Z |
| #829 | `5c9af0d9` | 11:22:10Z | 11:22:20Z |
| #830 | `5be067cb` | 11:42:21Z | 11:42:33Z |
| #831 | `92e08d33` | 12:21:36Z | 12:28:53Z |

Nothing shipped unverified. #830 is worth a specific note *in its favour*: its first head `c3aacc27`
**failed** `verify` at 11:41:03Z, was corrected, and only the green head was merged — the gate did its
job on the one PR of the four that changed published content.

What has not changed is the governance gap. #670's body still says *"Not merging this; the 07:00 pass
is the single merge authority"*, and that is now false on five of nine merges today and nine of
fourteen yesterday. Carried item **63**, 8th day. Either the docs or the practice should move; the
evidence is that the practice is not going to.

### 5.8 The stale-`main` exception fired again, third distinct shape in three days

Preflight failed on `git on main` alone, with `local main sane`, `tree clean` and `current with
origin/main` all passing — documented exception **(b)**. Ran both printed commands; the local `main`
ref was **7 commits behind**. Re-ran preflight: 10/10 green, nothing lost.

The shape keeps moving: **(b) at 29–32 commits** on 09-07, **(a)** on 09-08, **(b) at 7 commits**
today. Yesterday's report argued the instability favours root-causing over ratifying, and a third
distinct shape in three days strengthens that. Item **12**, 26th day.

### 5.9 #670 is still stuck, fourteenth day

`DIRTY`, 13 days open, untouched since 2026-08-27, base `bf9a772f`. Its five slugs are on no other
branch — two Thornton Wilder records resting on primary text read in full, three Branson records
honestly marked `attributed`. **Unlike #792, closing this one loses content.** Item **42**.

Given § 5.1, the loss is now larger than the five records: closing it without action also orphans its
`daily-wave` shard for 2026-08-27, which the census can only read while the PR is open.

---

## 6. Needs a decision

**One new item (75). 73 is the one to do today — it is the only item here with a deadline, and the
deadline is whenever that branch gets pruned.**

73. **Write the 2026-09-07 `daily-wave` shard to `main` — proposal 73 was closed without it.** #792
    was closed on 09-08 and the shard never landed, so `main` now says the 09-07 daily wave did not
    run. Doing it is: `git show origin/wave-d20260907:data/routine-log/2026-09-07T07-44-35-244Z-daily-wave.jsonl`
    onto a fresh branch, commit, PR — no reconstruction, the file is intact and complete.

4.  **Fix the report window.** *(33rd day — its largest instance yet: 47 records across r50, r51 and
    r52 merged on 09-08 after the report ran and appear in no daily report at all.)* Doing it is:
    change `daily-report.js:263` to window from the previous report's timestamp instead of the UTC
    calendar day, on its own branch.

75. **Rule on the harvester's `magnetAuthor`, and sanity-check the wave's two drops.** *(New.)*
    `qi-harvest` takes `magnetAuthor` from QI article metadata, so the person who *reported* a quote
    can be stamped `creditedTo` — a false-credit claim about a real person — and 2 of 5 stamps in
    today's wave were wrong. Doing it is: confirm the Lapham and Winchell drops were right, then
    decide whether `qi-harvest.js` can distinguish reporter from magnet or whether `--credited` needs
    a human gate.

43. **Flip GoatCounter → Settings → "Dashboard viewable by" → Anyone.** *(13th day.)* The anonymous
    endpoint returns 403, so this report has carried no traffic figure for thirteen days. One setting
    in the GoatCounter UI; no data is lost, and #829 proves the data is intact behind it.

17/58. **Fail the build when `Quotation.creator` is suppressed, and normalise `template.js`'s hero
    match.** *(20th day — fired for real today: one missing accent silently voided the node and shipped
    a page contradicting its own prose, violating the CLAUDE.md Schema.org RULE.)* Doing it is: make
    `creatorOk` compare on a normalised (accent-folded) name, and add a build assertion that a record
    with an `answer.authorName` cannot emit a page without a `creator`.

74. **Decide whether `schema.verdictNote` gets into `DOSSIER_SCHEMA`.** *(2nd day — a second
    consecutive wave had to hand-add it at the fix stage.)* Doing it is: find the bytes inside the
    4,072-byte `SCHEMA_BUDGET` ceiling (item **59**) or raise the ceiling, then add the field.

42. **Rebuild and merge #670, or close it.** *(14th day; `DIRTY`, 13d open.)* Closing it loses five
    records that exist nowhere else **and** orphans its 08-27 wave shard the way #792's was. Doing it
    is: merge `main` in, take `main`'s side on generated files, `node tools/build.js`,
    `node tools/scan.js`, push — or close it and accept both losses knowingly.

63. **Ratify or stop the out-of-band merges.** *(8th day — 5 of today's 9, all `verify` green; one of
    them landed while this report was being written.)* Doing it is: update the routine docs, which
    still call the 07:00 pass "the single merge authority", or stop merging outside it.

72. **`tools/review.js:267` reads rights from `meta.rights`/`rights`, but all records store it at
    `source.rights`, so every audit has seen `null`.** *(3rd day — untouched.)* Doing it is: change
    that one fallback chain, then decide separately whether to re-audit the rights claims never
    checked.

12. **Root-cause the stale local `main`, or ratify the exceptions in `preflight.js`.** *(26th day —
    exception (b) at 7 commits today, after (a) yesterday and (b) at 29–32 the day before; three
    distinct shapes in three days.)* Doing it is: find what leaves the container with a bad `main`
    ref, or write both exceptions into `preflight.js`.

2.  **Stop the daily wave and the backlog waves colliding over the queue.** *(16th day.)* Doing it is:
    give `harvest.js select` a way to see marks that live only on another branch, or serialise the two
    wave types by schedule.

70/71. **The 09-06 quota collision is still unfixed, and `daily-wave` / `daily-review` still fail
    silently.** Doing it is: move the routines off the contended window or add a retry; and add
    `routine_notify_push` to those two triggers.

18. **Fix `daily-report.js:194`'s `work` figure.** *(17th day — today it credited `daily-merge` with
    "4 record(s)" for 4 merged PRs.)* One field rename or a per-routine label.

36. **Make the wave log a cost figure.** *(25th day — live again today, § 7.)* One field in the wave's
    routine-log shard.

5.  **Declare which carried items you are not going to do.** *(17th day.)* The table below is 40 rows.
    Reply with the numbers to strike and tomorrow's report carries only what survives.

### Resolved today

**Handoff items 1 and 3**, by #830 — the author-hub gate now runs at ingest (calibrated on all 2,157
records, 4 real splits merged, 8 distinct-hub pairs recorded), and `verdictNote` no longer calls a
`kind: "context"` row a misattribution (3 pages). **Handoff item 9** (`looksLikePerson` accepting
works, characters and role-qualified names) closed too — **#831**, merged 12:28Z: the fallback
claimant goes from 22 live pages to 11, *"and the 11 that remain are exactly the 11 correct magnets"*,
with zero false positives measured across all 2,157 records. Also closed without a number:
`traffic.js` no longer dies when the referrer call flakes (#828), and the Sep 1 traffic figure is
formally retracted (#829, § 4).

That makes **all three of the handoff's actionable code items (1, 3, 9) closed within a day of the
handoff being written** — #827 → #830 → #831. Item 24 in the table below is largely absorbed by it.

**Struck previously:** 25, 28 (superseded by 59), 50, 52, 53, 55, 60. **73 is NOT struck** — see § 5.1.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" — 57 is its mirror | 16th |
| 6 | Make `prep-wave.js:206` stamp `creditedTo` when `confidence === 'disputed'` regardless of name equality — **75 is the opposite failure of the same guard** | 23rd |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 16th |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 22nd, clock running |
| 9 | Fix the middle-initial hub fork at the generator, scoped to `schema.creator` — **largely overtaken by #830** | 15th |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row | 15th |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 15th |
| 13 | Extend the egress allowlist | 29th |
| 14 | Decide the `isBasedOn → isPartOf` bulk migration (1,046 of 1,752 measured 08-24) | 17th |
| 15 | Decide what to do about Google Books' quota | 18th |
| 16 | Stop one source's outage hard-stopping the review pass | 21st |
| 19 | Decide whether `perf-check` belongs in the routine census | 19th |
| 20 | Settle the branch-name / draft conflict between the harness and the routine docs | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict | 18th |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason | 19th |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — cannot see unmerged PRs | 23rd |
| 24 | Work the disputed records with no `creditedTo` — **largely closed by #831: 11 of the 22 rejected, the other 11 verified as correct magnets** | 29th |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) | 22nd |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 22nd |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 22nd |
| 30/62 | Rule on "Verbatim:" and "rights held by X" corpus-wide | 22nd / 8th |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — 58 is queued behind it | 24th |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate — 67 is the second instance | 25th |
| 33 | Decide the house convention for characters with no HTML entity | 21st |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 23rd |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — **folded into 17/58, now live** | 23rd |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 26th |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 26th |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 22nd |
| 40 | Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`) | 14th |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 14th |
| 44 | Teach `merge-gate.js` to look at what actually changed (`:98` DIRTY→SKIP; `:123` behind→demote) — #670 is its case | 13th |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 13th |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — 54 is the ruling this needs | 13th |
| 47 | Decide whether QI's Title Case should be normalised centrally — **today's wave shipped 3 more Title-Cased `displayQuote`s** | 13th |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 13th |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 13th |
| 51 | Make the merge pass's rebuild step fetch before it merges | 12th |
| 54 | Rule on Quote Investigator *headlines* reaching `displayQuote` — **handoff item 5 says the true count is UNKNOWN and a title-shape heuristic failed; do not re-derive it** | 11th |
| 56 | Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog`, which is live | 10th |
| 57 | Give the orphaned `taxes-…` candidate a way back into the queue | 10th |
| 59 | Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it — **74 is waiting on this** | 10th |
| 61 | Fix the `mis-kind.js` comma gap; also add a `/fake/` rule to `REFUTATION_TAG` | 10th |
| 64 | Make the merge pass merge content PRs before routine-log PRs | not live today |
| 65 | Put the verdict before the re-poll chatter in the merge note — **recurred today** (the 10:51Z shard leads with four re-poll lines) | 7th |
| 66 | Fix `template.js:848`'s unconditional period on `disambiguatingDescription` | 7th |
| 67 | Rule on the Parker `displayQuote` repair, which shipped 09-05 | 5th |
| 68 | Fix nested quotes in `claimReviewed` | 5th |
| 69 | Rule on whether a Wikiquote *sourced-section* anchor is enough for `verified` on an in-copyright work | 5th |
| — | Add the token-subset test's false-alarm mode (`Edmund Kean → Edmund Gwenn`) to the #801 note | 2nd |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Add `traffic.js`'s specific error to `daily-report.js`'s `tool_failures` — folded into 43 | 12th |

---

## 7. Cost

**No routine reported a cost figure today.** `daily-wave` ran a full 5-record pipeline serially and
logged none — item **36**, 25th day. `weekly-discovery`, the only routine that has ever logged one,
was not scheduled today.

The series still stands at three measured points, all `weekly-discovery`: **~21K/record**
(2026-07-29), **~9.5K/record** (2026-08-31), **~16K/record** (2026-09-07).

**Interrupted runs: none.** No routine was rate-limited, rejected, or cut short. The environmental
hits were the GoatCounter 403 (§ 4) and, inside the wave, Quote Investigator's Cloudflare block —
*"`curl` gets 403 (Cloudflare) and the Wayback CDX has no snapshots"* — which the wave worked around
by cross-checking every QI fact across two or three independently-worded WebFetch passes rather than
asserting anything from a single read. Chronicling America was also unusable for phrase search
(*"returned 3.2M hits for a quoted phrase, i.e. it was not honouring the quoting"*). Neither stopped a
run, and both are recorded on the pages rather than papered over.
