# Daily report — 2026-08-21

**All four due routines ran, nothing failed, and nothing needs urgent attention.** The wave built its
full 5 — five attribution corrections (3 fake-Buddha lines, 2 Galileo misattributions) with **0
blockers** in audit — `daily-reports` and `daily-review` were both clean no-ops, and the merge pass
merged 6. Corpus **1,737 · 0 flagged · CI green on `main` · tree clean · 0 deferred**.

The one thing worth reading standing up is not a failure but a stall: **nothing under `tools/` or
`workflows/` has changed since 2026-08-18**, and that commit was a docs bump *recording* the `plain()`
bug, not fixing it. So **all 24 decisions below are open exactly as they were**, and two of them
(egress, the report window) were paid for again this morning in citation quality and lost reporting.

The report tool reported **no tool failures**, so the mechanical picture below is complete.

---

## 1. What ran

| Slot | Routine | Result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run — PR **#586**, **5 records**, `verify` green, **not yet merged** |
| 04:00 | `daily-reports` | ✓ 1 run — no-op (queue empty) |
| 05:00 | `daily-review` | ✓ 1 run — no-op (scanned **1,737**, flagged **0**) |
| 07:00 | `daily-merge` | ✓ **4 runs** — merged **6 PRs** |
| 12:00 | `reports-close` | · not yet due (17:00 UTC) — ran yesterday 2026-08-20, no-op |
| Mon 02:00 | `weekly-discovery` | · not scheduled today (Friday) |

**Nothing is missing.** Every routine that was due fired and left a shard. No routine reported an
error, and none stopped at preflight — the first day in three with no hard stop anywhere.

## 2. What shipped

- **6 PRs merged in the window** (#582, #583, #584, #585, #587, #588). **Every one is a routine-log
  shard.** No content shipped today.
- **Corpus: 1,737 records · 0 flagged · CI `success` on `main` (`d1b59bbb8`) · tree clean · 0 deferred
  · 0 unjudged runs · report ladder `pr`.**
- **Two PRs open, both the 07:00 pass's business, not this one's:** **#586** (`wave-d20260821`, CLEAN,
  `verify` **success** on head `3665212` at 09:36:57Z) and **#589** (`merge/2026-08-21`, CLEAN).
- **The 5 records built this morning are still in #586.** Corpus goes to **1,742** when it merges;
  harvest backlog **355 → 350**.
- Yesterday's projection reconciles exactly: yesterday's report said *"Corpus goes to 1,737 when
  [#573] merges"*; #573 merged at 11:33 ET and the tool reads **1,737** today.

---

## 3. Findings

### F1 — Egress blocked the specialist catalogue anchoring all three Buddha lines, and the block list contradicts yesterday's

The wave's anchor source for the three fake-Buddha records was unreachable, in its own words:

> **The egress policy blocks `fakebuddhaquotes.com`** — the specialist catalogue that is the anchor
> for all three Buddha lines. […] I read the three relevant pages through their Wayback captures and
> **cite the captures**, not the live URLs, since those are what I actually fetched and what an
> auditor can re-fetch from this container. `plumvillage.org`, `suttacentral.net` and
> `accesstoinsight.org` are blocked the same way and are cited the same way.

Also unreachable this run: *"`google.com`, `books.google.com/books`, the Google Books API (quota
exhausted), `openlibrary.org`, `jstor.org` and `hathitrust.org` […] Archive.org's search-inside is 403
for lending items."* The wave did not hide the consequence — three published pages will cite Wayback
captures rather than the live specialist source, and one record ships with **no page number** because
of it (see F5).

**The part that is new:** yesterday's wave reported `web.archive.org` as **policy-denied (403 at the
proxy gateway)** and listed it alongside `margaretthatcher.org` and PolitiFact. Today's wave reports
the opposite — *"Wikipedia, Wikiquote, Wikisource, Project Gutenberg, archive.org, web.archive.org and
Quote Investigator all reach fine"* — with the caveat that *"`web.archive.org` returned intermittent
503s ('temporarily offline') throughout; every fetch was retried with backoff until a real 200, and no
claim rests on a fetch that did not succeed."* Two routines, two consecutive days, opposite verdicts
on the same host. Today's whole Buddha citation chain **depends** on Wayback being reachable, so this
is not a bookkeeping detail: the egress picture is not stable enough to write an allowlist request
from without measuring it first. That is now folded into decision 11.

### F2 — Preflight exception (b) fired on a genuinely stale ref today — the 2026-08-16 correction earned its keep

`git on main` was this report's **only** failing check, with `local main sane`, `tree clean` and
`current with origin/main` all **passing** — the exact condition for exception (b). Running both
printed commands, the fast-forward moved **3 commits**:

```
Switched to branch 'main'
Your branch is behind 'origin/main' by 3 commits, and can be fast-forwarded.
```

This is the first time in this report series that the gap has been observed live and non-zero.
`DAILY-REPORT.md` warns that `local main sane` passes on a ref arbitrarily far behind because "sane"
only means *ancestor of* `origin/main`, and that `git checkout main` **alone** would move the tree
backward onto it. Today it would have moved the tree back 3 commits and this report would have been
written against a `main` that did not include #585, #587 and #588. **The corrected two-command remedy
is what prevented it.** The stale-ref exception is no longer theoretical bookkeeping.

Three of four preflighted passes hit it again today, on the **7th consecutive day**:

| Pass | Failing checks | Start state | Exception |
|---|---|---|---|
| `daily-wave` (#586) | `local main sane` + `git on main` | detached | **(a)** — `git checkout -B main origin/main`, re-run 14/14 green |
| `daily-review` (#588) | `git on main` only | detached on `claude/wizardly-fermat-ng81hr` | **(b)** — both commands, "Already up to date", re-run 14/14 green |
| `daily-reports` (#587) | none | clean and level | none needed |
| this report | `git on main` only | detached `on HEAD`, local `main` **3 behind** | **(b)** — fast-forward moved 3 commits |

Every one applied the exception exactly as printed, none improvised. The review pass states the open
question plainly: *"The underlying question the doc flags — what leaves the container detached — is
still open; this run adds one more instance of it."*

### F3 — The report window swallowed a fourth consecutive wave merge, and will swallow today's

`tools/daily-report.js:30` still windows on calendar day. Six PRs merged between yesterday's report
(08:13 ET) and today's window opening (00:00 UTC), reported by neither:

| PR | What | Merged |
|---|---|---|
| **#573** | **Wave d20260820 — 5 records (3 Thatcher, 2 Buddha → Thích Nhất Hạnh)** | 11:33 ET |
| #576, #578, #579, #581 | merge-pass log shards | 08:48–17:27 ET |
| #580 | reports-close no-op log | 14:38 ET |

The arithmetic still reconciles (1,732 + 5 = 1,737), so nothing is *lost* — but **four waves, twenty
records, four days, and not one wave's actual merge has appeared in any report.** Today repeats it
deterministically: **#586 is green now** (`verify` success at 09:36:57Z) and the merge pass runs
roughly every three hours, so it merges at ~12:45 UTC — after this file is written, before the window
rolls at 00:00 UTC.

### F4 — The wave: 14 issues, **0 blockers**, and the two `high` findings were both self-inflicted and both caught

**3 PASS / 2 FAIL · 14 issues · 0 blockers · 2 skeptic re-checks, both confirmed. Twelve of fourteen
fixed, two refused.** No blockers is the best audit result in this report series. Both `high` findings
were the wave's own errors, found before ship:

> **An invented date.** The rights note on #2 said Nhất Hạnh's books were published "from 1988
> onward." Nothing I consulted says 1988 — the only edition evidence held is a 1996 Parallax revised
> edition. Deleted. This is the exact failure mode the runbook names.

> **A quoted OCR reconstruction.** #5 printed a marginal gloss from the Bridges edition inside
> typographic quotation marks as "The key… [of the] sciences is mathematic." The scan's OCR of that
> gloss actually reads `The key / greitgj. / sciences is / mathe- / nmtic.` — the quoted string was a
> repair presented as a transcription of a period edition. The gloss is now described, not quoted.

The mediums are the same family: *"a citation that fused Bodhipaksa's page number with Wikipedia's
edition (two sources, one claim)"*; *"'the discourse he was paraphrasing **is** SN 12.31' where the
specialist writes 'seems to be'"*; *"'earliest attribution traced' where Wikiquote only says de
Santillana attributed it"*; *"'his copy' of Morin where the source says 'a copy'"*; and an unverified
claim about the Schaff *City of God* translation **dropped because a rights claim was resting on it**.

Two refusals, both defensible: the editor-mechanism hedge (*"the page already frames it as
'Bodhipaksa's reconstruction,' which carries the hedge"*) and the exhaustive negative *"not in the
Pali Canon,"* which *"is the anchor specialist's own conclusion and adopting it is what the pipeline
prescribes."*

Content quality is high and independently verified rather than relayed. On the Roger Bacon line:
*"Not relayed from Wikiquote: I pulled the Bridges 1897 Latin edition and found the sentence itself,
and located **p. 97** from the scan's own running heads."* On the Galileo line, the wave traced the
real phrase to Thomas Browne's *Religio Medici* (1643) and confirmed Browne's "honest Father" is
Augustine from *"the Gutenberg edition's own endnote […] independently confirming Wikiquote's chain."*

All five are misattribution-seam records — *"the seam `workflows/README.md` flags as overdue. Four
waves running had drawn essentially none."* Two consecutive days of misattribution draws now.

### F5 — Two records ship with no identified author, and three assert no rights status

By design, and stated on the pages:

> **Two records have no identifiable author** (#3, #4) and use `Unknown`, following the existing
> corpus precedent. `build-authors.js` deliberately mints no hub for that name and `template.js`
> renders it unlinked, so nothing dangles.

On *"True love is born from understanding"* the wave declined the obvious credit: *"It sounds like
Nhất Hạnh and the temptation was to credit him; the specialist who knows that literature could not
find it in any of his books, so the page names no author and asserts no rights status. Crediting on a
stylistic resemblance is how the Buddha got this one."* Three of five assert no rights status;
one (#2) ships **without a page number** because *"Bodhipaksa names '2008's Breathe, You Are Alive';
the only scan available is a 1996 Parallax revised edition. The page says so rather than guessing."*

This is the pipeline behaving correctly, but it grows the population behind decisions 17 and 21 by two
more `Unknown`-creator records and three more unasserted-rights records.

### F6 — Yesterday's preflight-latency stop did not recur, but nothing was fixed

`daily-review` reported *"`node tools/preflight.js --routine review` — 14/14 green before any work"*
— the `reach www.gutenberg.org` check that killed the entire review pass yesterday passed cleanly
today. Nothing changed in `tools/preflight.js`; the slow host simply answered in time. Decision 1
remains open and latent.

The review pass otherwise: *"1737 records, 6 detectors, incremental: ran 0 checks, skipped 10422
already settled, **flagged: 0**, state unchanged"*, plus `vocab-sweep` clean — *"6 vocabulary tables
over 1737 records, **0 under-used keys**, no warnings."* It correctly refused to manufacture work:
*"the doc is explicit that manufacturing work on a clean day is the failure mode, not the no-op."*

One deviation, correctly reasoned and worth recording: *"Branch is `review/2026-08-21`, not the
session's assigned `claude/…` branch. The scheduled prompt names a `claude/` development branch, but
`DAILY-REVIEW.md` requires the `review/` prefix because `tools/merge-gate.js` fails closed and would
classify any other prefix as `HUMAN` forever."*

### F7 — `daily-reports`: an empty queue, handled as one GET and no agents

> None. `node tools/review.js reports` printed `no pending reader reports` — the public
> `GET /reports/pending` answered normally with an empty queue (not an endpoint error).

15/15 preflight green, no `report-gate.js`, no stamp, no `--close-reports`, nothing under `tools/` or
`workflows/` touched. *"Did not manufacture work. An empty queue is the normal day and is not a
failure."*

### Two things that did NOT fire today

- **Decision 2 (`prep-wave.js`'s self-credit gap)** did not trigger: all five records credit an author
  *different* from the magnet (Thích Nhất Hạnh / Roger Bacon / none, against Buddha / Galileo), so
  `prep-wave`'s name-inequality guard stamped correctly. The defect is untriggered today, **not
  fixed** — it will fire on the next right-person-wrong-words draw.
- **No generator defect was found.** The wave states: *"No changes under `tools/` or `workflows/`
  (`git status --porcelain -- tools workflows` is empty). No defect in a shared generator was found
  this run."*

### No discrepancies

Nothing a routine claimed contradicts the log. The wave's *"1742 quotes"* is its unmerged PR's
projection against the tool's 1,737 on `main` — consistent. Differing preflight counts (14/14 wave and
review, 15/15 reports, 10/10 this report) are per-routine check sets, not a disagreement. The only
cross-run conflict is the `web.archive.org` reachability contradiction in F1, which is between two
days' runs, not between a claim and the log.

---

## 4. Needs a decision

**No new defect was reported today.** Every item below was open yesterday and is open unchanged,
because **no `tools/` or `workflows/` commit has landed since 2026-08-18**. The three that were paid
for this morning are first.

1. **Extend the egress allowlist — and get one authoritative list first.** Today it blocked
   `fakebuddhaquotes.com` (the specialist anchor for all three Buddha records), `plumvillage.org`,
   `suttacentral.net`, `accesstoinsight.org`, `openlibrary.org`, `jstor.org` and `hathitrust.org`,
   forcing three published pages onto Wayback captures — and yesterday's run called `web.archive.org`
   policy-denied while today's read it fine, so the list itself is unreliable. Doing it means one
   measured sweep of every host the waves have hit (`$HTTPS_PROXY/__agentproxy/status`), then a single
   allowlist request from that list. *(10th day; first day the list contradicted itself)*

2. **Fix the report window** so merges stop vanishing — `tools/daily-report.js:30` windows on calendar
   day; change `SINCE` to run report-to-report. Doing it is a one-line change to how `SINCE` is
   derived. **Fourth consecutive wave merge lost**, and today's #586 is green now and merges ~30
   minutes after this file is written. *(14th day)*

3. **Root-cause the stale local `main`, or write exception (b) up as permanent.** Fired in three of
   four passes again, and today the gap was live — the fast-forward moved **3 commits**, so the
   one-command remedy would have written this report against a stale tree. Doing it means finding what
   leaves the container detached with a stale ref, or accepting the two-command remedy as standing
   procedure. *(7th day; first day the gap was observed non-zero)*

4. **Make preflight distinguish a slow-but-reached origin from a blocked host.** The
   `reach www.gutenberg.org` check killed the whole review pass yesterday on a ~15s timeout while the
   same origin answered `504` and `200` to direct probes; it passed cleanly today, so the bug is
   latent, not gone. Doing it means raising the timeout, treating any answered status as "origin
   reached", or at minimum correcting the remedy text that points at the allowlist for a latency
   problem. *(2nd day)*

5. **Make `prep-wave.js` stamp `creditedTo` when `confidence === 'disputed'`, regardless of name
   equality.** Untriggered today by luck of the draw; it fired live on two Thatcher records yesterday,
   which published `ClaimReview 1 / "False"` against their own prose. Doing it is one guard relaxed
   for `disputed`, leaving `attributed` unstamped. *(4th day)*

6. **Fail the build when `Quotation.creator` is suppressed on a record that has an author.**
   Yesterday's RULE breach passed `validate-records`, `build`, `scan` and 51 corpus invariants and was
   caught only by an audit agent reading rendered JSON-LD. Doing it means making `creatorOk === false`
   on a record with an `authorName` a hard error. *(2nd day)*

7. **Decide the house convention for characters with no HTML entity.** "Hạnh" has no named entity, the
   entity style degraded it to "Hánh", and that misspelling both broke the creator node and produced a
   404 Wikiquote link. Doing it means ratifying the wave's literal-Unicode fix as a rule. *(2nd day —
   pairs with 6)*

8. **Do the deindexing watchlist diff** — the per-URL baseline was observed 2026-08-18 and commits to
   re-inspection *"in a few days"*; that is now overdue. It needs a human in GSC, nothing is scheduled
   to do it, and it is the highest-stakes open question on the site. *(3rd day, clock running)*

9. **Fix `plain()` to preserve unknown HTML entities** — an entity missing from `NAMED_ENTITIES`
   becomes a space and is then erased, dropping `©` from `copyrightNotice` on 11 records. Doing it
   means adding the ~40 missing entities **and** returning `&name;` verbatim for the unknown case.
   *(3rd day)*

10. **Make `parse-audit.js` union findings across repeated audits of the same slug** — it keeps one
    audit's issues and drops the other's, which cost r39 8 findings including an unreported `high`.
    *(3rd day)*

11. **Make the started-vs-result gate count coverage per slug, not journal lines** — `resumeFromRunId`
    appends to one journal, so a clean 27/27 run read as "69 started, only 31 results". *(3rd day)*

12. **Decide `verbatimNote()`'s "Verbatim:" label corpus-wide** — it renders on ~370 records; one
    editorial call, not 370. *(3rd day)*

13. **Add the mechanical near-duplicate check to the wave pipeline** — run by hand again today (batch
    × corpus and batch × batch, plus slug uniqueness), found nothing, still not in the tool. *(4th
    day)*

14. **Do the measured `mis-kind.js` pass** — four gap families characterised; a blind backfill *"would
    soften real debunks"*. *(9th day)*

15. **Accept or decline the detector `magnet-quoted-it-rated-as-never-said`** — 10 hits, 0.6%, all
    hand-checked. *(5th day)*

16. **Repair or retire the corrupt C. S. Lewis record and the Ali duplicate** — ranks 27 and 48, no
    longer blocking, still broken. *(6th day)*

17. **Decide the jurisdiction wording for `USE['public-domain']`** — asserts "no permission or licence
    needed" with no jurisdiction, across 54 records crediting authors who died 1956 or later. *(4th
    day)*

18. **Decide the `creatorOk` right-person-wrong-words creator emission corpus-wide** — 3 records emit
    a creator for words that person did not say. *(4th day — decide alongside 6)*

19. **Make the wave log a cost figure** — no run has reported tokens on any day this report has
    covered. *(6th day)*

20. **Add `verdictNote` to `DOSSIER_SCHEMA`** — 67 records carry one, all hand-set; `SCHEMA_BUDGET`
    has zero headroom, so check that first. *(9th day)*

21. **Decide the `Unknown`-creator `sameAs` convention**, defining the matching rule as part of it.
    Today's wave added two more `Unknown`-creator records. *(7th day)*

22. **Work the disputed records with no `creditedTo`** — items 5 and 20 would stop the class growing;
    today added three records asserting no rights status. *(10th day)*

23. **Fix the `themes` comment at `workflows/generate.js:168`** — one comment edit. *(7th day)*

24. **Correct the "~134 magnet authors harvested" figure** — 314 distinct authors have been touched;
    filter future pools against queue magnets **and** built author hubs. *(3rd day)*

**Nothing closed today, and nothing closed yesterday.** The last commit under `tools/` or `workflows/`
was 2026-08-18, and it recorded a defect rather than fixing one.

---

## 5. Cost

**No run reported a token count today**, as on every day this report has covered — that is decision 19.

Measured directly. Today's wave: **5 pages · 14 issues (0 blockers, 2 high, the rest medium/low) · 12 fixed
pre-commit, 2 refused with reasons · 2/2 skeptic re-checks confirmed · `validate-records` clean on all
five with zero warnings · 51 corpus invariants pass at 1,742 quotes · `scan.js` flagged 0 of 5 ·
near-dup max similarity checked by hand both ways · backlog 355 → 350.** Today's review: **1,737
records scanned, 0 checks run (10,422 already settled), 0 flagged, 0 records opened.** Today's reports
pass: **0 processed, empty queue, one GET and no agents.**

Three passes spent a preflight cycle each on the stale-`main` recovery — the visible price of decision
3. The Wayback detour on three Buddha records is the visible price of decision 1: the wave retried
`web.archive.org` through *"intermittent 503s ('temporarily offline') throughout"* with backoff on
every fetch, to cite captures of a source the allowlist should have let it read directly.
