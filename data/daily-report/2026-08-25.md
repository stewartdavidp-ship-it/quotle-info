# Daily report — 2026-08-25

## 1. Verdict

**All four due routines ran and `main` is healthy — but yesterday's daily wave is dead work: all five
of its records had already shipped inside a backlog wave that merged hours later, so PR #626 can
never merge and the merge gate will skip it forever.**

CI green on `main` (`7342d6b1b`), **1,832 records**, **0 flagged**, report ladder `pr`, 0 deferred,
working tree clean, **no tool failures** — these numbers are complete, not partial. The perf-check
canary passed at 11:19 UTC.

The night itself worked. The damage was done yesterday afternoon, outside every report window: two
hand-merged backlog waves (r41, r42) put **80 records** on `main` and, between them, consumed the
same five quotes the 03:00 daily wave was building at the time.

## 2. What ran

| | slot | routine | result |
|---|---|---|---|
| ✓ | 03:00 | `daily-wave` | 1 run: **pr** · 5 records → [#643](https://github.com/stewartdavidp-ship-it/quotle-info/pull/643) |
| ✓ | 04:00 | `daily-reports` | 1 run: no-op — "Empty pending queue via public `GET /reports/pending`" |
| ✓ | 05:00 | `daily-review` | 1 run: no-op — 1,832 scanned, 0 flagged |
| ✓ | 07:00 | `daily-merge` | 4 runs: merged ×4 · 6 PRs |
| · | 12:00 | `reports-close` | not yet due (17:00 UTC) — this report runs too early to judge it |
| · | Mon 02:00 | `weekly-discovery` | not scheduled today |

**Nothing was missing.** Yesterday's `reports-close` — the slot yesterday's report ran too early to
see — did run, no-op.

Not in the census, so checked by hand: **`perf-check` ran today at 11:19 UTC and passed** (run #6,
scheduled, on `7342d6b1b`). Six scheduled runs, six passes since the pre-fix dress rehearsal.

## 3. What shipped

- **6 PRs merged** in the tool's window: #637, #640, #641, #642, #644, #645 — **all six are
  routine-log shards.** No content merged inside today's window.
- **3 PRs open:** [#643](https://github.com/stewartdavidp-ship-it/quotle-info/pull/643) (today's wave,
  5 records, `CLEAN`) and [#646](https://github.com/stewartdavidp-ship-it/quotle-info/pull/646)
  (merge-pass shards, `CLEAN`) — the gate marks both `MERGE`, so the next merge pass takes them.
  [#626](https://github.com/stewartdavidp-ship-it/quotle-info/pull/626) is `DIRTY` at 1 day old and is
  dead — see 4.1.
- **Outside the window, and in no report ever written:** `main` went **1,752 → 1,832 records**
  yesterday afternoon via two hand-merged backlog waves —
  [#632](https://github.com/stewartdavidp-ship-it/quotle-info/pull/632) (r41, 40 records) at 14:23 UTC
  and [#638](https://github.com/stewartdavidp-ship-it/quotle-info/pull/638) (r42, 40 records) at
  21:59 UTC, plus two docs commits (#633, #639). Yesterday's report merged at 12:14 UTC, so it
  covered none of it, and today's window starts at midnight, so it covers none of it either.

**Health:** CI `success` on `7342d6b1b`; 1,832 records; 0 flagged; ladder `pr`; 0 deferred; 0 unjudged
runs; tree clean; **no tool failures**.

## 4. Findings

### 4.1 Yesterday's daily wave is dead work — PR #626 can never merge *(decision 1)*

#626 (Thatcher, Galileo ×2, Goldman ×2) opened at 07:49 UTC yesterday. At 14:23 UTC the backlog wave
r41 (#632) was merged by hand, and it contained **all five of the same quotes**. Checked directly:
every one of #626's five slugs is on `main` today, all five added by commit `675413019` (#632).

That makes #626 an `add/add` conflict against `main` on all five records and their five built pages.
`merge-gate.js` agrees:

```
SKIP     #626   wave-d20260824    merge conflict — needs a human, do not auto-resolve generated files
```

`SKIP` is terminal. The merge pass rebuilds `REBUILD` (behind) but leaves `SKIP` (conflicted) alone by
design, which is why the pass nursed it twice on 08-24 — *"#626 rebuilt and pushed — merges next pass"*
at 09:47 and again at 12:49 — and then never mentioned it again across the seven passes since. Nothing
in the system will ever recover it.

**Nothing of substance is lost by closing it, with one exception** (4.2): a diff of #626 against its
merge base touches only those five records, `data/harvest-queue.md`, and its own log shard.

The waste is real though: a full select → generate → ingest → build → audit → skeptic → fix pipeline
over five records with primary sources read in full — including the Galileo 1615 Italian located at
Favaro's *Edizione Nazionale* vol. 5 p. 317, and both ends of the Goldman/Lacassagne chain — was
performed, and then discarded because another wave drew the same five quotes.

### 4.2 One real defect from #626's audit is live on `main` right now *(decision 2)*

#626's audit found, and fixed, a `dateCreated` it called out in its own words:

> a `dateCreated: "1890"` on the Lacassagne record, which is Ellis's publication date, not a creation
> date any source supports. Both fields omitted rather than guessed.

The version that actually shipped — r41's — still carries it. Checked on `main` today:
`data/quotes/a-society-gets-all-the-criminals-it-deserves.json` has `schema.dateCreated: "1890"`.
The record's other two #626 findings do **not** apply to the shipped version: it already carries
`creditedTo: "Emma Goldman"` and no descriptive `rightsHolder`, and the `if-i-cant-dance` record on
`main` has no `isPartOf` at all, so #626's blocker never reached the site.

So exactly one field's worth of #626's audit work needs re-doing on a fresh branch.

### 4.3 The released-duplicate toll is now confirmed per-wave, within ten hours *(decision 4)*

Last night the operator wrote this into `workflows/README.md` (#639, 22:01 UTC):

> A dropped duplicate goes back to `queued` and WILL be redrawn. […] Until harvest.js can retire a
> candidate as "already covered by `<slug>`", the near-duplicate check is a per-wave toll that grows,
> not a one-time cost.

r42 had just released four such duplicates. **This morning's wave drew the same four.** #643's own
table:

| Drawn candidate | Already built as |
|---|---|
| Frankl, "…can bear **with** almost any how" | `he-who-has-a-why-to-live-can-bear-almost-any-how` (Nietzsche) |
| Carnegie, "…a great **business** who wants to do it all" | `no-man-will-make-a-great-leader-who-wants-to-do-it-all` |
| Carnegie, "The man who dies **thus** rich…" | `the-man-who-dies-rich-dies-disgraced` |
| Shankly, condensed football line | `some-people-believe-football-is-a-matter-of-life-and-death` |

and its diagnosis:

> `harvest.js sync` dedups on **exact normalised text**, so single-word variants of already-built
> quotes pass selection untouched and sit near the top of the demand ranking. This is the r34/r38
> failure mode still live. **Reported, not fixed** — the remedy is in `tools/`, and this is a content
> PR. […] Note the 4 variant duplicates return to the queue and will resurface until dedup is
> variant-aware.

Five of 22 drawn candidates were unusable; four of the five are this one bug. The prediction and its
confirmation are ten hours apart.

### 4.4 Today's wave: 5 records, 2 FAIL, all five issues resolved *(PR #643)*

3 PASS / 2 FAIL, 5 issues (0 blocker, 2 high, 2 medium, 1 minor); **19/19 source links re-fetched
200**; skeptic refuted 1 of 2 highs. The system working, including one place where it corrected
*itself*:

> **[high, CONFIRMED → fixed]** The Hill page described the Internet Archive scan it searched as "the
> 1937 edition". That item carries *no* edition metadata […] and one of the two scans is affirmatively
> a modern "Free Digital Download PDF eBook Edition" repackaging.

> **[high, REFUTED]** "Calling Pegler *the sportswriter* is unsupported." Refuted […] **[medium →
> fixed]** That check surfaced a real one: the page said Pegler wrote the piece *for* the *Chicago
> Tribune*, but he did not join the *Tribune* until 1925 — five years after the article.

> **[minor → my audit got this one wrong]** I filed the Napoleon ClaimReview rating as a
> generator-level issue to report and not fix. `tools/scan.js` then flagged it
> `coined-verb-mismatch [high]`, and `tools/detectors.js` names the correct **record-level** remedy:
> set `schema.claimVerb`.

That last one is worth noting against carried item **19**: a record-level lever for the ClaimReview
wording (`schema.claimVerb`) already exists and the detector already points at it. The carried item is
about the *rating*, not the verb, so it stands — but it is narrower than it reads.

### 4.5 What today's wave could not establish, and why

Recorded rather than guessed:

- **Hill, *Success Through a Positive Mental Attitude* (1960) p. 222** — lending-restricted,
  search-inside 403. Carried as Wikiquote's citation and labelled as such; record ships `attributed`,
  not `verified`.
- **Which printing the two *Think and Grow Rich* scans are.** Stated on the page.
- **The Ruth 1920 *Chicago Tribune* article** — archive returns 403, the Google Books reprint serves
  no readable text. Consequence, in the wave's words: **"Rights left `uncertain`, so the page renders
  no rights badge at all"**, rather than claiming public-domain off an unread 1920 source.
- **`news.google.com` is blocked by the egress policy**, so Wikiquote's *Pittsburgh Press* reprint
  links "are **not** cited anywhere" as corroboration.

Two of those four are access problems, not research problems — carried item **11** (egress) and item
**13** (Google Books quota).

### 4.6 Findings from r41 and r42 that no report has ever carried

Both merged outside every window (4.1, decision 5). Read by hand; two are decision-grade:

- **A validator warns on the corpus's own documented shape** *(decision 8)*, from r42:
  > `tools/validate-records.js:159` compares `answer.authorName` against each `creditedTo` and warns
  > *"the fake author is being published as real"* — but it **ignores `answer.realAuthorName`**. […]
  > **56 of the 58** records whose `realAuthorName` is "Unknown" have hero == creditedTo, so the
  > warning fires on the correct shape and is unfixable at record level. […] The validator is the odd
  > one out.
- **r41 logged the first honesty failures of the series** — `confidenceHonest` false on 2 pages,
  `rightsHonest` false on 2, after r38–r40 were clean on 65 pages. r42's own note is the reassuring
  half: on equally seam-heavy content "`confidenceHonest` and `rightsHonest` **true on all 40**", so
  the dip did not repeat.
- **r42 measured the `magnet` gap family: "exactly 10 rows corpus-wide, 3 already hand-set, 7 still
  rendering the false-credit ✕"**, enumerated by file. That is carried item **22** with a number
  attached to it at last.
- **An agent measured its own detector proposal and rejected it** (r41): the "kit quote word-identical
  to `claimQuoteText`" rule fires on 270 of 1,792 records (15%), far above the noise floor, and on a
  misattribution site would flag the editorial design rather than errors in it. Correct call; no
  action.

### 4.7 Two waves disagreed about the same record's translator — no action needed

#626 said it could not establish which published translation the Galileo 1615 English wording is, and
named no translator as rightsholder. The version that shipped (r41) names one:
`Stillman Drake (translation), published by Anchor Books / Doubleday`. r41's audit had independently
caught a related Drake problem on the sibling record — a Drake 1957 translation reproduced under a
public-domain badge, fixed by swapping in the 1859 Arago edition — so the shipped record is the more
carefully sourced of the two. Recording the divergence, not proposing anything: both ship
`in-copyright`, and the live page is the better one.

### 4.8 The stale local `main` fired again — 11th day

Preflight failed here on `local main sane` + `git on main` with `tree clean` passing, and the
documented exception **(a)** recovered it. `daily-reports` logged the same: *"preflight 15/15 after
exception (a) recovery of stale local main ref."* Yesterday it was exception (b) at 46 commits behind;
today it was (a), a diverged ref. The exception's own text still says finding the cause "would retire
this exception". Carried item **10**.

## 5. Needs a decision

Nine live items. The 28 carried below are unchanged and are listed only so nothing is lost —
**item 6 is the one that shortens that list.**

The arithmetic, since yesterday's report found two items had silently vanished in a renumbering:
**32 carried in from 2026-08-24, none closed, 5 added today (1, 2, 3, 4, 8) = 37.** Nothing was
dropped.

1. **Close #626 and delete `wave-d20260824`.** All five of its records already shipped inside r41
   (#632), so it is `add/add`-conflicted and `merge-gate.js` will mark it `SKIP` forever — no routine
   will ever recover it. Closing it costs nothing once item 2 is done.

2. **Re-land #626's one surviving fix: delete `schema.dateCreated: "1890"` from
   `a-society-gets-all-the-criminals-it-deserves`.** It is Ellis's publication date, not a creation
   date any source supports, and it is live on the page today. One field removed on a content branch,
   plus `build.js`.

3. **Stop the daily wave and the backlog waves drawing the same candidates.** `workflows/README.md`
   already warns "There is no lock on the queue", and yesterday that cost an entire daily wave —
   five records built, audited, fixed and thrown away. Doing it means either having the daily wave
   skip slugs present in any open `wave-` PR, or not running the daily wave on a day a backlog wave is
   planned.

4. **Teach `harvest.js` to retire a candidate as "already covered by `<slug>`".** r42 released four
   reworded duplicates last night and this morning's wave drew the same four, exactly as #639
   predicted, so the near-duplicate check is now a toll every wave pays forever. Doing it is a
   `retired` state in `harvest.js` plus running the Jaccard check inside `sync`. *(pairs with 21)*

5. **Fix the report window.** `tools/daily-report.js:30` windows on calendar day, so 80 records across
   r41 and r42 — the two biggest content events in a week — appear in **no daily report at all**, and
   the eighth consecutive wave merge is invisible. One line: derive `SINCE` from the previous report's
   timestamp instead of `new Date()`. *(18th day)*

6. **Declare which carried items you are not going to do.** Nothing has been committed under `tools/`
   since **2026-08-21**, so the list below is an archive pretending to be a queue. Reply with the
   numbers to strike and the next report carries only what survives. *(2nd day)*

7. **Make `prep-wave.js:206` stamp `creditedTo` when `confidence === 'disputed'`, regardless of name
   equality.** It bit again today — #643's Napoleon record is `disputed` with author == magnet, so the
   guard withheld the field and the wave stamped it by hand to stop the page publishing a false
   "never said it" verdict. One guard relaxed for `disputed`. *(8th day)*

8. **Fix `tools/validate-records.js:159` to respect `answer.realAuthorName`.** It warns "the fake
   author is being published as real" on 56 of the 58 records that follow the documented
   misattribution convention, making the warning unfixable at record level. One precedence change to
   match `template.js`, `build-authors.js:58`, `credits.js:84` and `build-themes.js:58`. *(NEW —
   reported by r42, never reported to you)*

9. **Do the deindexing watchlist diff.** The per-URL baseline was observed 2026-08-18 with
   re-inspection promised "in a few days", now seven days overdue, and it is still the highest-stakes
   open question on the site. It needs a human in Google Search Console — nothing here can reach it.
   *(7th day, clock running)*

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 10 | Root-cause the stale local `main`, or ratify exception (b) as permanent in `preflight.js` | 11th |
| 11 | Extend the egress allowlist — after one measured sweep of every host the waves have hit | 14th |
| 12 | Decide the `isBasedOn → isPartOf` bulk migration (1,046 of 1,752 measured 08-24; prerequisite landed 07-30) | 2nd |
| 13 | Decide what to do about Google Books' 429 — get a key with quota, or drop the sweep from the doc | 3rd |
| 14 | Stop one source's outage hard-stopping the review pass (`preflight.js` `head()` treats a 12s timeout as blocked) | 6th |
| 15 | Fail the build when `Quotation.creator` is suppressed on a record that has an author; normalise `template.js`'s hero match | 6th |
| 16 | Fix `daily-report.js:148`'s `work` figure — it sums `built + processed + sampled` | 2nd |
| 17 | Decide whether `perf-check` belongs in the routine census, or accept email-only | 4th |
| 18 | Settle the branch-name conflict between the harness (`claude/…`) and the routine docs — #643 deviated again today | recurring |
| 19 | Give `claimReviewed` a lever, or accept the coarse verdict (see 4.4 — narrower than it reads) | 3rd |
| 20 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason | 4th |
| 21 | Add the mechanical near-duplicate check to the wave pipeline — run by hand again today | 8th |
| 22 | Work the disputed records with no `creditedTo` — r42 measured it: 7 of 10 `magnet` rows still render the false-credit ✕ | 14th |
| 23 | Do the measured `mis-kind.js` pass — most gap families now carry a number | 13th |
| 24 | Fix `plain()` to preserve unknown HTML entities (~40 missing; `&copy;` dropped on 11 records) | 7th |
| 25 | Make `parse-audit.js` union findings across repeated audits of the same slug | 7th |
| 26 | Add `verdictNote` to `DOSSIER_SCHEMA` (83 records carry one; `SCHEMA_BUDGET` has zero headroom — measure first) | 13th |
| 27 | Make the started-vs-result gate count coverage per slug, not journal lines | 7th |
| 28 | Decide `verbatimNote()`'s "Verbatim:" label corpus-wide (~370 records, one editorial call) | 7th |
| 29 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` (10 hits, 0.6%) — blocks the next admission | 9th |
| 30 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate — led the draw for the **sixth** wave running | 10th |
| 31 | Decide the house convention for characters with no HTML entity (pairs with 15) | 6th |
| 32 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records assert "no permission or licence needed" | 8th |
| 33 | Decide the `creatorOk` right-person-wrong-words creator emission corpus-wide (decide with 15 and 19) | 8th |
| 34 | Make the wave log a cost figure — the wave shard still has no cost field | 10th |
| 35 | Decide the `Unknown`-creator `sameAs` convention, defining the matching rule as part of it | 11th |
| 36 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 11th |
| 37 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors have been touched | 7th |

## 6. Cost

**No run reported a cost figure today.** The `daily-wave` shard carries `date, routine, outcome,
built, pr, note` and no cost field — as do all four `daily-merge` shards, `daily-reports` and
`daily-review`. `weekly-discovery`, the only routine that ever reports one, was not scheduled today.

Today's wave was again the most expensive run by a wide margin — a full serial pipeline over five
records with primary sources read in full, an audit of 19 source links, a skeptic pass and a fix pass
— and it remains unmeasured. That is item 34, on its tenth day.
