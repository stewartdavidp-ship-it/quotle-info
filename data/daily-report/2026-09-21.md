# Daily report — 2026-09-21

## 1. Verdict

**Every routine due today ran, both content PRs are green and waiting, and the night's headline is a
diagnosis rather than a defect: the shallow-clone problem (item 12) is now solved on the evidence, and
the answer is not what the doc has been guessing for 48 days.** The wave built 5 records (#949 — 3
Rosa Parks, Miss Manners, "think of England"), the weekly discovery audited 20 and found nothing
wrong (#948), `daily-reports` and `daily-review` both correctly no-opped, and the merge pass landed 4
PRs across two runs. `main` is green at `73fd53190`, **2,243 records, 0 flagged, tree clean, 0
deferred**.

Three things are worth the operator's eye rather than a scroll past:

- **Exception (a) fired on every container today, where yesterday it was exception (b) on all four —
  and the cause is now measured, not guessed.** The stale local `main` sits at `0c5e03e0d` (the
  09-15 merge line) on the **far side of this clone's shallow graft boundary**, so git cannot compute
  ancestry at all. It is not "diverged, or an unrelated history" as preflight says; it is
  **truncation**. § 2.1 has the measurement. Item **12** is no longer a choice between two guesses.
- **The wave's audit caught three link cards describing the contents of pages the run never
  fetched** — all three blocked at CONNECT — *"and the TwainQuotes card presented Quote
  Investigator's finding as our own."* That is a fabrication-shaped defect the audit stopped at the
  gate. Item **91**, new today.
- **A routine reported a real cost figure for the first time.** #948: *"~215K new tokens for 20
  records ≈ 11K/record"*, against the 2026-07-29 datapoint of ~21K. Item **36** is 37 days old and
  today is the first time it got an answer from inside the system (§ 7).

Yesterday's traffic headline is confirmed for the third consecutive day, with fresh evidence: the
403 is the **egress proxy**, timestamped against this run's own two attempts (§ 4).

---

## 2. What ran

| slot | routine | result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run: `pr` — **5 records**, #949 (`CLEAN`, `verify` green, rebuilt by the 12:08Z pass, merges next pass) |
| 04:00 | `daily-reports` | ✓ 1 run: `no-op` — 0 pending, endpoint re-checked directly (shard `08:10:46Z`) |
| 05:00 | `daily-review` | ✓ 1 run: `no-op` — scanned 2,243, flagged 0 (shard `09:04:48Z`) |
| 07:00 | `daily-merge` | ✓ 2 runs: `merged`, `merged` — `03:34Z` (1 PR) and `12:08Z` (3 PRs) |
| 12:00 | `reports-close` | · not yet — due 17:00 UTC (yesterday's ran `no-op`, #944) |
| Mon 02:00 | `weekly-discovery` | ✓ 1 run: `pr` — **20 records audited, all PASS**, #948 (`CLEAN`, `verify` green, rebuilt by the 12:08Z pass, merges next pass) |

**Nothing is missing from the roster.** Every slot due today produced a shard — the sixth consecutive
day that has been true, and the first of those six that includes the weekly discovery slot. As on the
previous two days, the census counts the `12:08Z` merge run and the wave run from shards still on
unmerged branches; only the `03:34Z` merge shard, `08:10Z` reports shard and `09:04Z` review shard are
on `main`.

`daily-report.js` reported **1 tool-call failure** — `traffic: fetch failed`, at
`health.tool_failures`. **§ 4 is the only incomplete part of this report; §§ 2, 3, 5 are complete.**

### 2.1 Preflight — exception (a) on every container, and item 12 is now measured rather than guessed

This pass failed preflight 2/10: `local main sane` *("local main is NOT an ancestor of origin/main
(diverged, or an unrelated history)")* and the consequent `git on main: on HEAD`, with **`tree clean`
passing** — the documented 2026-08-04 exception (a). Ran the single command preflight printed
(`git checkout -B main origin/main`, not `git checkout main`), re-ran preflight: 10/10 green.

Every routine that ran today hit the same thing, and two of them named the same stale commit:

| pass | start state | stale `main` | outcome |
|---|---|---|---|
| `daily-wave` (#949) | exception (a) | not stated | re-ran **14/14 green** |
| `daily-reports` (#950) | exception (a), *"came up detached at the current tip"* | not stated | re-ran **15/15 green** |
| `daily-review` (#951) | exception (a), parked on `claude/wizardly-fermat-hfgca1` | **`0c5e03e0d`** | re-ran **14/14 green** |
| this report | exception (a), detached at tip | **`0c5e03e0d`** | re-ran **10/10 green** |

**This is the escalation yesterday's report predicted, arriving one day later.** § 2 of 2026-09-20
tracked the gap at 10/10/13 → 21/24 → 31/31/31/34 → 41/41/44 and warned that *"the growing gap is
what eventually produces exception (a)'s unfixable shape."* It did. Today nothing hit (b) at all.

**And the mechanism is now measured, from inside this container:**

```
.git/shallow present                      → YES
commits reachable from HEAD               → 50   (the clone depth)
shallow boundary commits                  → 31590d5b3 (09-10, #846)
                                            c5327b00e (09-16, #902)
oldest reachable commit from HEAD         → c5327b00e (2026-09-16T16:14Z)
stale local main                          → 0c5e03e0d (2026-09-15, #895)
git merge-base 0c5e03e0d HEAD             → (no output — no computable common ancestor)
```

The stale ref sits at 09-15, **one day older than the oldest commit this shallow clone can reach**.
It is on the far side of the graft, in a separate island rooted at `31590d5b3`. `0c5e03e0d` is a
perfectly ordinary true ancestor of `origin/main` in the real repository; this clone simply **cannot
see** that, so `merge-base --is-ancestor` answers no and preflight reports divergence.

Three consequences, all of which change item 12:

1. **The root cause the doc calls unexplained is explained.** `DAILY-REPORT.md` § Step 0 says
   *"something leaves the container detached with a stale `main`, and finding it would retire this
   exception."* The container does not do anything exotic: it creates `main` from a
   `refs/remotes/origin/main` that has gone stale, and the **50-commit clone depth** is what turns
   "behind" into "unprovable". The repo now merges ~8–10 commits a day, so a ref six days old falls
   off the end.
2. **The flip from (b) to (a) was inevitable and is permanent.** Exception (b)'s precondition is
   `local main sane` **PASSING**. Once the stale ref is older than the shallow horizon, that check can
   never pass again. Every container from here on starts in (a)'s shape, not (b)'s.
3. **`preflight.js`'s message and the doc's explanation of (a) are both now demonstrably wrong in
   this environment.** Preflight says *"diverged, or an unrelated history"*; `DAILY-REPORT.md` says
   *"(a)'s ref is not an ancestor at all, so nothing can fast-forward it and only `-B` will do."* The
   ref **is** an ancestor. The remedy is still correct and still safe — `-B` on a clean tree
   discarding only a stale local ref — but it is correct for a different reason than the one written
   down, and a reader debugging this is currently sent looking for a divergence that does not exist.
   Item **90**, new today.

Nothing was discarded on any of the four recoveries: every tree was clean and every ref was, in
truth, an ancestor.

---

## 3. What shipped

**Merged in the report's UTC-day window (4), all bookkeeping:**

| PR | what |
|---|---|
| #951 | `daily-review` 2026-09-21 no-op shard |
| #950 | `daily-reports` 2026-09-21 no-op shard |
| #947 | merge-pass 2026-09-21 shard |
| #946 | merge-pass 2026-09-20 shard |

**Merged since yesterday's report actually ran (12:07:55Z on 09-20) but invisible to today's list —
five PRs, including the only content PR:**

| PR | merged | what |
|---|---|---|
| **#938** | **09-20 15:32:15Z** | **the 09-20 wave — 3 Lewis Carroll, 2 Rosa Parks** |
| #941 | 09-20 15:32:26Z | merge-pass shard |
| #943 | 09-20 20:23:49Z | merge-pass shard |
| #944 | 09-20 20:23:53Z | `reports-close` 09-20 no-op |
| #945 | 09-20 23:00:57Z | merge-pass shard |

This is item **4**, 45th day, and the **fifth consecutive day** the window has eaten a content PR —
the same five-PR shape, one calendar turn later. #938's five records are the reason `records` moved
2,238 → 2,243; the census is right about the corpus and silent about what put it there. Only this
hand-written paragraph carries them.

**Open (4):**

| PR | gate state | CI | age | what |
|---|---|---|---|---|
| #949 | `CLEAN` — green, in scope, up to date | `verify` ✓ success | 0d | the 09-21 wave, 5 records |
| #948 | `CLEAN` — green, in scope, up to date | `verify` ✓ success | 0d | the weekly discovery, 20 records audited |
| #952 | `CLEAN` — green, in scope, up to date | `verify` ✓ success | 0d | merge-pass 2026-09-21 shard |
| #670 | `DIRTY` — merge conflict | — | **25d** | the 08-27 wave, 5 records |

**None of #949, #948 or #952 is this pass's to merge** — `DAILY-REPORT.md` limits the self-merge
exception to this report's own PR, and a green gate does not widen it. All three were rebuilt by the
`12:08Z` merge pass and `verify` has since gone green on every new head, so the next merge pass takes
all three with nothing outstanding.

**Records audited:** 2,243 scanned by `daily-review`, 0 flagged, *"state unchanged, nothing
written"*; plus 20 records re-reviewed in depth by `weekly-discovery`, **all 20 PASS**.
`vocab-sweep.js`: 6 tables, 0 under-used keys.

**Reports closed:** none pending. #950 again did not trust the empty print — it re-checked the public
endpoint directly (`HTTP 200 {"pending":[],"count":0}`), *"since an error from `/reports/pending`
would have been a hard stop instead."*

---

## 4. Traffic

**Traffic: unavailable.** `traffic` is `null` in the tool's JSON and `health.tool_failures` carries
`traffic: fetch failed`. **This is an analytics outage, not a quiet day — it is not a zero**, and no
figure for today, 7d, 30d or all-time can be given.

### The cause is settled for the third consecutive day, now against this run's own clock

The agent proxy's status endpoint logged both of this pass's attempts, by host and to the second:

```
{ "ts": "2026-09-21T12:18:28.264Z", "kind": "connect_rejected",
  "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
  "host": "quotle.goatcounter.com:443" }
{ "ts": "2026-09-21T12:18:34.892Z", ... same ... }
```

Meanwhile `tools/traffic.js` prints:

> `traffic.js: 403 from GoatCounter — the dashboard is no longer public.`
> `Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.`

**That sentence is false in this environment.** Nothing this routine sends reaches GoatCounter; the
403 is the gateway's answer to `CONNECT`, before any HTTP request is made. Note also that the two
tools fail *differently* on the same block — `daily-report.js` carries an 8s `AbortSignal.timeout`
and reports `fetch failed`, while `traffic.js` has no timeout, receives the gateway's 403 and
mislabels it. Same cause, two different stories, neither of them the true one. That is item **87**,
and it is the argument for doing it before item **86**.

This establishes, again, that **no cloud routine can read traffic until the egress allowlist changes**
(item 13). It does **not** establish the dashboard's actual state, so item 43 stays demoted, not
struck.

**Reminder on the figures themselves, for when they return:** GoatCounter counts everybody — AI
assistants, Bing, DuckDuckGo, direct — and is the only traffic number this report carries; do not
substitute Search Console. The figures are an **upper bound**, unfiltered for referrer spam
(`spamFiltered` is always `false` here), so report them as "up to N". `node tools/traffic.js --refs`
gives the referrer breakdown. Neither has been readable since 08-28 — **24 days blind**.

---

## 5. Findings

### 5.1 The wave shipped 5 records after its audit stopped three fabrication-shaped defects

#949's audit returned **1 PASS / 4 FAIL — 10 issues (0 blocker, 4 high, 4 medium, 2 minor)**, with
**5 skeptic re-checks: 4 confirmed, 1 refuted** (20%, in the runbook's expected 15–20% band). All 9
surviving issues were fixed in-record before ship. In its own summary, *"the audit earned its keep —
it caught three things that would otherwise have shipped"*:

1. **Three links carrying claims that were never fetched.** *"`twainquotes.com`, `en.wiktionary.org`
   and `phrases.org.uk` are all refused at CONNECT by this environment's egress proxy, yet their
   cards described their contents — and the TwainQuotes card presented Quote Investigator's finding
   as our own."* Replaced with sources actually read (two Wikiquote pages checked in full, plus
   Wikipedia's Stanley Baldwin article); the TwainQuotes absence is now attributed to QI in prose.
   **This is the most serious shape in today's night** — see § 5.2.
2. **Two unsourced statistics, plus a wrong one.** The `pushed around` page asserted the Rosa Parks
   Papers hold *"around 7,500 manuscript items and 2,500 photographs"* and came to the Library *"on
   long-term loan in 2014"*. Neither figure appears in anything fetched — *"'2014' occurs solely
   inside the LCCN string `mm2014085943`."* Both removed. The same page said Parks had been an NAACP
   secretary for *"a decade"*; it was twelve years. And the `quilt` page *"invented passenger counts
   ('four… three moved') that no fetched source gives."*
3. **An unsupported wording-drift claim, on the page's central point.** The `People always say` page
   asserted in six places that the book reads *"People always **said**"*. Wikiquote transcribes
   "said"; **Wikipedia, citing the identical source and page** (`Parks|Haskins|1992|p=116`),
   transcribes "say". The book itself was not readable. *"The drift row was dropped; the page now
   states the discrepancy honestly… Net effect: the citation got **stronger** (two independent
   witnesses to p. 116), the claim got narrower."*

The one refuted finding was the Wiktionary card, flagged for an unverified content claim: *"'dictionary
entry for the idiom' asserts essentially nothing falsifiable."* Link still replaced, finding dropped.

**What it refused to establish**, stated on the pages rather than papered over: the `quilt` line's
book source (*"widely cited to My Story or Quiet Strength; neither could be confirmed"* — the
documented appearance is Greenhaw's CNN column); **page 116 itself** (*"both `verified` Parks book
citations rest on two independent sources citing the same page, not on our own reading"* — Internet
Archive is lending-only and search-inside is blocked); the 1943 *Washington Post* and 1979 *Bulletin*
columns (ProQuest/GenealogyBank, so QI's citations are the anchor); and the **1943 copyright
renewal**, so the Lyons column is asserted `in-copyright` rather than guessed into public domain.
**No record this wave claims public domain.**

The strongest record of the wave is primary: Parks's own handwritten draft in the Rosa Parks Papers
(LoC folder `mss859430226`, image 27), *"whose volunteer transcription reproduces the sentence
including the words she crossed out."*

**Egress shaped this wave too** (item 13, 41st day): `cnn.com`, `openlibrary.org`, `twainquotes.com`,
`en.wiktionary.org`, `phrases.org.uk`, `kinginstitute.stanford.edu`, `read.dukeupress.edu`,
`achievement.org`, `nps.gov` all blocked; Google Books API over daily quota (item 15, 30th day);
`loc.gov` 403s to non-browser clients again (10th day) and was verified via the JSON API;
`web.archive.org` *"served the CNN article twice early in the run and then began resetting
connections on re-fetch"* — the page now carries both the canonical and snapshot URLs so the
citation does not depend on one host.

### 5.2 The "described a page we never opened" shape is new, and egress is what produces it

§ 5.1's first finding is not a transcription slip. A blocked host produced a link card that
**asserted what was on the other end**, and in one case **re-attributed a third party's research to
this site**. That is the exact failure mode the corpus exists to be trusted against, and it was
caught by audit rather than by design — nothing in the pipeline stops a generator writing a card for
a URL that returned nothing.

Two properties make it worth a rule rather than a note:

- It scales with item **13**. Today nine hosts were refused; the more that are blocked, the more
  cards get written about pages nobody read.
- It is mechanically detectable in a way today's fixes were not: a card whose URL never appears in
  the run's fetch trail.

Item **91**, new today. Adjacent to item 81 (archived-vs-canonical citation), but distinct: 81 asks
*which URL to cite when you did fetch something*; 91 asks *whether you may cite at all when you
fetched nothing*.

### 5.3 The weekly discovery found nothing, measured two detector candidates, and rejected both

#948 sampled 20 records via `review.js due --limit 20` — all *"never re-reviewed since build"* (5
`attributed`, 15 `disputed`) — and stamped **all 20 PASS**. Unlike the 2026-07-29 run it re-fetched
live sources, *"because several records rested on a single checkable negative"*: Wikiquote's
`Rosa_Parks`, `Talk:Rosa_Parks`, `Sun_Tzu` and `Karl_Marx`, plus the Giles 1910 *Art of War*
(Gutenberg 132), where `"victory is reserved"` and `"pay its price"` both returned **0 hits** — *"the
record's central negative holds."* One apparent discrepancy was chased and dismissed as a CRLF
artifact in its own search, not a defect in the record.

Per the doc's instruction to report rejected candidates rather than add them, it proposed, measured
and killed two:

1. **`contested-record-without-verified-claims`** — *"Measured: 1,032 records (46.0% of corpus).
   `sourcesVerified` is simply a newer field — 1,567 records predate it entirely and 0 carry an empty
   array. This is field adoption history, not a defect."* Rejected on the >5% threshold.
2. **`magnet-in-schema-creator`** — *"Measured: 250 records (11.1% of corpus)."* Rejected twice over:
   most are name-form variance rather than magnets (*"'W. B. Hayler' vs 'W. B. \"Bill\" Hayler'"*),
   and *"this duplicates a render guard that already exists and is deliberate"* — `template.js`'s
   `creatorOk`, plus `detectors.js`'s already-rejected `claimant-not-a-person`, on whose documented
   precedent it stood down: *"Duplicating a render guard as a flag produces a worse version of a tool
   we already have."*

**Its residue is the real finding**, and it is the one thing in #948 that needs a person: *"the
**pages** are correct in every case (the guard works), while the **records** keep a stale
`schema.creator`. Nothing wrong ships. If that ever matters for JSON consumers (`search.json`,
`openapi.json`, `worker/`), the fix belongs in `generate`, not in a detector."* Those consumers read
records, not pages. Item **92**, new today.

### 5.4 The `creditedTo` gap the wave hand-fixed is item 6/76/80's missing evidence

#949 found that `prep-wave --credited` stamps `creditedTo` from the **batch magnet** and correctly
withholds it when the magnet equals the record's own author — but *"on both disputed records this
wave the magnet **is** the true author (the QI harvest tags articles by the person QI credits), so
nothing was stamped — and the falsely-credited party, which is what `creditedTo` actually means, went
unrecorded."* The visible damage: *"a `ClaimReview` with no claimant rating a bare quote string
`1/False`, and Mark Twain's author hub not listing this among the 33 lines misattributed to him."*

It set both by hand from what QI documents (`"Mark Twain"`; `["Queen Victoria", "Lady Alice
Hillingdon"]`), both now render correctly, and it was explicit that *"this is a data fix, not a
generator change"* — scope gate verified clean, nothing under `tools/` or `workflows/` touched. It
then flagged the general case rather than reaching for it: *"**77 existing disputed records** sit on
the same claimant-less path, so this is a standing shape rather than something this wave introduced."*

**This is the evidence item 6/76/80 has been missing for 35 days.** That item pits
`workflows/README.md`'s r35 note (*stamp it*) against `prep-wave.js:207`'s deliberate withholding
(citing **41 records that got a false misattribution claim** that way). Today's wave demonstrates a
third possibility neither side covers: the withholding rule is right in general and wrong
specifically when the magnet is the **true** author, which is exactly the QI-harvest case. That is a
distinguishable condition, not a judgement call.

**One number to reconcile, flagged rather than resolved:** a corpus-wide count on `main` finds **99**
`disputed` records with no `creditedTo` at all, against the wave's **77** *"on the same path"*. These
are almost certainly different sets — 77 reads as the subset the withholding rule explains, 99 as
every disputed record missing the field for any reason — and nothing in either PR reconciles them.
**Not reported as a discrepancy**, because the two claims do not contradict; reported because item 24
(41st day) is *"work the disputed records with no `creditedTo`"* and has never carried a current size.
It is 99.

### 5.5 The merge pass put bookkeeping ahead of content for the fifth day — and today it cost two PRs

The `12:08Z` pass merged #947, #950 and #951 — three log shards — then recorded:

> *"#948 rebuilt and pushed — merges next pass, once verify is green on the new head · #949 rebuilt
> and pushed — merges next pass, once verify is green on the new head"*

Nothing was lost, and both have since gone green. But #948 was open, ready and `CLEAN` from 06:10Z
and #949 from 07:51Z, and merging them first would have cost three one-line shards a trivial rebuild
instead of costing **five records and a 20-record audit** a pass each. Yesterday it deferred one
content PR; today, two. Item **64/51**, fifth consecutive day.

Also in that note, item **65** (18th day): the merge note still leads with re-poll chatter — *"re-poll
1/4: 5 PR(s) with mergeability not yet computed — waiting 6s"*, twice — before the verdict.

### 5.6 #951 departed from its harness prompt, correctly, and said so — for the second day running

> *"The scheduled prompt's harness boilerplate designates branch `claude/wizardly-fermat-hfgca1` and
> says to open PRs as drafts. `DAILY-REVIEW.md` requires the branch `review/<YYYY-MM-DD>` and a PR
> opened **ready, never draft**… `tools/merge-gate.js` allowlists branch *prefixes* and fails closed,
> so a `claude/…` branch would be classed `HUMAN` and never auto-merge, and the 07:00 merge pass skips
> drafts outright."*

The doc won, preflight independently confirmed the choice (*"✓ branch prefix recognised"*), and the
PR merged. Item **20**, working as intended and still unresolved at the source. Worth noting the
harness branch name is also what #951 found its container **parked on** (§ 2.1) — the same
`claude/wizardly-fermat-hfgca1` — so the two problems touch.

### 5.7 No discrepancies

Every routine's claim reconciles with the log and the census: #949's 5 records against the wave
shard's `pr`; #951's 2,243 scanned against `health.records` 2,243; #948's 20 audited against the
census's `20 record(s)`; the two merge notes' 1 + 3 = 4 PRs against `MERGED (4)`. The census's
four-PR list is *complete for the UTC day* and *incomplete for the night* — that is item 4's defect
(§ 3), not a disagreement between sources. The 77-vs-99 `creditedTo` figures (§ 5.4) are different
sets, not a contradiction.

---

## 6. Needs a decision

**Three items are added (90, 91, 92). Item 12 is not new but is the one to read first — § 2.1 turns
it from a choice between two guesses into a measured fault with a known fix. Nothing here is blocking
content from shipping:** #949, #948 and #952 are all green and merge at the next pass.

12. **Unshallow the container clone — item 12 now has a measured cause, and (b) will not do.**
    *(38th day, escalated.)* Every container today failed preflight on exception (a), because the
    stale local `main` (`0c5e03e0d`, 09-15) lies **beyond this clone's 50-commit shallow horizon**, so
    git cannot compute ancestry; (b)'s precondition — `local main sane` passing — can never hold
    again. Doing it is: `git fetch --unshallow` (or a much deeper `clone_depth`) in container setup,
    which **retires both exceptions outright**; the previously offered alternative of making
    `preflight.js:171` shallow-aware now only teaches preflight to describe the fault more politely.

90. **Correct what `preflight.js` and `DAILY-REPORT.md` say exception (a) *is*.** *(New — § 2.1.)*
    Preflight prints *"diverged, or an unrelated history"* and the doc states *"(a)'s ref is not an
    ancestor at all"*, but the ref **is** a true ancestor — the clone just cannot see past its graft,
    so a reader debugging this hunts a divergence that does not exist. Doing it is: reword
    `preflight.js`'s `local main sane` failure to name shallow truncation as the likely cause when
    `.git/shallow` exists, and amend the exception-(a) paragraph in `DAILY-REPORT.md` to match —
    the `-B` remedy is unchanged and still correct.

91. **Rule that a record may not cite a source the run never fetched — and make it mechanical.**
    *(New — § 5.1, § 5.2.)* Today's audit caught three link cards describing the contents of hosts
    refused at CONNECT, one of which *"presented Quote Investigator's finding as our own"*, and
    nothing in the pipeline prevents it. Doing it is: one line in the wave doc forbidding a source
    card for a URL with no successful fetch in the run's trail, plus a `prep-wave`/`scan.js` check
    that a cited URL appears in the run's fetch log — which makes this item 31's detector queue
    rather than a one-off.

4.  **Fix the report window.** *(45th day — five PRs lost overnight again, including #938, the 09-20
    wave's **five records**; fifth consecutive day it has eaten a content PR.)* Doing it is: change
    `daily-report.js:30`/`:263` to window from the previous report's timestamp instead of the UTC
    calendar day, on its own branch.

13. **Extend the egress allowlist.** *(41st day — it shaped a fifth consecutive wave, and § 5.2 says
    it now produces a new defect class rather than just thinner records.)* Today nine hosts refused:
    `cnn.com`, `openlibrary.org`, `twainquotes.com`, `en.wiktionary.org`, `phrases.org.uk`,
    `kinginstitute.stanford.edu`, `read.dukeupress.edu`, `achievement.org`, `nps.gov`. Doing it is:
    add `quotle.goatcounter.com` (unblocks § 4 outright), `web.archive.org`, those nine, the
    previously requested film-transcript hosts, JSTOR/Crossref, `barrypopik.com`, `annefrank.org`,
    `theguardian.com`, Google Books and HathiTrust — or rule that the pipeline lives without them and
    **81** becomes the answer instead.

6/76/80. **Reconcile `workflows/README.md`'s r35 note with `prep-wave.js:207` — § 5.4 supplies the
    distinguishing case.** *(35th day.)* The README says stamp `creditedTo` on a disputed record whose
    author is the magnet; `prep-wave.js:207` withholds, citing 41 records given a false misattribution
    claim that way. Today's wave shows a third reading both miss: withholding is right *except* when
    the magnet is the **true** author, which is the QI-harvest case and is mechanically
    distinguishable. Doing it is: read that comment and the 41 records against the wave's two
    hand-stamps, then one central edit encoding the three-way rule.

64/51. **Make the merge pass merge content PRs before routine-log PRs.** *(Fifth day — the 12:08Z
    pass merged three log shards, then deferred **both** content PRs a pass: #949's five records and
    #948's twenty-record audit.)* Doing it is: order the merge queue content-first, so content PRs
    merge before `main` moves under them.

92. **Decide whether the 250 stale `schema.creator` values get cleaned at the generator.** *(New —
    § 5.3.)* `template.js`'s `creatorOk` guard suppresses them at render so **no page is wrong**, but
    the records keep the stale value and `search.json`, `openapi.json` and `worker/` read records, not
    pages. Doing it is: either rule that record-level staleness is acceptable because the render guard
    is the contract, or clear the field in `generate` and backfill the 250 — **not** a detector, which
    #948 rejected on the repo's own documented precedent.

73. **Write the 2026-09-07 `daily-wave` shard to `main`.** *(13th day — still the only item with a
    deadline; `origin/wave-d20260907` confirmed still present at `6e6d0d2f9` this morning.)* `main`
    still says that wave did not run. Doing it is:
    `git show origin/wave-d20260907:data/routine-log/2026-09-07T07-44-35-244Z-daily-wave.jsonl` onto a
    fresh branch, commit, PR — **it stays recoverable only until that branch is pruned.**

42. **Rebuild and merge #670, or close it.** *(26th day; `DIRTY` and untouched since 08-27;
    `origin/wave-d20260827` confirmed still present at `86be66b36`.)* Closing it loses five records
    that exist nowhere else — including **two Thornton Wilder records verified against two
    independently scanned 1927 printings** — **and** orphans its 08-27 wave shard. Doing it is: merge
    `main` in, take `main`'s side on generated files, `node tools/build.js`, `node tools/scan.js`,
    push — or close it and accept both losses knowingly.

87. **Make `tools/traffic.js` stop asserting "the dashboard is no longer public" on any 403.**
    *(3rd day — § 4 adds that the two tools tell **different** false stories about the same block:
    `daily-report.js` times out at 8s and says `fetch failed`, `traffic.js` receives the gateway 403
    and blames GoatCounter.)* Doing it is: at `traffic.js:61`, print the observed status and host and
    say the cause is *either* a GoatCounter setting *or* an egress denial; **then** item 86's change
    to `daily-report.js` propagates something true.

36. **Decide whether #948's cost convention becomes the standard.** *(37th day — and the first day it
    has an answer rather than a complaint; see § 7.)* The weekly discovery reported *"~215K new tokens
    for 20 records ≈ 11K/record"* with a caveat about sample clustering; no other routine reports
    anything. Doing it is: add a `cost` field to the routine-log shard schema and one line in each
    routine doc asking for it, so the trend is machine-readable instead of living in prose.

43′. **Run `node tools/traffic.js` once locally, unproxied.** *(3rd day, still narrowed.)* The
    container-side 403 is proven to be the egress gateway, so this would only reveal the dashboard's
    own state, which matters after item 13 lands. One command, whenever convenient.

85. **Give `DOSSIER_SCHEMA` a multi-writer creator, or rule that co-written screen quotes name one
    writer.** *(5th day.)* Doing it is: let `creatorName` accept an array and have `toRecord` emit a
    `Person` per name, then a scripted backfill of the ten affected records — or decide the one-name
    convention and fix the ten to match.

88. **Rule on #938's `verified` for `one-of-the-deep-secrets-of-life`.** *(2nd day — #938 merged
    yesterday afternoon, so the record is **live** now and the ruling is retrospective.)* The wave
    flagged it as its one debatable call: `verified` although the circulating wording streamlines
    Carroll's documented sentence. Doing it is: read the page, then either leave it and say so — it
    matches 397 existing non-disputed records — or rule that a streamlined headline wording caps a
    record at `attributed`, a corpus-wide convention affecting those 397.

89. **Sweep the 110 records that pair a Gutenberg catalogue link with a verbatim claim.** *(2nd day —
    and § 5.2's item 91 is the same family: evidence that does not contain the evidence.)* Doing it
    is: read the 110, repoint genuine cases at the reading URL and re-fetch to confirm the passage is
    present — and, if the shape recurs, a `scan.js` detector, which makes this item 31's queue.

84. **Decide whether the wave is allowed to run without `WebSearch`.** *(5th day — not live today;
    #949 reported no missing tool.)* Doing it is one of: (a) find why `WebSearch` was absent on 09-17
    and restore it; or (b) one line in the wave doc saying a seam without a usable on-site search is a
    STOP rather than a thinner record.

81. **Rule on archived-vs-canonical citation when a host is blocked by egress.** *(9th day — live
    again today: `loc.gov` cited on the strength of its JSON API after the human-facing pages 403'd,
    and the CNN column cited from a `web.archive.org` snapshot that then stopped serving.)* Doing it
    is: one line in the wave doc — cite what was fetched, or cite canonical and record the archived
    URL as the trail — plus a decision on whether to backfill.

83. **Refuse `rights` at the generator when the record's own prose dates the headline wording after
    the work the rights are reasoned from.** *(6th day — #949 did this by hand again today, declining
    public domain on the 1943 Lyons column because the copyright renewal could not be checked.)* One
    rule in `prep-wave.js` clearing `rights` when a record's dating fields and its headline wording
    disagree.

79. **Give `mis-kind.js` patterns for `Unconfirmed` and `Earliest located`.** *(10th day.)* Both fall
    through `kindForTag` to `''`, which `template.js` renders as the burgundy ✕ meaning *refuted*.

77. **Add `meta.description` and `meta.ogDescription` to `PLAIN_TEXT_FIELDS`
    (`tools/html-safety.js:107`).** *(11th day.)* Two strings in that array, then a separate
    corpus-wide sweep for pages already shipping literal `&lt;em&gt;` in their search snippet.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 2 | Stop the daily wave and the backlog waves colliding over the queue | 28th |
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" | 28th |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 28th |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 34th, clock running |
| 9 | Fix the middle-initial hub fork at the generator | 27th |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row — **79 is the adjacent case** | 27th |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 27th |
| 15 | Decide what to do about Google Books' quota — **over quota again today, #949** | 30th |
| 16 | Stop one source's outage hard-stopping the review pass | 33rd |
| 19 | Decide whether `perf-check` belongs in the routine census | 31st |
| 20 | Settle the branch-name / draft conflict between the harness and the routine docs — **#951 hit it again today, doc followed; same `claude/…` name the container was parked on** | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict | 30th |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason | 31st |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — cannot see unmerged PRs | 35th |
| 24 | Work the disputed records with no `creditedTo` — **current size measured today: 99** (§ 5.4) | 41st |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) | 34th |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 34th |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 34th |
| 30/62 | Rule on "Verbatim:" and "rights held by X" corpus-wide — **89 and 91 are the adjacent cases** | 34th / 20th |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — 58 is queued behind it; **89 and 91 both want to join the queue** | 36th |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate | 37th |
| 33 | Decide the house convention for characters with no HTML entity | 33rd |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 35th |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — **92 is the record-side half of this** | 35th |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 38th |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 38th |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 34th |
| 40 | Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`) | 26th |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 26th |
| 43 | Flip GoatCounter → "Dashboard viewable by" → Anyone — **still demoted: § 4 proves it is not this environment's failure; do 13 first** | 25th |
| 44 | Teach `merge-gate.js` to look at what actually changed — #670 is its case | 25th |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 25th |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — 54 is the ruling this needs | 25th |
| 47 | Decide whether QI's Title Case should be normalised centrally | 25th |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 25th |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 25th |
| 54 | Rule on Quote Investigator *headlines* reaching `displayQuote` — the true count is UNKNOWN; do not re-derive | 23rd |
| 56 | Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog`, which is live | 22nd |
| 57 | Give the orphaned `taxes-…` candidate a way back into the queue | 22nd |
| 59 | Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it — 74 is waiting on this | 22nd |
| 61 | Fix the `mis-kind.js` comma gap; also add a `/fake/` rule to `REFUTATION_TAG` | 22nd |
| 63 | Out-of-band merges — **no occurrence today**; streak at three clean days | 3 clean days |
| 65 | Put the verdict before the re-poll chatter in the merge note — **live again today**: two re-poll lines before the verdict | 19th |
| 66 | Fix `template.js:848`'s unconditional period on `disambiguatingDescription` | 19th |
| 67 | Rule on the Parker `displayQuote` repair, which shipped 09-05 | 17th |
| 68 | Fix nested quotes in `claimReviewed` | 17th |
| 69 | Rule on whether a Wikiquote *sourced-section* anchor is enough for `verified` on an in-copyright work — **live again: #949's two `verified` Parks records rest on two sources citing p. 116, not on the book** | 17th |
| 70/71 | The 09-06 quota collision; `daily-wave` / `daily-review` still fail silently | carried |
| 74 | Decide whether `DOSSIER_SCHEMA` gets the hand-set override fields — waiting on 59 | 14th |
| 75 | Rule on the harvester's magnet-as-claimant defect — **6/76/80 and § 5.4 are the live face of this** | 13th |
| 86 | Make `daily-report.js` carry the traffic tool's error — **amend per 87 before doing it** | 4th |
| — | `loc.gov` human-facing page URLs 403 to non-browser clients — **hit again today, #949**; 81 is the general version | 10th |
| — | Add the token-subset test's false-alarm mode (`Edmund Kean → Edmund Gwenn`) to the #801 note | 14th |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Make the no-op shards carry a `note` — **#950's shard did today**, carrying its preflight state; `daily-review`'s still carries counts only | 10th |
| — | `pull_request_read/get_status` reads empty for `verify` because it is a check run, not a commit status | 5th |
| — | `web.archive.org` served, then began resetting connections mid-run (#949) — **new today**, watch for recurrence | 1st |

**Struck today:** none.

**Struck previously:** 25, 28 (superseded by 59), 50, 52, 53, 55, 60, 78, and the `looksLikePerson`
anonymity-note row (closed by #914).

---

## 7. Cost

**One run reported a real cost figure — the first since this report started asking.** Item **36**,
37th day, is no longer unanswered.

`weekly-discovery` (#948) measured itself deliberately, *"replacing the estimate"*:

> *"This run: **~215K new tokens for 20 records ≈ 11K/record**, plus 4 live source fetches (~600KB).
> That is roughly **half** the 2026-07-29 measurement of ~21K/record, and the difference is
> informative rather than noise: most of the saving came from cross-checking all 20 records
> **mechanically** first… and reading full prose only where that flagged something. The two
> corpus-wide measurements that killed both detector candidates cost well under 1K each and were the
> highest-value spend in the run."*

It also supplied its own caveat, which is why the number is trustworthy: *"this sample clustered into
four author groups (Parks ×5, Marx ×5, Twain ×3), so one fetch settled several records at once. A
sample of 20 unrelated records needing 20 separate source fetches would cost substantially more."*
Its honest range: **~10–21K/record**, *"both runs being consistency reads rather than full source
re-verification."*

Nothing else carries a figure. Today's shards carry `outcome`, `scanned`/`processed` and a prose
`note` for `daily-reports` only — no tokens, time or dollars. That is what item 36 now proposes to
fix by schema rather than by asking.

The wave again volunteered an effort profile in prose: 5 records built, an audit returning **10 issues
across 5 records with 4 highs**, **5 skeptic re-checks of which 1 was refuted**, a full-text LoC
verification run through the JSON API after the HTML 403'd, and nine hosts refused at the proxy. That
bought four corrections before ship — three invented figures, one invented passenger count, three
link cards describing unread pages, and a central wording claim narrowed from an assertion to a
documented disagreement.

Two routines cost close to nothing, correctly: `daily-reports` stopped at step 1 on an empty queue,
having first confirmed the queue was really empty rather than trusting a clean print, and
`daily-review` stopped at `flagged: 0` across 2,243 records — *"No records opened, no
`scan-fixes.js`, no edits, no `build.js`."* Both said so in their PR bodies instead of manufacturing
work. That is the system behaving.
