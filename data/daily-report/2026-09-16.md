# Daily report — 2026-09-16

## 1. Verdict

**The wave is back on and the night worked. Five records were built, audited hard enough to catch
two blockers, and are sitting in a green PR waiting for the next merge pass — nothing is broken and
nothing is urgent.**

All four routines due today ran. `daily-reports` and `daily-review` came back no-op, `daily-merge`
ran twice and merged four bookkeeping PRs, and **`daily-wave` fired for the first time in three
days**. `main` is green at `5a85eefb0`, 2,220 records, 0 flagged, tree clean, 0 deferred.

Three things to read before anything else.

1. **Item 82 is resolved — someone re-enabled the wave at 04:39 this morning and it ran.**
   `trig_01HKDxYQmKcshaCLZVfidmTa` now reads `enabled: true` with `updated_at`
   **`2026-09-16T04:39:23Z`**; it fired at `07:08:20Z` and finished `SUCCEEDED` at `07:37:58Z`. The
   two-day gap (09-14, 09-15) stands as ten records not built, but it is closed. § 5.1.
2. **The wave's audit caught two blockers, and one of them was shipping a false legal claim.**
   `any-road` carried `rights: public-domain` reasoned from the 1865 Cheshire Cat passage while the
   badge rendered against a 20th-century proverb — *"the card told readers it was free for commercial
   and paid use."* Withdrawn before ship. The other was a fabricated detail in the one primary text
   its page argues from. **The audit working is the headline; the shape that produced blocker 2 is
   new item 83.** § 5.2.
3. **No record reached `main` today.** The corpus is unchanged at 2,220 because the wave's five
   records are in #897, which is `CLEAN` and whose `verify` run **passed on the rebuilt head**
   (`90d8c14f3`, run 1818, success at 10:56:53Z). It merges at the next pass. Nothing to do. § 3.

Traffic is unavailable for the **twentieth** consecutive day, same one setting (§ 4). Item **73** is
now 8 days old and is still the only item with a deadline.

---

## 2. What ran

| slot | routine | result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run: `pr` — **5 records**, #897 (shard absent from `main`; see § 2 note) |
| 04:00 | `daily-reports` | ✓ 1 run: `no-op` — 0 processed (shard 08:10:41Z) |
| 05:00 | `daily-review` | ✓ 1 run: `no-op` — scanned 2,220, flagged 0 (shard 09:05:03Z) |
| 07:00 | `daily-merge` | ✓ 2 runs: `merged`, `merged` — 03:34Z and 10:55Z |
| 12:00 | `reports-close` | · not yet — due 17:00 UTC (yesterday's ran `no-op`, #892) |
| Mon 02:00 | `weekly-discovery` | · not scheduled today (Wednesday; last ran 09-14, next 09-21) |

**Nothing is missing from the roster.** Every slot due today produced a shard, which is the first
time that has been true since 09-13.

`daily-report.js` reported **1 tool-call failure** — `traffic: fetch failed`. **§ 4 is the only
incomplete part of this report; §§ 2, 3, 5 are complete.**

Quality note, unchanged for a **fifth** day: today's `daily-reports` and `daily-review` shards again
carry **no `note` field** —

```
{"date":"2026-09-16","routine":"daily-reports","outcome":"no-op","processed":0}
{"date":"2026-09-16","routine":"daily-review","outcome":"no-op","scanned":2220,"flagged":0}
```

— while both PR bodies describe their preflight recovery in detail. The evidence exists; it does not
reach the shard, which is what the census reads.

---

## 3. What shipped

**Merged inside today's window (4) — all bookkeeping, no content:**

| PR | what |
|---|---|
| #899 | `daily-review` 2026-09-16 — no-op, 2,220 scanned, 0 flagged |
| #898 | `daily-reports` 2026-09-16 — no-op, empty reader-report queue |
| #896 | merge-pass log shard, 2026-09-16 |
| #895 | merge-pass log shard, 2026-09-15 |

**Records built: 5. Records on `main`: 0.** That is not a discrepancy — it is the pipeline working at
its normal latency. The wave opened #897 at 07:36Z; the 10:55Z merge pass rebuilt and pushed it
(*"#897 rebuilt and pushed — merges next pass, once verify is green on the new head"*) and `verify`
has since gone **green** on that new head. It needs nothing from anyone.

**Corpus:** 2,220 records · 0 flagged · report ladder `pr` · 0 deferred · working tree clean · CI on
`main` **success** at `5a85eefb0`.

**Open PRs — none of them mine to merge:**

- **#897** (`wave-d20260916`): `CLEAN`, 0d, 41 files. Green. The next merge pass collects it.
- **#900** (`merge/2026-09-16`): `CLEAN`, 0d, opened 10:56Z by `merge.yml` — the 10:55Z pass's own
  shard. `verify` green. Needs nothing.
- **#670**: `DIRTY`, open since 08-27 and untouched since — item **42**, 21st day, unchanged.

**The report window lost four more merged PRs overnight** — #890, #892, #893 and #894, all merged
between yesterday's report (#891, merged `2026-09-15T12:23:27Z`) and `00:00Z` today. All four are
bookkeeping this time, so nothing content-bearing was lost, but no report will carry their shipping
row. Item **4**, 40th day.

---

## 4. Traffic

**Traffic: unavailable — twentieth consecutive day.** `traffic` is `null` and `tool_failures`
carries `traffic: fetch failed`. This is an **analytics outage, not a quiet day**, and it is not a
zero.

`node tools/traffic.js` run directly gives the error the JSON does not:

```
traffic.js: 403 from GoatCounter — the dashboard is no longer public.
Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.
```

So there is no figure to report as an upper bound, spam-adjusted or otherwise, and no referrer
breakdown. **Twenty days of traffic are not lost** — GoatCounter is still collecting; this report
cannot read it. Item **43**, one setting in the GoatCounter UI.

---

## 5. Findings

### 5.1 The wave was switched back on, and it ran — item 82 closed

| field | value |
|---|---|
| id | `trig_01HKDxYQmKcshaCLZVfidmTa` |
| **enabled** | **`true`** (was `false`) |
| **updated_at** | **`2026-09-16T04:39:23Z`** ← switched back on |
| next_run_at | `2026-09-17T07:04:34Z` |
| last_run | `SUCCEEDED` — fired `2026-09-16T07:08:20Z`, finished `07:37:58Z` |

Yesterday's report read the 09-13 pause as probably deliberate and asked for a ruling. The ruling
arrived as an action rather than a sentence, which is fine. **Item 82 drops off the list.** The gap
cost two days and ten records; the backlog resumes draining today.

### 5.2 The wave's own findings — 2 PASS / 3 FAIL, 14 issues, 2 blockers

This is the substantive PR of the night and its body is where the work is. Quoting it:

**Blocker 1 — `how-long-is-forever`.** The excerpt note glossed the Hatter's quarrel with Time as
Time *"having been beaten by the Hatter at a concert"*. The primary text says the opposite: the Hatter
sang at the Queen of Hearts' concert and *the Queen* bawled *"He's murdering the time!"*. The wave's
own summary of why this is the serious one:

> A fabricated detail about the one text the page argues from, in the same paragraph that argues
> from it.

Rewritten from the passage, with the wording quoted.

**Blocker 2 — `any-road`.** `rights` was set `public-domain`, reasoned from the 1865 Cheshire Cat
passage —

> But the badge, the "Can you use it?" card and the JSON-LD public-domain mark render against the
> page's **headline quote**, which is the 20th-century proverb the page itself says is tied to no
> pre-1931 work — and the card told readers it was free for commercial and paid use.

Badge withdrawn, `rights` removed, prose-only, and the card now reads *"don't present it as public
domain."* **This is the only finding of the night with a legal edge**, and it is the one I would
want a guard for rather than an auditor — see item **83**.

**Refuted by the skeptic pass: 2 of 14 (14%)** — in the band the runbook predicts. 12 fixed. The four
highs were all on the Frost page and all the same class, *"specificity the source does not carry"*,
including an end page (`66-71`) in the copy-to-clipboard citation that *"came from a search snippet,
not from QI."*

**On the method, which is worth recording:** the four Carroll lines were checked against a corpus
assembled locally from Project Gutenberg — twelve English works, 2.1M characters, line breaks
flattened — plus an `insource:` search across all of Wikiquote. `"forever"` occurs **zero** times in
the corpus; `"obligation"` twice, both unrelated and both read; `"any road"` **zero**. That is a
negative result strong enough to publish, and the pages do.

**A note on the failure rate.** 3 of 5 FAIL and 2 blockers is well above the 08-27 wave's 5 PASS / 0
FAIL. It is not a regression signal: four of the five records were disputed Carroll attributions with
every corroborating source behind a blocked host, which is the hardest shape this pipeline handles.
Worth watching, not worth acting on.

### 5.3 What the wave could not establish — three blocked hosts, one policy question

The wave declined to invent anything and said so on the pages. All four cases are egress refusals:

- **The origins of "How long is forever?" and "I am under no obligation to make sense to you."**
  *"Every film-transcript host tried (`springfieldspringfield`, `imsdb`, `subslikescript`,
  `scripts.com`, the Alice fandom wiki) is refused by the egress proxy, so no script was read and
  **no studio is credited**."* Both records ship with the author unestablished.
- **The French trail behind "But, said Alice…"** — `awfullybigblogadventure.blogspot.com` blocked,
  *"the Wayback availability API returns no snapshot"*. Recorded in `sourcesVerified` with
  `containsClaim: false`; *"the claim rests on Wikiquote's summary alone."*
- **Reginald Cook's 1945 *College English* article** — *"JSTOR and Crossref are both refused by the
  proxy (403 on CONNECT)"*, so the Frost wording *"rests on QI's [transcription], not on our own
  reading of page 69."* The page says this.

This is item **13** (extend the allowlist) with five new named hosts, and it is the second wave in
four days to run straight into item **81** (cite what was fetched, or cite canonical and record the
archived URL). Neither is new; both got more expensive today.

### 5.4 Item 12 — the shallow clone, now with a second shape in one morning

Two containers hit preflight failures this morning and **they were not the same failure**:

| session | failure | exception | what recovered it |
|---|---|---|---|
| wave (#897) | `local main sane` + `git on main` | **(a)** | `git checkout -B main origin/main` |
| review (#899) | `git on main` alone, on `claude/wizardly-fermat-p31wyq` | **(b)** | checkout + ff |
| **this report** | `git on main` alone, on detached HEAD | **(b)** | checkout + ff |

This container is shallow too — `is-shallow-repository` → `true`, `.git/shallow` carries two grafts
(`31590d5b3`, `63670593b`) — but `local main sane` **passed** here, because the local `main` ref had
been created from `refs/remotes/origin/main` inside the same graft segment rather than across the
boundary. So the shallow clone is the standing condition and *which* exception fires depends on where
the container's `main` ref happened to land.

**One thing worth recording precisely, because the doc was corrected on exactly this point.** The
checkout alone would have been wrong again today: local `main` was **3 commits behind** `origin/main`
and `git checkout main` moved the tree backward onto it; the `git merge --ff-only origin/main` that
exception (b) requires is what closed the gap (`Updating 0c5e03e0d..5a85eefb0`, fast-forward, three
routine-log shards). That is the fourth independent confirmation that the 2026-08-16 correction is
right and the 2026-08-14 version was not.

Item **12**'s two options are unchanged, and today strengthens (a): `git fetch --unshallow` in
container setup retires **both** exceptions at once, which a preflight-side patch cannot, because the
two shapes need different remedies.

### 5.5 What the no-op routines said

**#899 (`daily-review`)** — *"Nothing. `node tools/scan.js` — 2220 records, 6 detectors, incremental:
ran 0 checks, skipped 13320 already settled, **flagged: 0**. Scan state unchanged, nothing written."*
`vocab-sweep`: 6 tables, **0 under-used keys**. It listed what it deliberately did not do — no
records opened, no `scan-fixes.js`, no `build.js`, no `stamp` — because *"stamping unflagged records
would churn content hashes for no reason."*

**#898 (`daily-reports`)** — empty queue, and it checked rather than inferred:

> `GET https://quotle-community.stewartd.workers.dev/reports/pending` → `HTTP 200
> {"pending":[],"count":0}` — A genuine empty queue, not a down endpoint.

*"Did not authenticate to anything. No `ADMIN_TOKEN` was looked for or used."*

**Neither reported a defect, and neither manufactured one.** No detector candidate was proposed today
— discovery runs Mondays and last ran 09-14.

### 5.6 Carried items re-confirmed this morning

- **Item 18 fired again:** `daily-report.js` credits `daily-merge` with *"4 record(s)"* from shards
  carrying `processed: 1` and `processed: 3`. Those are merged PRs, not records. Today the true
  record figure (5, from the wave) is reported correctly in the adjacent row, which makes the
  mislabel easier to misread, not harder.
- **Item 20 recurred, and #899 spelled it out:** *"My session's standing instruction is to open PRs
  as drafts; `workflows/DAILY-REVIEW.md` says the opposite and explains why… The scheduled prompt
  says the doc wins where they disagree, so I followed the doc."* This report overrode both defaults
  as well.
- **Item 63 (out-of-band merges) — clean, seventh consecutive day.** All four merges went through the
  07:00 pass.
- **Item 65 — clean a third day.** Both merge notes lead with the verdict (`merged #895`,
  `merged #896 #898 #899`) before the re-poll chatter.
- **The no-`note` shards item — 5th day** (§ 2).

---

## 6. Needs a decision

**One item is added (83) and one is removed (82, done). The list is otherwise yesterday's, a day
older. Nothing here is blocking content from shipping today.**

83. **Refuse `rights` at the generator when the record's own prose dates the headline wording after
    the work the rights are reasoned from.** *(New — § 5.2, blocker 2.)* `any-road` shipped a
    public-domain badge and a "free for commercial use" card derived from an 1865 passage while the
    page itself said the headline proverb traces to 1941, and only the audit caught it. Doing it is:
    one rule in `prep-wave.js` that clears `rights` (and forces prose-only `rightsNote`) when a
    record's dating fields and its headline wording disagree — the same shape as item **80**, on the
    one field where being wrong is a legal claim rather than a factual one.

12. **Pick the fix for the shallow clone.** *(33rd day.)* Every routine burns its first minutes on a
    preflight exception, and today proved the shape varies by container (§ 5.4). Doing it is **one**
    of: (a) `git fetch --unshallow` in container setup — **retires both exceptions at once**; or
    (b) make `preflight.js:171` shallow-aware, which is smaller but only covers exception (a)'s
    shape and leaves (b) in place. **Today is an argument for (a).**

73. **Write the 2026-09-07 `daily-wave` shard to `main`.** *(8th day — still the only item with a
    deadline.)* `main` still says that wave did not run. Doing it is:
    `git show origin/wave-d20260907:data/routine-log/2026-09-07T07-44-35-244Z-daily-wave.jsonl` onto
    a fresh branch, commit, PR — **it stays recoverable only until that branch is pruned.**

43. **Flip GoatCounter → Settings → "Dashboard viewable by" → Anyone.** *(20th day.)* Twenty days
    with no traffic figure in this report; one setting in the GoatCounter UI, no data lost.

4.  **Fix the report window.** *(40th day — four more PRs lost overnight, all bookkeeping this
    time.)* Doing it is: change `daily-report.js:30`/`:263` to window from the previous report's
    timestamp instead of the UTC calendar day, on its own branch.

42. **Rebuild and merge #670, or close it.** *(21st day; `DIRTY` and untouched since 08-27.)*
    Closing it loses five records that exist nowhere else **and** orphans its 08-27 wave shard.
    Doing it is: merge `main` in, take `main`'s side on generated files, `node tools/build.js`,
    `node tools/scan.js`, push — or close it and accept both losses knowingly.

13. **Extend the egress allowlist.** *(36th day — five new hosts named today.)* Today it cost two
    records their author attribution entirely (§ 5.3). Doing it is: add the film-transcript hosts
    (`imsdb`, `subslikescript`, `scripts.com`, `springfieldspringfield`), JSTOR/Crossref, and the
    previously requested `barrypopik.com`, `annefrank.org`, Google Books and HathiTrust — or rule
    that the pipeline lives without them and **81** becomes the answer instead.

81. **Rule on archived-vs-canonical citation when a host is blocked by egress.** *(4th day — asked
    for by the 09-13 wave, hit again today.)* Doing it is: one line in the wave doc — cite what was
    fetched, or cite canonical and record the archived URL as the verification trail — plus a
    decision on whether to backfill.

80. **Detect the used-but-didn't-coin shape in `prep-wave.js` and stamp the four levers.** *(4th
    day.)* One rule setting `claimVerb`/`verdictLead`/`pkitWarn` centrally. **Distinct from 76; 83
    is its rights-field sibling.**

79. **Give `mis-kind.js` patterns for `Unconfirmed` and `Earliest located`.** *(5th day.)* Both fall
    through `kindForTag` to `''`, which `template.js` renders as the burgundy ✕ meaning *refuted*.

77. **Add `meta.description` and `meta.ogDescription` to `PLAIN_TEXT_FIELDS`
    (`tools/html-safety.js:107`).** *(6th day.)* Two strings in that array, then a separate
    corpus-wide sweep for pages already shipping literal `&lt;em&gt;` in their search snippet.

76. **Widen `rightPersonWrongWords()` or split the magnet from the false-credit claim in
    `prep-wave.js`.** *(7th day.)* One central edit on its own branch.

14. **Decide the `isBasedOn → isPartOf` bulk migration.** *(24th day — 1,053 / 814 / 21 both / 332
    neither, as measured by #879 on 09-14.)* Rule on the distinction, then one scripted backfill.

17/58. **Fail the build when `Quotation.creator` is suppressed, and normalise `template.js`'s hero
    match.** *(27th day.)* Make `creatorOk` compare on an accent-folded name, and assert that a
    record with an `answer.authorName` cannot emit a page without a `creator`.

72. **`tools/review.js:267` reads rights from `meta.rights`/`rights`, but records store it at
    `source.rights`, so every audit has seen `null`.** *(10th day — and § 5.2's blocker 2 is exactly
    a rights claim the audit had to catch by reading prose.)* Change that fallback chain, then
    decide separately whether to re-audit the rights claims never checked.

18. **Fix `daily-report.js:194`'s `work` figure.** *(24th day.)* One field rename or a per-routine
    label.

36. **Make the wave log a cost figure.** *(32nd day — § 7.)* One field in the wave's routine-log
    shard.

5.  **Declare which carried items you are not going to do.** *(24th day.)* The table below is 41
    rows. Reply with the numbers to strike and tomorrow's report carries only what survives.

### Resolved or corrected today

- **Item 82 is done** — the wave Routine is `enabled: true` again as of `2026-09-16T04:39:23Z` and
  ran successfully at 07:08Z. Removed from the list.
- **#897 needs nothing** — green on its rebuilt head, `CLEAN`, collected by the next merge pass.
- **#900 needs nothing** — the merge pass's own shard, `CLEAN`, opened by `merge.yml`.
- **Item 12 gains a fact rather than a fix:** the shallow clone produces *two different* preflight
  failures depending on where the container's `main` ref lands (§ 5.4), which is a direct argument
  for remedy (a) over (b).
- **Item 63 clean a seventh day; item 65 clean a third.** Both stay listed.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 2 | Stop the daily wave and the backlog waves colliding over the queue | 23rd |
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" | 23rd |
| 6 | Make `prep-wave.js:206` stamp `creditedTo` when `confidence === 'disputed'` regardless of name equality — **75/76/80 are the same guard's other failures** | 30th |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 23rd |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 29th, clock running |
| 9 | Fix the middle-initial hub fork at the generator | 22nd |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row — **79 is the adjacent case** | 22nd |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 22nd |
| 15 | Decide what to do about Google Books' quota | 25th |
| 16 | Stop one source's outage hard-stopping the review pass | 28th |
| 19 | Decide whether `perf-check` belongs in the routine census | 26th |
| 20 | Settle the branch-name / draft conflict between the harness and the routine docs — **#899 and this report both overrode it again** | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict | 25th |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason | 26th |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — cannot see unmerged PRs | 30th |
| 24 | Work the disputed records with no `creditedTo` | 36th |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) | 29th |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 29th |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 29th |
| 30/62 | Rule on "Verbatim:" and "rights held by X" corpus-wide | 29th / 15th |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — 58 is queued behind it | 31st |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate | 32nd |
| 33 | Decide the house convention for characters with no HTML entity | 28th |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 30th |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — folded into 17/58 | 30th |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 33rd |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 33rd |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 29th |
| 40 | Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`) | 21st |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 21st |
| 44 | Teach `merge-gate.js` to look at what actually changed — #670 is its case | 20th |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 20th |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — 54 is the ruling this needs | 20th |
| 47 | Decide whether QI's Title Case should be normalised centrally | 20th |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 20th |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 20th |
| 51 | Make the merge pass's rebuild step fetch before it merges | 19th |
| 54 | Rule on Quote Investigator *headlines* reaching `displayQuote` — the true count is UNKNOWN; do not re-derive | 18th |
| 56 | Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog`, which is live | 17th |
| 57 | Give the orphaned `taxes-…` candidate a way back into the queue | 17th |
| 59 | Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it — 74 is waiting on this | 17th |
| 61 | Fix the `mis-kind.js` comma gap; also add a `/fake/` rule to `REFUTATION_TAG` | 17th |
| 63 | Out-of-band merges — **clean, seventh consecutive day**; stays listed | not live today |
| 64 | Make the merge pass merge content PRs before routine-log PRs | not live today |
| 65 | Put the verdict before the re-poll chatter in the merge note — **clean a third day** | 14th |
| 66 | Fix `template.js:848`'s unconditional period on `disambiguatingDescription` | 14th |
| 67 | Rule on the Parker `displayQuote` repair, which shipped 09-05 | 12th |
| 68 | Fix nested quotes in `claimReviewed` | 12th |
| 69 | Rule on whether a Wikiquote *sourced-section* anchor is enough for `verified` on an in-copyright work | 12th |
| 70/71 | The 09-06 quota collision; `daily-wave` / `daily-review` still fail silently | carried |
| 74 | Decide whether `DOSSIER_SCHEMA` gets the hand-set override fields — waiting on 59 | 9th |
| 75 | Rule on the harvester's magnet-as-claimant defect | 8th |
| — | `loc.gov` human-facing page URLs 403 to non-browser clients — 81 is the general version | 5th |
| — | `looksLikePerson` still accepts an anonymity note ("Nobody — it has no traceable author") | 7th |
| — | Add the token-subset test's false-alarm mode (`Edmund Kean → Edmund Gwenn`) to the #801 note | 9th |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Make the no-op shards carry a `note` — the evidence is in the PR bodies but not in what the census reads | 5th |

**Struck previously:** 25, 28 (superseded by 59), 50, 52, 53, 55, 60, 78. **Nothing is struck today.
Item 82 is done, not struck.**

---

## 7. Cost

**No run reported a cost figure.** Today's four shards carry `processed`, `scanned` and `flagged` —
no cost field anywhere, including the wave's. Item **36**, 32nd day.

The wave did volunteer an effort profile in prose, which is the closest thing to a number this report
has: four records checked against **twelve Gutenberg works, 2.1M characters**, plus an `insource:`
search across all of Wikiquote, plus a full audit (14 findings) and a skeptic re-check of every
`high`/`blocker`. That is the expensive shape of night, and today it bought two blockers caught
before ship — including one that would have told readers a copyrighted-status-unknown proverb was
free for commercial use.

Yesterday's spend was near zero because nothing shipped. Today's was not, and that is the better
outcome.
