# Daily report — 2026-09-23

## 1. Verdict

**Every routine due today ran, nothing is flagged, and the night's headline is an audit blocker the
wave caught in its own freshly-written prose and fixed before shipping.** The wave built 5 records
(#968 — 2 David Ogilvy, 3 Sam Walton), `daily-reports` and `daily-review` both correctly no-opped,
and the merge pass landed 4 PRs across two runs. `main` is green at `914553e8a`, **2,253 records,
0 flagged, tree clean, 0 deferred**.

Four things are worth the operator's eye rather than a scroll past:

- **#968's audit found a page asserting the opposite of the source it cited**, on the one page whose
  entire argument is that people repeat claims without checking: *"Snopes says the opposite: it
  presents that narrative as the circulating account under assessment and corrects it in its own
  voice."* Fixed in-record before the PR opened. 1 blocker + 4 high, **4/4 skeptic-confirmed** (§ 5.1).
- **The 07:00 pass rebuilt #968's head, then exited 23 seconds before that head's `verify` went
  green** — so five records wait a full 3-hour merge cycle for a check that had already passed. New
  item **94**, measured against both runs' own clocks (§ 5.2).
- **Exception (b) fired on 4/4 containers, and on this one the fast-forward actually moved.**
  Yesterday's report stated that *"(b)'s precondition can never hold again"*. That is falsified by
  today's observation, and the correction matters because the bare checkout would have moved this
  tree **backward 3 commits** (§ 5.3).
- **The report window ate 5 PRs overnight, including #958's five records** — the seventh consecutive
  day, the seventh in a row in which it swallowed a content PR (§ 3). Item **4**.

Traffic is unreadable for the **26th consecutive day**, and the 403 is again timestamped to the
egress gateway rather than to GoatCounter — this run's own two attempts, to the second (§ 4).

**No item is added beyond 94, and nothing is struck.** Nothing in § 6 is blocking content from
shipping.

---

## 2. What ran

| slot | routine | result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run — **pr**, 5 records (#968) |
| 04:00 | `daily-reports` | ✓ 1 run — no-op (empty queue, endpoint confirmed up) |
| 05:00 | `daily-review` | ✓ 1 run — no-op (2,253 scanned, 0 flagged) |
| 07:00 | `daily-merge` | ✓ 2 runs — merged, merged; 4 PRs |
| 12:00 | `reports-close` | · not yet due (17:00 UTC) — **this report runs before it** |
| Mon 02:00 | `weekly-discovery` | · not scheduled today (Wednesday) |

**Nothing is missing.** No routine failed to run.

Judged late, from yesterday's slot this report ran too early to see: **`reports-close` 2026-09-22 ran,
1 run, no-op.** So the 09-22 census is now complete and shows all six slots accounted for.

---

## 3. What shipped

**Merged in the report's window (4):**

| PR | what |
|---|---|
| #970 | `daily-review` 2026-09-23 no-op shard |
| #969 | `daily-reports` 2026-09-23 no-op shard |
| #967 | merge-pass 2026-09-23 shard |
| #966 | merge-pass 2026-09-22 shard |

**Merged outside it, and therefore in no report — this is item 4, seventh consecutive day:**

| PR | merged (UTC) | what |
|---|---|---|
| #963 | 09-22 16:22:30 | `reports-close` 09-22 shard |
| #961 | 09-22 16:22:43 | merge-pass shard |
| **#958** | **09-22 20:56:34** | **5 records — Satchel Paige ×3, David Ogilvy ×2** |
| #964 | 09-22 20:56:45 | merge-pass shard |
| #965 | 09-22 23:31:37 | merge-pass shard |

Five PRs merged after yesterday's report ran (12:17 UTC) and before today's UTC day began. **The
corpus moving 2,248 → 2,253 is the only trace #958 leaves in any report**, and it leaves it as an
unexplained delta rather than as a merge. Yesterday this cost four PRs; today five.

**Open (3):**

- **#968** `wave-d20260923` — CLEAN, 0d, `verify` **green** on the rebuilt head `2d694e6c6`. Merges
  next pass. Not mine to merge.
- **#971** `merge/2026-09-23` — CLEAN, 0d, the 10:52 pass's own shard.
- **#670** `wave-d20260827` — **27 days open, DIRTY, untouched since 08-27.** `origin/wave-d20260827`
  confirmed still present at `86be66b36` this morning. Item **42**.

**Corpus:** 2,253 records · 0 flagged · 0 deferred · tree clean · CI green on `main` at `914553e8a` ·
report ladder `pr`. Backlog after #968's draw: **214 queued, 0 selected.**

Distribution, from #970's `vocab-sweep`: confidence verified **727** / attributed **269** / disputed
**1,257**; rights public-domain **753** / in-copyright **750** (+750 `uncertain` defaults on the reuse
chip); misattribution kinds refuted **4,794** / context **1,668** / genuine **128**. **0 under-used
vocabulary keys** across all 6 tables.

---

## 4. Traffic

**Traffic: unavailable.** `traffic` is `null` in the tool's JSON and `health.tool_failures` carries
`traffic: fetch failed`. **This is an analytics outage, not a quiet day — it is not a zero**, and no
figure for today, 7d, 30d or all-time can be given.

The cause is settled for the fifth consecutive day, against this run's own clock. The agent proxy's
status endpoint logged both of this pass's attempts, by host and to the second:

```
{ "ts": "2026-09-23T12:04:22.207Z", "kind": "connect_rejected",
  "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
  "host": "quotle.goatcounter.com:443" }
{ "ts": "2026-09-23T12:04:28.939Z", ... same ... }
```

Meanwhile `node tools/traffic.js`, run directly, printed:

> `traffic.js: 403 from GoatCounter — the dashboard is no longer public.`
> `Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.`

**That sentence is false in this environment.** Nothing this routine sends reaches GoatCounter; the
403 is the gateway's answer to `CONNECT`, before any HTTP request is made. Item **87**. It does not
establish the dashboard's actual state either way, so item **43** stays demoted rather than struck.

**Reminder on the figures themselves, for when they return:** GoatCounter counts everybody — AI
assistants, Bing, DuckDuckGo, direct — and is the only traffic number this report carries; do not
substitute Search Console. The figures are an **upper bound**, unfiltered for referrer spam
(`spamFiltered` is always `false` here), so report them as "up to N". `node tools/traffic.js --refs`
gives the referrer breakdown. Neither has been readable since 08-28 — **26 days blind**.

---

## 5. Findings

### 5.1 The wave shipped 5 records, and its audit's blocker was a source read backwards

#968's audit returned **2 PASS / 5, 3 FAIL — 10 issues (1 blocker, 4 high, 3 medium, 2 minor)**, all
fixed in-record. Every source link on all five pages was re-fetched and tested against the specific
claim pinned to it.

**The blocker.** `i-pay-low-wages…` stated as a Snopes-sourced fact that Walton *"restructured his
stores to fall under the exemption threshold… a court later ruled against him and ordered back pay."*
The wave's own words on what it found instead:

> **Snopes says the opposite:** it presents that narrative as the *circulating account under
> assessment* and corrects it in its own voice — the structure was "already in place for other
> reasons; not, as suggested above, one that was concocted solely to skirt minimum wage laws". The
> court ruling and back pay appear only inside the quoted account. On a page whose whole argument is
> that people repeat claims without checking the source, this was the worst possible defect.

**The three high findings share one shape, and it is the corpus's documented #1 failure mode.** All
three Walton records pinned full-text findings about the memoir to
`archive.org/details/samwaltonmadeina0000walt` — *"a lending-restricted catalogue page (Borrow only,
re-fetched to confirm) that cannot demonstrate them… a real link that does not support the claim
attached to it."* Each `sourcesVerified` row was split so the catalogue URL carries only what it
supports.

**On the skeptic pass, the wave reported a rate below expectation and refused to fake it:**

> All 4 high/blocker findings were re-checked as a skeptic, defaulting to "this finding is wrong";
> **4/4 survived**. That is below the ~15–20% refutation rate the runbook expects — I looked for one
> to drop and did not find an honest candidate, and manufacturing a refutation to hit the rate would
> defeat the point.

That is the correct call, and the mirror image of yesterday, where the same brake killed four
candidate findings. It is worth watching across waves rather than acting on: one run at 0% refutation
is noise.

**The negative result rests on an instrument that was tested.** Three of the five quotes are
`disputed` with rights deliberately *unasserted*. The wave did not conclude "not found" from empty
searches — it searched a full-text scan of the Bantam printing of *Sam Walton: Made in America*,
*"the printing whose own front matter states it 'contains the complete text of the original hardcover
edition' and that 'not one word has been omitted'"*, and then checked that control phrases
(*"chintzy"*, *"payroll is one of the most…"*, *"self-esteem"*) **did** hit. Absence measured with a
demonstrably working instrument is a finding; absence from a silent search is not.

**What it could not establish, and therefore did not assert** — four gaps, three of them egress:

- **The 1955 date for "the consumer isn't a moron."** Asserted by Wikipedia, sourced to a 2012
  HuffPost feature *"blocked by the egress proxy"*. The page dates it to the 1963 book and leaves
  1955 open.
- **Who first wrote any of the three Walton sentences.** The earliest printing found for one is *"a
  Wired humour listicle, 21 Apr 2005 — 13 years posthumous, unsourced"*, which presents the line as
  already known. The other two have no dated printing at all.
- **A rival J.C. Penney attribution** for "There is only one boss" — *"goodreads.com is
  egress-blocked, so it could not be verified and is therefore not asserted anywhere on the page."*
- **Page numbers in both Ogilvy records are other people's locators** (QI's 99, Wikiquote's 87/96);
  the Archive's *Confessions* scan is lending-restricted. *"The pages say so plainly rather than
  implying we opened the book."*

Three of those four are item **13**. The fourth is item **81**.

### 5.2 The merge pass missed #968's green check by 23 seconds — item 94, new

The 10:52 merge pass merged the three log shards, rebuilt #968 against the new `main`, and then wrote,
in its own shard:

> `#968 rebuilt and pushed — merges next pass, once verify is green on the new head`

Against the clocks:

| event | UTC |
|---|---|
| merge commit pushed to `wave-d20260923` | 10:52:27 |
| `verify` run 1987 starts on `2d694e6c6` | 10:52:44 |
| **merge workflow run 320 completes** | **10:52:51** |
| `verify` run 1987 **succeeds** | 10:53:14 |

The pass gave up **23 seconds** before the check it was waiting for went green, and the cost is a
**full 3-hour merge cycle** for five finished, audited records. It already re-polls — *"re-poll 1/4:
4 PR(s) with mergeability not yet computed — waiting 6s"* — but it re-polls **mergeability**, not the
check run on a head it has just pushed itself. This is adjacent to **64/51** and not the same thing:
64/51 is about *ordering* content ahead of log shards, this is about *waiting* for the head the pass
created. Fixing 64/51 alone would not have landed #968 today.

### 5.3 Exception (b) fired on 4/4 containers — and yesterday's claim about it is falsified

Every routine reported the same opening state, and all four took exception **(b)**, not (a):

- `daily-reports` (#969): *"`git on main: on HEAD` — with `local main sane`, `tree clean` and
  `current with origin/main` all **passing**… preflight re-run **15/15 green**."*
- `daily-review` (#970): *"That is exactly exception **(b)** in the doc… Second run: all 14 checks
  passed."*
- `daily-wave` (#968): *"Preflight was green after the documented exception (b)… re-run green: 14/14."*
- This pass: identical, then 10/10 green.

**Yesterday's report stated that item 12's underlying condition meant *"(b)'s precondition can never
hold again"*. Today contradicts that on four containers, and the correction is not cosmetic.** On
this container the fast-forward was **not** a no-op — it moved `main` forward three commits
(`247efa186..914553e8a`, the 09-23 merge/reports/review shards). Had this pass run `git checkout main`
alone, as the withdrawn 2026-08-14 version of exception (b) said, the tree would have moved
**backward three commits** and this report would have been written against a `main` that predates
today's three merges. **The two-command remedy is load-bearing, and today is the first observed day
it did real work rather than printing "Already up to date".**

Item **12** is unchanged on its merits — unshallowing retires both exceptions — but its supporting
argument needs this correction. The clone here is still shallow: **50 commits reachable, oldest
`c942cd225` of 09-18.**

### 5.4 The `<em>` leak is item 77, and it is exactly 9 pages

#968 reported, without touching them, that *"9 pre-existing pages leak a literal `&lt;em&gt;` into
rendered text"* and named all nine. **Verified independently against `main`: 9 pages, the same nine
slugs, no more and no fewer.**

One precision the wave's phrasing loses, and it cuts both ways: the leak is **not** in visible body
copy. Every occurrence is inside a `<meta>` tag — `<meta name="description">` and its `og:` twin:

```
<meta name="description" content="Not Galileo at his trial. The line&rsquo;s earliest known
printing is Giuseppe Baretti&rsquo;s &lt;em&gt;The Italian Library&lt;/em&gt; (1757), p. 52 …
```

So no reader sees it on the page — and **every search engine, voice assistant and AI crawler does**,
because the meta description is exactly what they quote. On a site whose stated thesis is
voice-assistant and featured-snippet optimisation, the snippet is the worst of the two places for it
to be. This is carried item **77** precisely: `meta.description` and `meta.ogDescription` are missing
from `PLAIN_TEXT_FIELDS` at `tools/html-safety.js:107`. Twelfth day, now with a count.

### 5.5 The generator conflict the wave declined to resolve

#968's second operator note is carried item **6/76/80**, live again:

> `README.md` says a *disputed* record whose author **is** the magnet should be stamped `creditedTo`
> (it is what makes `rightPersonWrongWords()` render "Yes and no." instead of falling through to the
> fabrication default). `prep-wave.js` refuses to stamp exactly that case. I did not resolve the
> conflict — I used the corpus's existing "Unknown" hero pattern… which stamps correctly and renders
> a self-consistent page; the JSON-LD was inspected and carries no "Actually by Sam Walton"
> contradiction. But the two documents still disagree.

That is the right call for a content wave and the wrong place for the fix to live permanently — the
workaround is a per-wave judgement that a doc should be making. **36th day.**

### 5.6 No discrepancies

Every routine's claim reconciles with the census. Two figures that look like disagreements and are
not:

- #970 reports *"2253 scanned"* and the census agrees; #968's five records are not in it because
  #968 is unmerged. Its head carries 2,258.
- #968 reports *"51 corpus invariants reconcile"* and *"`scan.js` flagged 0"* against its own head,
  not `main`. `main`'s own `scan.js` result, from #970, is independently `flagged: 0` over 2,253.

Two populations re-measured this morning against `main`, both cited in § 6: **269 `attributed`
records, 33 with a hand-set `schema.verdictNote` → 236 riding the generic default** (item 93, up 1
from yesterday's 235); **1,257 `disputed` records, 99 without `creditedTo`** (item 24, unchanged).

---

## 6. Needs a decision

**One item is added (94). Nothing is struck. Nothing here blocks content from shipping** — #968 is
green and merges at the next pass. Items 12 and 4 remain the two whose fixes retire a daily cost.

94. **Make the merge pass wait for `verify` on a head it just pushed.** *(New — § 5.2.)* The pass
    rebuilt #968, then finished **23 seconds** before that head's check went green, costing five
    audited records a full 3-hour cycle; it re-polls mergeability but not the check run it caused.
    Doing it is: after a rebuild-and-push, poll the new head's check run on the same 6s cadence
    already used for mergeability, then merge in the same pass — **independent of 64/51**, which
    reorders the queue but would not have landed #968 today.

4.  **Fix the report window.** *(47th day — **five** PRs lost overnight, including **#958's five
    records**; seventh consecutive day it has eaten a content PR.)* Doing it is: change
    `daily-report.js:30`/`:263` to window from the previous report's timestamp instead of the UTC
    calendar day, on its own branch.

12. **Unshallow the container clone.** *(40th day.)* All four containers took exception **(b)** today,
    and on this one the fast-forward moved `main` three commits — the bare checkout would have moved
    the tree backward. The clone is still shallow (50 commits, oldest `c942cd225` of 09-18). Doing it
    is: `git fetch --unshallow` (or a much deeper `clone_depth`) in container setup, which **retires
    both exceptions outright**. *Note for the record: yesterday's report claimed (b)'s precondition
    could never hold again — § 5.3 falsifies that; the item stands on its own merits.*

13. **Extend the egress allowlist.** *(43rd day — it shaped a seventh consecutive wave, and is the
    proximate cause of three of #968's four "could not establish" gaps as well as § 4's blindness.)*
    Today: `huffpost.com`, `goodreads.com`, and `quotle.goatcounter.com`. Doing it is: add
    `quotle.goatcounter.com` (unblocks § 4 outright), `web.archive.org`, the named hosts,
    JSTOR/Crossref, Google Books and HathiTrust — or rule that the pipeline lives without them and
    **81** becomes the answer instead.

87. **Make `tools/traffic.js` stop asserting "the dashboard is no longer public" on any 403.** *(5th
    day — § 4 again timestamps the gateway denial against this run's own two attempts.)* Doing it is:
    at `traffic.js:61`, print the observed status and host and say the cause is *either* a GoatCounter
    setting *or* an egress denial; **then** item 86's change to `daily-report.js` propagates something
    true.

77. **Add `meta.description` and `meta.ogDescription` to `PLAIN_TEXT_FIELDS`
    (`tools/html-safety.js:107`).** *(12th day — promoted out of the carried table with a measurement:
    **exactly 9 pages**, § 5.4, all nine confirmed against `main`.)* The leak is invisible to readers
    and visible to every crawler and voice assistant, because it sits in the snippet. Doing it is: one
    line in `html-safety.js`, then rebuild — the nine pages regenerate clean, no record edits.

42. **Rebuild and merge #670, or close it.** *(28th day; `DIRTY` and untouched since 08-27;
    `origin/wave-d20260827` confirmed still present at `86be66b36` this morning.)* Closing it loses
    five records that exist nowhere else — including **two Thornton Wilder records verified against two
    independently scanned 1927 printings** — **and** orphans its 08-27 wave shard. Doing it is: merge
    `main` in, take `main`'s side on generated files, `node tools/build.js`, `node tools/scan.js`,
    push — or close it and accept both losses knowingly.

73. **Write the 2026-09-07 `daily-wave` shard to `main`.** *(15th day — still the only item with a
    deadline; `origin/wave-d20260907` confirmed still present at `6e6d0d2f9` this morning.)* `main`
    still says that wave did not run. Doing it is:
    `git show origin/wave-d20260907:data/routine-log/2026-09-07T07-44-35-244Z-daily-wave.jsonl` onto a
    fresh branch, commit, PR — **it stays recoverable only until that branch is pruned.**

93. **Derive the `attributed` verdict note in `toRecord` instead of letting it default.** *(2nd day;
    population re-measured this morning: **236 of 269**, up 1.)* `DOSSIER_SCHEMA` cannot emit
    `schema.verdictNote`, so an `attributed` record whose prose cites a primary source ships JSON-LD
    saying the opposite until an audit catches it by reading the page. Doing it is: one central edit in
    `workflows/generate.js`'s `toRecord` deriving the note from whether a primary source is cited —
    which costs **no `SCHEMA_BUDGET` bytes**, so unlike item 74 it is not queued behind item 59 — plus
    a decision on whether to sweep the 236.

64/51. **Make the merge pass merge content PRs before routine-log PRs.** *(Seventh day — the second
    pass merged three log shards, then deferred #968's five records a pass.)* Doing it is: order the
    merge queue content-first, so content PRs merge before `main` moves under them. **See 94** — on
    its own this would not have landed #968 today.

6/76/80. **Reconcile `workflows/README.md`'s r35 note with `prep-wave.js:207`.** *(37th day — live
    again: § 5.5, and #968 routed around it with the "Unknown" hero pattern rather than resolving it.)*
    The distinguishing case is the magnet-is-the-true-author one, which is mechanically
    distinguishable. Doing it is: read that comment and the 41 records against the two hand-stamps,
    then one central edit encoding the three-way rule.

90. **Correct what `preflight.js` and `DAILY-REPORT.md` say exception (a) *is*.** *(3rd day — **not
    live today**: all four containers took (b), and (b)'s own text is accurate.)* Preflight prints
    *"diverged, or an unrelated history"* for `local main sane`, but the ref can be a true ancestor a
    shallow clone cannot see. Doing it is: reword that failure to name shallow truncation when
    `.git/shallow` exists, and amend the exception-(a) paragraph to match — the `-B` remedy is
    unchanged and still correct.

81. **Rule on archived-vs-canonical citation when a host is blocked by egress.** *(11th day — live
    again: the lending-restricted IA scans drove three of #968's four high/blocker findings, and it
    again kept an unconfirmed page locator **out** of the copy-to-clipboard string.)* Doing it is: one
    line in the wave doc — cite what was fetched, or cite canonical and record the archived URL as the
    trail — plus a decision on whether to backfill, and whether the locator convention becomes house
    style.

91. **Rule that a record may not cite a source the run never fetched — and make it mechanical.** *(3rd
    day — #968 followed the unwritten rule voluntarily again, which is evidence it is writable.)*
    Doing it is: one line in the wave doc forbidding a source card for a URL with no successful fetch
    in the run's trail, plus a `prep-wave`/`scan.js` check that a cited URL appears in the run's fetch
    log.

36. **Decide whether the routine-log shards get a `cost` field.** *(39th day — **no run reported a cost
    figure today**, § 7.)* Doing it is: add a `cost` field to the shard schema and one line in each
    routine doc asking for it, so the trend is machine-readable instead of living in prose.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 2 | Stop the daily wave and the backlog waves colliding over the queue | 30th |
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" | 30th |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 30th |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 36th, clock running |
| 9 | Fix the middle-initial hub fork at the generator | 29th |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row — **79 is the adjacent case** | 29th |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 29th |
| 15 | Decide what to do about Google Books' quota — not hit today | 32nd |
| 16 | Stop one source's outage hard-stopping the review pass | 35th |
| 19 | Decide whether `perf-check` belongs in the routine census | 33rd |
| 20 | Settle the branch-name / draft conflict between the harness and the routine docs — **#970 hit it again today, doc followed** | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict | 32nd |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason — **live: #968 ships three records with rights unasserted** | 33rd |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — cannot see unmerged PRs | 37th |
| 24 | Work the disputed records with no `creditedTo` — **re-measured today: 99, unchanged** | 43rd |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) — **77 is the meta-tag face of this** | 36th |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 36th |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 36th |
| 30/62 | Rule on "Verbatim:" and "rights held by X" corpus-wide — **89 and 91 are the adjacent cases** | 36th / 22nd |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — 58 is queued behind it; 89 and 91 both want to join the queue | 38th |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate | 39th |
| 33 | Decide the house convention for characters with no HTML entity | 35th |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 37th |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — **92 is the record-side half of this** | 37th |
| 37 | Decide the `Unknown`-creator `sameAs` convention — **live: #968 used the "Unknown" hero pattern deliberately** | 40th |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 40th |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 36th |
| 40 | Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`) | 28th |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 28th |
| 43 | Flip GoatCounter → "Dashboard viewable by" → Anyone — **still demoted: § 4 proves it is not this environment's failure; do 13 first** | 27th |
| 43′ | Run `node tools/traffic.js` once locally, unproxied — one command, whenever convenient | 5th |
| 44 | Teach `merge-gate.js` to look at what actually changed — #670 is its case | 27th |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 27th |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — 54 is the ruling this needs | 27th |
| 47 | Decide whether QI's Title Case should be normalised centrally | 27th |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 27th |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 27th |
| 54 | Rule on Quote Investigator *headlines* reaching `displayQuote` — the true count is UNKNOWN; do not re-derive | 25th |
| 56 | Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog`, which is live | 24th |
| 57 | Give the orphaned `taxes-…` candidate a way back into the queue | 24th |
| 59 | Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it — 74 is waiting on this; **93 deliberately routes around it** | 24th |
| 61 | Fix the `mis-kind.js` comma gap; also add a `/fake/` rule to `REFUTATION_TAG` | 24th |
| 63 | Out-of-band merges — **no occurrence today**; streak at five clean days | 5 clean days |
| 65 | Put the verdict before the re-poll chatter in the merge note — **half-fixed today**: the verdict leads, but the consequential line (#968 deferred) is last, after two re-poll lines | 21st |
| 66 | Fix `template.js:848`'s unconditional period on `disambiguatingDescription` | 21st |
| 67 | Rule on the Parker `displayQuote` repair, which shipped 09-05 | 19th |
| 68 | Fix nested quotes in `claimReviewed` | 19th |
| 69 | Rule on whether a Wikiquote *sourced-section* anchor is enough for `verified` on an in-copyright work — **live again: #968's "consumer isn't a moron" is `verified` on a Wikiquote sourced section + Wikipedia, both citing a book nobody opened** | 19th |
| 70/71 | The 09-06 quota collision; `daily-wave` / `daily-review` still fail silently | carried |
| 74 | Decide whether `DOSSIER_SCHEMA` gets the hand-set override fields — waiting on 59; **93 is the byte-free subset** | 16th |
| 75 | Rule on the harvester's magnet-as-claimant defect — 6/76/80 is the live face of this | 15th |
| 79 | Give `mis-kind.js` patterns for `Unconfirmed` and `Earliest located` | 12th |
| 83 | Refuse `rights` at the generator when the record's own prose dates the headline wording after the work the rights are reasoned from — **8th day; #968 did this by hand again, shipping three records with no rights status asserted rather than a guess** | 8th |
| 84 | Decide whether the wave is allowed to run without `WebSearch` — not live today; #968 reported no missing tool | 7th |
| 85 | Give `DOSSIER_SCHEMA` a multi-writer creator, or rule that co-written screen quotes name one writer | 7th |
| 86 | Make `daily-report.js` carry the traffic tool's error — **amend per 87 before doing it** | 6th |
| 88 | Rule on #938's `verified` for `one-of-the-deep-secrets-of-life`, which is live | 4th |
| 89 | Sweep the 110 records that pair a Gutenberg catalogue link with a verbatim claim | 4th |
| 92 | Decide whether the 250 stale `schema.creator` values get cleaned at the generator | 3rd |
| — | `loc.gov` human-facing page URLs 403 to non-browser clients — not hit today; 81 is the general version | 12th |
| — | Add the token-subset test's false-alarm mode (`Edmund Kean → Edmund Gwenn`) to the #801 note | 16th |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Make the no-op shards carry a `note` — **still counts only; today's two no-op shards carry no prose, while the PR bodies carry plenty** | 12th |
| — | `pull_request_read/get_status` reads empty for `verify` because it is a check run, not a commit status — **confirmed again today**: `total_count: 0` on #968's green head | 7th |
| — | `web.archive.org` served, then began resetting connections mid-run (#949) — **no recurrence today** | 3rd |
| — | QI serves HTTP 307 via a Sucuri JS interstitial on rapid repeat fetches (#958) — **no recurrence today**; #968 reached QI cleanly | 2nd |
| — | An audit skeptic pass at 0% refutation (#968, 4/4 survived) — **new today**, and the wave was right to refuse to manufacture one; watch across waves, do not act on one run | 1st |

**Struck today:** none.

**Struck previously:** 25, 28 (superseded by 59), 50, 52, 53, 55, 60, 78, and the `looksLikePerson`
anonymity-note row (closed by #914).

---

## 7. Cost

**No run reported a cost figure today.** Item **36**, 39th day. Today's shards carry `outcome`,
`processed`, `scanned`/`flagged` and nothing else. The weekly-discovery measurement of 09-21 —
*"~215K new tokens for 20 records ≈ 11K/record"* — remains the only datapoint the system has produced
about itself, and that routine does not run today.

The wave again volunteered an effort profile in prose rather than a number, and it was a heavy one:
5 records, **10 audit issues across 5 pages (1 blocker, 4 high, 3 medium, 2 minor), all fixed**,
**4 skeptic re-checks, 4 confirmed**, plus a full-text search of a book scan with control phrases run
to prove the instrument worked. Three of the five records ended `disputed` with rights unasserted —
which is to say a large share of the run's cost bought a **negative** result, stated honestly. That is
the expensive shape of this pipeline and the one worth measuring; § 6's item 36 is how it stops being
a paragraph.

Two routines cost close to nothing, correctly. `daily-reports` stopped at step 1 on an empty queue,
having first confirmed the endpoint was live rather than trusting a clean print — *"GET
/reports/pending → HTTP 200, body `{"pending":[],"count":0}`… That is a genuinely empty queue, not a
swallowed failure."* `daily-review` stopped at `flagged: 0` across 2,253 records — *"ran 0 checks,
skipped 13518 already settled"*, with *"no records opened, no `scan-fixes.js`, no record edits, no
`build.js`."* Both said so in their PR bodies instead of manufacturing work. That is the system
behaving.
