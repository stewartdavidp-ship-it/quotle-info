# Daily report — 2026-09-20

## 1. Verdict

**Every routine due today ran, the night's work was good, and nothing is blocking content from
shipping.** The wave built 5 records (#938 — 3 Lewis Carroll, 2 Rosa Parks), `daily-reports` and
`daily-review` both correctly no-opped, and the merge pass landed 4 PRs across two runs. `main` is
green at `5dbe211d2`, **2,238 records, 0 flagged, tree clean, 0 deferred**.

Two things are worth the operator's eye rather than a scroll past:

- **The wave asked for a human ruling on one record and is right to.** #938 marked
  `one-of-the-deep-secrets-of-life` **`verified`** while acknowledging the circulating wording is a
  streamlined form of Carroll's actual sentence — *"the wave's one debatable call"*, in its own
  words. That is item **88**, new today.
- **Its audit failed all five pages and fixed 13 issues, two of which were the same defect:** a page
  claiming a passage *"appears verbatim in the complete Project Gutenberg edition"* while linking a
  Gutenberg **catalogue record**, which contains no book text. That shape may sit in up to **110
  existing records** (§ 5.2). Item **89**, new today.

Yesterday's headline is confirmed, not superseded: **traffic is still blind, and it is still the
egress proxy, not GoatCounter.** The proxy's own status endpoint now names the host in its failure
log (§ 4). Item 43 has been asking for the wrong fix for 24 days.

---

## 2. What ran

| slot | routine | result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run: `pr` — **5 records**, #938 (`CLEAN`, rebuilt by the 10:44Z pass, merges next pass) |
| 04:00 | `daily-reports` | ✓ 1 run: `no-op` — 0 pending (shard `08:09:41Z`) |
| 05:00 | `daily-review` | ✓ 1 run: `no-op` — scanned 2,238, flagged 0 (shard `09:04:15Z`) |
| 07:00 | `daily-merge` | ✓ 2 runs: `merged`, `merged` — `03:38Z` (1 PR) and `10:44Z` (3 PRs) |
| 12:00 | `reports-close` | · not yet — due 17:00 UTC (yesterday's ran `no-op`, #934) |
| Mon 02:00 | `weekly-discovery` | · not scheduled today (Sunday; next 09-21) |

**Nothing is missing from the roster.** Every slot due today produced a shard — the fifth
consecutive day that has been true. As yesterday, the census counts the `10:44Z` merge run from a
shard still on the unmerged #941 branch; only the `03:38Z` shard is on `main`.

`daily-report.js` reported **1 tool-call failure** — `traffic: fetch failed`, at
`health.tool_failures`. **§ 4 is the only incomplete part of this report; §§ 2, 3, 5 are complete.**

### Preflight — exception (b) on every container again, and the gap is still widening

This pass failed preflight 1/10 on `git on main: on HEAD`, with `local main sane`, `tree clean` and
`current with origin/main` all passing — the documented exception (b) start state. Ran the two
commands preflight printed; the fast-forward moved the tree **44 commits**. Re-ran preflight: 10/10
green.

Every routine that ran today hit the same thing and said so:

| pass | gap | outcome |
|---|---|---|
| `daily-reports` (#939) | **41 commits** | exception (b), re-ran 15/15 green |
| `daily-review` (#940) | **41 commits** | exception (b), re-ran 14/14 green |
| `daily-wave` (#938) | not stated | exception (b), *"ran the two commands it printed"* |
| this report | **44 commits** | exception (b), re-ran 10/10 green |

#939 states the case plainly: *"`git checkout main` alone would have moved the tree **backward 41
commits** onto the stale ref. The `merge --ff-only` is what recovered it."* The 2026-08-16 correction
to the doc is still load-bearing and was still needed four times this morning.

**The trend, for item 12:** 10/10/13 → 21/24 → 31/31/31/34 → **41/41/44**. The gap has quadrupled in
five days. Nothing was discarded on any of the four recoveries — every tree was clean and every ref
was an ancestor — but the underlying cause (something leaves each container detached with a stale
`main`) is untouched, and every extra day makes the window in which a container starts on a
*non-ancestor* ref — exception (a)'s shape, which no fast-forward can fix — a little wider.

---

## 3. What shipped

**Merged in the report's UTC-day window (4), all bookkeeping:**

| PR | what |
|---|---|
| #940 | `daily-review` 2026-09-20 no-op shard |
| #939 | `daily-reports` 2026-09-20 no-op shard |
| #937 | merge-pass 2026-09-20 shard |
| #936 | merge-pass 2026-09-19 shard |

**Merged since yesterday's report actually ran (12:08Z on 09-19) but invisible to today's list —
five PRs, including the only content PR:**

| PR | merged | what |
|---|---|---|
| **#928** | **09-19 15:26:48Z** | **the 09-19 wave — 3 Rosa Parks records** |
| #931 | 09-19 15:26:59Z | merge-pass shard |
| #933 | 09-19 20:07:25Z | merge-pass shard |
| #934 | 09-19 20:07:28Z | `reports-close` 09-19 no-op |
| #935 | 09-19 22:41:37Z | merge-pass shard |

This is item **4**, 44th day, and the **fourth consecutive day** the window has eaten a content PR.
#928's three records are the reason `records` moved 2,235 → 2,238; the census is right about the
corpus and silent about what put it there. Yesterday's report ran before the merge, today's runs
after the window closed, so those three records appear in **neither** report's shipped list. Only
this hand-written paragraph carries them.

**Open (3):**

| PR | gate | age | what |
|---|---|---|---|
| #938 | `MERGE` — green, in scope, up to date | 0d | the 09-20 wave, 5 records |
| #941 | `MERGE` — green, in scope, up to date | 0d | merge-pass 2026-09-20 shard |
| #670 | `SKIP` — *"merge conflict — needs a human, do not auto-resolve generated files"* | **24d** | the 08-27 wave, 5 records |

**Neither #938 nor #941 is this pass's to merge** — `DAILY-REPORT.md` limits the self-merge
exception to this report's own PR, and the gate marking them `MERGE` does not widen it. The next
merge pass takes both.

**Records audited:** 2,238 scanned by `daily-review`, 0 flagged, `state: unchanged, nothing
written`. `vocab-sweep.js`: 6 tables, 0 under-used keys.

**Reports closed:** none pending. #939 did not trust the empty print — it re-checked the endpoint
directly (`HTTP 200 {"pending":[],"count":0}`) *"since a swallowed fetch failure and a quiet night
look identical at the call site"*. The public endpoint is healthy.

---

## 4. Traffic

**Traffic: unavailable.** `traffic` is `null` in the tool's JSON and `health.tool_failures` carries
`traffic: fetch failed`. **This is an analytics outage, not a quiet day — it is not a zero**, and no
figure for today, 7d, 30d or all-time can be given.

### The cause is settled, from inside the container, for the second day running

Yesterday opened item 43′ asking the operator to run `tools/traffic.js` locally because this
container could not tell a GoatCounter 403 from a proxy 403. **That question can now be answered
without them.** The agent proxy's own status endpoint logged, during this run:

```
"kind": "connect_rejected",
"detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
"host": "quotle.goatcounter.com:443"
```

and a direct `curl` fails identically — `CONNECT tunnel failed, response 403`, **before any HTTP
request reaches GoatCounter**. Meanwhile `tools/traffic.js` prints:

> `traffic.js: 403 from GoatCounter — the dashboard is no longer public.`
> `Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.`

That sentence is false in this environment. Nothing this routine sends ever arrives at GoatCounter.

**What this does and does not establish.** It establishes that **no cloud routine can ever read
traffic until the egress allowlist changes** (item 13), and that `traffic.js`'s remedy string is
unearned (item 87). It does **not** establish that the dashboard is public — its actual state is
still unknown from here, so item 43 cannot be struck, only demoted. 43′ shrinks accordingly: the
local run is no longer needed to diagnose *this* failure, only to learn the dashboard's state
once egress is fixed.

**Reminder on the figures themselves, for when they return:** GoatCounter counts everybody — AI
assistants, Bing, DuckDuckGo, direct — and is the only traffic number this report carries; do not
substitute Search Console. The figures are an **upper bound**, unfiltered for referrer spam
(`spamFiltered` is always `false` here), so they are reported as "up to N". `node tools/traffic.js
--refs` gives the referrer breakdown, and neither has been readable since 08-28.

---

## 5. Findings

### 5.1 The wave built 5, audited every page to FAIL, and fixed 13 issues before shipping

#938 drew 8, built 5, and released 3 back to the queue via `sync` + `unselect --wave`. Its audit
returned **0 PASS / 5 FAIL — 13 issues, 0 blockers, 3 high**, and *"every high was skeptic-checked
(defaulting to 'this finding is wrong'); all 3 survived and all 3 are fixed."* Three pages are
`verified`, two `attributed`; **only two claim public domain**.

The three high findings, in its own words:

1. **×2 — *"verbatim claims linked pages with no book text."*** Both Carroll pages asserted the
   passage *"appears verbatim in the complete Project Gutenberg edition"* while linking
   `gutenberg.org/ebooks/11` and `/12`, *"which are catalogue records. Repointed at the reading
   URLs, re-fetched and confirmed to contain the passages."* — see § 5.2.
2. **A false statement about a linked source.** The deep-secrets page said Collingwood's *Life and
   Letters of Lewis Carroll* (1898) does not print the Ellen Terry letter. *"Re-reading the full
   text, it **does** print Carroll–Terry correspondence — the Shylock letter at p. 182. Only the
   November 1890 letter is absent. Reworded to say exactly what was checked."*

Also corrected: the Cheshire Cat page fixes two things that *"travel badly"* — the speaker (the Cat,
not the Hatter) and the clipping; the White Queen page documents the Red Queen / Mad Hatter / Alice
confusions and traces the last *"to Burton's 2010 film, where Alice does count them off."* All 20
outbound links were re-fetched after the fixes.

**What it refused to do, which is the part that matters:**

- **No primary source for either Rosa Parks line**, and it says so on the pages rather than
  asserting one: *"zero hits in a full-text search of the Library of Congress's digitised Rosa Parks
  Papers"*, absent from Wikiquote entirely, no Quote Investigator article. Neither claims public
  domain.
- **A specific occasion circulates for the "Racism is still with us" line** — a late-1990s
  newspaper-columnist conversation — and it is **not on the page**, because the column was
  unreachable: *"A named date and setting is exactly the kind of detail that hardens into fact
  through repetition, and search-result synthesis is not a source."*
- **Rights withheld on the Carroll–Terry letter despite Carroll dying in 1898**, because its first
  publication could not be established (not in Collingwood 1898; earliest documentable printing is
  Langford Reed, 1932). *"Because no pre-1931 printing could be shown, rights are withheld rather
  than claimed as public domain."* That is the conservative call on the one field where being wrong
  is a legal claim — exactly what item 83 asks the generator to enforce.

**The one thing it asks a human for** is § 6 item 88.

**Egress shaped this wave too** (item 13, 40th day): `washingtonpost.com`, `biography.com`,
`nps.gov`, `achievement.org` and `kinginstitute.stanford.edu` all refused; *Quiet Strength*, *My
Story* and *Dear Mrs. Parks* access-restricted on archive.org; Google Books API over quota (429,
item 15); HathiTrust behind Cloudflare.

### 5.2 The Gutenberg catalogue-link defect may not be confined to today's two pages

The audit's two high findings were the same mistake made twice in one wave: a verbatim claim pinned
to `gutenberg.org/ebooks/N`, which is a **catalogue record containing no book text**. The wave fixed
its own two pages by repointing at the reading URLs and re-fetching.

Sizing the exposure in the existing corpus (a read, not a re-derivation of any tool figure):

- **544** records link `gutenberg.org` at all.
- **305** of those link a catalogue URL (`/ebooks/N`) rather than a reading URL.
- **110** of those 305 also carry verbatim-style wording (*"verbatim"*, *"word for word"*).

**110 is an upper bound, not a count of defects** — co-occurrence in one record does not prove the
verbatim claim is pinned to that particular link, and some catalogue links are cited correctly as
catalogue records. It is the size of the set a sweep would have to read. Item **89**.

### 5.3 The merge pass put bookkeeping ahead of content for the fourth day

The `10:44Z` pass merged #937, #939 and #940 — three log shards — then found the wave behind `main`
and recorded: *"#938 rebuilt and pushed — merges next pass, once verify is green on the new head"*.

Nothing was lost and the pass did the right thing with the ordering it has. But #938 was open,
ready and `CLEAN` from 07:30Z, and merging it first would have cost the three one-line shards a
trivial rebuild instead of costing five records a pass. This report's own mandated self-merge stales
it once more. Item **64/51**, fourth consecutive day.

Also in that note, item **65** (17th day): both merge notes still lead with re-poll chatter — *"re-poll
1/4: 4 PR(s) with mergeability not yet computed — waiting 6s"* three times over — before the verdict.

### 5.4 #940 departed from its harness prompt, correctly, and said so

*"My session harness instructed a `claude/wizardly-fermat-ypc2wk` branch and a **draft** PR.
`DAILY-REVIEW.md` requires `review/<YYYY-MM-DD>` and **ready, never draft**, and both are
load-bearing: `merge-gate.js` fails closed on unrecognised branch prefixes (`claude/` would be
classed `HUMAN` and never merge), and the 07:00 merge pass skips drafts."*

The doc won, the PR merged, and the deviation is documented in the body. That is item **20** working
as intended and still unresolved at the source.

### 5.5 No discrepancies

Every routine's claim reconciles with the log and the census: #938's 5 records against the wave
shard's `pr`; #940's 2,238 scanned against `health.records` 2,238; the two merge notes' 1 + 3 = 4
PRs against `MERGED (4)`. The census's four-PR list is *complete for the UTC day* and *incomplete for
the night* — that is item 4's defect, not a disagreement between sources.

---

## 6. Needs a decision

**Two items are added (88, 89). Item 43′ is narrowed and item 43 demoted — § 4 answered from inside
the container what 43′ was going to ask the operator to answer. Nothing is struck, and nothing here
is blocking content from shipping.** #938 and #941 need nothing; they merge at the next pass.

88. **Rule on #938's `verified` for `one-of-the-deep-secrets-of-life` — the wave flagged it as its
    one debatable call and is waiting.** The record is `verified` although the circulating wording is
    a streamlined form of Carroll's documented sentence (he wrote it as a *question*), with
    `schema.quotationText` carrying his wording and `claimQuoteText` the searched wording; the wave
    argues `attributed` would be false (the letter is pinned to 13 Nov 1890, Cohen ed. p. 200) and
    `disputed` would contradict Quote Investigator. Doing it is: read the page, then either leave it
    and say so — it matches *"397 existing non-disputed records that carry a fuller documented
    `quotationText`"* — or rule that a streamlined headline wording caps a record at `attributed`,
    which is a corpus-wide convention affecting those 397.

89. **Sweep the 110 records that pair a Gutenberg catalogue link with a verbatim claim.** *(New —
    § 5.2.)* Today's audit caught this exact defect twice in five new pages: a page asserting a
    passage appears verbatim in a Gutenberg edition while linking `/ebooks/N`, which carries no text,
    so the claim's own evidence does not contain the evidence. Doing it is: read the 110, repoint the
    genuine cases at the reading URL (`/ebooks/N/pages/…` or `/files/…`) and re-fetch to confirm the
    passage is present — and, if the shape recurs, a `scan.js` detector so the next wave cannot ship
    it, which makes this item 31's queue rather than a one-off.

13. **Extend the egress allowlist.** *(40th day — and § 4 makes it the traffic fix too, not just the
    wave's.)* Today it shaped a fourth consecutive wave: `washingtonpost.com`, `biography.com`,
    `nps.gov`, `achievement.org`, `kinginstitute.stanford.edu` all refused, so two Rosa Parks records
    ship `attributed` with no primary source. Doing it is: add `quotle.goatcounter.com` (unblocks
    § 4 outright), `web.archive.org`, `time.com`/`content.time.com`, the aggregators, the
    previously requested film-transcript hosts, JSTOR/Crossref, `barrypopik.com`, `annefrank.org`,
    `theguardian.com`, Google Books and HathiTrust — or rule that the pipeline lives without them and
    **81** becomes the answer instead.

4.  **Fix the report window.** *(44th day — five PRs lost overnight, including #928, the 09-19
    wave's **three records**; fourth consecutive day it has eaten a content PR.)* Doing it is: change
    `daily-report.js:30`/`:263` to window from the previous report's timestamp instead of the UTC
    calendar day, on its own branch.

12. **Pick the fix for the shallow clone.** *(37th day — four containers this morning at 41/41/44
    and one unstated; the gap has quadrupled in five days.)* Doing it is **one** of: (a)
    `git fetch --unshallow` in container setup — **retires both exceptions at once**; or (b) make
    `preflight.js:171` shallow-aware, which is smaller but covers only exception (a)'s shape.
    **Today is a sixth argument for (a), and the growing gap is what eventually produces exception
    (a)'s unfixable shape.**

6/76/80. **Reconcile `workflows/README.md`'s r35 note with `prep-wave.js:207` — then decide which one
    changes.** *(34th day; the remedy this report carried for 32 days is probably backwards.)* The
    README says STAMP IT on a disputed record whose author is the magnet; `prep-wave.js:207`
    deliberately withholds, citing **41 records that got a false misattribution claim** that way.
    Doing it is: read that comment and the 41 records, decide which rule is right, then one central
    edit to whichever is wrong — **not** the one-line generator patch previously proposed here.

64/51. **Make the merge pass merge content PRs before routine-log PRs.** *(Fourth day — the 10:44Z
    pass merged three log shards, then rebuilt #938 and deferred it a pass; #907 cost four passes and
    sixteen hours, #918 and #928 went through it on the two previous days.)* Doing it is: order the
    merge queue content-first, so the wave PR merges before `main` moves under it.

73. **Write the 2026-09-07 `daily-wave` shard to `main`.** *(12th day — still the only item with a
    deadline; `origin/wave-d20260907` confirmed still present at `6e6d0d2f9` this morning.)* `main`
    still says that wave did not run. Doing it is:
    `git show origin/wave-d20260907:data/routine-log/2026-09-07T07-44-35-244Z-daily-wave.jsonl` onto
    a fresh branch, commit, PR — **it stays recoverable only until that branch is pruned.**

42. **Rebuild and merge #670, or close it.** *(25th day; `SKIP` at the gate — *"merge conflict —
    needs a human, do not auto-resolve generated files"* — and untouched since 08-27;
    `origin/wave-d20260827` confirmed still present at `86be66b36`.)* Closing it loses five records
    that exist nowhere else — including **two Thornton Wilder records verified against two
    independently scanned 1927 printings** — **and** orphans its 08-27 wave shard. Doing it is: merge
    `main` in, take `main`'s side on generated files, `node tools/build.js`, `node tools/scan.js`,
    push — or close it and accept both losses knowingly.

87. **Make `tools/traffic.js` stop asserting "the dashboard is no longer public" on any 403, and
    amend item 86 to carry the cause rather than that string.** *(2nd day — § 4 now has the proxy
    naming the host, so the string is demonstrably false here.)* Doing it is: at `traffic.js:61`,
    print the observed status and host and say the cause is *either* a GoatCounter setting *or* an
    egress denial, instead of naming one; then item 86's change to `daily-report.js` propagates
    something true.

43′. **Run `node tools/traffic.js` once locally, unproxied — but it is no longer urgent and no longer
    diagnostic.** *(2nd day, **narrowed** — § 4.)* The container-side 403 is now proven to be the
    egress gateway, so the local run would only reveal the dashboard's own state, which matters only
    after item 13 lands. Doing it is one command, whenever convenient.

85. **Give `DOSSIER_SCHEMA` a multi-writer creator, or rule that co-written screen quotes name one
    writer.** *(4th day.)* Doing it is: let `creatorName` accept an array and have `toRecord` emit a
    `Person` per name, then a scripted backfill of the ten affected records — or decide the one-name
    convention and fix the ten to match.

84. **Decide whether the wave is allowed to run without `WebSearch`, or should stop and say so.**
    *(4th day — not live today; #938 reported no missing tool.)* Doing it is one of: (a) find why
    `WebSearch` was absent on 09-17 and restore it; or (b) one line in the wave doc saying a seam
    without a usable on-site search is a STOP rather than a thinner record.

81. **Rule on archived-vs-canonical citation when a host is blocked by egress.** *(8th day — live
    again today; three `loc.gov` links on the Parks pages are cited on the strength of the JSON API
    after the human-facing pages 403'd.)* Doing it is: one line in the wave doc — cite what was
    fetched, or cite canonical and record the archived URL as the verification trail — plus a
    decision on whether to backfill.

83. **Refuse `rights` at the generator when the record's own prose dates the headline wording after
    the work the rights are reasoned from.** *(5th day — #938 did this by hand today on the
    Carroll–Terry letter, correctly, which is the argument for making it mechanical.)* One rule in
    `prep-wave.js` clearing `rights` (and forcing a prose-only `rightsNote`) when a record's dating
    fields and its headline wording disagree.

79. **Give `mis-kind.js` patterns for `Unconfirmed` and `Earliest located`.** *(9th day.)* Both fall
    through `kindForTag` to `''`, which `template.js` renders as the burgundy ✕ meaning *refuted*.

77. **Add `meta.description` and `meta.ogDescription` to `PLAIN_TEXT_FIELDS`
    (`tools/html-safety.js:107`).** *(10th day.)* Two strings in that array, then a separate
    corpus-wide sweep for pages already shipping literal `&lt;em&gt;` in their search snippet.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 2 | Stop the daily wave and the backlog waves colliding over the queue | 27th |
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" | 27th |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 27th |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 33rd, clock running |
| 9 | Fix the middle-initial hub fork at the generator | 26th |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row — **79 is the adjacent case** | 26th |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 26th |
| 15 | Decide what to do about Google Books' quota — **429 again today, #938** | 29th |
| 16 | Stop one source's outage hard-stopping the review pass | 32nd |
| 19 | Decide whether `perf-check` belongs in the routine census | 30th |
| 20 | Settle the branch-name / draft conflict between the harness and the routine docs — **#940 hit it again today, doc followed** | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict | 29th |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason | 30th |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — cannot see unmerged PRs | 34th |
| 24 | Work the disputed records with no `creditedTo` | 40th |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) | 33rd |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 33rd |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 33rd |
| 30/62 | Rule on "Verbatim:" and "rights held by X" corpus-wide — **89 is the adjacent case** | 33rd / 19th |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — 58 is queued behind it, and **89** may add a third | 35th |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate | 36th |
| 33 | Decide the house convention for characters with no HTML entity | 32nd |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 34th |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — folded into 17/58 | 34th |
| 36 | No routine reports a cost figure — § 7 | 36th |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 37th |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 37th |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 33rd |
| 40 | Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`) | 25th |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 25th |
| 43 | Flip GoatCounter → "Dashboard viewable by" → Anyone — **demoted: § 4 proves it is not this environment's failure; do 13 first** | 24th |
| 44 | Teach `merge-gate.js` to look at what actually changed — #670 is its case | 24th |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 24th |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — 54 is the ruling this needs | 24th |
| 47 | Decide whether QI's Title Case should be normalised centrally | 24th |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 24th |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 24th |
| 54 | Rule on Quote Investigator *headlines* reaching `displayQuote` — the true count is UNKNOWN; do not re-derive | 22nd |
| 56 | Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog`, which is live | 21st |
| 57 | Give the orphaned `taxes-…` candidate a way back into the queue | 21st |
| 59 | Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it — 74 is waiting on this | 21st |
| 61 | Fix the `mis-kind.js` comma gap; also add a `/fake/` rule to `REFUTATION_TAG` | 21st |
| 63 | Out-of-band merges — **no occurrence today**; streak at two clean days | 2 clean days |
| 65 | Put the verdict before the re-poll chatter in the merge note — **live again today**: the 10:44Z note carries three re-poll lines before its verdict | 18th |
| 66 | Fix `template.js:848`'s unconditional period on `disambiguatingDescription` | 18th |
| 67 | Rule on the Parker `displayQuote` repair, which shipped 09-05 | 16th |
| 68 | Fix nested quotes in `claimReviewed` | 16th |
| 69 | Rule on whether a Wikiquote *sourced-section* anchor is enough for `verified` on an in-copyright work | 16th |
| 70/71 | The 09-06 quota collision; `daily-wave` / `daily-review` still fail silently | carried |
| 74 | Decide whether `DOSSIER_SCHEMA` gets the hand-set override fields — waiting on 59 | 13th |
| 75 | Rule on the harvester's magnet-as-claimant defect | 12th |
| 86 | Make `daily-report.js` carry the traffic tool's error — **amend per 87 before doing it** | 3rd |
| — | `loc.gov` human-facing page URLs 403 to non-browser clients — **hit again today, #938**; 81 is the general version | 9th |
| — | Add the token-subset test's false-alarm mode (`Edmund Kean → Edmund Gwenn`) to the #801 note | 13th |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Make the no-op shards carry a `note` — **#939's shard did today**, carrying its preflight gap; the other two still carry `outcome` only | 9th |
| — | `pull_request_read/get_status` reads empty for `verify` because it is a check run, not a commit status | 4th |

**Struck today:** none.

**Struck previously:** 25, 28 (superseded by 59), 50, 52, 53, 55, 60, 78, and the `looksLikePerson`
anonymity-note row (closed by #914).

---

## 7. Cost

**No run reported a cost figure.** Item **36**, 36th day. Today's three shards carry `outcome`,
`processed`/`scanned` and — for `daily-reports` only — a prose `note`. Nothing carries tokens, time
or dollars.

The wave again volunteered an effort profile in prose, which remains the closest thing to a number
this report has: 8 candidates drawn to build 5, a full audit returning **13 issues across 5 records
with 3 highs, all 3 skeptic-checked and none refuted**, **all 20 outbound links re-fetched after the
fixes**, and a Library of Congress full-text search run to a **negative** result on two quotes and
reported as such. That bought three corrections before ship, including two verbatim claims resting on
catalogue pages and one flatly false statement about what a linked 1898 biography contains.

Two routines cost close to nothing, correctly: `daily-reports` stopped at step 1 on an empty queue —
having first confirmed the queue was really empty rather than trusting a clean print — and
`daily-review` stopped at `flagged: 0` across 2,238 records, *"skipped 13428 already settled"*, with
*"No records opened, no `scan-fixes.js`, no edits, no `build.js`."* Both said so in their PR bodies
instead of manufacturing work. That is the system behaving.
