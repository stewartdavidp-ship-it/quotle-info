# Daily report — 2026-08-29

## 1. Verdict

**All four due routines ran and `main` is healthy — but the system paid an Opus wave to redo research
it already had. Today's wave (#702) rebuilt three of the five records that yesterday's wave (#689)
already researched, because #689 has sat unmerged and conflicted for a day.**

Carried decision 2 predicted this on 2026-08-26 and again yesterday: *"its five researched slugs are
back in the draw because it did not merge."* Yesterday that was a forecast. Today it is a bill.

`main` is otherwise sound: CI green (`76a1dd303`), **1,972 records**, **0 flagged**, ladder `pr`,
0 deferred, 0 unjudged runs, tree clean. Nothing did not run.

**This report is incomplete in one respect and says so:** `daily-report.js` reported
`traffic: fetch failed`. See § 4 — it is an analytics outage with a named cause and a named fix, on
its second day, not a quiet day.

## 2. What ran

| | slot | routine | result |
|---|---|---|---|
| ✓ | 03:00 | `daily-wave` | 1 run: **pr** · 5 records → [#702](https://github.com/stewartdavidp-ship-it/quotle-info/pull/702) |
| ✓ | 04:00 | `daily-reports` | 1 run: no-op — public `/reports/pending` returned `count:0` → [#703](https://github.com/stewartdavidp-ship-it/quotle-info/pull/703) |
| ✓ | 05:00 | `daily-review` | 1 run: no-op → [#704](https://github.com/stewartdavidp-ship-it/quotle-info/pull/704) |
| ✓ | 07:00 | `daily-merge` | 1 run (05:50 UTC): **merged 1** — #700 → shard in [#701](https://github.com/stewartdavidp-ship-it/quotle-info/pull/701) |
| · | 12:00 | `reports-close` | not yet due (17:00 UTC) — this report runs too early to judge it |
| · | Mon 02:00 | `weekly-discovery` | not scheduled today |

**No routine is missing a shard. Nothing did not run.** Yesterday's `reports-close` — the slot
yesterday's report ran too early to see — did run, no-op.

Today's wave PR (#702, opened 07:36 UTC) arrived *after* the 05:50 merge pass, so it was not skipped;
it is queued for tomorrow's pass by design.

## 3. What shipped

**One PR merged since yesterday's report, and it was a log shard, not content:**
[#700](https://github.com/stewartdavidp-ship-it/quotle-info/pull/700) `chore(routine-log): merge pass
2026-08-28`.

**No content has reached the site in two days.** Three wave PRs are open carrying **15 researched
records** between them, and none has merged:

| PR | branch | age | state | CI | records |
|---|---|---|---|---|---|
| [#702](https://github.com/stewartdavidp-ship-it/quotle-info/pull/702) | `wave-d20260829` | 0d | CLEAN | ✓ `verify` success | 5 |
| [#689](https://github.com/stewartdavidp-ship-it/quotle-info/pull/689) | `wave-d20260828` | 1d | **DIRTY** | ✓ `verify` success | 5 |
| [#670](https://github.com/stewartdavidp-ship-it/quotle-info/pull/670) | `wave-d20260827` | 2d | **DIRTY** | — | 5 |

Also open and clean: #701, #703, #704 (log shards).

The corpus figure has not moved: **1,972 records**, the same as yesterday. #689 reported it would
take the corpus to 1,977; it has not, because it did not merge.

## 4. Traffic

**Traffic: unavailable.** `traffic` is `null` in the tool's JSON and `tool_failures` carries
`traffic: fetch failed`. This is **an analytics outage, not a zero.**

Probed directly for the diagnosis the census swallows:

```
traffic.js: 403 from GoatCounter — the dashboard is no longer public.
Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.
```

Same cause as yesterday, unchanged — this is **decision 43, second day**. No data is lost; the
counter keeps recording, only the public read endpoint is closed. Nothing in this repo can reach that
setting. Until it is flipped, this report carries no traffic figure at all, and no substitute:
Search Console counts Google alone, and Google was absent from the top six referrers when the numbers
were last visible.

## 5. Findings

### 5.1 Today's wave rebuilt three records yesterday's wave had already researched

Both PRs add the **same three record files**:

```
data/quotes/a-ship-in-harbor-is-safe-but-that-is-not-what-ships-are.json
data/quotes/common-sense-is-nothing-more-than-a-deposit-of-prejudices.json
data/quotes/education-is-what-remains-after-you-have-forgotten.json
```

#689 released its unbuilt candidates back to the queue on 2026-08-28 — correctly, per the runbook —
but because #689 itself never merged, the **built** slugs never left the draw either. Today's 03:00
wave drew three of them again and re-ran the full serial pipeline on each: research, ingest, build,
audit, skeptic, fix. #702 even re-derived findings #689 had already recorded, independently reaching
the same conclusion on the same Barnett/Einstein wording.

This is not a near-duplicate that the pipeline's duplicate check should have caught — the two PRs are
both unmerged, so neither is visible in the corpus the other checks against.

**It also creates a merge hazard.** #689 and #702 currently conflict only in generated output. Once
either merges, the other conflicts in `data/quotes/` **record files** as well, which is no longer a
mechanical rebuild.

### 5.2 The two routines reached opposite verdicts on the same record, one day apart

#689 declared the Decca/Beatles candidate **unbuildable and dropped it from the batch**:

> `beatles-rejection-we-dont-like-their-sound-groups-of` → nobody said "**Beatles Rejection:**"
> … `displayQuote` drives the page title, the slug and the copy-to-clipboard citation, so these
> cannot ship as-is.

#702 built and shipped that exact slug, recording the same defect as an observation it chose not to
act on:

> The harvested text is "Beatles Rejection: We Don't Like Their Sound…", so the slide-kit credit line
> presents a research headline as quoted words. … Fixing it would change the slug and the locked URL
> contract, so it was left alone.

Both reasoned carefully; they disagree on whether the locked URL contract outranks the defect. #689's
own words are that this candidate *"is worth rebuilding once the stored text can be repaired"* — and
#702 rebuilt it **without** the repair. This needs a person, not a third routine's opinion.

### 5.3 The merge pass rebuilt #689 against a stale `origin/main`, and announced success

Yesterday's 14:56 UTC merge pass logged:

> `#689 rebuilt and pushed — merges next pass, once verify is green on the new head`

`verify` went green at 14:57:35. Two merge passes have run since (23:53 UTC, 05:50 UTC). **#689 did
not merge and is DIRTY again** — in fact it never stopped being dirty.

The rebuild merge commit (`f837bd504`) has merge-base `0170db6f4` = **#691, 10:56 UTC**. By 14:56,
`main`'s tip was `2d27b45b6` (14:23 UTC). The rebuild merged an `origin/main` ref that was **five
commits and 3½ hours stale**, missing #695–#699 — including #695, the 72-song cover-link feature that
regenerated `search.json`, `index.html` and `corpus-state.json`, which are precisely the files #689
still conflicts on:

```
data/corpus-state.json   index.html   search.json
themes.json   verify-index.json   who-said/index.html
```

The three commits that landed *after* the rebuild touch a single `data/routine-log/*.jsonl` each and
could not have caused this. The rebuild did not fetch before merging, so it could never have cleared
the conflict, and the log recorded it as done.

**This is the same stale-`main` pathology as preflight exception (b)** — carried item 12, now on its
**15th day**. It fired three more times today: this pass, `daily-reports` (*"preflight 15/15 after doc
exception (b) ff-only recovery"*), and the wave (*"Preflight passed 14/14 after exception (b)"*).
Preflight catches it for routines that run preflight. The merge pass's rebuild step does not, and that
is where it did real damage.

### 5.4 #670 is now two days stale and was decision 42 yesterday

Yesterday's most urgent item — *"Rebuild and merge #670, today"* — was not done. It is now 2 days old
and conflicts in **20+ files**. Every conflict is still generated output; **no `data/quotes/` record
conflicts.** It was built when the corpus was 1,897 records; `main` is at 1,972.

Its five slugs are unrelated to #689/#702, so it has not yet been re-drawn — but it is on the same
clock that produced § 5.1.

### 5.5 What the routines could not establish (unchanged, and honestly reported)

#702 again names what it refused to invent:

> **John A. Shedd's life dates.** No Wikipedia or Wikiquote page exists, and no copy of *Salt from My
> Attic* is on the Internet Archive. The page says "life dates not established" rather than guessing.

and an unresolved source discrepancy it deliberately did not surface on the page:

> For the 2 Dec 1907 first strong match, QI names *The Education Times: Journal of the College of
> Preceptors*; Wikiquote names *The Education Outlook*, vol. 60. Same date, same page (532), same
> Google Books id. We followed QI, which verified it. Unresolved, and not surfaced on the page.

Egress blocks remain unchanged and correctly uncited: `independent.co.uk`, `beatlesbible.com`,
`bigthink.com`, HathiTrust (403), openlibrary and the archive.org search-inside API. That is carried
item 13, **18th day**.

#702 also repeats #689's template-level finding, which no one has ruled on:

> On an `attributed` record whose documented wording belongs to a reporter, JSON-LD
> `Quotation.creator` names the attributed person while the visible prose says the sentence is
> someone else's (here: creator = Einstein, wording = Barnett).

### 5.6 `daily-report.js` hides a diagnosable failure behind "fetch failed"

The census reported `traffic: fetch failed`. `traffic.js`, run directly, reports the status code, the
cause and the exact remedy. Two days of reports have had to shell out to a second tool to say
something useful about the same outage.

## 6. Needs a decision

**Three new (50–52). Nothing closed. 42 and 43 are unchanged from yesterday and both got worse
overnight.**

**Most urgent: 50** — it is the only one where waiting changes what the fix costs.

50. **Decide which of #689 and #702 ships the three duplicated records, then rebuild the other around
    it.** Both PRs add the same three `data/quotes/` files, so whichever merges second will conflict
    in record files rather than in regenerable output. Doing it is: pick one (#702's three are the
    later research and carry two extra audit fixes; #689's carry the `stupidity/genius` and
    `income-taxes` records that #702 does not have), drop the duplicate records from the loser's
    branch, merge `main` in, `node tools/build.js`, `node tools/scan.js`, push. *(NEW)*

51. **Make the merge pass's rebuild step fetch before it merges.** #689's rebuild merged an
    `origin/main` five commits stale, so it never cleared the conflict, and the pass logged
    "merges next pass" for a branch that could not merge. Doing it is adding an explicit
    `git fetch origin main` before the merge in the rebuild path, and refusing to log success unless
    the resulting merge-base equals `origin/main`'s tip. *(NEW — and it is § 5.3's root cause)*

52. **Rule on whether `beatles-rejection-we-dont-like-their-sound-groups-of` may ship with QI's
    headline prefix in `displayQuote`.** #689 dropped it as unbuildable and #702 shipped it, one day
    apart, both with reasons; it is live in #702 right now. Doing it is either pulling that one record
    from #702, or ratifying the prefix and writing the rule into the runbook so the next wave stops
    re-litigating it. *(NEW — pairs with 46 and 47)*

42. **Rebuild and merge #670 — and now #689 with it.** *(2nd day, worse)* #670 is 2 days old and 20+
    files conflicted; #689 is 1 day old and 6 files conflicted; **every conflict in both is generated
    output, zero record conflicts.** Doing it is, per branch: merge `main` in, take `main`'s side on
    the generated files, `node tools/build.js`, `node tools/scan.js`, push, let the next pass take it.

43. **Flip GoatCounter → Settings → "Dashboard viewable by" → Anyone.** *(2nd day)* The public
    endpoint 403s, so this report carries no traffic figure for the second day running. One setting in
    the GoatCounter UI; nothing here can reach it and no data is lost.

2. **Stop the daily wave and the backlog waves colliding over the queue.** *(5th day — now proven,
   with a cost)* § 5.1 is this item happening: three records researched twice because an unmerged PR's
   slugs stay drawable. Doing it means either having the wave skip slugs present in any open `wave-`
   PR, or not running the daily wave on a day a backlog wave is planned.

44. **Teach `merge-gate.js` to look at what actually changed.** *(2nd day)* `:98` routes every `DIRTY`
   PR to `SKIP` regardless of cause, so a generated-only conflict needs a human for what
   `DAILY-MERGE.md` calls a routine rebuild; `:123` demotes any PR that is behind by any commit, so
   merging this report alone demotes #702. Two conditions.

5. **Declare which carried items you are not going to do.** *(6th day)* The list is now **52** and no
   numbered item has ever been implemented. Reply with the numbers to strike and tomorrow's report
   carries only what survives.

1. **Delete `schema.dateCreated: "1890"` from `a-society-gets-all-the-criminals-it-deserves`.**
   *(5th day — still the only live content defect on the site)* One field removed on a content branch,
   plus `build.js`.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" (pairs with 25) | 5th |
| 4 | Fix the report window — `daily-report.js:30` windows on calendar day | 22nd |
| 6 | `prep-wave.js:206` should stamp `creditedTo` when `confidence === 'disputed'` regardless of name equality | 12th |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 5th |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 11th, clock running |
| 9 | Fix the middle-initial hub fork at the generator, scoped to `schema.creator` | 4th |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row | 4th |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 4th |
| 12 | Root-cause the stale local `main`, or ratify exception (b) in `preflight.js` — **fired 3× today, and § 5.3 is it doing damage outside preflight** | 15th |
| 13 | Extend the egress allowlist — The Independent, Yale Book of Quotations | 18th |
| 14 | Decide the `isBasedOn → isPartOf` bulk migration (1,046 of 1,752 measured 08-24) | 6th |
| 15 | Decide what to do about Google Books' quota | 7th |
| 16 | Stop one source's outage hard-stopping the review pass (`preflight.js` `head()` treats a 12s timeout as blocked) | 10th |
| 17 | Fail the build when `Quotation.creator` is suppressed; normalise `template.js`'s hero match | 10th |
| 18 | Fix `daily-report.js:148`'s `work` figure — it sums `built + processed + sampled` | 6th |
| 19 | Decide whether `perf-check` belongs in the routine census | 8th |
| 20 | Settle the branch-name conflict between the harness (`claude/…`) and the routine docs | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict | 7th |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason | 8th |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — **§ 5.1 shows it cannot see unmerged PRs** | 12th |
| 24 | Work the disputed records with no `creditedTo` — 7 of 10 `magnet` rows still render the false-credit ✕ | 18th |
| 25 | Do the measured `mis-kind.js` pass | 17th |
| 26 | Fix `plain()` to preserve unknown HTML entities | 11th |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 11th |
| 28 | Add `verdictNote` to `DOSSIER_SCHEMA` (`SCHEMA_BUDGET` has zero headroom — measure first) | 17th |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 11th |
| 30 | Decide `verbatimNote()`'s "Verbatim:" label corpus-wide (~370 records) | 11th |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — blocks the next admission | 13th |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate | 14th |
| 33 | Decide the house convention for characters with no HTML entity (pairs with 17) | 10th |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 12th |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission (decide with 17 and 21) | 12th |
| 36 | Make the wave log a cost figure — **still no cost field on any shard** | 14th |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 15th |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 15th |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 11th |
| 40 | Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`) | 3rd, half-closed |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 3rd |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 2nd |
| 46 | Read the 15 queue candidates whose text opens `Topic:` and mark which are QI filing labels | 2nd |
| 47 | Decide whether QI's Title Case should be normalised centrally (35 of 1,913 records) | 2nd |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 2nd |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 2nd |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Add `traffic.js`'s specific error to `daily-report.js`'s `tool_failures` (§ 5.6) — folded into 43; strike if 43 is done | 1st |

## 7. Cost

**No routine reported a cost figure today.** Today's shards carry only
`date, routine, outcome, built/scanned/processed/flagged, note`. The `daily-wave` shard in #702 — a
full serial pipeline with 20 research links re-fetched and 5 pages adversarially audited — carries
**no cost field**. That is carried item 36, on its **14th day**, and today is the day it stopped being
a bookkeeping nicety.

**The one cost this report can state is a waste figure, and it is a floor, not an estimate.** Three of
#702's five records duplicate research #689 had already completed and audited — **60% of a full Opus
wave**, spent on work that exists, unmerged, in a branch on this repo. Because item 36 is unresolved,
nobody can say what that was in dollars.

**Nothing reported an interrupted run** — no `529`, no rate limit, no capacity ceiling. #702's only
environmental notes are the standing egress blocks (§ 5.5), all correctly routed around and none cited
as corroboration.

The only forward-looking figure remains #688's *"roughly 2.5M tokens as a batched backfill"* for the
74 remaining song cover links — decision 49, undecided on its second day.
