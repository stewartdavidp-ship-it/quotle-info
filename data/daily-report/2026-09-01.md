# quotle.info — daily report, 2026-09-01

## 1. Verdict

**Every routine that was due ran, nothing is flagged, CI is green — and the night produced one new
defect that needs a ruling before the next merge pass, in a few hours: the 2026-09-01 wave (#730)
re-ingested the *identical five records* as the 2026-08-31 wave (#722), which never merged.**

Yesterday's report closed by noting that with #689 closed, *"no open branch duplicates another's
records for the first time in four days."* That regressed within twenty-four hours, and this time
the two duplicating branches were produced by the same routine on consecutive nights.

Traffic is unavailable for the **fifth** consecutive day. That is an analytics outage with a named,
one-click fix, not a quiet day (§ 4).

---

## 2. What ran

```
  ✓ 03:00      daily-wave         1 run(s): pr · 5 record(s)
  ✓ 04:00      daily-reports      1 run(s): no-op
  ✓ 05:00      daily-review       1 run(s): no-op
  ✓ 07:00      daily-merge        2 run(s): merged, merged · 3 record(s)
  ·  12:00     reports-close      no run yet — due 17:00 UTC
  ·  Mon 02:00 weekly-discovery   not scheduled today
```

**Nothing is missing.** `reports-close` is not yet due (this pass runs before its slot); yesterday's
12:00 `reports-close` did run, outcome `no-op`. `weekly-discovery` is a Monday routine and today is
Tuesday — an off-day, not a skip.

`daily-merge` fired **2 times** today. It is scheduled `0 */3 * * *` (8 slots/day); by 12:47 UTC five
slots were due. Recent counts: 08-29 four runs, 08-30 five, 08-31 three, today two. Carried item 40
continues to be evidenced rather than resolved.

### ⚠ This report is INCOMPLETE, not empty

`daily-report.js` reported **1 tool-call failure**: `traffic: fetch failed`. Everything else in this
report is complete. See § 4.

---

## 3. What shipped

| | |
|---|---|
| Merged since yesterday | **3** — #731, #729, #728 (all routine-log shards; no content) |
| Records built | **5**, in PR #730 — none merged to `main` |
| Records audited | 5 (wave self-audit: 2 PASS / 3 FAIL, 5 issues, 4/4 skeptic-confirmed) |
| Records scanned | **1,981** by the review pass — **0 flagged** |
| Reports closed | 0 (slot not yet due) |
| Corpus | **1,981 records**, 0 flagged, ladder `pr`, 0 deferred |
| CI on `main` | **success** (`d18ba1bd4`) |
| Working tree | clean |

**No content reached `main` today.** All three merges were routine-log bookkeeping. The five records
the wave built sit in #730; the five from 08-31 sit in #722; the five from 08-27 sit in #670. Fifteen
record-slots of finished, audited work is parked in open branches — and, as § 5.1 shows, ten of those
fifteen are the same five records built twice.

---

## 4. Traffic

**Traffic: unavailable — fifth consecutive day.** `traffic` is `null` in the tool's JSON and
`tool_failures` carries `traffic: fetch failed`. This is **not** a zero and not a quiet day.

Running the underlying tool directly gives the diagnosis `daily-report.js` does not surface:

```
traffic.js: 403 from GoatCounter — the dashboard is no longer public.
Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.
```

The tool names the exact remedy. Nothing in this repository can reach that setting; it needs the
operator, in the GoatCounter UI, once. **No data is lost** — GoatCounter is still collecting, the
public read endpoint is simply closed. This is carried item **43**, now on its fifth day.

Worth restating why this matters more than a missing number usually would: GoatCounter is the only
figure that counts the site's actual audience. When this project last judged its reach on Search
Console alone it saw ~8 clicks in six weeks and concluded it had none, while GoatCounter had recorded
4,163 visits with **chatgpt.com as the largest single referrer and Google absent from the top six**.
Five days without that number is five days of flying blind on the one metric that has ever been
right. Referrers need the authenticated API (`node tools/traffic.js --refs`).

---

## 5. Findings

### 5.1 THE HEADLINE — #722 and #730 build the identical five records

The 08-31 wave and the 09-01 wave produced the same five record slugs, verified by diffing both
branches against current `main`:

```
any-time-you-see-anything-big-and-working-well-you-want-to.json
it-will-take-them-ten-years-to-prove-me-wrong.json
the-telegraph-is-like-a-very-very-long-cat-or-dog.json
theyre-cheering-us-both-you-because-nobody-understands-you.json
we-dont-know-who-discovered-water-but-we-know-it-wasnt-a.json
```

Five in #722, the same five in #730, none of them on `main`. Two full Opus wave pipelines — source
fetching, audit, skeptic re-check, theme-tagging — spent on one set of five quotes.

**Neither wave did anything wrong, and both ran their duplicate checks.** #730 records:

> Near-duplicate check run **both ways**: all 12 against every built `displayQuote`, and
> intra-batch, plus a slug-uniqueness assertion. No collisions.

That check is correct and still blind, for two reasons that compound:

1. **The corpus side.** The check compares against *built* pages. #722's five pages exist only inside
   an unmerged branch, so there was nothing to collide with. This is carried item **23** — *"still
   cannot see unmerged or closed PRs"* — on its 14th day, now with a concrete price.
2. **The queue side, which is new.** On `main`, all five candidates are still `status: "queued"`,
   `wave: null`. The 08-31 wave marked them `ingested` **inside #722**, so `main`'s queue re-offered
   them the next night. Verified directly in `data/harvest-queue.json` on `main`.

So the rule is general and will recur: **any wave PR that does not merge the same day will be rebuilt
by the next night's wave.** #670 has been open five days; #722 one.

**Which one should ship: #730.** It is the strictly better of the two and the only mergeable one:

| | #722 (08-31) | #730 (09-01) |
|---|---|---|
| Mergeable | **DIRTY** (conflicted) | **CLEAN** |
| CI | — | `verify` **success** |
| `it-will-take-them-ten-years` rating | `2 / "Unproven"` | `2 / "Unproven — no documented source"` + `verdictNote` |
| `theyre-cheering-us-both` `creditedTo` | `Albert Einstein` | **`Charlie Chaplin`** |

That last row is #730's own HIGH finding #2, which #722 does not carry:

> `creditedTo` was stamped from the batch magnet (Albert Einstein), so the ClaimReview rated *"Albert
> Einstein said [the punchline]"* — a claim nobody makes — while the credit actually in circulation
> […] is **Charlie Chaplin**.

Neither branch carries the `%%22` malformed-href bug #722 reported fixing; that fix is present in
both. **Recommendation: merge #730, close #722 unmerged.** Nothing unique is lost.

### 5.2 #730 will merge in a few hours, and it carries the record item 56 asked you to rule on

#730 is CLEAN with `verify` green. Today's 07:00 merge pass already noted:

> #730 rebuilt and pushed — merges next pass, once verify is green on the new head

`verify` went green at 08:21 UTC. **The next merge slot takes it.** It contains
`the-telegraph-is-like-a-very-very-long-cat-or-dog` — the record carried item **56** asked you to rule
on before #722 merged. #722 is no longer the vehicle; #730 is, and it is hours away rather than
blocked. Nobody said "(or Dog)"; that is Quote Investigator's article headline serving as the record's
title, slug, canonical URL and ClaimReview text. PR #689 refused this exact slug for this exact
reason on 08-28.

#730's own body flags the generator-level version of the same thing, unprompted:

> The `qi-harvest-2026-08-27` queue stores QI *article titles* as `displayQuote` — title-cased
> headlines, not sentences any source transcribes (50 records already shipped this way, so this wave
> follows the established convention rather than diverging).

That is carried items **54** and **46**, third night running, now with a specific page about to ship.

### 5.3 A new one-line generator defect, found and correctly not fixed by the wave

#730 reports a real gap in `tools/mis-kind.js`:

> The pattern `/\bdocumented\b[^,]*\bunverified\b/` cannot match a tag written `documented, but
> unverified` because `[^,]*` forbids the comma. My row rendered the burgundy ✕ ("this credit is
> false") against prose saying QI reached *no* verdict. I worked around it by rewording my own tag to
> `documented but unverified` rather than editing the shared rule.

Confirmed by reading `tools/mis-kind.js`: the pattern sits in `CONTEXT_TAG_REPORTED` and the comment
beside it reads *"r46, hand-set by its agent; derive it from now on"* — so the intent was exactly to
catch this form, and the character class defeats it. The wave was right not to touch a shared rule
from inside a content PR, and right that the fix needs its own measured pass (this file's header is
emphatic that softening a genuine debunk is the worse error).

### 5.4 Egress blocks — unchanged, and now costing link quality

Both Churchill records lost their best source. #730:

> **this container's egress proxy hard-blocks that domain** (403 CONNECT tunnel failure via curl;
> `EGRESS_BLOCKED` via WebFetch). Rather than ship a link I could not confirm supports its claim, I
> replaced it with Wikiquote's Churchill page — whose Misattributed section I read, and which does
> **not** list either anecdote. The link text says so explicitly rather than implying corroboration.

That is the honest handling and it still ships a weaker page. Blocked and wanted:
`winstonchurchill.org`, `richardlangworth.com` (carried item **13**, 20th day), plus the standing
`books.google.com` / HathiTrust / `virgin.com` / `theguardian.com` set from #670.

### 5.5 What the wave could not establish (correctly left out)

From #730, each omission stated on the page rather than guessed:

- `rightsHolder` omitted on the 1933 Chaplin and 1947 Plesch texts — holder could not be established.
- `dateCreated` omitted on the Chaplin record — the Berlin visit's date is not established anywhere read.
- Which of Einstein's sons spoke — *"Chaplin didn't record it and QI says so; the hero reads 'Hans Albert or Eduard Einstein'."*
- Culkin named only as earliest credited, not settled author.

The wave also declined to overclaim in structured data, fixing a **blocker** where the JSON-LD rated a
claim `1 / "False"` while the prose correctly reported that QI *"expressly declines a verdict."*

### 5.6 The stale local `main` fired at least four more times — 18th day

Every routine that ran today hit it and recovered via documented exception (b):

| routine | gap |
|---|---|
| `daily-reports` | *"local main ref was 39 commits behind"* |
| `daily-wave` | *"39 commits fast-forwarded"* |
| `daily-review` | *"fast-forwarded local `main` **41 commits** (`b014b74b8..d18ba1bd4`)"* |
| this report | 41 commits (`b014b74b8..d18ba1bd4`) |

One nuance worth recording, because it narrows the search: **the start states differ.** The review
pass began *on a branch* — `git on main: on claude/wizardly-fermat-j36pjs` — while this pass began
**detached** (`on HEAD`). Same stale ref, two different HEAD states, so whatever leaves the container
in this condition is not simply "the harness checks out a branch". The review pass's reading of why
the guards miss it is exact:

> `current with origin/main` measures HEAD and `local main sane` only requires the ref be an
> *ancestor*, so a 41-behind ref passes both. `git checkout main` alone would have moved the tree
> backward by 41 commits.

Carried item **12**, 18th day. The exception's remedy is correct as written; the cause is untouched.

### 5.7 Two routines re-flagged the doc/harness branch-name conflict

The review pass again refused its assigned branch name and said why:

> This session's standing instructions named a `claude/…` branch; `merge-gate.js` fails closed on
> unrecognised prefixes and would class that `HUMAN`, so the PR would never merge. Following the doc,
> and flagging the disagreement here as instructed.

Both routines resolved it the same correct way. Carried item **20**.

### 5.8 No discrepancies between claims and the log

Every routine's self-reported outcome matches `daily-report.js`: the wave claims 5 records and #730
adds 5; the review claims 1,981 scanned / 0 flagged and health reports 1,981 / 0; the merge pass
claims #728, #729, #731 and those are the three merged. `daily-reports` reported an empty public
queue and opened nothing. **Nothing to correct.**

---

## 6. Needs a decision

**Two new (60, 61). Item 56 is unchanged in substance but its deadline moved to today. 42 and 43 are
on their 5th day.**

**Most urgent: 60 and 56 — both are decided by the next merge pass, not by you, unless you act first.**

60. **Rule on the duplicate pair: merge #730, close #722 unmerged.** Both branches build the identical
    five records (§ 5.1); #730 is CLEAN with green CI and carries a `creditedTo` fix #722 lacks, while
    #722 is DIRTY. Doing it is: close #722 with a one-line reason, and let the next merge pass take
    #730 — or, if you want #722's five to be the ones that ship, close #730 instead and rebuild #722.
    *(NEW)*

56. **Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog` before #730 merges.** *(2nd day —
    but now hours, not days: #730 is CLEAN and `verify` is green.)* Its title, slug, canonical URL and
    ClaimReview all quote QI's headline *"The Telegraph is Like a Very, Very Long Cat (or Dog)"* —
    nobody said "(or Dog)" — and #689 refused this exact slug for this reason on 08-28. Doing it is
    dropping the one record from #730 in a further commit (the other four are unaffected), or letting
    it ship on the grounds that its debunk rating makes it a refutation rather than a certification.

61. **Fix the `mis-kind.js` comma gap.** `/\bdocumented\b[^,]*\bunverified\b/i` cannot match
    `documented, but unverified`, so a row whose prose says no verdict was reached renders the ✕ that
    means the credit is false (§ 5.3). Doing it is one character class widened on its own branch, plus
    a corpus count of how many existing rows the widening would flip — this file's own header requires
    the measurement. *(NEW)*

43. **Flip GoatCounter → Settings → "Dashboard viewable by" → Anyone.** *(5th day)* The public endpoint
    403s, so this report carries no traffic figure for the fifth day running. One setting in the
    GoatCounter UI; nothing here can reach it and no data is lost.

42. **Rebuild and merge #670.** *(5th day — still the only stale branch with genuinely unique content)*
    Five records that exist nowhere else, two of them `verified` Thornton Wilder quotes resting on two
    independently scanned 1927 printings. Untouched since 08-27 and unmentioned by any merge pass since
    then. Doing it is: merge `main` in, take `main`'s side on generated files, `node tools/build.js`,
    `node tools/scan.js`, push, let the next pass take it.

57. **Give the orphaned `taxes-…` candidate a way back into the queue.** *(2nd day — still true:
    verified today that `taxes-this-is-a-question-too-difficult-for-a-mathematician` is marked
    `ingested` in `data/harvest-queue.json` while no such record exists on `main`.)* A well-sourced 1944
    AP-wire Einstein quotation is permanently unreachable. Doing it is a one-line status edit, or the
    `harvest.js` `ingested → queued` demote that item 3 already wants.

58. **Accept or decline the `disputed-creator-name-mismatch` detector, and rule on its 99-record sweep.**
    *(2nd day — confirmed not accepted: the detector appears nowhere under `tools/`.)* The gate measured
    99 hits / 5.0% and said BACKFILL, but a blind sweep would put prose fragments into `Person.name`.
    Doing it is `node tools/propose-detector.js /tmp/cand.js --accept` plus a decision to sweep only the
    name-variant cause.

59. **Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it.** *(2nd day —
    re-flagged unprompted by #722 tonight: "`DOSSIER_SCHEMA` cannot emit `claimRatingValue`/
    `claimRatingName` […] so a wave physically cannot produce the hedged verdict that `template.js`
    already supports". Both waves this week paid an audit-and-patch cycle for exactly this.)* Doing it
    is one measurement pass on its own branch: re-run `verify-corpus.js` to see whether the ceiling is
    still real at 1,981 records, then rank the three queued fields.

54. **Rule on Quote Investigator *headlines* reaching `displayQuote`.** *(3rd day — #730 flagged it
    again on its own initiative, noting 50 records have already shipped this way.)* Doing it is deciding
    the rule for QI headline text generally and writing it into the runbook, so the next wave stops
    re-litigating a predecessor's refusal for the fifth night running.

40. **Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`).** *(6th day)* Today 2 runs against
    5 due slots; 08-31 three, 08-30 five, 08-29 four. Doing it is one line in `.github/workflows/merge.yml`.

5. **Declare which carried items you are not going to do.** *(9th day)* The list below is 50+ long and
   two items were struck last week, so the mechanism works. Reply with the numbers to strike and
   tomorrow's report carries only what survives.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 2 | Stop the daily wave and the backlog waves colliding over the queue — **§ 5.1 is this item's bill** | 8th |
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" — **57 is its mirror image** | 8th |
| 4 | Fix the report window — `daily-report.js:30` windows on calendar day | 25th |
| 6 | `prep-wave.js:206` should stamp `creditedTo` when `confidence === 'disputed'` regardless of name equality — **#730's HIGH #2 is this** | 15th |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 8th |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 14th, clock running |
| 9 | Fix the middle-initial hub fork at the generator, scoped to `schema.creator` | 7th |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row | 7th |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 7th |
| 12 | Root-cause the stale local `main`, or ratify exception (b) in `preflight.js` — **fired 4× today, at 39 and 41; two different HEAD states (§ 5.6)** | 18th |
| 13 | Extend the egress allowlist — `richardlangworth.com`, `winstonchurchill.org` — **cost two Churchill pages their best source again** | 21st |
| 14 | Decide the `isBasedOn → isPartOf` bulk migration (1,046 of 1,752 measured 08-24) | 9th |
| 15 | Decide what to do about Google Books' quota | 10th |
| 16 | Stop one source's outage hard-stopping the review pass (`preflight.js` `head()` treats a 12s timeout as blocked) | 13th |
| 17 | Fail the build when `Quotation.creator` is suppressed; normalise `template.js`'s hero match — **folded into 58** | 13th |
| 18 | Fix `daily-report.js:148`'s `work` figure — it sums `built + processed + sampled` | 9th |
| 19 | Decide whether `perf-check` belongs in the routine census | 11th |
| 20 | Settle the branch-name conflict between the harness (`claude/…`) and the routine docs — **re-flagged today (§ 5.7)** | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict — **56 is this item with teeth** | 10th |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason | 11th |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — cannot see unmerged PRs — **this is what § 5.1 cost** | 15th |
| 24 | Work the disputed records with no `creditedTo` — 7 of 10 `magnet` rows still render the false-credit ✕ | 21st |
| 25 | Do the measured `mis-kind.js` pass — **61 is a second, separate gap in the same file** | 20th |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) | 14th |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 14th |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 14th |
| 30 | Decide `verbatimNote()`'s "Verbatim:" label corpus-wide (~370 records) | 14th |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — **58 is queued behind it** | 16th |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate | 17th |
| 33 | Decide the house convention for characters with no HTML entity | 13th |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 15th |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — **folded into 58** | 15th |
| 36 | Make the **wave** log a cost figure — tonight's shard again carries only `date, routine, outcome, built, pr, note` | 17th |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 18th |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 18th |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 14th |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 6th |
| 44 | Teach `merge-gate.js` to look at what actually changed (`:98` DIRTY→SKIP; `:123` behind→demote) | 5th |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 5th |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — **54 is the ruling this needs** | 5th |
| 47 | Decide whether QI's Title Case should be normalised centrally (35 of 1,913 records) | 5th |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 5th |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 5th |
| 51 | Make the merge pass's rebuild step fetch before it merges — *#730 was rebuilt successfully today; still unverified as a general fix* | 4th |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Add `traffic.js`'s specific error to `daily-report.js`'s `tool_failures` — **folded into 43; § 4 shows the gap again today** | 4th |

**Struck previously:** 28 (superseded by 59), 50, 52 (folded), 53 and 55 (done 08-30/08-31).

---

## 7. Cost

**No routine reported a cost figure today.** `weekly-discovery` — the only routine that has ever
logged one — is a Monday pass and did not run. Tonight's `daily-wave` shard again carries only
`date, routine, outcome, built, pr, note`, so carried item **36** stands unchanged: `daily-wave` has
never logged a cost.

The standing datapoints, all from `weekly-discovery`, remain the project's only measured numbers:
**~9.5K new tokens/record** (2026-08-31 run, 20 records) against **~21K/record** (2026-07-29), both on
a new-tokens basis. Neither speaks to a pass that must re-fetch sources.

**The wasted-work figure is back, and it is the largest yet.** Two full wave pipelines — each with
source fetching, a five-issue audit and a skeptic re-check — were spent on the same five quotes on
consecutive nights (§ 5.1). One of the two is pure loss whichever way item 60 is decided, and because
no wave logs a cost, **there is no way to say what that loss was.** That is item 36 ceasing to be
bookkeeping: without it, the price of the duplication bug cannot be quoted to justify fixing it.

**Nothing reported an interrupted run** — no 529, no rate limit, no capacity ceiling. The only
environmental hits were the standing egress blocks (§ 5.4) and the GoatCounter 403 (§ 4).
