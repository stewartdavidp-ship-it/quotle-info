# Daily report — 2026-08-09

**Every routine due by this hour ran, `main` is green, the tree is clean, 0 flagged — and no content
has reached the site in two days. Both daily waves are stuck for two different reasons: yesterday's
`d20260808` (4 records) went to merge conflict when your own `mis-kind` fix rebuilt 149 pages under
it, and today's `d20260809` (3 records) fails CI's stale-output gate on 6 files. `main` has sat at
1,506 records since 08-08. Nothing is broken on the live site; seven researched records are queued
behind two mechanical problems, and only one of them is a rebase.**

Written at 12:11 UTC, after the 07:00 merge pass. Preflight failed once on `git on main` (the
harness started detached at `4dd95cf41`); Step 0's own `git checkout main` cleared it and the
fast-forward pulled 42 commits. Re-run came back **10/10**. `local main sane` passed, so the
2026-08-04 recovery was not needed.

---

## What ran

| | routine | outcome | what happened |
|---|---|---|---|
| Mon 02:00 | weekly-discovery | · not scheduled today | correct — today is Sunday |
| 03:00 | daily-wave | ✓ `pr` · 3 records | PR **#404**, 07:28 UTC. Open, **CI FAILURE** — Finding 1 |
| 04:00 | daily-reports | ✓ `no-op` | shard 08:11 UTC. Empty reader queue — the success path |
| 05:00 | daily-review | ✓ `no-op` | shard 09:05 UTC. 1506 scanned, 0 flagged |
| 07:00 | daily-merge | ✓ `merged` ×4 · 6 PRs | ran 01:42, 04:18, 06:55, 09:38 UTC — all merged, all shards |
| 12:00 ET | reports-close | · not yet due | its slot is 17:00 UTC; nothing to judge at this hour |

**Nothing DID NOT RUN today.** Yesterday's `reports-close` also ran (`no-op`, queue empty).
`daily-report.js` reported `tool_failures: []`, so the roster verdict below is fully evidenced, not
partial.

`merge.yml` is on `0 */3` and fired four times. Every pass merged what the gate offered it; **it was
never offered a wave.** That is the gate working correctly, not the merge pass failing — see
Finding 1.

### Health

| | |
|---|---|
| CI on `main` | **success** (`4dd95cf41`) |
| records | **1506** — unchanged since 08-08. #389 takes it to 1510, #404 to 1509 |
| flagged | 0 |
| quote backlog | **1631 candidates**: 460 queued (284 genuine-famous, **85 misattributed**, 84 disputed, 14 other), 1156 ingested, 8 skipped, **0 stranded `selected`** |
| report ladder | `pr`, 1 run awaiting the operator's verdict (dated 2026-07-29 — **11th day**) |
| deferred items | 0 |
| working tree | clean |
| open PRs | 6 — #407 `MERGE` (the 07:00 pass's), #389 and #404 `SKIP`, #357 `SKIP`, #345 and #317 `HUMAN` |

The misattribution seam is refilled: **13 → 85 queued `misattributed`**, from #396. That closes
yesterday's decision item 3.

---

## What shipped

**To `main`: nothing but log shards.** The six PRs the tool lists as merged (#400–#403, #405, #406)
are routine-log shards and one review log. Zero records, zero generator changes.

**In the gap window** — between yesterday's report (12:10 UTC, 08-08) and midnight, which
`daily-report.js` still does not cover (decision item 6, recurring) — two substantive merges, both
yours, both recovered here from `git log`:

- **#394 — `fix(mis-kind)`: 161 rows across 149 records stop rendering a false ✕.** This is
  yesterday's decision item 1, closed, and closed *carefully*: the commit records that the bare
  participle test fires on 131 rows including person subjects (`"Serena Williams | trimmed"`,
  `"Audre Lorde | Truncated"`), so the wording-edit family was admitted only where the row subject is
  not a person — 131 narrowed to 92, each read. It also records what it deliberately did **not** do:
  *"NOT DONE, and recorded as an OPEN GAP in the file rather than guessed at: the ~250-row
  variant/wording seam."*
- **#396 — `harvest(Track A)`: +127 candidates across 12 new magnet authors.** Yesterday's item 3.

**Today's wave, `d20260809`: 3 records, not 5** (#404, open, CI red). Three Feynman misattributions —
`any-field-that-has-the-word-science-in-its-name-is` (really **Frank Harary**, per QI, via Weinberg
1975), `if-youre-not-part-of-the-solution-youre-part-of-the` (anonymous; the Feynman credit traces to
Wes Craven's 1999 novel *Fountain Society*, which QI believes invented it), and
`the-philosophy-of-science-is-as-useful-to-scientists-as` (anonymous; absent from *The Quotable
Feynman*). Audit **1 PASS / 2 FAIL, 4 issues, 1 refuted by the skeptic, 3 fixed pre-ship**.

The tool agrees with the wave's claim: `work: 3`, and the wave states why — *"The instruction is
explicit: if the budget does not fit, build fewer records, never fewer stages."* **No routine's claim
contradicts the log today.**

---

## Findings

### 1. Two waves, seven records, neither can merge — and the two causes are unrelated

This is the whole day. `merge-gate.js` right now:

```
SKIP     #389   wave-d20260808    merge conflict — needs a human, do not auto-resolve generated files
SKIP     #404   wave-d20260809    CI not green (FAILURE)
```

**#389 was `MERGE` when yesterday's report was written.** It was branched from `2ae26e03a` (08:47
UTC). At 10:10 you merged #394, which rebuilt 149 pages; at 10:36, #396. #389's generated HTML then
conflicted with `main`'s, and every merge pass since has correctly refused to auto-resolve it. This
is precisely what `merge-gate.js` prints under its own verdict — *"Each merge puts every other branch
BEHIND and invalidates its build"* — playing out across a day boundary rather than within a pass. No
routine can fix it: the gate is built to refuse generated-file conflicts, and the wave routine does
not revisit yesterday's PR.

**#404 is a different failure.** Its branch is current (base `8d6a279f9`, after #394), but CI's
stale-output job fails:

```
##[error]Committed output is stale — rebuilding changed files that were not committed.
 M authors/richard-feynman/index.html   | 14 +++-
 M data/corpus-state.json               |  2 +-
 M flagged/index.html                   |  2 +-
 M index.html                           |  2 +-
 M search.json                          |  2 +-
 M under-review/index.html              | 43 ++++++---
```

All six files **are** in the PR's 32 changed files — they were committed, just from an earlier build
than the branch's final record state. The audit fixes (the deleted `dateCreated`, the two
misattribution rows) landed after the aggregate pages were generated, so the per-quote pages are
current and the roll-ups are not. `under-review/index.html` at +43 lines is the tell: three new
disputed records that the committed roll-up does not list.

The wave's own PR body reports *"Corpus 1506 → 1509. 51 invariants reconcile; `scan.js` flagged 0"* —
all true of the records, and all true of a build that was then superseded. **It does not claim CI is
green, and it did not check.** The fix is one `node tools/build.js` and a commit on `wave-d20260809`.

### 2. `mis-kind.js` has a second gap, and it bit twice in a three-record wave — the day after #394

#394 closed the `CONTEXT_TAG` side. Today's wave hit the other side, in its own words:

> **`tools/mis-kind.js` cannot mark an affirming row that does not open with actual/real/true/genuine/verified.** `AFFIRMS_TRUTH` keys on the scope prefix, so "Who Quote Investigator credits" and "The family it belongs to" both fell through to the default ✕ against their own affirming prose. Findings 2 and 3 above are the same gap, hit twice in one three-record wave.

Verified independently — `tools/mis-kind.js:149`:

```js
const AFFIRMS_TRUTH = /^(the\s+)?(actual|real|true|genuine|verified)\b/i;
```

It is anchored to the start of the scope string, so any affirming row phrased as a question
("Who QI credits") or a category ("The family it belongs to") is invisible to it. The wave worked
both around by hand rather than editing `tools/` — correct per its doc, and the third consecutive
wave to pay a per-record tax for a `mis-kind` pattern gap.

### 3. `/themes` silently drops new disputed records — and every existing check says the tagging passed

The wave's second reported gap, measured rather than asserted:

> **`tools/build-themes.js` hard-caps the misattributed bucket at `fake.slice(0, 18)` and never sorts it.** Insertion order is alphabetical by record filename, so two of these three records are correctly tagged and simply never render on `/themes`: they land at positions 30–167 on busy themes (wisdom has 210 disputed entries, character 131, truth 126). … Tagging itself is complete — `apply-tags.js --manifest` reports 3/3 with the completeness gate green — so this is invisible to every existing check. **A disputed wave can satisfy the theme-tag stage and still add nothing visible to `/themes`.**

Confirmed at `tools/build-themes.js:254`. This is yesterday's decision item 18 — which was framed as
"decide what the cap should sort by" — now upgraded from an aesthetic question to a measured content
loss: two of today's three records, on the seam the site is differentiated on.

### 4. #357 is on its fourth day stuck on a *cancelled* job, not a failed one

`verify` on `reports/2026-08-06-close` was **cancelled** at 15m01s. `merge-gate.js` reads that as
`CI not green (CANCELLED)` and will never mark it `MERGE`; no routine retries a cancelled job. The PR
is a one-line routine-log shard, 1 file, 1 addition. It needs one click in the Actions tab.
*(Carried, 3rd day.)*

### 5. #345 has gone from green-but-invisible to conflicted

Both #345 (`fix/`) and #317 (`claude/`) are `HUMAN` at the gate — *"branch is not a known routine
prefix — left alone"* — which is by design for generator changes. But #345 is now **DIRTY**: 1,382
files, of which 1,380 are rebuilt pages, against a corpus that has moved twice since it was opened.
#317 is still `CLEAN` (5 files, 10 additions) and merges as-is. The asymmetry is worth noting: the
small fix has stayed mergeable for five days; the one carrying a full rebuild decays every time
anything lands.

### 6. Egress allowlist blocked four hosts, and the wave handled it correctly

Reported for the record, not as a defect:

> **Four hosts are blocked by this environment's egress allowlist**: `nature.com`,
> `feynmanlectures.caltech.edu`, `press.princeton.edu`, `newspapers.case.edu`. QI, Wikiquote,
> Wikipedia and archive.org are all reachable, so the anchors were read in full. **No link to a host
> I could not fetch appears in any record.**

Same shape as 08-08's report (PBS, AFI, Open Library, Google Books). Four waves have now routed
around this allowlist and each has said so. Nothing is wrong with the pages; it is a standing
constraint on which sources can ever be first-hand.

### 7. The near-duplicate draw problem cost a slot for the third day running

The wave dropped *"If you think you understand quantum mechanics…"* because
`anyone-who-claims-to-understand-quantum-physics-is` *"already covers that exact string as a
misattribution item with the same finding, so a separate page would be near-duplicate content."*
`sync` dedups on exact normalised text against **queued candidates**, not against what built pages
already cover inside their misattribution sections. That is decision item 8, hit on 08-07 (r34),
08-08 (Ali), and again today.

---

## Needs a decision

**New**

1. **Rebuild and commit on `wave-d20260809` so #404's CI goes green** — six roll-up files
   (`index.html`, `search.json`, `under-review/`, `flagged/`, `authors/richard-feynman/`,
   `corpus-state.json`) were committed from a build that predates the wave's own audit fixes. One
   `node tools/build.js` + commit on that branch; no record changes, and the 07:00 pass takes it from
   there.

2. **Resolve #389's conflict, or close it and re-run those four quotes** — its 4 records (Mae West,
   Jordan, Coelho ×2) conflict with `main` only in generated HTML, because #394 rebuilt 149 pages
   under it. Merge `main` in, discard the branch's generated files, re-run `node tools/build.js`, and
   push; the records themselves are unaffected.

3. **Extend `AFFIRMS_TRUTH` past the actual/real/true/genuine/verified prefix** — it is anchored to
   the start of the scope string, so affirming rows phrased as a question or a category take the
   refutation ✕ against their own prose; it hit twice in one three-record wave. One pattern at
   `tools/mis-kind.js:149`, measured against the corpus the way #394 was.

4. **Make the wave's ship stage verify CI, not just the build** — #404 truthfully reported "51
   invariants reconcile" while shipping output that CI rejects, because the invariant check and the
   stale-output gate are different questions. Either re-run `build.js` immediately before the final
   commit or have the ship stage poll its own check run.

**Carried, in the order I would spend the time**

5. **Sort the `fake.slice(0, 18)` bucket on `/themes/*` before capping it** — now measured: two of
   today's three records are correctly tagged and render nowhere, and every existing check reports
   success. One sort key at `tools/build-themes.js:254`. *(5th day — promoted from "decide what it
   should sort by" by Finding 3.)*

6. **Re-run the cancelled `verify` on #357** — cancelled at 15m01s, not failed, so the gate will
   never mark it `MERGE` and no routine will retry it. One re-run from the Actions tab. *(3rd day.)*

7. **Merge #317, and decide #345 separately** — #317 is `CLEAN`, 5 files, and closes the
   `docLang()` substring bug ("circulating" matching "latin"). #345 is now `DIRTY` and needs a rebase
   plus a full rebuild before it is judgeable at all. Splitting them stops the small fix waiting on
   the large one. *(5th day.)*

8. **Make harvest dedup variant-aware, and duplicate-aware against built pages** — `sync` compares
   exact normalised text against queued candidates, so a line already covered inside a built page's
   misattribution section still leads a draw. Third consecutive wave to lose a slot to it. *(10th
   day.)*

9. **Make `daily-report.js` window run report-to-report, not calendar-day** — #394 and #396 merged
   in the gap between yesterday's report and midnight and appear in no tool output; I recovered them
   from `git log` for the second day running. Default `--since` to the timestamp of the last
   `daily-report` shard. *(2nd day.)*

10. **Judge the 2026-07-29 report-gate run so the ladder can move** —
    `node tools/report-gate.js --judge agree|disagree`. It is the only thing between the ladder and
    its next step. *(11th day.)*

11. **Fix the trailing-ellipsis strip in `toRecord`** — `/\s*\.\s*$/` turns a quote ending `. . .`
    into `. .`; confirmed still present at `workflows/generate.js:48`, `:337` and
    `workflows/prep-wave.js:62`. It has now led and been dropped from seven draws. *(3rd day.)*

12. **Make `harvest.js unselect` refuse to release more than you named** — it ignores its positional
    slug and releases every `selected` candidate, so one mid-wave invocation would discard a whole
    draw. ~10 lines in `cmdUnselect` (`tools/harvest.js:290`). *(3rd day.)*

13. **Correct the audit's remedy for `Quotation.text`** — it names `presentation.verifiedQuote`,
    which reaches only the copy/share kit, so following it "would have looked like a fix and shipped
    the same markup". One string in the audit prompt pointing at `schema.quotationText`. *(2nd day.)*

14. **Fold the dropped Ali record's sourcing into the surviving page** — r34's research (Marines
    Magazine 1917 → Moran and Mack, Chicago Tribune 9 Mar 1919 → Abbott and Costello 1945 → Satchel
    Paige) exists only in a PR body. One record edit on
    `im-so-fast-that-last-night-i-turned-off-the-light-switch-in` plus a rebuild. *(2nd day.)*

15. **Correct `no-longer-talk-at-all-about-the-kind-of-man-that-a-good-man`** — the live page still
    asserts the wording is *"not any historical translation"* when #306 traced it to Haines 1916, and
    the record contains zero occurrences of "Haines". One record edit plus a rebuild. *(7th day.)*

16. **Write down the loc.gov convention in the wave doc** — cite, link, verify liveness via the
    JSON/ALTO API, say so on the page. Seven waves have now derived this unaided and identically.
    One paragraph. *(9th day.)*

17. **Add the no-wrong-credit arm to `disambiguatingDescription`** — on a `disputed` page where
    nobody else is credited, the field still reads *"This attribution is disputed. Actually by X"*
    about the very person the page credits. One more arm on the conditional at `template.js:791`.
    *(4th day.)*

18. **Make `harvest.js report` read-only** — the command the runbook tells you to run to *check*
    state writes three committed files (`tools/harvest.js:384`). *(6th day.)*

19. **Fix `template.js`'s `wordingDrift` punctuation false-positive** — `plain()` (line 89) strips
    markup and entities but not trailing sentence punctuation, so a record whose `claimQuoteText`
    differs only by a final period is classified as a wording drift. *(6th day.)*

20. **Decide whether `daily-report.js` should suppress the roster verdict when the shard fetch
    fails** — no failure today either, and the `UNKNOWN` path is wired. Worth ten minutes or worth
    closing. *(3rd day.)*

21. **Close the ~250-row variant/wording seam #394 left open** — the commit records it deliberately:
    the bucket holds "unsourced variant", "corrupted variant", "Garbled variant", "fabricated
    wording", so a bare `/\bvariant\b/` would soften genuine debunks at scale. Visible cost today:
    `better-to-reign-in-hell` renders ~ on one row and ✕ on its "minor variant" row. Wants its own
    measured pass. *(1st day — inherited from #394's own note.)*

**Closed since yesterday:** the `mis-kind` `CONTEXT_TAG` gap (#394 — 161 rows, 149 records) and the
misattribution seam refill (#396 — 13 → 85 queued `misattributed`).

---

## Cost

**Nothing reported a token figure today.** The daily wave's PR body carries no cost line, and none of
the five routine-log shards carries one. Its only resource statement is qualitative, and it is the
one that matters: *"Built 3, not 5. … Three records got full-depth research and a genuine per-page
audit rather than five getting a thin one."*

Two consecutive days of "budget did not fit five" is now a pattern rather than an incident. Both
waves reached the same conclusion independently and both spent the shortfall on depth, which is the
instruction working — but the corpus is growing at 3–4 records a day against a 460-candidate queue,
and nothing has actually landed for two of them.
