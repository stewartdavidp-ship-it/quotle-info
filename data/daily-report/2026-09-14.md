# Daily report — 2026-09-14

## 1. Verdict

**The wave did not run — its Routine is switched off — and that is the whole story of the night.**
Everything else was clean: `daily-reports`, `daily-review` and `daily-merge` all ran, the Monday
`weekly-discovery` ran and found nothing, `main` is green at `49dd4e4a7`, 2,220 records, 0 flagged,
tree clean.

Three things to read before anything else.

1. **`daily-wave` did not run, and the cause is not a failure — the Routine is disabled.**
   `trig_01HKDxYQmKcshaCLZVfidmTa` ("quotle.info daily wave (5 from backlog)", cron `0 7 * * *`)
   reads `enabled: false` with **no `ended_reason` and no `suspension_reason`**, which in the
   triggers API means *paused by a person*, not auto-disabled and not subscription-held. Its last run
   fired 2026-09-13 07:05:19Z and **SUCCEEDED**. So it was switched off some time between yesterday
   morning and this morning's 07:00 slot. No records shipped today. Item **82** — and if you paused it
   deliberately, say so and it stops being reported as a fault. § 5.1.
2. **The report window lost five merged PRs again — including the entire 09-13 wave — for the third
   consecutive day.** #869 (5 records), #872, #874, #875 and #876 all merged on 09-13 between 15:41Z
   and 22:54Z, after yesterday's report was written and before today's UTC window opened. **No report
   will ever carry the shipping row for the 09-13 wave**, and the corpus moving 2,215 → 2,220 is
   unexplained by anything in today's merged table. Item **4**, 38th day. § 5.3.
3. **The stale-`main` gap widened again — 50 commits, and the reflog dates the stale ref to 09-09.**
   Local `main` was at `786529ac4` with `origin/main` 50 commits ahead. Against 49 yesterday, 39 the
   day before, 28–31 when the exception was written. Item **12**, 31st day. § 5.5.

Item **73** is still not done — sixth day, and still the only item with a deadline (§ 5.4). Traffic is
unavailable for the eighteenth consecutive day, same one setting (§ 4).

---

## 2. What ran

| slot | routine | result |
|---|---|---|
| 03:00 | `daily-wave` | ✗ **DID NOT RUN** — no shard; the Routine is `enabled: false` (§ 5.1) |
| 04:00 | `daily-reports` | ✓ 1 run: `no-op` — 0 processed (shard 08:10:31Z) |
| 05:00 | `daily-review` | ✓ 1 run: `no-op` — scanned 2,220, flagged 0 (shard 09:04:41Z) |
| 07:00 | `daily-merge` | ✓ 2 runs: `merged`, `merged` (03:37:10Z and the 11:56Z pass) |
| 12:00 | `reports-close` | · not yet — due 17:00 UTC (yesterday's ran `no-op`, #875) |
| Mon 02:00 | `weekly-discovery` | ✓ 1 run: `pr` — **20 records audited** → **#879** |

**One expected shard is missing, and it is missing because the Routine never fired.** This is a
"did not run", not "no activity": the census compares against the roster, and the trigger record
confirms the mechanism rather than inferring it.

`daily-report.js` reported **1 tool-call failure** — `traffic: fetch failed`. **§ 4 is the only
incomplete part of this report; §§ 2, 3, 5 are complete.**

Quality note, unchanged for a third day: today's `daily-reports` and `daily-review` shards again carry
**no `note` field** (`{"outcome":"no-op","processed":0}` and `{"outcome":"no-op","scanned":2220,
"flagged":0}`), even though both PR bodies describe the exception (a) recovery in detail. The evidence
exists; it does not reach the shard, which is what the census reads.

---

## 3. What shipped

**Merged since the last report window opened (4) — all bookkeeping, no content:**

| PR | what |
|---|---|
| #881 | `daily-review` 2026-09-14 — no-op, 2,220 scanned, 0 flagged |
| #880 | `daily-reports` 2026-09-14 — no-op, no pending reader reports |
| #878 | merge-pass log shard, 2026-09-14 |
| #877 | merge-pass log shard, 2026-09-13 |

**No content PR merged inside today's window, and none was built either.** The 5 records that moved
the corpus 2,215 → 2,220 came in on #869, which merged at 15:41Z yesterday — inside the gap (§ 5.3).
Today's wave produced nothing to merge, because it did not run.

**Corpus:** 2,220 records · 0 flagged · report ladder `pr` · 0 deferred · working tree clean · CI on
`main` **success** at `49dd4e4a7`.

**Open PRs — none of them mine to merge:**

- **#879** (weekly discovery, 20 records re-reviewed, stamps only): `CLEAN`, `verify` **green** on the
  rebuilt head `119aa2d1` at 11:57:20Z. The merge pass's own note says *"#879 rebuilt and pushed —
  merges next pass, once verify is green on the new head."* It is green. **Needs nothing.**
- **#882** (merge-pass shard): `CLEAN`, `verify` green on `b55656de`, opened by `merge.yml`. Needs
  nothing.
- **#670**: `DIRTY`, open since 08-27, untouched since 08-27 — item **42**, unchanged.

---

## 4. Traffic

**Traffic: unavailable — eighteenth consecutive day.** `traffic` is `null` and `tool_failures`
carries `traffic: fetch failed`. This is an **analytics outage, not a quiet day**, and it is not a
zero.

I ran `node tools/traffic.js` directly, which gives the error the JSON does not:

```
traffic.js: 403 from GoatCounter — the dashboard is no longer public.
Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.
```

So there is no figure to report as an upper bound, spam-adjusted or otherwise, and no referrer
breakdown. **Eighteen days of traffic data are not lost** — GoatCounter is still collecting; this
report simply cannot read it. Item **43**, one setting in the GoatCounter UI.

---

## 5. Findings

### 5.1 `daily-wave` is switched off — the headline

The census says `DID NOT RUN`; the trigger record says why. Read from the Routines API:

| field | value |
|---|---|
| id | `trig_01HKDxYQmKcshaCLZVfidmTa` |
| name | `quotle.info daily wave (5 from backlog)` |
| cron | `0 7 * * *` |
| **enabled** | **`false`** |
| ended_reason | *(empty)* |
| suspension_reason | *(empty)* |
| next_run_at | `2026-09-14T07:04:34Z` — the slot it would have taken |
| last_run | `SUCCEEDED`, fired `2026-09-13T07:05:19Z`, finished `07:39:37Z` |

Both reason fields empty is the API's signal for **user-paused** — distinct from `ended_reason`
(permanently auto-disabled) and `suspension_reason` (a billing hold that lifts by itself). Nothing
crashed and nothing timed out: yesterday's run succeeded and shipped #869.

Two readings, and I cannot tell them apart from here:

- **You paused it** (after the 09-13 wave, perhaps to stop the queue churn behind items 2 and 23) — in
  which case nothing is broken and this line should stop appearing as a fault.
- **It was paused incidentally** — in which case content shipping is stopped indefinitely and every
  future report will read `DID NOT RUN` until it is switched back on.

Either way the consequence is the same while it stays off: **no new records, and the 252-candidate
backlog stops draining.** → item **82**.

### 5.2 Weekly discovery: 20 records re-audited, zero findings, and two detector candidates killed with measurements

#879 is the substantive pass of the night and it reports **"No findings. All 20 audited records are a
PASS. This PR is stamps only."** The 20 were everything `review.js due --limit 20` ranked as "never
re-reviewed since build". Worth knowing what that clean result cost: **~25 source re-fetches** against
the specific claim pinned to each link, plus two mechanical passes over all 20 built pages checking
render artifacts, JSON-LD parseability, canonical-vs-slug trailing slash, and record↔JSON-LD field
agreement. **Zero hits.**

It proposed **no detector**, and killed two candidates with corpus measurements rather than leaving
them to be re-derived next week:

1. **`schema.isBasedOn` where `isPartOf` is meant** — *"Corpus-wide: 1,053 `isBasedOn`, 814
   `isPartOf`, 21 both, 332 neither."* Not new: `workflows/README.md` L923 already documents it as a
   pending backfill. **This is item 14's number, refreshed** — 1,053 of 1,867 now carry `isBasedOn`,
   against "1,046 of 1,752 measured 08-24".
2. **`Quotation.creator` in the record but absent from the built page** — one hit
   (`briefest-correspondence`), and it is *"the deliberate disputed-page guard at `tools/template.js`
   L228–L310 working as designed — and here it is **more** correct than the record."* Not a defect.

One near-miss it recorded rather than reported, correctly, under "default to finding nothing":
`briefest-correspondence` renders QI's *"QI **conjectures** that all of these tales… were fictional"*
as *"Quote Investigator **concludes**…"*. The source does not contradict the paraphrase.

**No new proposal comes out of this pass.** That is the right outcome and it is what a clean audit
looks like.

### 5.3 The report window, 38th day — third consecutive whole wave lost

Yesterday's report merged at 12:10Z. Between then and 00:00Z today, **five PRs merged**:

| PR | merged | what |
|---|---|---|
| #869 | 09-13 15:41:48Z | **the 09-13 wave — 5 records** |
| #872 | 09-13 15:41:58Z | merge-pass shard |
| #874 | 09-13 20:24:06Z | merge-pass shard |
| #875 | 09-13 20:24:17Z | `reports-close` no-op |
| #876 | 09-13 22:54:36Z | merge-pass shard |

None appears in yesterday's report (they merged after it) and none appears in today's (they fall
outside the UTC calendar day). **Three consecutive days have now each lost an entire 5-record wave's
shipping row** — 09-11, 09-12 and 09-13. The symptom is visible in § 3: today's merged table contains
no content PR, yet the corpus grew by exactly 5.

The tool does catch the *census* case across the boundary — its `YESTERDAY` section correctly credits
the 09-13 `reports-close` run. It is only the **shipping row** that is lost.

### 5.4 Item 73 — day 6, re-confirmed this morning

`data/routine-log/` on `main` carries nine shards dated 2026-09-07 — `daily-merge` ×4,
`weekly-discovery`, `daily-reports`, `daily-review`, `daily-report`, `reports-close` — and **no
`daily-wave`**. Read from `main`, that wave still did not run. `origin/wave-d20260907` still resolves
and the file is still one `git show` away. It stays recoverable only until that branch is pruned.

### 5.5 Item 12 — the gap is now 50, and the reflog dates the stale ref

Preflight failed on `local main sane` + the consequent `git on main`, `tree clean` passing — exception
**(a)**, and I ran the single command it printed (`git checkout -B main origin/main`), then re-ran:
10/10 green. Nothing was discarded.

The measurement, from the reflog before the reset:

| time | routine | local `main` behind |
|---|---|---|
| 08:10Z | `daily-reports` (#880) | exception (a), gap not stated |
| 09:04Z | `daily-review` (#881) | exception (a), local `main` at `786529ac4` |
| 12:17Z | this report | `786529ac4` — **50 commits** |

New detail worth having: the container's reflog contains **only two prior entries, both dated
2026-09-09**, and the stale ref `786529ac4` is the same commit `daily-review` named this morning. So
this is not a fresh clone drifting — the container comes up carrying a **09-09 snapshot** of `main`
while HEAD sits at the current tip. That narrows the open question in the doc from "something leaves
the container detached" to "the image or cache layer holding `.git` is five days stale".

Sequence so far: 28–31 (08-16) → 36 → 39 → 49 → **50**.

### 5.6 Carried items re-confirmed on `main` this morning

- **Item 18 fired again:** `daily-report.js` credits `daily-merge` with "4 record(s)" from two shards
  carrying `processed: 1` and `processed: 3` — those are merged PRs, not records.
- **Item 20 recurred:** #881 had to override both harness defaults again — *"the doc requires
  `review/<YYYY-MM-DD>` because `tools/merge-gate.js` allowlists by branch prefix and fails closed"*
  and *"the session default is to open PRs as drafts; the doc forbids it."* This report overrode both
  as well. #880 reports no conflict, so it is the review/wave docs specifically.
- **Item 65 did NOT recur — first clean day.** Both merge notes lead with the verdict
  (`merged #877`, `merged #878 #880 #881 · re-poll…`) before the re-poll chatter. One day, not a fix.
- **Item 63 (out-of-band merges) — clean day, fifth consecutive.** All four merges went through the
  07:00 pass.

---

## 6. Needs a decision

**One new item (82), and it outranks everything else because it is the only one currently stopping
content from shipping. Nothing is struck today.**

82. **Re-enable the `daily-wave` Routine, or tell me it is paused on purpose.** *(New.)* Trigger
    `trig_01HKDxYQmKcshaCLZVfidmTa` is `enabled: false` with both reason fields empty (= user-paused),
    so no wave ran today and none will until it is switched back on. Doing it is: flip it back on in
    the Routines list — or say "paused deliberately" and it drops out of the fault list.

73. **Write the 2026-09-07 `daily-wave` shard to `main`.** *(6th day — carried unchanged; `main` still
    says that wave did not run, and it is still the only item with a deadline.)* Doing it is:
    `git show origin/wave-d20260907:data/routine-log/2026-09-07T07-44-35-244Z-daily-wave.jsonl`
    onto a fresh branch, commit, PR — I re-confirmed this morning the branch and file are both intact.

4.  **Fix the report window.** *(38th day — third consecutive day it lost a whole 5-record wave; the
    09-11, 09-12 and 09-13 waves will never appear in any report's shipping row.)* Doing it is: change
    `daily-report.js:30`/`:263` to window from the previous report's timestamp instead of the UTC
    calendar day, on its own branch.

43. **Flip GoatCounter → Settings → "Dashboard viewable by" → Anyone.** *(18th day.)* The anonymous
    endpoint returns 403, so this report has carried no traffic figure for two and a half weeks. One
    setting in the GoatCounter UI; no data is lost, only unreadable.

12. **Root-cause the stale local `main`, or ratify the exceptions in `preflight.js`.** *(31st day — gap
    now 50, and today's reflog dates the stale ref to 2026-09-09, five days back.)* Doing it is: find
    what ships a 09-09 `.git` into a fresh container, or write both exceptions into `preflight.js` so
    every routine stops spending its first minutes on the same recovery.

42. **Rebuild and merge #670, or close it.** *(19th day; `DIRTY`, open since 08-27 and untouched
    since.)* Closing it loses five records that exist nowhere else **and** orphans its 08-27 wave
    shard. Doing it is: merge `main` in, take `main`'s side on generated files, `node tools/build.js`,
    `node tools/scan.js`, push — or close it and accept both losses knowingly.

80. **Detect the used-but-didn't-coin shape in `prep-wave.js` and stamp the four levers, instead of
    relying on the audit to catch it.** *(2nd day.)* Doing it is: one rule in `prep-wave.js` that fires
    when a record's prose concedes use while denying coinage, setting
    `claimVerb`/`verdictLead`/`pkitWarn` centrally. **Distinct from 76.**

81. **Rule on archived-vs-canonical citation when a host is blocked by egress.** *(2nd day — the 09-13
    wave explicitly asked for it.)* Doing it is: one line in the wave doc — cite what was fetched, or
    cite canonical and record the archived URL as the verification trail — plus a decision on whether
    to backfill.

79. **Give `mis-kind.js` patterns for `Unconfirmed` and `Earliest located`.** *(3rd day.)* Both fall
    through `kindForTag` to `''`, which `template.js` renders as the burgundy ✕ meaning *refuted*.

77. **Add `meta.description` and `meta.ogDescription` to `PLAIN_TEXT_FIELDS`
    (`tools/html-safety.js:107`).** *(4th day.)* Two strings in that array, then a separate corpus-wide
    sweep for pages already shipping literal `&lt;em&gt;` in their search snippet.

76. **Widen `rightPersonWrongWords()` or split the magnet from the false-credit claim in
    `prep-wave.js`.** *(5th day.)* One central edit on its own branch.

14. **Decide the `isBasedOn → isPartOf` bulk migration.** *(22nd day — number refreshed by #879 today:
    1,053 `isBasedOn` / 814 `isPartOf` / 21 both / 332 neither, against "1,046 of 1,752" on 08-24.)*
    Doing it is: rule on the distinction, then one scripted backfill.

17/58. **Fail the build when `Quotation.creator` is suppressed, and normalise `template.js`'s hero
    match.** *(25th day.)* Make `creatorOk` compare on an accent-folded name, and assert that a record
    with an `answer.authorName` cannot emit a page without a `creator`.

72. **`tools/review.js:267` reads rights from `meta.rights`/`rights`, but records store it at
    `source.rights`, so every audit has seen `null`.** *(8th day.)* Change that one fallback chain, then
    decide separately whether to re-audit the rights claims never checked.

18. **Fix `daily-report.js:194`'s `work` figure.** *(22nd day — today it again credited `daily-merge`
    with "4 record(s)" for two shards carrying `processed: 1` and `processed: 3`.)* One field rename or
    a per-routine label.

36. **Make the wave log a cost figure.** *(30th day — § 7.)* One field in the wave's routine-log shard.

5.  **Declare which carried items you are not going to do.** *(22nd day.)* The table below is 41 rows.
    Reply with the numbers to strike and tomorrow's report carries only what survives.

### Resolved today

- **#879 needs nothing.** `verify` **green** on the rebuilt head `119aa2d1` at 11:57:20Z; the merge
  pass takes it next pass, by its own note.
- **#882 needs nothing.** The merge pass's own shard, `CLEAN`, `verify` green, opened by `merge.yml`.
- **Item 65 was clean for the first time** — both merge notes put the verdict before the re-poll
  chatter. Stays listed; one day is not a fix.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 2 | Stop the daily wave and the backlog waves colliding over the queue | 21st |
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" | 21st |
| 6 | Make `prep-wave.js:206` stamp `creditedTo` when `confidence === 'disputed'` regardless of name equality — **75/76/80 are the same guard's other failures** | 28th |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 21st |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 27th, clock running |
| 9 | Fix the middle-initial hub fork at the generator | 20th |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row — **79 is the adjacent case** | 20th |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 20th |
| 13 | Extend the egress allowlist — `barrypopik.com`, `annefrank.org`, Google Books, HathiTrust; **81 is the policy question it forces** | 34th |
| 15 | Decide what to do about Google Books' quota | 23rd |
| 16 | Stop one source's outage hard-stopping the review pass | 26th |
| 19 | Decide whether `perf-check` belongs in the routine census | 24th |
| 20 | Settle the branch-name / draft conflict between the harness and the routine docs — **#881 and this report both had to override it again** | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict | 23rd |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason | 24th |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — cannot see unmerged PRs | 28th |
| 24 | Work the disputed records with no `creditedTo` | 34th |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) | 27th |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 27th |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 27th |
| 30/62 | Rule on "Verbatim:" and "rights held by X" corpus-wide | 27th / 13th |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — 58 is queued behind it | 29th |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate | 30th |
| 33 | Decide the house convention for characters with no HTML entity | 26th |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 28th |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — folded into 17/58 | 28th |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 31st |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 31st |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 27th |
| 40 | Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`) | 19th |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 19th |
| 44 | Teach `merge-gate.js` to look at what actually changed — #670 is its case | 18th |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 18th |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — 54 is the ruling this needs | 18th |
| 47 | Decide whether QI's Title Case should be normalised centrally | 18th |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 18th |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 18th |
| 51 | Make the merge pass's rebuild step fetch before it merges | 17th |
| 54 | Rule on Quote Investigator *headlines* reaching `displayQuote` — the true count is UNKNOWN; do not re-derive | 16th |
| 56 | Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog`, which is live | 15th |
| 57 | Give the orphaned `taxes-…` candidate a way back into the queue | 15th |
| 59 | Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it — 74 is waiting on this | 15th |
| 61 | Fix the `mis-kind.js` comma gap; also add a `/fake/` rule to `REFUTATION_TAG` | 15th |
| 63 | Out-of-band merges — **clean day, fifth consecutive**; stays listed | not live today |
| 64 | Make the merge pass merge content PRs before routine-log PRs | not live today |
| 65 | Put the verdict before the re-poll chatter in the merge note — **clean for the first time today** | 12th |
| 66 | Fix `template.js:848`'s unconditional period on `disambiguatingDescription` | 12th |
| 67 | Rule on the Parker `displayQuote` repair, which shipped 09-05 | 10th |
| 68 | Fix nested quotes in `claimReviewed` | 10th |
| 69 | Rule on whether a Wikiquote *sourced-section* anchor is enough for `verified` on an in-copyright work | 10th |
| 70/71 | The 09-06 quota collision; `daily-wave` / `daily-review` still fail silently | carried |
| 74 | Decide whether `DOSSIER_SCHEMA` gets the hand-set override fields — waiting on 59 | 7th |
| 75 | Rule on the harvester's magnet-as-claimant defect | 6th |
| — | `loc.gov` human-facing page URLs 403 to non-browser clients — 81 is the general version | 3rd |
| — | `looksLikePerson` still accepts an anonymity note ("Nobody — it has no traceable author") | 5th |
| — | Add the token-subset test's false-alarm mode (`Edmund Kean → Edmund Gwenn`) to the #801 note | 7th |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Make the no-op shards carry a `note` — the evidence is in the PR bodies but not in what the census reads | 3rd |

**Struck previously:** 25, 28 (superseded by 59), 50, 52, 53, 55, 60, 78. **Nothing is struck today.**

---

## 7. Cost

**No run reported a cost figure.** Today's three shards carry `processed`, `scanned` and `flagged` —
no cost field anywhere. Item **36**, 30th day.

The one run that described its spend did so in prose, not in a field. #879 (`weekly-discovery`):

> Effort profile this run: **~25 source re-fetches** (the previous measured run needed none), ~300 KB
> of record JSON read, plus two mechanical passes over all 20 built pages. Fetching was curl + grep
> for specific strings rather than loading pages into context, which is what kept a fetch-heavy pass
> affordable — the fetch count went up an order of magnitude from the 2026-07-29 baseline without a
> proportionate cost increase.

That is the useful shape of a cost note, and it is exactly what item 36 would make machine-readable.
Today's spend was otherwise unusually low for the obvious reason: the wave, the most expensive routine
of the day, did not run.
