# Daily report — 2026-08-17

**All six scheduled routines ran, nothing is broken, and two real JSON-LD defects were found and
fixed. One thing genuinely needs you: a detector is proposed and waiting on a yes/no. The
stale-local-`main` condition recurred on all four passes at a bigger gap than ever — the remedy you
shipped on 2026-08-16 handled every one of them cleanly, so this is now a root-cause question, not
an outage.**

The report tool reported **no tool failures**, so this is a complete picture, not a partial one.

---

## 1. What ran

| Slot | Routine | Result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run — PR, **3 records** |
| 04:00 | `daily-reports` | ✓ 1 run — no-op |
| 05:00 | `daily-review` | ✓ 1 run — no-op (0 flagged) |
| 07:00 | `daily-merge` | ✓ 5 runs — merged, **9 records** |
| 12:00 | `reports-close` | · not yet due (17:00 UTC) — ran yesterday, no-op |
| Mon 02:00 | `weekly-discovery` | ✓ 1 run — PR, **20 records** |

Nothing missing. `reports-close` has not run because this report runs before its slot; its
2026-08-16 run was a no-op.

## 2. What shipped

- **9 records merged** across 9 PRs (#519–#527).
- **3 new records** from wave `d20260817` (#524) — two Hawking misattributions reassigned, one
  Groucho attribution.
- **20 records audited** by the weekly discovery sweep (#522): 18 PASS, **2 fixed**.
- **Corpus: 1,577 records · 0 flagged · CI green on `main` (`a9a0d212c`) · working tree clean ·
  0 deferred items.**
- One PR open: **#528** `merge/2026-08-17`, CLEAN, 0 days old. **That is the 07:00 pass's business,
  not this one's** — I merged only my own report PR.

**Not a discrepancy, recorded so tomorrow doesn't flag it:** the review pass (#526) reports a
1,574-record corpus and the tool reports 1,577. The review ran at 09:09 UTC; the wave's three
records merged at 12:42 UTC. Both numbers are right for their moment.

---

## 3. Findings

### F1 — The stale local `main` recurred on every pass, at a larger gap. The fix works; the cause is still unknown. *(3rd day)*

Your 2026-08-16 correction (#515, `fix(preflight): the git-on-main remedy is checkout AND
fast-forward`) shipped and **worked on all four passes today**. Every one hit the same single failing
check, applied documented exception (b), and came back green. But the gap grew:

| Pass | Behind `origin/main` | Start state |
|---|---|---|
| `weekly-discovery` | **41 commits** | (not stated) |
| `daily-wave` | **42 commits** | (not stated) |
| `daily-review` | **42 commits** | **named branch** `claude/wizardly-fermat-3ikkr2` |
| this report | **48 commits** | **detached HEAD** |

Against the 28/31 recorded on 2026-08-16. Discovery's own words:

> The underlying condition — something leaves the container with a stale local `main` — **recurred
> again today**, one day after the 2026-08-16 corrections, and at a larger gap (41 commits, vs the
> 28/31 recorded then). The exception handled it cleanly, but it has not gone away and still has no
> root cause.

**New evidence, and it contradicts the doc.** `DAILY-REPORT.md` states the start state
"is **not** 'off the branch': it is **detached** at the current tip with a stale local `main` ref
behind it." The review pass found otherwise:

> the start state here was not detached-HEAD but a *named session branch*
> (`claude/wizardly-fermat-3ikkr2`) with a local `main` ref 42 behind. […] the shape of the start
> state differs from what the doc's exception (b) narrative describes.

My own run today **was** detached. So both shapes reach the same failing check, and the doc's
narrative currently describes only one of them. That is a factual gap in the doc, and it also widens
the search for the root cause: whatever leaves the container stale is not specific to detaching.

### F2 — Preflight's reachability check does not cover the archive host the corpus actually cites

`web.archive.org` was unreachable through the egress proxy for the whole discovery run (curl HTTP
000; `WebFetch` refuses the host outright). Discovery's words:

> Preflight's `reach archive.org` check passes because it tests the apex, not the wayback host — so
> **preflight does not currently cover the host this corpus actually cites most for archived primary
> sources**. Corroboration came from the record's other cited source instead. Flagging, not fixing:
> `tools/` is out of scope for this pass.

A green preflight therefore does not mean archived sources are reachable. This did not corrupt
anything today — the fix was corroborated another way — but it means the guard is not guarding.

### F3 — A detector is proposed, hand-checked, and waiting on you

`magnet-quoted-it-rated-as-never-said` — **10 hits, 0.6%**, gate verdict BACKFILL, **all 10
hand-checked as genuine**. The class: the page says the credited person *did utter* the line but did
not originate it, yet with no `claimVerb`/`claimReviewed` the ClaimReview machine-readably denies an
utterance the same page documents. It was deliberately refined so it only fires when the quoter is
the magnet — an unrefined draft hit 17 at ~55% precision.

Not admitted, by design: "admission is deliberately serial." Affected records include
`good-artists-copy-great-artists-steal`, `the-hottest-places-in-hell-are-reserved-for-`,
`what-is-important-is-seldom-urgent-and-what-` and seven others.

Discovery also **measured and rejected** a second candidate, `creator-credits-disputed-wording`
(137 hits, 8.7%), recording why so nobody re-derives it: **113 of 137 are dormant** because
`template.js`'s `creatorOk` guard already suppresses the creator, and the rest are the legitimate
right-person-wrong-words class. Its closing instruction is worth keeping:

> **Anyone re-proposing this shape must test the emitted page, not the record field.**

### F4 — Two real JSON-LD defects found and fixed (the system working)

Both were prose-vs-JSON-LD disagreements, both verified against a source the record already cited:

1. **`if-you-cant-explain-something-to-a-first-year-student-then`** — `quotationText` carried the
   circulating paraphrase, so the JSON-LD asserted Feynman authored a sentence the page states four
   times he never said ("Feynman is not recorded saying or writing this sentence anywhere"). Set to
   the documented wording; the paraphrase stays in `claimQuoteText`.
2. **`eternity-is-a-very-long-time-especially-towards-the-end`** — the ClaimReview rated
   `Stephen Hawking said: "…"` as **False**, denying an utterance the record's own `docMeta`
   documents (he said it on CNN *Talk Asia*, 4 July 2006, crediting Woody Allen in the same breath).
   Reviewed claim changed from utterance to **origination**; the 1/False correctly stands.

### F5 — The wave built 3, not 5, and said why *(2nd day of under-quota waves)*

> **Wave sized to 3, not 5.** `DAILY-WAVE.md` says that if the budget does not fit, build fewer
> records and never fewer stages.

This is the doc's own rule, followed, and every stage ran. The two dropped candidates (both Bill
Gates lines) were released back to the queue **before any research** via `unselect` + re-`select 3` —
"They are queued again, not skipped." Still no run reports a token figure (see §5).

### F6 — Three things the wave could not establish, and correctly did not assert

- **The Myhrvold attribution rests on Wikiquote alone.** The Foreword to *The Illustrated A Brief
  History of Time* could not be read: the Internet Archive copy is access-restricted, search-inside
  is proxy-blocked, and the Google Books API was over quota. **The page says so on its face.**
  Search-engine summaries did assert the Foreword text; the wave explicitly did not treat those as
  verification and did not cite them.
- **The Rosten page-17 match is a snippet-view confirmation, not a reading.**
- **The PBS page is proxy-blocked**, so the programme is named on Wikiquote's authority but not
  linked.

A dating conflict was resolved explicitly (QI's 1994 over Wikiquote's 1995 for *Cleopatra's Nose*).
This is the egress-allowlist item, now in its 6th day, costing real verification depth.

### F7 — A near-duplicate is sitting in the queue, flagged before it costs anything

> the Gates line "I see little commercial potential for the internet…" overlaps the existing
> `the-internet-is-just-a-passing-fad` page. It was one of the two released candidates, so the
> overlap is unresolved and still queued — worth a look before a future wave draws it.

### F8 — `generate.js:168`'s themes comment is still wrong, and today proved it *(3rd day)*

The comment says themes now come from the researching agent, "which removes the separate tag-themes
workflow stage." Today's wave lists its pipeline as `select → generate → prep → ingest → build →
audit → fix → **theme-tag** → ship`. The stage ran. `DOSSIER_SCHEMA` still carries no `themes` field
(it is at `SCHEMA_BUDGET` with zero headroom), so the agent cannot emit them. The comment describes a
change that was reverted for budget reasons and never got its comment back.

### F9 — Two stale numbers, both harmless today

- `workflows/README.md:8` says **"Corpus: 1506"** against an actual **1,577**. It has drifted every
  wave since it was written.
- `sourcesVerified` now persists (your #492 fix) but only **10 of 1,577** records carry it — every
  record built since 2026-08-14. That is expected growth, not a defect; noting it so the low number
  isn't mistaken for a regression.

### F10 — Discovery asked for a doc wording change it would not make itself

> **Recommended wording for the doc:** keep the ~21K/record figure, note it is now backed by one
> instrumented run plus one uninstrumented run of matching shape, and add that the digest-first
> method is what keeps a finding-bearing pass near the no-finding cost.

---

## 4. Needs a decision

New first, then carry-overs oldest-cost-first. Day counts are how long each has been on this list.

1. **Accept or decline the proposed detector `magnet-quoted-it-rated-as-never-said`** — 10 hits,
   0.6%, all hand-checked, catching ClaimReviews that deny utterances the same page documents.
   Accepting is `node tools/propose-detector.js --accept`, plus the `prep-wave.js` change the gate's
   remedy also asks for and a sweep of the 10 named records (they were outside this run's sample).
   *(new)*

2. **Root-cause the stale local `main`, or accept it and correct the doc** — it recurred on all four
   passes today at 41–48 commits behind, up from 28–31, and today's evidence shows it happens from a
   *named session branch* as well as detached HEAD, which `DAILY-REPORT.md`'s exception (b) narrative
   says it does not. Either investigate what leaves the container stale, or spend five minutes
   updating the narrative in the six `DAILY-*.md` docs so it stops asserting a start state that is
   only half the story. *(3rd day; your #515 remedy is working, so this is no longer urgent)*

3. **Make preflight check `web.archive.org`, not just the `archive.org` apex** — the wayback host was
   unreachable all run while preflight stayed green, so the guard passes on the one host the corpus
   cites most for archived primary sources. One line in `tools/preflight.js`, and worth pairing with
   decision 7. *(new)*

4. **Decide whether under-quota waves are fine, or make the wave log a cost figure** — 3 records
   today against the doc's `select 5`, and still no run anywhere reporting tokens, so "the budget did
   not fit" remains an assertion nobody can check. Adding a cost field to the wave's log shard is
   small and makes it measurable. *(2nd day)*

5. **Fix the `themes` comment at `workflows/generate.js:168`** — it says the theme-tag stage was
   removed; today's wave ran that stage, and the schema has no `themes` field to make the comment
   true. One comment edit; the risk of leaving it is a future wave trusting it and shipping untagged
   records. *(3rd day — now with live evidence)*

6. **Extend the egress allowlist** — `perseus.tufts.edu`, `web.archive.org`, the PBS host and Google
   Books quota each blocked verification again today, and the Myhrvold attribution now ships resting
   on a single source because of it. *(6th day — the first day it visibly narrowed a record's
   evidence base)*

7. **Update `workflows/README.md:8`'s corpus line, or generate it from the manifest** — it says 1506
   against 1,577. Generating it stops the drift permanently. *(3rd day)*

8. **Make an unrecognised tag in `tools/mis-kind.js` render the neutral marker instead of the ✕** —
   it still accuses a source of being refuted whenever it meets a tag it does not know. *(5th day)*

9. **Add `verdictNote` to `DOSSIER_SCHEMA` so a wave can emit it** — still absent from `generate.js`
   and `prep-wave.js`; 67 records carry one, all hand-set. Check `SCHEMA_BUDGET` headroom first —
   #492 confirms there is currently **zero**, so this likely needs bytes freed elsewhere before it is
   possible at all. *(5th day)*

10. **Decide the `Unknown`-creator `sameAs` convention** — 81 records with an `Unknown` creator carry
    a `sameAs`; the subset pointing at a named person's page (yesterday counted 26, a strict pattern
    match today finds 9) reads as equating the unnamed creator with that person. **Define the
    matching rule as part of the decision** — the count moves depending on it, which is itself a
    reason it keeps getting deferred. *(3rd day)*

11. **Work the 68 disputed records with no `creditedTo`** — unchanged from yesterday; decision 9
    would give some of them the `verdictNote` they need. *(6th day)*

12. **Make `tools/daily-report.js` window report-to-report instead of calendar-day**
    (`tools/daily-report.js:30`) — still the only structural reason a real merge can vanish from
    every report. *(10th day)*

13. **Look at the Gates internet near-duplicate before a future wave draws it** — "I see little
    commercial potential for the internet…" overlaps the existing `the-internet-is-just-a-passing-fad`
    page and is back in the queue unresolved. *(new)*

**Closable:** yesterday's item 5 (the 2026-08-15 `daily-review` miss) — review has now run normally
two days running. Call it a one-off and drop it.

---

## 5. Cost

**No run reported a token count today**, and the discovery pass was explicit about refusing to invent
one:

> **I am not able to report a token count for this run**, so I have deliberately not invented one.
> The doc's `~21K tokens a record` came from instrumentation this session doesn't expose, and a
> made-up figure would corrupt the very number the doc is trying to establish.

What it could measure directly: **20 records audited, ~55 tool calls, ~30 minutes wall clock, 4
outbound source fetches** (2 failed on the unreachable archive host), 2 detector measurements against
the full corpus, 3 full record reads, **0 full built-page reads** — the JSON-LD was extracted
programmatically instead. It notes this run differed from the 2026-07-29 baseline in one way worth
recording: that run "needed no source re-fetch to settle any of the 20"; **this one did**, twice, and
stayed cheap only because the sweep went digest-first.

The wave reported no cost figure at all. That is decision 4.
