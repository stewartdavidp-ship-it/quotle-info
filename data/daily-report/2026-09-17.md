# Daily report — 2026-09-17

## 1. Verdict

**Every routine due today ran, the wave built five records and audited them into a green PR, and
nothing is broken. The one thing worth acting on is 21 days old and takes one click.**

`daily-wave`, `daily-reports`, `daily-review` and `daily-merge` all fired. The wave produced #907 —
five Lewis Carroll misattributions, `CLEAN`, `verify` green on its rebuilt head — which merges at the
next pass. `main` is green at `61bba26c9`, **2,225 records, 0 flagged, tree clean, 0 deferred**.

Three things to read before anything else.

1. **Traffic is unavailable for the twenty-first consecutive day, and the cause is one setting.**
   `tools/traffic.js` prints the reason the JSON hides: *"403 from GoatCounter — the dashboard is no
   longer public."* No data is lost; this report simply cannot read it. Item **43**. § 4.
2. **`WebSearch` was unavailable for the entire wave session — the first time any daily report has
   recorded that.** The wave fell back to Quote Investigator's own on-site search to find its
   sources. Combined with three blocked hosts, this is the wave working with one hand tied.
   New item **84**. § 5.2.
3. **#897 — the 09-16 wave, five records — merged at `20:55:44Z` last night and appears in NO
   report's merged list.** Yesterday's report closed at 12:18Z; today's window starts at UTC
   midnight. The corpus going 2,220 → 2,225 is the only trace it left. This is item **4**, 41st day,
   and it has now lost a *content* PR rather than bookkeeping. § 3.

---

## 2. What ran

| slot | routine | result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run: `pr` — **5 records**, #907 (shard `07:31:17Z`, on the PR branch, not yet on `main`) |
| 04:00 | `daily-reports` | ✓ 1 run: `no-op` — 0 processed (shard `08:09:52Z`) |
| 05:00 | `daily-review` | ✓ 1 run: `no-op` — scanned 2,225, flagged 0 (shard `09:04:12Z`) |
| 07:00 | `daily-merge` | ✓ 2 runs: `merged`, `merged` — `03:40Z` and `11:03Z`, 4 PRs |
| 12:00 | `reports-close` | · not yet — due 17:00 UTC (yesterday's ran `no-op`) |
| Mon 02:00 | `weekly-discovery` | · not scheduled today (Thursday; next 09-21) |

**Nothing is missing from the roster.** Every slot due today produced a shard — the second
consecutive day that has been true.

`daily-report.js` reported **1 tool-call failure** — `traffic: fetch failed`. **§ 4 is the only
incomplete part of this report; §§ 2, 3, 5 are complete.**

### Preflight — exception (b), third container in a row

This pass failed preflight 1/10 on `git on main: on HEAD`, with `local main sane`, `tree clean` and
`current with origin/main` all PASSING — the documented 2026-08-14 exception (b). Both printed
commands were run; the local `main` ref was **13 commits behind** origin/main, so the checkout alone
would indeed have moved the tree backward exactly as the 2026-08-16 correction warns. Preflight
re-ran green at 10/10 before any work started.

That is now **three routines on three containers in one morning** hitting the same shape: #908
reported 10 commits behind, #909 reported 10 commits behind (from a `claude/…` session branch rather
than a detached HEAD), this pass 13. Item **12** is the fix and today is a third argument for
remedy (a).

Quality note, unchanged for a **sixth** day: today's `daily-reports` and `daily-review` shards again
carry **no `note` field** —

```
{"date":"2026-09-17","routine":"daily-reports","outcome":"no-op","processed":0}
{"date":"2026-09-17","routine":"daily-review","outcome":"no-op","scanned":2225,"flagged":0}
```

— while both PR bodies describe their preflight recovery in detail. The wave's shard, by contrast,
carries a full `note` and is the reason § 5.2 exists. The evidence exists; for two routines it does
not reach the shard, which is what the census reads.

---

## 3. What shipped

**Merged today, per the tool (4):** #909, #908, #906, #905 — all routine-log bookkeeping. **No
content PR is in today's merged list.**

**But five records did reach `main`.** The corpus stands at **2,225**, up from 2,220 yesterday,
because #897 (`wave-d20260916` — 1 Frost verified, 4 Carroll disputed) merged at
**`2026-09-16T20:55:44Z`**. Yesterday's report was written at 12:18Z and said of it *"it merges at
the next pass"*; today's window begins at UTC midnight. **#897 therefore falls in the gap between
two reports and is recorded in neither's merged list.** That is item **4** producing its first
content-PR loss rather than a bookkeeping one.

**Built but not yet on `main`:** #907, `wave-d20260917`, 5 records, 40 files. `CLEAN`, not draft,
`verify` run 1841 **success** on the rebuilt head `f70a4279` at `11:05:08Z`. The 11:03Z merge pass
rebuilt and pushed it and logged *"#907 rebuilt and pushed — merges next pass, once verify is green
on the new head"* — verify is now green on that head, so it needs nothing.

> Note: `pull_request_read/get_status` returns `total_count: 0` for #907 because `verify` reports as
> a **check run**, not a commit status. The workflow-run list is the reliable read. Not a defect,
> but it will mislead anyone checking the PR's health the quick way.

**Open (3):**

| PR | branch | state | age | note |
|---|---|---|---|---|
| #910 | `merge/2026-09-17` | CLEAN | 0d | the merge pass's own shard, opened by `merge.yml` — needs nothing |
| #907 | `wave-d20260917` | CLEAN | 0d | today's wave, green — needs nothing |
| #670 | `wave-d20260827` | **DIRTY** | **21d** | untouched since 08-27; item **42** |

**Reader reports:** 0 pending, 0 closed. Ladder position `pr`, 0 deferred, 0 unjudged runs.

---

## 4. Traffic

**Traffic: unavailable — twenty-first consecutive day.** `traffic` is `null` and `tool_failures`
carries `traffic: fetch failed`. This is an **analytics outage, not a quiet day**, and it is not a
zero.

`node tools/traffic.js` run directly gives the error the JSON does not:

```
traffic.js: 403 from GoatCounter — the dashboard is no longer public.
Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.
```

So there is no figure to report as an upper bound, spam-adjusted or otherwise, and no referrer
breakdown. **Twenty-one days of traffic are not lost** — GoatCounter is still collecting; this report
cannot read it. Item **43**, one setting in the GoatCounter UI.

---

## 5. Findings

### 5.1 The wave's audit — 3 PASS / 2 FAIL, 11 issues, **0 blockers**

From the shard: *"audit: 3 PASS / 2 FAIL, 11 issues, 0 blockers; 4 highs all skeptic-confirmed, 0
refuted."*

No blockers, which is the first clean-of-blockers wave since 09-14. The two findings that mattered
are both the audit catching a page asserting more than its own source supports:

1. **A first-credit date the page's own linked source contradicted.** In the wave's words: *"The
   record said Carroll 'was not credited until April 2012'. The QI article linked three times on
   that page cites a **January 2012** tweet reading `(Carroll – Alice in Wonderland)`. … The page
   also contradicted itself: its `detailsBody` already listed the January 2012 instance. Fixed in
   all three places."*

2. **A merchandising claim nobody verified.** *"Two sections asserted the line is 'routinely sold
   over Tenniel's original Wonderland engravings'. Very likely true — and sourced to nothing. The
   harvest note cited `alice-in-wonderland.net`, which is blocked by this environment's egress proxy
   and was never read. Both instances rewritten to keep only the supported rights point."*

**12 fixes applied. One remedy refused,** and the refusal is correct: the audit wanted page 4's
JSON-LD `Quotation.text` reduced to one speaker, and the wave declined because *"the page's entire
finding **is** the speaker split, and collapsing `text` to a single voice would reintroduce in
machine-readable form the exact error the page exists to correct."*

**On the 0% skeptic refutation rate.** The runbook expects 15–20%; yesterday's wave refuted 14%.
Today's refuted none of four, and the wave gives its reason — *"lower than the 15–20% the runbook
expects, because only claims already traced to a source were filed in the first place."* That is a
plausible account and the four findings it kept are each substantiated in the PR body. Recording it
as a number to watch across waves, not as a defect. No proposal.

### 5.2 `WebSearch` was unavailable for the whole wave session — new, never previously reported

From the PR body: *"`books.google.com`, `theguardian.com` and `alice-in-wonderland.net` are all
blocked by this environment's egress proxy (403 at the CONNECT tunnel, via both curl and WebFetch).
**`WebSearch` was unavailable for the whole session** — Quote Investigator's own on-site search was
used to locate its articles instead."*

**No daily report in the archive mentions `WebSearch` at all**, so this is the first occurrence on
record. It matters differently from the egress allowlist (item **13**): a blocked host costs one
citation, but no search costs the wave its ability to *find* sources it does not already know about,
which is the whole discovery half of the pipeline. The wave still shipped five records because
Quote Investigator has a usable on-site search; a seam without one would have come up empty.

Concretely it cost three things this wave, all correctly declared on the pages rather than papered
over:

- the **1916 De Casseres scan was never read** — that record *"adopts QI's transcription and citation
  and adds nothing to them — no edition detail, no page number of our own — and says so on the page"*;
- whether Linda Woolverton **coined** the "bonkers" line is unestablished — *"we found no earlier
  appearance, but absence of a find is not proof of coinage and the page does not claim one"*;
- the Guardian's 2021 Carroll-misquotes piece is cited *"as QI's report of it, never as a link we
  opened."*

New item **84**.

### 5.3 Generator-level defect the wave reported and deliberately did not fix

*"Page 5's JSON-LD renders two writers as one `Person` node (`"Vladimir Cvetko & David H. Goodman"`).
`DOSSIER_SCHEMA` offers a single `creatorName` string and `toRecord` maps it to one Person; seven
existing corpus records carry the same shape. Not fixable inside a content wave — `merge-gate.js`
treats any `tools/` or `workflows/` path as a scope escape."*

Refusing to widen a content PR into `tools/` is exactly right, and the scope gate was verified clean.

**The defect is real; the count is wrong.** Measured against `main` just now, **nine** records carry
a conjoined `schema.creator.name`, not seven — and #907 would make ten:

```
Aileen Miga & Janice Hughes                                   be-careful-what-you-water-your-dreams-with…
Christopher Markus, Stephen McFeely and Michael Petroni        hardships-often-prepare-ordinary-people…
Julius J. Epstein, Philip G. Epstein and Howard Koch           if-that-plane-leaves-the-ground…
Noel Langley, Florence Ryerson and Edgar Allan Woolf           pay-no-attention-to-the-man-behind-the-curtain
Julius J. Epstein, Philip G. Epstein and Howard Koch           play-it-again-sam
Mark Protosevich and Akiva Goldsman                            the-people-who-are-trying-to-make-this-world-worse…
Noel Langley, Florence Ryerson and Edgar Allan Woolf           toto-i-dont-think-were-in-kansas-anymore
Billy Wilder and I. A. L. Diamond                              well-nobodys-perfect
Bert Kalmar and Harry Ruby                                     who-are-you-going-to-believe-me-or-your-own-eyes
```

Eight of the nine join with the word "and" rather than `&`, which is the likely reason a `&`-shaped
search found one shape and missed the rest. Every one is a screenplay — this is a screen-quote seam
defect, and the seam is growing. New item **85**.

### 5.4 What the wave verified in-session, and what it declined to claim

Worth recording because it is the expensive, correct shape: eleven Project Gutenberg texts covering
ten of Carroll's English works, **2,411,335 characters**, line breaks flattened; twelve phrases
searched, *"all zero occurrences"*; site-wide Wikiquote `insource:` searches per phrase; all 14 cited
URLs return 200; canonical/`og:url`/JSON-LD `@id`s all carry the trailing slash on all five;
`scan.js` flagged 0; 51 build invariants reconcile; the staleness check prints nothing.

And it declined to claim what it could not show: *"No `public-domain` claimed anywhere in this wave.
Three records assert no rights status at all, because the wording in circulation is anonymous or a
later rewrite and there is no datable work to attach a status to."* Two records are marked
`in-copyright` (Walt Disney Pictures; ABC Studios).

### 5.5 Two findings the wave put on the page rather than in a backlog

Both are the site doing the thing it exists to do, and neither needs a decision:

- *"**Have I gone mad?** is a **two-hander**. Wikiquote's transcription of the 2010 film has the
  Hatter asking the question and **Alice** giving the famous reply … The popular version collapses
  both onto the Mad Hatter, which inverts the meaning — from a madman's boast back into the sane
  character reassuring the frightened one."*
- *"The script reads 'Everyone wants **some** magical solution **for** their problem'; the
  circulating form reads '**a** magical solution **to** their problem, and…'"* — and Wikiquote's own
  Misattributed entry names the right episode while quoting the drifted wording.

---

## 6. Needs a decision

**Two items are added (84, 85). Nothing is resolved today. Nothing here is blocking content from
shipping.** #907 and #910 both need nothing — they are green and merge at the next pass.

43. **Flip GoatCounter → Settings → "Dashboard viewable by" → Anyone.** *(21st day — the single
    highest value-per-effort item on this list.)* Doing it is one dropdown in the GoatCounter UI; no
    data is lost, and it restores the only traffic figure this project trusts.

84. **Decide whether the wave is allowed to run without `WebSearch`, or should stop and say so.**
    *(New — § 5.2.)* Today's wave lost a search tool for its whole session, shipped anyway on Quote
    Investigator's on-site search, and correctly declared the three things it could not establish.
    Doing it is one of: (a) find why `WebSearch` was absent and restore it; or (b) one line in the
    wave doc saying a seam without a usable on-site search is a STOP rather than a thinner record.

4.  **Fix the report window.** *(41st day — and it has now lost a **content** PR: #897, five
    records, merged 20:55Z last night, in no report's merged list.)* Doing it is: change
    `daily-report.js:30`/`:263` to window from the previous report's timestamp instead of the UTC
    calendar day, on its own branch.

42. **Rebuild and merge #670, or close it.** *(22nd day; `DIRTY` and untouched since 08-27.)*
    Closing it loses five records that exist nowhere else **and** orphans its 08-27 wave shard.
    Doing it is: merge `main` in, take `main`'s side on generated files, `node tools/build.js`,
    `node tools/scan.js`, push — or close it and accept both losses knowingly.

73. **Write the 2026-09-07 `daily-wave` shard to `main`.** *(9th day — still the only item with a
    deadline; `origin/wave-d20260907` confirmed still present at `6e6d0d2f9` this morning.)* `main`
    still says that wave did not run. Doing it is:
    `git show origin/wave-d20260907:data/routine-log/2026-09-07T07-44-35-244Z-daily-wave.jsonl` onto
    a fresh branch, commit, PR — **it stays recoverable only until that branch is pruned.**

12. **Pick the fix for the shallow clone.** *(34th day — three containers hit it this morning
    alone, at 10, 10 and 13 commits behind, in two different shapes.)* Doing it is **one** of:
    (a) `git fetch --unshallow` in container setup — **retires both exceptions at once**; or
    (b) make `preflight.js:171` shallow-aware, which is smaller but covers only exception (a)'s
    shape. **Today is a third argument for (a).**

85. **Give `DOSSIER_SCHEMA` a multi-writer creator, or rule that co-written screen quotes name one
    writer.** *(New — § 5.3.)* Nine corpus records already emit two or three screenwriters as a
    single JSON-LD `Person`, and #907 makes ten. Doing it is: let `creatorName` accept an array and
    have `toRecord` emit a `Person` per name, then a scripted backfill of the nine — or decide the
    one-name convention and fix the nine to match.

13. **Extend the egress allowlist.** *(37th day — three more hosts named today:
    `alice-in-wonderland.net`, `theguardian.com`, `books.google.com`.)* Today it cost one record its
    own reading of the 1916 source and one page a merchandising claim that had to be cut. Doing it
    is: add the film-transcript hosts (`imsdb`, `subslikescript`, `scripts.com`,
    `springfieldspringfield`), JSTOR/Crossref, `alice-in-wonderland.net`, `theguardian.com`, and the
    previously requested `barrypopik.com`, `annefrank.org`, Google Books and HathiTrust — or rule
    that the pipeline lives without them and **81** becomes the answer instead.

83. **Refuse `rights` at the generator when the record's own prose dates the headline wording after
    the work the rights are reasoned from.** *(2nd day.)* One rule in `prep-wave.js` clearing
    `rights` (and forcing a prose-only `rightsNote`) when a record's dating fields and its headline
    wording disagree — the one field where being wrong is a legal claim rather than a factual one.

81. **Rule on archived-vs-canonical citation when a host is blocked by egress.** *(5th day — hit
    again today by the Guardian piece, cited as QI's report of it.)* Doing it is: one line in the
    wave doc — cite what was fetched, or cite canonical and record the archived URL as the
    verification trail — plus a decision on whether to backfill.

80. **Detect the used-but-didn't-coin shape in `prep-wave.js` and stamp the four levers.** *(5th
    day.)* One rule setting `claimVerb`/`verdictLead`/`pkitWarn` centrally. **Distinct from 76; 83
    is its rights-field sibling.**

79. **Give `mis-kind.js` patterns for `Unconfirmed` and `Earliest located`.** *(6th day.)* Both fall
    through `kindForTag` to `''`, which `template.js` renders as the burgundy ✕ meaning *refuted*.

77. **Add `meta.description` and `meta.ogDescription` to `PLAIN_TEXT_FIELDS`
    (`tools/html-safety.js:107`).** *(7th day.)* Two strings in that array, then a separate
    corpus-wide sweep for pages already shipping literal `&lt;em&gt;` in their search snippet.

76. **Widen `rightPersonWrongWords()` or split the magnet from the false-credit claim in
    `prep-wave.js`.** *(8th day.)* One central edit on its own branch.

72. **`tools/review.js:267` reads rights from `meta.rights`/`rights`, but records store it at
    `source.rights`, so every audit has seen `null`.** *(11th day.)* Change that fallback chain,
    then decide separately whether to re-audit the rights claims never checked.

14. **Decide the `isBasedOn → isPartOf` bulk migration.** *(25th day — 1,053 / 814 / 21 both / 332
    neither, as measured by #879 on 09-14.)* Rule on the distinction, then one scripted backfill.

17/58. **Fail the build when `Quotation.creator` is suppressed, and normalise `template.js`'s hero
    match.** *(28th day.)* Make `creatorOk` compare on an accent-folded name, and assert that a
    record with an `answer.authorName` cannot emit a page without a `creator`.

18. **Fix `daily-report.js:194`'s `work` figure.** *(25th day.)* One field rename or a per-routine
    label.

36. **Make the wave log a cost figure.** *(33rd day — § 7.)* One field in the wave's routine-log
    shard.

5.  **Declare which carried items you are not going to do.** *(25th day.)* The table below is 41
    rows. Reply with the numbers to strike and tomorrow's report carries only what survives.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 2 | Stop the daily wave and the backlog waves colliding over the queue | 24th |
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" | 24th |
| 6 | Make `prep-wave.js:206` stamp `creditedTo` when `confidence === 'disputed'` regardless of name equality — **75/76/80 are the same guard's other failures** | 31st |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 24th |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 30th, clock running |
| 9 | Fix the middle-initial hub fork at the generator | 23rd |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row — **79 is the adjacent case** | 23rd |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 23rd |
| 15 | Decide what to do about Google Books' quota | 26th |
| 16 | Stop one source's outage hard-stopping the review pass | 29th |
| 19 | Decide whether `perf-check` belongs in the routine census | 27th |
| 20 | Settle the branch-name / draft conflict between the harness and the routine docs — **#909 overrode it again today, and said why** | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict | 26th |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason | 27th |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — cannot see unmerged PRs | 31st |
| 24 | Work the disputed records with no `creditedTo` | 37th |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) | 30th |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 30th |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 30th |
| 30/62 | Rule on "Verbatim:" and "rights held by X" corpus-wide | 30th / 16th |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — 58 is queued behind it | 32nd |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate | 33rd |
| 33 | Decide the house convention for characters with no HTML entity | 29th |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 31st |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — folded into 17/58 | 31st |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 34th |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 34th |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 30th |
| 40 | Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`) | 22nd |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 22nd |
| 44 | Teach `merge-gate.js` to look at what actually changed — #670 is its case | 21st |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 21st |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — 54 is the ruling this needs | 21st |
| 47 | Decide whether QI's Title Case should be normalised centrally | 21st |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 21st |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 21st |
| 51 | Make the merge pass's rebuild step fetch before it merges | 20th |
| 54 | Rule on Quote Investigator *headlines* reaching `displayQuote` — the true count is UNKNOWN; do not re-derive | 19th |
| 56 | Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog`, which is live | 18th |
| 57 | Give the orphaned `taxes-…` candidate a way back into the queue | 18th |
| 59 | Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it — 74 is waiting on this | 18th |
| 61 | Fix the `mis-kind.js` comma gap; also add a `/fake/` rule to `REFUTATION_TAG` | 18th |
| 63 | Out-of-band merges — **clean, eighth consecutive day**; stays listed | not live today |
| 64 | Make the merge pass merge content PRs before routine-log PRs | not live today |
| 65 | Put the verdict before the re-poll chatter in the merge note — **live again today**, both notes lead with re-poll chatter | 15th |
| 66 | Fix `template.js:848`'s unconditional period on `disambiguatingDescription` | 15th |
| 67 | Rule on the Parker `displayQuote` repair, which shipped 09-05 | 13th |
| 68 | Fix nested quotes in `claimReviewed` | 13th |
| 69 | Rule on whether a Wikiquote *sourced-section* anchor is enough for `verified` on an in-copyright work | 13th |
| 70/71 | The 09-06 quota collision; `daily-wave` / `daily-review` still fail silently | carried |
| 74 | Decide whether `DOSSIER_SCHEMA` gets the hand-set override fields — waiting on 59 | 10th |
| 75 | Rule on the harvester's magnet-as-claimant defect | 9th |
| — | `loc.gov` human-facing page URLs 403 to non-browser clients — 81 is the general version | 6th |
| — | `looksLikePerson` still accepts an anonymity note ("Nobody — it has no traceable author") | 8th |
| — | Add the token-subset test's false-alarm mode (`Edmund Kean → Edmund Gwenn`) to the #801 note | 10th |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Make the no-op shards carry a `note` — the evidence is in the PR bodies but not in what the census reads | 6th |
| — | `pull_request_read/get_status` reads empty for `verify` because it is a check run, not a commit status — will mislead a quick health check | new |

**Struck previously:** 25, 28 (superseded by 59), 50, 52, 53, 55, 60, 78. **Nothing is struck today.**

---

## 7. Cost

**No run reported a cost figure.** Today's four shards carry `built`, `processed`, `scanned` and
`flagged` — no cost field anywhere, including the wave's. Item **36**, 33rd day.

The wave again volunteered an effort profile in prose, which is the closest thing to a number this
report has: **eleven Gutenberg texts, 2,411,335 characters**, twelve phrase searches, site-wide
Wikiquote `insource:` searches, 14 source URLs re-fetched, a full audit (11 findings) and a skeptic
re-check of every `high`. That bought 12 fixes before ship, including a date the page's own cited
source refuted and an unsourced merchandising claim — and it did it with `WebSearch` unavailable and
three hosts blocked, which is the expensive shape of night made more expensive.

Two routines cost close to nothing, correctly: `daily-reports` stopped at step 1 on an empty queue,
and `daily-review` stopped at `flagged: 0`. Both said so in their PR bodies rather than manufacturing
work. That is the system behaving.
