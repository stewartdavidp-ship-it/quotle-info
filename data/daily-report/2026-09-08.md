# Daily report — 2026-09-08

## 1. Verdict

**The night worked, and it was the largest content day the corpus has had: 2045 → 2110, +65 records.**
All five due routines ran. `daily-wave` built 5, two operator backlog waves landed 40 (r48) and 20
(r49), six fix branches merged, `main` is green, 0 flagged, tree clean.

Three things to read before anything else.

1. **Yesterday's daily wave (#792) is dead content — every one of its five quotes shipped inside r48
   nine hours later.** The wave itself worked this out, compared both versions, found r48's equal or
   better on every axis, and refused to resolve the conflict. It asks for a decision on closing, and
   names one loose end: **the only record that the 2026-09-07 daily wave ran at all is a log shard
   trapped on that branch.** New item **73**. § 5.1.
2. **A generator gap the wave hit today and reported instead of editing:** `schema.verdictNote` — the
   sanctioned override for prose/structured-data disagreement — is absent from `DOSSIER_SCHEMA` and
   dropped by `toRecord`, so no wave can emit it. New item **74**. § 5.2.
3. **Nine of today's fourteen merges did not come from the 07:00 pass.** All nine had `verify` green
   before merging — this is not a safety failure, but the merge pass is no longer the single merge
   authority in practice. Carried item **63**, escalated. § 5.5.

Traffic is unavailable for the twelfth consecutive day, same one setting (§ 4).

---

## 2. What ran

| slot | routine | result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run: `pr` — 5 records → **#807** (shard 07:51:34Z) |
| 04:00 | `daily-reports` | ✓ 1 run: `no-op` (shard 08:12:13Z) |
| 05:00 | `daily-review` | ✓ 1 run: `no-op` (shard 09:04:49Z) |
| 07:00 | `daily-merge` | ✓ 2 runs: `merged`, `merged` (03:17:36Z and ~10:41Z) |
| 12:00 | `reports-close` | · not yet — due 17:00 UTC (yesterday's ran `no-op`, shard 18:03:08Z) |
| Mon 02:00 | `weekly-discovery` | · not scheduled today (ran Mon 09-07, shard 06:21:45Z) |

**Every expected shard for today is present. Nothing due today failed to run.**

But **one shard is missing from `main` for yesterday**: there is no `2026-09-07 daily-wave` entry in
`data/routine-log/`. That wave *did* run and *did* build 5 records — its shard
(`2026-09-07T07-44-35-244Z-daily-wave.jsonl`) exists only on the unmerged `wave-d20260907` branch. So
read from `main` alone, yesterday's daily wave looks like it never fired. See item **73**.

`daily-report.js` reported **1 tool-call failure** — `traffic: fetch failed`. **§ 4 is the only
incomplete part of this report; §§ 2, 3, 5 are complete.**

---

## 3. What shipped

**Merged inside today's tool window (14 PRs) — 65 new records:**

| PR | what |
|---|---|
| #800 | `wave(r48)` — **40 records**, 2045 → 2085 |
| #806 | `wave(r49)` — **20 records**, 2085 → 2105 |
| #807 | `wave(d20260908)` — **5 records**, 2105 → 2110 (the scheduled daily wave) |
| #802 | `mis-kind` — five measured patterns, **55 rows** ✕ → ~ |
| #813 | `mis-kind` — the qualified `genuine` tag, **14 rows** ✕ → ~ |
| #803 | `template` — record-level levers for "Verbatim:" and "rights held by X" |
| #812 | `template` — record overrides for hardcoded `inLanguage` and the slide warning |
| #811 | `prep-wave` — strip markup from `esc()`'d fields at ingest |
| #804 | `sources` — primary anchors for the 3 records resting on QI alone |
| #801 | `docs` — corrects the `--credited` guard description |
| #799 #805 #808 #809 | routine-log shards |

**Merged in the report blind spot (4 PRs)** — between yesterday's report (12:27Z) and 00:00Z, so
neither yesterday's report nor today's tool window can see them: **#791** (weekly discovery, **20
records audited**, 21:10:12Z), #795, #797, #798 (log shards). Item **4**, 32nd day — this is the fifth
consecutive night, and this time it hid a 20-record audit.

- **Built today:** 5 records by the scheduled wave, 60 by the two operator backlog waves. All merged.
- **Audited today:** 5 (wave audit, 3 PASS / 2 FAIL). `daily-review` scanned and returned `no-op`.
- **Reader reports:** none pending. `daily-reports` ran `no-op`.

> The readable output credits `daily-merge` with "5 record(s)". That is carried item **18** counting
> merged **PRs** as records — the merge pass merged 5 PRs. **16th day.**

---

## 4. Traffic

**Traffic: unavailable — twelfth consecutive day.** `traffic` is `null` and `tool_failures` carries
`traffic: fetch failed`. **This is an analytics outage, not a quiet day. It is not a zero.**

Cause unchanged and not transient. `node tools/traffic.js` here returns, verbatim:

> `traffic.js: 403 from GoatCounter — the dashboard is no longer public.`
> `Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.`

No figure can be carried tomorrow either. Item **43**, 12th day — one setting in the GoatCounter UI,
no data lost in the meantime. Note the tool's generic `fetch failed` still hides this specific 403;
§ 4 had to shell out to `traffic.js` for the eleventh time to learn the real cause.

---

## 5. Findings

### 5.1 The 2026-09-07 daily wave is fully superseded, and its only run-record is trapped with it

`#792` built 5 records at 07:44Z on 09-07. `wave(r48)` (#800) landed at 02:18Z on 09-08 carrying **all
five of the same slugs**. The wave diagnosed this itself and did not force it through. In its words:

> The branch is consequently **conflicted** (38 paths, including all five `data/quotes/*.json` as
> add/add) […] Merging it would mean **reverting r48's versions in favour of thinner ones**, so I
> aborted the merge rather than resolving it, and pushed nothing.

It then compared the two research passes rather than assuming its own was better, and reported that
**both passes agree on every verdict** — same sources, same dates, same page locators, including
Fisher 1907 p. 6 for the parrot joke and the Meynell 1903 hedge for the Disraeli lines. It also
retracted its own suspicion: *"I initially suspected r48 overclaimed authorship […] **That was
wrong**."* Its conclusion is that r48's records are richer (8–12 `sourcesVerified` rows vs 3–4) and
that convergence is *"a good signal for the corpus, not a defect in either."*

**Why it happened is structural, not a skipped check.** The near-duplicate check ran both ways at
07:40Z on 09-07 against all 2,045 records on `main` and correctly found nothing, because r48's records
did not exist anywhere visible yet:

> `harvest.js select` draws from `status === 'queued'`, and a concurrent wave's `selected` marks live
> only on its own branch until it merges. […] The mitigation recorded there (copy the other wave's
> uncommitted `selected` marks before drawing) requires knowing the other wave exists, which a
> scheduled unattended routine has no way to learn.

That is carried items **2** and **23** demonstrated end to end, with a cost attached: one full wave's
research discarded.

**The loose end it flags is the one that matters for this report.** Closing #792 discards
`data/routine-log/2026-09-07T07-44-35-244Z-daily-wave.jsonl`, the only evidence that the 09-07 daily
wave ran — *"an unrecorded run is exactly the 'broken night looks like a quiet one' failure the routine
log exists to prevent."* Confirmed: that shard is not on `main`. New item **73**.

Nothing on `main` is wrong and no content is missing — all five quotes are live via r48.

### 5.2 `schema.verdictNote` cannot be emitted by any wave

Today's wave needed it and reported it rather than editing `tools/`:

> **`schema.verdictNote` is not in `DOSSIER_SCHEMA` and `toRecord` drops it.** It is the documented
> remedy for prose/structured-data disagreement, and `template.js` supports it, but no wave can emit
> it through `generate.js` — it had to be added to the record during the fix stage.

It used it correctly in the fix stage on the Warner record, where a `disambiguatingDescription` said
*"Actually by Jack L. Warner"* while the page's own prose called the evidence thin. So the override
works; only the ingest path is missing. New item **74**. Note this collides with carried item **59** —
`DOSSIER_SCHEMA` is at the 4,072-byte `SCHEMA_BUDGET` ceiling with zero headroom, so adding a field is
a budget decision, not a one-liner.

### 5.3 Two waves of measured `mis-kind` work landed — 69 rows corrected

#802 landed five patterns and **declined four**, recording the declines in the file so they are not
re-proposed. Every candidate was run over all 6,042 misattribution rows and every hit read. It refused
`^overstated$` (12 rows, mixed) and `\bpredates\b` (16 rows, mixed) because on some the ✕ is correct.

#813 then caught what #802 missed, **found by an auditor running against the patterns landed the same
morning** — `/^genuine$/i` needs the whole tag, so `tag="genuine, but only the setup"` fell through
and the Pope record rendered the burgundy ✕ *"this credit is false"* beside prose saying the line
really is Pope's. 14 rows moved. Its safety note is worth keeping:

> One row reads **`"genuine fake Seneca"`** — where `genuine` modifies **fake**, not the credit. A bare
> `/^genuine\b/` softens a real debunk, the one error this file is built to avoid.

It also found that `REFUTATION_TAG` *"carries a `/fabricat/` rule but **no `/fake/` rule**, so that
row's correct ✕ today is an accident of nothing matching."* Recorded, not fixed.

**Carried item 25 (the measured `mis-kind` pass) is done.** Item **61** (the comma gap,
`documented, but unverified`) was not among the landed patterns and still stands.

### 5.4 A run corrected its own published claim — and the correction is a measurement lesson

#806 originally reported that the `--manifest` completeness gate *"exits 0"* on a dropped record. It
struck that through the next day:

> **CORRECTED 2026-09-08 — this claim was wrong.** `apply-tags.js` exits **1** correctly
> (`workflows/apply-tags.js:122`) […] The original check piped the command through `tail`, and `$?`
> after a pipeline reports the *last* command's status — so it measured `tail`, not `node`. The gate
> is sound; the measurement was not.

**No decision needed.** The gate itself earned its keep the same night: `tag-themes` returned
`covered: 19/20` and silently dropped a record, which *"the journal cannot detect […] what an agent
never returns leaves no trace in it."*

Same class, different direction, in #801: `workflows/README.md` had described the `--credited` guard as
*"comparing LAST WORDS"* and warned about `"Confucius (Kong Qiu)"`. Measured against the live helper:
false. The doc *"warned about a failure mode the code doesn't have, and was silent on the one it
does"* — middle-name expansion, which stamped `creditedTo: "Hillary Clinton"` on Hillary Rodham
Clinton's own verified words in r48 and was caught by hand before ingest. One small follow-up r49 asks
for and nobody has done: the token-subset test's own false-alarm mode (`Edmund Kean → Edmund Gwenn`,
shared forename) *"should be added to the #801 note."*

### 5.5 Nine of fourteen merges bypassed the 07:00 pass — all with green CI

The merge pass's own shards say it merged **#799** (03:17Z) and **#807 #805 #808 #809** (10:41Z). The
other nine — #800, #801, #802, #803, #804, #806, #811, #812, #813 — merged in tight clusters seconds
after their PRs opened.

**Checked before reporting it as a risk: `verify` completed green on each head before the merge**
(#800 CI done 02:18:25Z / merged 02:18:37Z; #806 04:00:54Z / 04:01:14Z; #813 11:29:21Z / 11:29:40Z).
So nothing shipped unverified. What has changed is governance: #670's body still says *"Not merging
this; the 07:00 pass is the single merge authority,"* and on today's evidence that is no longer how
the repo works. Carried item **63** was a single out-of-band merge (#741); it is now the majority
case, and either the docs or the practice should move.

### 5.6 Quote Investigator is administratively excluded from the Wayback Machine

#804 established this and quantified the exposure:

> the sparkline API returns `{"error":{"type":"blocked","block_reason":"admin"}}`, and CDX and the
> availability API both return zero domain-wide. Its `robots.txt` allows all, so the block is
> archive-side and **Save Page Now cannot fix it.** The site returns 200, so no link-checker sees a
> problem.

**917 records cite QI; only 3 had no non-aggregator fallback, and all 3 are now anchored** to primary
documents QI itself names (Google Books full view, archive.org, Founders Online). QI is kept as the
citation in every case. **No decision needed** — the acute exposure is closed. Recorded so nobody
re-derives it, and because `founders.archives.gov` *"answers 202 with an empty body to scripts by
design"*, so a link-checker will flag a link that is in fact durable.

### 5.7 The wave's own audit: 2 high findings, both real, both fabrication-class

`daily-wave` ran 3 PASS / 2 FAIL, 9 issues, 1 refuted on skeptic re-check. Both highs were things the
page asserted that no source carried:

- **A fabricated date.** *"`writers-are-just-schmucks-with-underwoods` said 'Peter Jennings said
  "schnooks" on air in 1990.' That year is in no source […] The year had been manufactured from the
  surrounding narrative."*
- **A link that did not carry its claim.** A Wikipedia article pinned to a description of Potash and
  Perlmutter *"which contains no occurrence of *cloak*, *suit*, *garment*, *malapropism* or
  *dialect*."*

The refuted finding was refused for the right reason — the `in-copyright` badge on the Washington
record *"follows the stated rule for a nameable 1931+ containing work […] and the alternative was the
*less* conservative call."*

Two of the wave's operator notes are worth carrying: **four of the five records are Samuel Goldwyn
magnets** (that is what rank order gave; no selection policy was improvised to avoid it), and **two
candidates in the draw are stored as QI *article titles*, not quotes** — Woollcott's *"The Vanishing
Lady and the Vanishing Hotel Room"* (rank 7) and Dershowitz's *"Legal Advice: Pound the Facts…"* (rank
9). Neither can build as a quote, and *"they are now back in the queue at high demand rank and the next
wave will re-derive the same conclusion."* That is carried item **3** with two named instances.

### 5.8 The repeated-defect loop closed properly once today

`source.cutTag` shipping `<em>` markup broke the build gate in r48 **and again** in r49. Rather than
hand-fixing a third time, #811 moved the strip to `prep-wave`:

> Twice in two waves is a generator habit, not an accident […] **The build gate stays.** `prep` only
> sees a wave's own records, so anything hand-edited afterwards, or arriving by another route, is
> still caught there.

It verified by replaying r49's real generate journal and confirming the build gate still fails on a
planted tag. This is the pattern the other carried items are waiting for: measure, fix centrally, keep
the gate.

### 5.9 The stale-`main` exception fired again — and changed shape

Preflight failed this morning on `local main sane` **and** `git on main`, with `tree clean` passing:
documented exception **(a)** — *"local main is NOT an ancestor of origin/main (diverged, or an
unrelated history)."* Yesterday it was exception **(b)** at 32 commits behind. **The failure mode is
not stable**, which weakens the case for simply ratifying the two exceptions and strengthens the case
for root-causing them. Ran the printed command, re-ran preflight, 10/10 green; nothing lost. Item
**12**, 25th day.

### 5.10 #670 is still stuck, thirteenth day — and it is *not* the #792 case

`DIRTY`, 12 days open, untouched since 2026-08-27. **Checked: none of its five slugs exists on `main`**
(two Thornton Wilder records resting on primary text read in full, three Branson records honestly
marked `attributed`). Unlike #792, closing this one **does** lose content. Item **42**.

---

## 6. Needs a decision

**Two new items (73, 74). 73 is the one to do today — it is the only item on this list where waiting
costs a record of what happened.**

73. **Close #792, but re-record the 2026-09-07 wave first.** All five of its quotes shipped in r48, so
    the branch is conflicted and merging it would revert r48's better records — but its routine-log
    shard is the only evidence on `main` that yesterday's daily wave ran at all. Doing it is: write a
    `daily-wave` shard for 2026-09-07 with the true outcome (built 5, superseded by r48, 0 merged) on
    a fresh branch, then close #792 unmerged.

74. **Decide whether `schema.verdictNote` gets into `DOSSIER_SCHEMA`.** It is the sanctioned override
    for prose/structured-data disagreement and `template.js` supports it, but `generate.js` cannot
    emit it and `toRecord` drops it, so every use must be hand-added at the fix stage. Doing it is:
    find the bytes inside the 4,072-byte `SCHEMA_BUDGET` ceiling (item **59**) or raise the ceiling,
    then add the field.

72. **`tools/review.js:267` reads rights from `meta.rights`/`rights`, but all 1,385 records that have
    a rights value store it at `source.rights`, so every audit has seen `null`.** *(2nd day —
    untouched today.)* Doing it is: change that one fallback chain to read `rec.source.rights`, on its
    own branch — then decide separately whether to re-audit the rights claims never actually checked.

43. **Flip GoatCounter → Settings → "Dashboard viewable by" → Anyone.** *(12th day.)* The endpoint
    returns 403 "the dashboard is no longer public", so this report has carried no traffic figure for
    twelve days and cannot carry one tomorrow. One setting in the GoatCounter UI; no data is lost.

4.  **Fix the report window.** *(32nd day — live again: #791's 20-record audit merged 21:10Z on 09-07,
    invisible to both yesterday's report and today's.)* `daily-report.js:263` windows `listMerged` on
    the UTC calendar day. One change — window from the previous report's timestamp — on its own branch.

63. **Ratify or stop the out-of-band merges.** *(7th day, escalated — 9 of today's 14 merges did not
    come from the 07:00 pass, all with `verify` green.)* Doing it is: either update the routine docs
    (which still call the 07:00 pass "the single merge authority") to describe what actually happens,
    or stop merging content branches outside it.

2.  **Stop the daily wave and the backlog waves colliding over the queue.** *(15th day — it cost a
    whole wave today, § 5.1.)* Doing it is: give `harvest.js select` a way to see marks that live only
    on another branch, or serialise the two wave types by schedule.

42. **Rebuild and merge #670, or close it.** *(13th day; DIRTY, 12d open.)* **Unlike #792, closing
    this loses content** — its five slugs are on no other branch. Doing it is: merge `main` in, take
    `main`'s side on generated files, `node tools/build.js`, `node tools/scan.js`, push — or close it
    and let a later wave redraw them.

70. **The quota collision that killed three routines on 09-06 is still unfixed; they have simply got
    through twice since.** Doing it is: move the routines off the contended window, raise the account
    ceiling, or accept the collision and add a retry.

71. **`daily-wave` and `daily-review` still fail silently** — only `daily-reports` carries the
    `routine_notify_push` tag. Doing it is: add `routine_notify_push` to those two triggers.

21. **Give `claimReviewed` a lever, or accept the coarse verdict.** *(17th day.)* `disputed` collapses
    "demonstrably fabricated" and "unsupported but conceivable" onto the same "False", and the rating
    derives from `confidence` corpus-wide. Doing it is: add a confidence tier or a per-record rating
    override in `template.js`, then count the disputed records whose evidence is "conceivable".

13. **Extend the egress allowlist.** *(28th day — three more hosts today: `factcheck.org`,
    `washingtonpost.com`, `rbhayes.org`, which pushed Hayes's real 1877 reaction onto the page at one
    remove.)* Doing it is an allowlist entry per host.

12. **Root-cause the stale local `main`, or ratify the exceptions in `preflight.js`.** *(25th day —
    fired as exception **(a)** today after **(b)** yesterday; § 5.9 argues the shifting shape favours
    root-causing.)* Doing it is: find what leaves the container with a bad `main` ref, or write both
    exceptions into `preflight.js` so no routine has to reason about them.

18. **Fix `daily-report.js:194`'s `work` figure.** *(16th day — today it credited `daily-merge` with
    "5 record(s)" for 5 merged PRs.)* It sums `processed`, which the merge pass uses for PRs. Doing it
    is one field rename or a per-routine label.

36. **Make the wave log a cost figure.** *(24th day — live today: a full 5-record pipeline ran and
    logged none, § 7.)* Doing it is one field in the wave's routine-log shard.

30 / 62. **Rule on "Verbatim:" and "rights held by X" corpus-wide.** *(21st / 7th day — #803 landed
    the per-record levers today: `presentation.wordingUnpinned` and `source.rightsHolderPresumed`,
    each applied to exactly one record. 502 and 645 pages keep today's wording.)* Doing it is: decide
    the default, or accept that these stay per-record judgement calls.

5.  **Declare which carried items you are not going to do.** *(16th day.)* The table below is 40 rows.
    Reply with the numbers to strike and tomorrow's report carries only what survives.

### Resolved today

**25** — the measured `mis-kind` pass, done by #802 (55 rows, 4 patterns declined and recorded) and
#813 (14 rows). **Item 61 is not covered by it and still stands.** Also closed without needing a
number: the QI/Wayback exclusion (§ 5.6, all 3 exposed records anchored), the `source.cutTag` markup
loop (§ 5.8, fixed at `prep-wave`), the hardcoded `inLanguage` and slide-warning strings (#812, now
record-overridable), and the `--credited` doc divergence (#801).

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" — **two new instances today (Woollcott, Dershowitz), 57 is its mirror** | 15th |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 15th |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 21st, clock running |
| 9 | Fix the middle-initial hub fork at the generator, scoped to `schema.creator` | 14th |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row | 14th |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 14th |
| 14 | Decide the `isBasedOn → isPartOf` bulk migration (1,046 of 1,752 measured 08-24) | 16th |
| 15 | Decide what to do about Google Books' quota | 17th |
| 16 | Stop one source's outage hard-stopping the review pass | 20th |
| 17 | Fail the build when `Quotation.creator` is suppressed; normalise `template.js`'s hero match — **folded into 58** | 20th |
| 19 | Decide whether `perf-check` belongs in the routine census | 18th |
| 20 | Settle the branch-name / draft conflict between the harness and the routine docs | recurring |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason | 18th |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — **cannot see unmerged PRs; this is what cost #792** | 22nd |
| 24 | Work the disputed records with no `creditedTo` — 7 of 10 `magnet` rows still render the false-credit ✕ | 28th |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) | 21st |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 21st |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 21st |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — **58 is queued behind it** | 23rd |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate — **67 is the second instance** | 24th |
| 33 | Decide the house convention for characters with no HTML entity | 20th |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 22nd |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — **folded into 58** | 22nd |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 25th |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 25th |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 21st |
| 40 | Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`) | 13th |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 13th |
| 44 | Teach `merge-gate.js` to look at what actually changed (`:98` DIRTY→SKIP; `:123` behind→demote) — **#670 and #792 are both its case** | 12th |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 12th |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — **54 is the ruling this needs** | 12th |
| 47 | Decide whether QI's Title Case should be normalised centrally — **106 of 2,110 records carry Title-Cased `displayQuote`s** | 12th |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 12th |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 12th |
| 51 | Make the merge pass's rebuild step fetch before it merges | 11th |
| 54 | Rule on Quote Investigator *headlines* reaching `displayQuote` (50+ records shipped this way) | 10th |
| 56 | Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog`, which is live — nobody said "(or Dog)" | 9th |
| 57 | Give the orphaned `taxes-…` candidate a way back into the queue | 9th |
| 58 | Accept or decline the `disputed-creator-name-mismatch` detector, and rule on its 99-record sweep | 9th |
| 59 | Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it — **74 is now waiting on this** | 9th |
| 61 | Fix the `mis-kind.js` comma gap — `/\bdocumented\b[^,]*\bunverified\b/i` cannot match `documented, but unverified`; **also add a `/fake/` rule to `REFUTATION_TAG` (§ 5.3)** | 9th |
| 64 | Make the merge pass merge content PRs before routine-log PRs | **not live today** — #807 merged alongside the shards |
| 65 | Put the verdict before the re-poll chatter in the merge note — **recurred today** | 6th |
| 66 | Fix `template.js:848`'s unconditional period on `disambiguatingDescription` | 6th |
| 67 | Rule on the Parker `displayQuote` repair, which shipped 09-05 | 4th |
| 68 | Fix nested quotes in `claimReviewed` — `said: "…"Check Enclosed""` in the JSON-LD | 4th |
| 69 | Rule on whether a Wikiquote *sourced-section* anchor is enough for `verified` on an in-copyright work | 4th |
| 6 | Make `prep-wave.js:206` stamp `creditedTo` when `confidence === 'disputed'` regardless of name equality | 22nd |
| — | Add the token-subset test's false-alarm mode (`Edmund Kean → Edmund Gwenn`) to the #801 note | new, 1st |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Add `traffic.js`'s specific error to `daily-report.js`'s `tool_failures` — **folded into 43** | 11th |

**Struck previously:** 25 (done today), 28 (superseded by 59), 50, 52 (folded), 53 and 55 (done
08-30/08-31), 60 (done 09-02).

---

## 7. Cost

**No routine reported a cost figure today.** `daily-wave` ran a full 5-record pipeline serially and
logged none — item **36**, 24th day. `weekly-discovery`, the only routine that has ever logged one,
was not scheduled today.

The series still stands at three measured points, all `weekly-discovery`: **~21K/record** (2026-07-29),
**~9.5K/record** (2026-08-31), **~16K/record** (2026-09-07).

**Interrupted runs: none.** No routine was rate-limited, rejected, or cut short. The environmental hits
were the GoatCounter 403 (§ 4) and the egress denials on `factcheck.org`, `washingtonpost.com` and
`rbhayes.org` (item **13**), neither of which stopped a run. Three lending-restricted Internet Archive
items and the ProQuest/GenealogyBank paywalls limited what today's wave could verify directly, and the
pages say so rather than papering over it.
