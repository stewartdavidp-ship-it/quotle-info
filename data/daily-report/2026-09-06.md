# Daily report — 2026-09-06

## 1. Verdict

**Three of the four due routines did not run. They fired and were rejected by an account-level rate
limit before doing any work — `daily-wave`, `daily-reports` and `daily-review` each died in 14–17
seconds with 0 tokens used.** No quote records were built, no corpus scan ran, and no reader-report
queue was checked. `daily-merge` is a GitHub Action, not a cloud routine, which is the only reason
anything happened at all last night. Corpus stands at **2045**, `main` is green, 0 flagged.

This is not a code defect and nothing in the repo can fix it. The cause is quota, and it is
documented exactly in § 5.1.

Three things to read before anything else.

1. **The outage has a named cause and a live risk of repeating.** All three routines were rejected
   with `"You've hit your session limit · resets 10am (UTC)"` — a `five_hour` window that expired at
   10:00 UTC, i.e. after all three of their slots (07:05, 08:08, 09:03). Separately, an unrelated
   interactive session on the same account was recorded at 11:31 UTC today with a **`seven_day`**
   limit in `status: "rejected"`, resetting **2026-09-08 21:00 UTC**. § 5.1.
2. **Two of the three failures were silent.** `daily-reports` carries the `routine_notify_push` tag;
   `daily-wave` and `daily-review` do not. The two most consequential routines failed without
   notifying anybody, and this report is the first thing that noticed. New item **71**.
3. **A wave PR merged in the report blind spot for the third consecutive night.** #772's 5 records
   went in at **2026-09-05T17:10:18Z** — after yesterday's report and before 00:00Z — so they appear
   in neither report's merged list. Carried item **4**, now three-for-three. § 5.2.

Traffic is unavailable for the tenth consecutive day, same one setting (§ 4).

---

## 2. What ran

| slot | routine | result |
|---|---|---|
| 03:00 | `daily-wave` | ✗ **DID NOT RUN** — fired 07:05:33Z, **FAILED** 07:05:50Z (17s, rate-limited) |
| 04:00 | `daily-reports` | ✗ **DID NOT RUN** — fired 08:08:52Z, **FAILED** 08:09:06Z (14s, rate-limited) |
| 05:00 | `daily-review` | ✗ **DID NOT RUN** — fired 09:03:15Z, **FAILED** 09:03:29Z (14s, rate-limited) |
| 07:00 | `daily-merge` | ✓ 2 runs: `merged`, `merged` (shards 03:10 and ~10:21 UTC) |
| 12:00 | `reports-close` | · not yet — due 17:00 UTC (yesterday's ran `no-op`) |
| Mon 02:00 | `weekly-discovery` | · not scheduled today — **but see § 5.1, it is next in the firing line** |

All three failing routines are **still enabled and still scheduled** for tomorrow (07:04:34Z,
08:07:49Z, 09:02:30Z). Nothing is disabled, nothing is suspended, no `ended_reason` is set. They will
fire again; whether they survive depends on quota, not on this repo.

`daily-report.js` reported **1 tool-call failure** — `traffic: fetch failed`. **§ 4 is the only
incomplete part of this report; §§ 2, 3, 5 are complete.**

---

## 3. What shipped

- **Merged inside today's tool window (2):** #782 (merge log 09-06), #781 (merge log 09-05). **Both
  are routine-log shards. Zero quote records merged inside the window.**
- **Merged since yesterday's report but outside every report window (6 PRs, 5 records):** #772 at
  **17:10:18Z** — the 5-record Parker/Lewis/Disraeli wave — plus #777, #778, #779, #780, all log
  shards. Corpus **2040 → 2045**. § 5.2.
- **Built today:** nothing. `daily-wave` never got a turn.
- **Audited today:** nothing. `daily-review` never got a turn — **the corpus has not been scanned
  since 2026-09-05 (2040 records, 0 flagged).** Today's `flagged: 0` is yesterday's number carried
  forward on an unchanged corpus, not a fresh result.
- **Reader reports:** unknown. `daily-reports` never got a turn, so the pending queue was not checked
  today. `reports-close` is still due at 17:00 UTC and may yet cover it.

> The readable output credits `daily-merge` with "2 record(s)". That is the known `work`-figure
> defect (carried item **18**) counting merged **PRs** as records. **Zero quote records merged inside
> today's window.** 14th day.

---

## 4. Traffic

**Traffic: unavailable — tenth consecutive day.** `traffic` is `null` and `tool_failures` carries
`traffic: fetch failed`. **This is an analytics outage, not a quiet day. It is not a zero.**

Cause unchanged and not transient. `node tools/traffic.js` here returns, verbatim:

> `traffic.js: 403 from GoatCounter — the dashboard is no longer public.`
> `Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.`

Ten days of GoatCounter data are still being recorded and are not lost — they are unreadable from
this checkout until someone flips that one setting. Nothing here can reach it. Carried item **43**,
tenth day.

Reminder for when the number returns: it is an **upper bound**, not spam-filtered
(`spamFiltered: false` always in cloud), and on 2026-09-01 roughly **37% of attributed referrals were
bots impersonating search engines on `.info` domains**. Say "up to N". `node tools/traffic.js --refs`
gives the breakdown once the dashboard is readable again.

---

## 5. Findings

### 5.1 The three routines were rate-limited out of existence — cause, evidence, and what is at risk

Each of the three failed sessions carries the identical `post_turn_summary`, verbatim:

> `"status_category": "failed", "status_detail": "You've hit your session limit · resets 10am (UTC)"`

and the identical rate-limit record:

> `"rate_limit_info": {"isUsingOverage": false, "rateLimitType": "five_hour", "resetsAt": 1788688800, "status": "rejected"}`

`1788688800` is **2026-09-06 10:00 UTC**. Every one of the three slots — 07:05, 08:08, 09:03 — falls
inside that window. `"context_usage": {"used_tokens": 0}` on all three: they were rejected at
startup, before reading a single file. This report's own routine fired at 12:02 UTC, *after* the
reset, which is the only reason you are reading it.

**The quota is not this project's alone.** Session `session_01VbTWVR4MoEF7L4VmNJF919` ("App naming:
We Both Care", an interactive CLI session, `effort_level: xhigh`, unrelated to quotle.info) was last
updated **2026-09-06 11:31 UTC** carrying:

> `"rateLimitType": "seven_day_overage_included", "resetsAt": 1788901200, "status": "rejected"`

`1788901200` is **2026-09-08 21:00 UTC**. That is a *seven-day* ceiling in a rejected state roughly
thirty minutes before this run started. I cannot tell from here whether that ceiling binds the
routines' environment too — this session is plainly running — so I am reporting the observation, not
predicting tomorrow. What is certain: **the routines share an account quota with interactive work,
they have no retry, and a rejection costs the whole night's content.**

**`weekly-discovery` fires tomorrow (Monday) at 06:00 UTC**, one hour earlier than `daily-wave` and
into the same part of the day that was exhausted today. It is the only routine that has ever reported
a cost figure and it is the discovery pass for the harvest queue.

### 5.2 #772 merged in the report blind spot — third consecutive night

`daily-report.js:263` calls `listMerged(SINCE)` where `SINCE` is the UTC calendar day, so a PR merged
after one report and before the next 00:00Z boundary is invisible to both. #752 hit this on 09-03,
#762 on 09-04 at 20:20:38Z, and now **#772 on 09-05 at 17:10:18Z, carrying 5 records** — the largest
one yet. The five slugs (`the-two-most-beautiful-words-in-the-english-language-are`,
`friendship-is-born-at-that-moment-when-one-man-says-to`,
`half-of-the-town-councilors-are-not-fools`,
`he-is-a-self-made-man-yes-and-he-worships-his-creator`,
`if-you-are-not-a-liberal-at-25-you-have-no-heart-if-you-are`) are live and correct; the defect is
that no report's merged list will ever show them. Item **4**, 30th day.

Consequence for item **67**: yesterday's report asked you to ratify or reverse the wave's narrow
repair of the Parker `displayQuote` **before #772 merged**. It merged four hours later. **The repair
is now live on the site.** The decision is no longer "ratify or reverse before it ships" — it is
"ratify, or revert a shipped record".

### 5.3 Two of three failures were silent

Reading the trigger records: `daily-reports` carries tags `["config:routine-lineage-none",
"routine_notify_push"]`. `daily-wave` and `daily-review` carry only `["config:routine-lineage-none"]`.
So the wave — the routine that produces all the content — and the review pass — the routine that
catches bad records — can both fail without notifying anyone. The census in this report is the whole
detection mechanism, and it runs five hours after the wave's slot. New item **71**.

### 5.4 The stale-`main` exception fired again, 22 commits behind — the worst yet

This report's preflight failed on `git on main` alone, with `local main sane`, `tree clean` and
`current with origin/main` all passing: exception (b). The remedy behaved exactly as the doc warns —
`git checkout main` moved the tree **22 commits backward** onto the stale ref, and
`git merge --ff-only origin/main` restored it. Every previous report has recorded a smaller gap
(10 and 13 commits yesterday). The exception worked; item **12** is the root cause and is on its 23rd
day.

Worth stating plainly: the exception has now fired on **every routine that has run preflight for at
least three days**. It is not an edge case any more.

### 5.5 #670 is still stuck, eleventh day

`mergeable_state` **DIRTY**, 10 days open, unchanged. No routine touched it — the wave that might
have did not run.

---

## 6. Needs a decision

**One new pair (70, 71) and it is the whole story: the routines are competing for quota with your
interactive work and two of them fail silently. Everything else below is carried. If you do nothing
else, do 70 and 71.**

70. **The three morning routines were rejected by a shared account rate limit and lost the whole
    night's content.** Doing it is: move the routines off the contended window (they fire 07:05/08:08/
    09:03 UTC into a five-hour cap that expired at 10:00), or raise the account ceiling, or accept the
    collision and add a retry — plus check `weekly-discovery` before 06:00 UTC tomorrow, since it
    fires earliest of all.

71. **`daily-wave` and `daily-review` failed without notifying anybody** — only `daily-reports`
    carries the `routine_notify_push` tag. Doing it is: add `routine_notify_push` to those two
    triggers, so a rejected run pushes at 07:05 instead of surfacing here five hours later.

67. **Rule on the Parker `displayQuote` repair — it has now SHIPPED.** *(#772 merged 09-05 17:10:18Z;
    yesterday's report asked for the ruling before it merged.)* The record's restored closing quote is
    live, while `data/harvest-queue.json` still holds the truncated string and no `harvest.js`
    subcommand can fix it. Doing it is: ratify record-level transcription repair as policy and add a
    queue-repair subcommand, or revert the shipped record.

43. **Flip GoatCounter → Settings → "Dashboard viewable by" → Anyone.** *(10th day.)* The endpoint
    returns 403 "the dashboard is no longer public", so this report has carried no traffic figure for
    ten days and cannot carry one tomorrow. One setting in the GoatCounter UI; no data is lost.

4.  **Fix the report window.** *(30th day — third consecutive night, this time hiding #772's 5
    records.)* `daily-report.js:263` windows `listMerged` on the UTC calendar day. One change — window
    from the previous report's timestamp — on its own branch.

69. **Rule on whether a Wikiquote *sourced-section* anchor is enough for `verified` on an
    in-copyright work.** The C. S. Lewis record shipped `verified` although no primary text was read;
    the wave flagged this itself. Doing it is: write the rule into `workflows/` (either "verified
    requires primary text" plus a new verdict tier, or an explicit carve-out), then count how many
    shipped `verified` records rest on the same anchor.

68. **Fix nested quotes in `claimReviewed`.** A quote ending in a quoted phrase renders
    `said: "…"Check Enclosed""` in the JSON-LD, across many already-shipped records. Doing it is one
    quoting change in `tools/template.js` on its own branch, plus a corpus count of affected records.

12. **Root-cause the stale local `main`, or ratify the exceptions in `preflight.js`.** *(23rd day —
    fired again today at **22 commits behind**, the largest gap recorded, § 5.4.)* Doing it is: find
    what leaves the container detached with a stale `main` ref, or accept it and write exceptions (a)
    and (b) into `preflight.js` itself so no routine has to reason about them.

42. **Rebuild and merge #670, or close it.** *(11th day; DIRTY, 10d open.)* Closing loses the research
    but no content — its five candidates are back at `queued`. Doing it is: merge `main` in, take
    `main`'s side on generated files, `node tools/build.js`, `node tools/scan.js`, push — or close it
    and let a later wave redraw them.

18. **Fix `daily-report.js:194`'s `work` figure.** *(14th day — today it credited `daily-merge` with
    "2 record(s)" when zero records merged.)* It sums `processed`, which the merge pass uses for PRs.
    Doing it is one field rename or a per-routine label.

64. **Make the merge pass merge content PRs before routine-log PRs.** *(4th day — **inert today**: no
    wave ran, so there was no content PR to demote.)* Doing it is one sort key in
    `tools/merge-run.js`'s planner, plus a test case.

6.  **Make `prep-wave.js:206` stamp `creditedTo` when `confidence === 'disputed'` regardless of name
    equality.** *(20th day — **inert today**, no wave.)* Doing it is one condition in
    `tools/prep-wave.js` on its own branch, plus a count of already-shipped disputed-self-magnet
    records carrying the contradiction.

13. **Extend the egress allowlist — three hosts.** *(26th day.)* Add `hansard.parliament.uk` to
    `bioguide.congress.gov` and `finance.senate.gov`. Doing it is an allowlist entry per host.

5.  **Declare which carried items you are not going to do.** *(14th day.)* The table below is 42 rows.
    Reply with the numbers to strike and tomorrow's report carries only what survives.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 2 | Stop the daily wave and the backlog waves colliding over the queue | 13th |
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" — **57 is its mirror image** | 13th |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 13th |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 19th, clock running |
| 9 | Fix the middle-initial hub fork at the generator, scoped to `schema.creator` | 12th |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row | 12th |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 12th |
| 14 | Decide the `isBasedOn → isPartOf` bulk migration (1,046 of 1,752 measured 08-24) | 14th |
| 15 | Decide what to do about Google Books' quota | 15th |
| 16 | Stop one source's outage hard-stopping the review pass | 18th |
| 17 | Fail the build when `Quotation.creator` is suppressed; normalise `template.js`'s hero match — **folded into 58** | 18th |
| 19 | Decide whether `perf-check` belongs in the routine census | 16th |
| 20 | Settle the branch-name / draft conflict between the harness and the routine docs | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict — **56 is this item live on the site; 68 is a separate quoting defect in the same field** | 15th |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason — **62 is its sibling** | 16th |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — cannot see unmerged PRs | 20th |
| 24 | Work the disputed records with no `creditedTo` — 7 of 10 `magnet` rows still render the false-credit ✕ | 26th |
| 25 | Do the measured `mis-kind.js` pass — **61 is one line of this** | 25th |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) | 19th |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 19th |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 19th |
| 30 | Decide `verbatimNote()`'s "Verbatim:" label corpus-wide (~370 records) | 19th |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — **58 is queued behind it** | 21st |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate — **67 is the second instance** | 22nd |
| 33 | Decide the house convention for characters with no HTML entity | 18th |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 20th |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — **folded into 58** | 20th |
| 36 | Make the **wave** log a cost figure — **inert today, no wave ran** | 22nd |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 23rd |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 23rd |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 19th |
| 40 | Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`) — today's runs landed at 03:10 and 10:21 UTC | 11th |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 11th |
| 44 | Teach `merge-gate.js` to look at what actually changed (`:98` DIRTY→SKIP; `:123` behind→demote) — **#670 is its case; 64 is its ordering half** | 10th |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 10th |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — **54 is the ruling this needs** | 10th |
| 47 | Decide whether QI's Title Case should be normalised centrally | 10th |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 10th |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 10th |
| 51 | Make the merge pass's rebuild step fetch before it merges | 9th |
| 54 | Rule on Quote Investigator *headlines* reaching `displayQuote` (50+ records shipped this way) | 8th |
| 56 | Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog`, which is live — nobody said "(or Dog)" | 7th |
| 57 | Give the orphaned `taxes-…` candidate a way back into the queue | 7th |
| 58 | Accept or decline the `disputed-creator-name-mismatch` detector, and rule on its 99-record sweep | 7th |
| 59 | Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it | 7th |
| 61 | Fix the `mis-kind.js` comma gap — `/\bdocumented\b[^,]*\bunverified\b/i` cannot match `documented, but unverified` | 7th |
| 62 | Fix `template.js:1227`'s flat "rights held by X" in the visible copy kit | 5th |
| 63 | Ratify or investigate the out-of-band merge of #741 | 5th |
| 65 | Put the verdict before the re-poll chatter in the merge note — **did NOT recur today** | 4th |
| 66 | Fix `template.js:848`'s unconditional period on `disambiguatingDescription` | 4th |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Add `traffic.js`'s specific error to `daily-report.js`'s `tool_failures` — **folded into 43; § 4 had to shell out again** | 9th |

**Struck previously:** 28 (superseded by 59), 50, 52 (folded), 53 and 55 (done 08-30/08-31), 60 (done
09-02).

---

## 7. Cost

**No routine reported a cost figure today, and for the first time the reason is that three of them
never ran.** `weekly-discovery` — the only routine that has ever logged one — is a Monday pass.
Carried item **36** stands, 22nd day.

**The failed runs cost nothing in tokens.** All three sessions report
`"context_usage": {"max_tokens": 1000000, "used_tokens": 0}` — they were rejected before their first
turn. The cost was a night of content: five records not built, 2045 records not scanned, one reader
queue not checked.

The standing datapoints, both from `weekly-discovery`, remain the project's only measured numbers:
**~9.5K new tokens/record** (2026-08-31, 20 records) against **~21K/record** (2026-07-29).

**Interrupted runs: three, all quota.** `rateLimitType: "five_hour"`, `status: "rejected"`,
`isUsingOverage: false` — the account was not in overage, it was over the window ceiling. The one
other environmental hit was the GoatCounter 403 (§ 4).
