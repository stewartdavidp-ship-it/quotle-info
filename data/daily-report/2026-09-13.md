# Daily report — 2026-09-13

## 1. Verdict

**The night worked. All four due routines ran, `main` is green at `69510fbe1`, 0 flagged, tree clean,
corpus 2,215.** Nothing failed. The wave shipped 5 records and — this is the headline — caught a
blocker-grade honesty failure in its own output before it reached the site.

Four things to read before anything else.

1. **The wave caught the used-but-didn't-coin shape at audit, one record short of shipping it.** The
   Truman recession/depression page rated the proposition *"Harry S. Truman said this"* as **False**
   in machine-readable ClaimReview while the page's own prose says he did say it. This is the exact
   failure `template.js:1419` documents and that **r49 shipped once**. Fixed in-record with the four
   levers the template carries for it; no `tools/` edits. § 5.1 — and item **80** is the proposal to
   stop relying on the audit to catch it.
2. **The report window lost five merged PRs again — including the entire 09-12 wave — for the second
   consecutive day.** #859 (5 records), #862, #864, #865 and #866 all merged on 09-12 between 15:04Z
   and 22:51Z, after yesterday's report was written and before today's UTC window opened. **No report
   will ever carry the shipping row for the 09-12 wave**, and the corpus moving 2,210 → 2,215 is
   unexplained by anything in today's merged table. Item **4**, 37th day. § 5.3.
3. **Item 73 was not done, for the fifth day, and it is still the only item with a deadline.** The
   2026-09-07 `daily-wave` shard is still absent from `main` — nine 09-07 shards, no `daily-wave`.
   Read from `main`, that wave still did not run. `origin/wave-d20260907` is still intact; it stays
   recoverable only until that branch is pruned. § 5.4.
4. **The stale-`main` gap widened again — 49 commits, and it measurably grew during the night.** The
   08:10Z and 09:05Z passes each recorded **46**; my 11:35Z preflight fast-forwarded **49**. Against
   39 yesterday, 36 the day before and 28–31 when the exception was written. Item **12**, 30th day.
   § 5.5.

Traffic is unavailable for the seventeenth consecutive day, same one setting (§ 4).

---

## 2. What ran

| slot | routine | result |
|---|---|---|
| 03:00 | `daily-wave` | ✓ 1 run: `pr` — **5 records** → **#869** (shard 07:38:39Z) |
| 04:00 | `daily-reports` | ✓ 1 run: `no-op` — 0 processed (shard 08:09:39Z) |
| 05:00 | `daily-review` | ✓ 1 run: `no-op` — scanned 2,215, flagged 0 (shard 09:04:41Z) |
| 07:00 | `daily-merge` | ✓ 2 runs: `merged`, `merged` (03:26:13Z and 11:11:14Z) |
| 12:00 | `reports-close` | · not yet — due 17:00 UTC (yesterday's ran `no-op`) |
| Mon 02:00 | `weekly-discovery` | · not scheduled today (last ran Mon 09-07) |

**Every expected shard for today exists. Nothing due today failed to run.**

Two of those shards are **not on `main`**: today's `daily-wave` shard rides #869 and the 11:11Z
merge-pass shard rides #872. `daily-report.js` reads shards off open PR branches, which is the only
reason both read ✓.

`daily-report.js` reported **1 tool-call failure** — `traffic: fetch failed`. **§ 4 is the only
incomplete part of this report; §§ 2, 3, 5 are complete.**

Quality note, unchanged from yesterday: today's `daily-reports` and `daily-review` shards again carry
**no `note` field** (`{"outcome":"no-op","processed":0}` and `{"outcome":"no-op","scanned":2215,
"flagged":0}`) — even though both PR bodies describe the exception (b) recovery in detail. The
evidence exists; it just does not reach the shard, which is what the census reads.

---

## 3. What shipped

**Merged since the last report window opened (4):**

| PR | what |
|---|---|
| #871 | `daily-review` 2026-09-13 — no-op, 2,215 scanned, 0 flagged |
| #870 | `daily-reports` 2026-09-13 — no-op, no pending reader reports |
| #868 | merge-pass log shard, 2026-09-13 |
| #867 | merge-pass log shard, 2026-09-12 |

**No content PR merged inside today's window.** The 5 records that moved the corpus 2,210 → 2,215
came in on #859, which merged at 20:14Z yesterday — inside the gap (§ 5.3).

**Corpus:** 2,215 records · 0 flagged · report ladder `pr` · 0 deferred · working tree clean · CI on
`main` **success** at `69510fbe1`.

**Queue arithmetic reconciles.** `main` carries **257** queued candidates. The wave drew 11 (257 →
246 queued + 11 selected), built 5, and released the 6 unbuilt with the documented `sync` →
`unselect --wave` order, giving the **252 queued, 0 selected** it reports. Both of its claims check
out against `main`.

**Open PRs — none of them mine to merge:**

- **#869** (today's wave, 5 records, 47 files): `CLEAN`, `verify` **green** on the rebuilt head
  `fb75cbfe` at 11:11:51Z. The merge pass's own note says *"#869 rebuilt and pushed — merges next
  pass, once verify is green on the new head."* It is green. Needs nothing.
- **#872** (merge-pass shard): `CLEAN`, the merge pass's own bookkeeping, opened by `merge.yml`.
- **#670**: `DIRTY`, **18 days** open, untouched since 08-27 — item **42**, unchanged.

---

## 4. Traffic

**Traffic: unavailable — seventeenth consecutive day.** `traffic` is `null` and `tool_failures`
carries `traffic: fetch failed`. This is an **analytics outage, not a quiet day**, and it is not a
zero.

I ran `node tools/traffic.js` directly, which gives the error the JSON does not:

```
traffic.js: 403 from GoatCounter — the dashboard is no longer public.
Fix: GoatCounter -> Settings -> "Dashboard viewable by" -> Anyone.
```

So there is no figure to report as an upper bound, spam-adjusted or otherwise, and no referrer
breakdown. **Seventeen days of traffic data are not lost** — GoatCounter is still collecting; this
report simply cannot read it. Item **43**, one setting in the GoatCounter UI.

---

## 5. Findings

### 5.1 The wave's one FAIL — a false machine-readable statement about a named person, caught at audit

Audit result: **4 PASS / 1 FAIL, 6 issues (1 blocker · 2 high · 3 minor)**, with 2 of 8 candidate
findings refuted by the skeptic pass. All 23 source links across the five pages were re-fetched:
**23/23 live**.

The Truman page ("It's a recession when your neighbor loses his job…") hit the shape in the wave's
own words:

> With the hero author anonymous and the magnet real, `rightPersonWrongWords()` correctly saw no
> match — and three derived outputs then asserted Truman never said it:
>
> 1. **blocker** — ClaimReview rated `Harry S. Truman said: "…"` as **1 / False**. That proposition
>    is *true*, and the page's own prose says so. A false machine-readable statement about a named
>    person, in the half assistants read.
> 2. **high** — the FAQ answer, the one string read aloud, opened **"No.** Truman really did use this
>    line, but he did not invent it."
> 3. **high** — the presentation-kit banner warned *"The slide-ready mistake: crediting this to Harry
>    S. Truman"* directly beneath a credit block reading *"Harry Truman used this line, but he did not
>    coin it."* The two cancel and the reader cannot tell what to paste.

Truman **did** say it (Wikiquote → *The Observer*, 13 April 1958) but did not coin it: it is in print
on **9 February 1954** in the *Harlan News-Advertiser* as a remark "overheard on the street".

Fixed in-record via `schema.claimVerb: "coined"`, `schema.verdictLead: "Yes and no"`,
`schema.verdictNote` and `presentation.pkitWarn`. Scope gate verified — `git status --porcelain --
tools workflows` prints nothing.

**Why this is a new item and not item 76.** Item 76 is the `creditedTo`-guard failure. This is a
different route to the same wrong output: here `rightPersonWrongWords()` behaved *correctly* and the
defaults downstream of it were wrong anyway, because nothing detects "prose concedes use, denies
coinage". That shape has now reached the audit stage **twice** (r49 shipped it once; today it was
caught), and both times the remedy was four hand-set fields. → item **80**.

### 5.2 `barrypopik.com` is blocked, and the wave asks for a ruling it cannot make itself

> `barrypopik.com` is refused by this environment's egress proxy (`CONNECT tunnel failed, 403`) — a
> restriction on this session, not evidence about the site, **which the existing corpus links
> directly**. Popik's chronology was read in full via its **Internet Archive snapshot**, and the
> record cites that archived URL rather than the canonical one, because the archived URL is what was
> actually fetched and verified. **Worth an operator decision: other waves may want the canonical link
> restored.**

The wave is right that this is not its call. The corpus now contains both citation styles for the
same host, decided by which environment happened to run the wave. → item **81**.

Also blocked and worked around with no loss: `annefrank.org` / `annefrank.ch`, Google Books,
HathiTrust, and `web.archive.org`'s CDX endpoint (the `/web/` path works). Item **13**.

### 5.3 The report window, 37th day — and the second consecutive whole wave lost

Yesterday's report was written at 12:08Z. Between then and 00:00Z today, **five PRs merged**:

| PR | merged | what |
|---|---|---|
| #862 | 09-12 15:04:57Z | merge-pass shard |
| #859 | 09-12 20:14:06Z | **the 09-12 wave — 5 records** |
| #864 | 09-12 20:14:17Z | merge-pass shard |
| #865 | 09-12 20:14:20Z | `reports-close` no-op |
| #866 | 09-12 22:51:19Z | merge-pass shard |

None appears in yesterday's report (they merged after it) and none appears in today's (they fall
outside the UTC calendar day). **Two consecutive days have now each lost an entire 5-record wave's
shipping row.** The symptom is visible in § 3: today's merged table contains no content PR, yet the
corpus grew by exactly 5.

### 5.4 Item 73 — day 5, re-confirmed this morning

`data/routine-log/` on `main` carries nine shards dated 2026-09-07 — `daily-merge` ×4,
`weekly-discovery`, `daily-reports`, `daily-review`, `daily-report`, `reports-close` — and **no
`daily-wave`**. `origin/wave-d20260907` still resolves and the file is still one `git show` away.

### 5.5 Item 12 — the gap is now 49 commits, and it grew within the night

Three independent measurements today, all from routines running the documented exception (b):

| time | routine | local `main` behind |
|---|---|---|
| 08:09Z | `daily-reports` (#870) | 46 commits |
| 09:04Z | `daily-review` (#871) | 46 commits |
| ~11:35Z | this report | **49 commits** |

Both earlier passes reported the trap faithfully. #870's words: *"the checkout alone moved the tree
**46 commits backward** onto the stale local `main` ref, and the fast-forward recovered it."* Every
pass recovered green and nothing was improvised beyond what preflight printed. The doc's open
question — *something* leaves this container detached at the tip with a stale local `main` — is
still open and the number is still climbing: 28–31 (08-16) → 36 → 39 → **49**.

### 5.6 What the wave established, and what it honestly did not

Established, worth knowing:

- **Anne Frank — a line Wikiquote cannot date, dated.** Wikiquote carries it annotated only "Date?".
  The wave located it in the **1947 Contact first edition of *Het Achterhuis*** under `Dinsdag, 6 Juni
  1944` — the D-Day entry — with the Dutch original.
- **"Run mad as often as you chuse; but do not faint—"** read in two independent primary
  transcriptions (1922 Stokes first edition scan and Project Gutenberg). The popular version
  modernises `chuse` and softens the dash.

Could not be established, and **stated rather than guessed** — this is the part that matters:

- Which English translation produced Anne Frank's familiar wording (two exist; the page names both,
  and the hedge is now carried in `schema.verdictNote` so the machine layer hedges where the prose
  does).
- **US rights in *Catharine, or the Bower*** — Austen's words (c. 1792) but **first printed from
  manuscript in 1951**, so `rights: uncertain` and **no public-domain claim is made**. The right call:
  that is the costly error, on an author whose novels are otherwise the safest PD in the corpus.
- The originator of the recession/depression definition — earliest print is anonymous; the page
  asserts no creator.

One audit finding was **refused**, with reasons I agree with: the *Catharine* `Quotation.text`
capitalisation stays, because lower-casing it would make the most prominent machine-readable string a
mid-sentence fragment, and the page's `excerptNote` already explains the lift.

### 5.7 Carried items re-confirmed unfixed on `main` this morning

I read these rather than assuming them: **79** (`mis-kind.js` has no pattern for `Unconfirmed` or
`Earliest located`), **77** (`PLAIN_TEXT_FIELDS` is still twelve fields; neither `meta.description`
nor `meta.ogDescription` is among them), **72** (`review.js:267` still reads
`(rec.meta && rec.meta.rights) || rec.rights`, never `source.rights`), **73** (§ 5.4).

**Item 18 fired again:** `daily-report.js` credits `daily-merge` with "4 record(s)" from two shards
carrying `processed: 1` and `processed: 3` — those are merged PRs, not records.

**Item 65 recurred on both passes again today:** both merge notes lead with re-poll chatter before
the verdict.

**Item 63 (out-of-band merges) — clean day, fourth consecutive.** All four merges went through the
07:00 pass.

---

## 6. Needs a decision

**Two new items (80, 81). Nothing is struck today. 73 is still the one to do first: fifth day, still
the only item with a deadline, and the deadline is still whenever that branch gets pruned.**

73. **Write the 2026-09-07 `daily-wave` shard to `main`.** *(5th day — carried unchanged; `main` still
    says that wave did not run.)* Doing it is:
    `git show origin/wave-d20260907:data/routine-log/2026-09-07T07-44-35-244Z-daily-wave.jsonl`
    onto a fresh branch, commit, PR — I re-confirmed this morning the branch and file are both intact.

80. **Detect the used-but-didn't-coin shape in `prep-wave.js` and stamp the four levers, instead of
    relying on the audit to catch it.** *(New — second time this shape has reached audit; r49 shipped
    it once.)* Today it produced a blocker-grade **false** ClaimReview about a named living-memory
    figure, and the remedy was four hand-set record fields. Doing it is: one rule in `prep-wave.js`
    that fires when a record's prose concedes use while denying coinage — hero anonymous, magnet real,
    a dated pre-magnet print — and sets `claimVerb`/`verdictLead`/`pkitWarn` centrally. **Distinct
    from 76**, which is the `creditedTo` guard; here `rightPersonWrongWords()` was correct and the
    defaults downstream were wrong anyway.

81. **Rule on archived-vs-canonical citation when a host is blocked by egress.** *(New — the wave
    explicitly asked for this.)* It cited `barrypopik.com` via its Internet Archive snapshot because
    that is what it actually fetched, while the existing corpus links Popik directly — so the corpus
    now carries both styles for one host, decided by which environment ran the wave. Doing it is: one
    line in the wave doc — cite what was fetched (today's behaviour), or cite canonical and record the
    archived URL as the verification trail — plus a decision on whether to backfill.

4.  **Fix the report window.** *(37th day — second consecutive day it lost a whole 5-record wave;
    neither the 09-11 nor the 09-12 wave will ever appear in any report's shipping row.)* Doing it is:
    change `daily-report.js:30`/`:263` to window from the previous report's timestamp instead of the
    UTC calendar day, on its own branch.

43. **Flip GoatCounter → Settings → "Dashboard viewable by" → Anyone.** *(17th day.)* The anonymous
    endpoint returns 403, so this report has carried no traffic figure for two and a half weeks. One
    setting in the GoatCounter UI; no data is lost, only unreadable.

12. **Root-cause the stale local `main`, or ratify the exceptions in `preflight.js`.** *(30th day —
    gap now 49, up from 39 and 36, and measurably widening within a single night: 46 → 46 → 49.)*
    Doing it is: find what leaves the container detached with a stale `main` ref, or write both
    exceptions into `preflight.js`.

42. **Rebuild and merge #670, or close it.** *(18th day; `DIRTY`, 18d open.)* Closing it loses five
    records that exist nowhere else **and** orphans its 08-27 wave shard. Doing it is: merge `main`
    in, take `main`'s side on generated files, `node tools/build.js`, `node tools/scan.js`, push — or
    close it and accept both losses knowingly.

79. **Give `mis-kind.js` patterns for `Unconfirmed` and `Earliest located`.** *(2nd day —
    re-confirmed unfixed; neither string appears in the file.)* Both fall through `kindForTag` to
    `''`, which `template.js` renders as the burgundy ✕ meaning *refuted*. Doing it is: two narrow
    patterns on their own branch — noting "Unconfirmed" asserts *uncertainty*, not a non-authorship
    role, so it probably wants its own rule rather than widening `CONTEXT_TAG_REPORTED`.

77. **Add `meta.description` and `meta.ogDescription` to `PLAIN_TEXT_FIELDS`
    (`tools/html-safety.js:107`).** *(3rd day — re-confirmed unfixed.)* Doing it is: two strings in
    that array, which makes `prep-wave`'s scrub and `validate-records` cover the fields
    automatically — then a separate corpus-wide sweep for pages already shipping literal `&lt;em&gt;`
    in their search snippet.

76. **Widen `rightPersonWrongWords()` or split the magnet from the false-credit claim in
    `prep-wave.js`.** *(4th day — see 80 for today's adjacent-but-different instance.)*
    `template.js:2219` needs `creditedTo == hero` and `prep-wave.js:207` stamps `creditedTo` only when
    hero ≠ magnet, so no wave-built record can reach the 2/"Mostly False" rating. Doing it is: one
    central edit on its own branch.

17/58. **Fail the build when `Quotation.creator` is suppressed, and normalise `template.js`'s hero
    match.** *(24th day.)* Doing it is: make `creatorOk` compare on an accent-folded name, and assert
    that a record with an `answer.authorName` cannot emit a page without a `creator`.

72. **`tools/review.js:267` reads rights from `meta.rights`/`rights`, but records store it at
    `source.rights`, so every audit has seen `null`.** *(7th day — re-confirmed unfixed.)* Doing it
    is: change that one fallback chain, then decide separately whether to re-audit the rights claims
    never checked.

18. **Fix `daily-report.js:194`'s `work` figure.** *(21st day — today it again credited `daily-merge`
    with "4 record(s)" for two shards carrying `processed: 1` and `processed: 3`.)* One field rename
    or a per-routine label.

36. **Make the wave log a cost figure.** *(29th day — § 7.)* One field in the wave's routine-log
    shard.

5.  **Declare which carried items you are not going to do.** *(21st day.)* The table below is 41 rows.
    Reply with the numbers to strike and tomorrow's report carries only what survives.

### Resolved today

- **#869 needs nothing.** `verify` **green** on the rebuilt head `fb75cbfe` at 11:11:51Z; the merge
  pass takes it next pass, by its own note.
- **#872 needs nothing.** It is the merge pass's own shard, `CLEAN`, opened by `merge.yml`.
- **The wave's arithmetic reconciles against `main`** — 257 queued, 11 drawn, 5 built, 6 released →
  252; 2,215 + 5 = the 2,220 its `build.js` reports. Both claims check out.

### Carried, unchanged — one line each

| # | item | day |
|---|---|---|
| 2 | Stop the daily wave and the backlog waves colliding over the queue | 20th |
| 3 | Teach `harvest.js` to retire a candidate as "already covered by `<slug>`" | 20th |
| 6 | Make `prep-wave.js:206` stamp `creditedTo` when `confidence === 'disputed'` regardless of name equality — **75/76/80 are the same guard's other failures** | 27th |
| 7 | `tools/validate-records.js` should respect `answer.realAuthorName` | 20th |
| 8 | Do the deindexing watchlist diff — needs a human in Google Search Console | 26th, clock running |
| 9 | Fix the middle-initial hub fork at the generator | 19th |
| 10 | Default `kind: "context"` in `prep-wave.js` for `attributed` records with no denying fact-check row — **79 is the adjacent case** | 19th |
| 11 | Ask r44 to name its two unfixed generator-level issues; confirm the Porter page's two honesty failures | 19th |
| 13 | Extend the egress allowlist — **today's instance is `barrypopik.com`; 81 is the policy question it forces** | 33rd |
| 14 | Decide the `isBasedOn → isPartOf` bulk migration (1,046 of 1,752 measured 08-24) | 21st |
| 15 | Decide what to do about Google Books' quota — blocked again today | 22nd |
| 16 | Stop one source's outage hard-stopping the review pass | 25th |
| 19 | Decide whether `perf-check` belongs in the routine census | 23rd |
| 20 | Settle the branch-name / draft conflict between the harness and the routine docs — **#870 and #871 both had to override it again today** | recurring |
| 21 | Give `claimReviewed` a lever, or accept the coarse verdict — **80 is today's instance** | 22nd |
| 22 | Fix `template.js`'s canned no-rights copy to state the absence rather than assert a reason | 23rd |
| 23 | Add the mechanical near-duplicate check to the wave pipeline — cannot see unmerged PRs | 27th |
| 24 | Work the disputed records with no `creditedTo` | 33rd |
| 26 | Fix `plain()` to preserve unknown HTML entities (33 records) | 26th |
| 27 | Make `parse-audit.js` union findings across repeated audits of the same slug | 26th |
| 29 | Make the started-vs-result gate count coverage per slug, not journal lines | 26th |
| 30/62 | Rule on "Verbatim:" and "rights held by X" corpus-wide | 26th / 12th |
| 31 | Accept or decline the detector `magnet-quoted-it-rated-as-never-said` — **80 is its third instance; 58 is queued behind it** | 28th |
| 32 | Repair or retire the corrupt C. S. Lewis record and the Ali duplicate | 29th |
| 33 | Decide the house convention for characters with no HTML entity | 25th |
| 34 | Decide the jurisdiction wording for `USE['public-domain']` — 54 records | 27th |
| 35 | Decide the `creatorOk` right-person-wrong-words creator emission — folded into 17/58 | 27th |
| 37 | Decide the `Unknown`-creator `sameAs` convention | 30th |
| 38 | Fix the `themes` comment at `workflows/generate.js:168` — one comment edit | 30th |
| 39 | Correct the "~134 magnet authors harvested" figure — 314 distinct authors touched | 26th |
| 40 | Move `merge.yml` off minute 0 (`0 */3 * * *` → `17 */3 * * *`) | 18th |
| 41 | Decide whether the routine docs should stop saying `gh pr create`, or whether `tools/pr-open.js` should exist | 18th |
| 44 | Teach `merge-gate.js` to look at what actually changed — #670 is its case | 17th |
| 45 | Replace the wave-number command in `workflows/README.md:14` and `ingestion-wave/SKILL.md:44` | 17th |
| 46 | Read the 15 queue candidates whose text opens `Topic:` — 54 is the ruling this needs | 17th |
| 47 | Decide whether QI's Title Case should be normalised centrally | 17th |
| 48 | Decide what happens to the 96 queued candidates carrying `AMBIGUOUS MAGNET` | 17th |
| 49 | Decide whether to fund the song cover-link backfill (74 songs, ~2.5M tokens) | 17th |
| 51 | Make the merge pass's rebuild step fetch before it merges | 16th |
| 54 | Rule on Quote Investigator *headlines* reaching `displayQuote` — the true count is UNKNOWN; do not re-derive | 15th |
| 56 | Rule on `the-telegraph-is-like-a-very-very-long-cat-or-dog`, which is live | 14th |
| 57 | Give the orphaned `taxes-…` candidate a way back into the queue | 14th |
| 59 | Decide what the 4,072-byte `SCHEMA_BUDGET` ceiling gets spent on, or raise it — **74 is waiting on this** | 14th |
| 61 | Fix the `mis-kind.js` comma gap; also add a `/fake/` rule to `REFUTATION_TAG` | 14th |
| 63 | Out-of-band merges — **clean day, fourth consecutive**; stays listed, four clean days do not settle it | not live today |
| 64 | Make the merge pass merge content PRs before routine-log PRs | not live today |
| 65 | Put the verdict before the re-poll chatter in the merge note — **recurred on both passes again today** | 11th |
| 66 | Fix `template.js:848`'s unconditional period on `disambiguatingDescription` | 11th |
| 67 | Rule on the Parker `displayQuote` repair, which shipped 09-05 | 9th |
| 68 | Fix nested quotes in `claimReviewed` | 9th |
| 69 | Rule on whether a Wikiquote *sourced-section* anchor is enough for `verified` on an in-copyright work | 9th |
| 70/71 | The 09-06 quota collision; `daily-wave` / `daily-review` still fail silently | carried |
| 74 | Decide whether `DOSSIER_SCHEMA` gets the hand-set override fields — waiting on 59 | 6th |
| 75 | Rule on the harvester's magnet-as-claimant defect | 5th |
| — | `loc.gov` human-facing page URLs 403 to non-browser clients — **81 is the general version of this** | 2nd |
| — | `looksLikePerson` still accepts an anonymity note ("Nobody — it has no traceable author") | 4th |
| — | Add the token-subset test's false-alarm mode (`Edmund Kean → Edmund Gwenn`) to the #801 note | 6th |
| — | The r45 Nietzsche near-duplicate pair — two canonical pages, opposite confidence values | carried since r45 |
| — | Make the no-op shards carry a `note` — the evidence is in the PR bodies but not in what the census reads | 2nd |

**Struck previously:** 25, 28 (superseded by 59), 50, 52, 53, 55, 60, 78. **Nothing is struck today.**

---

## 7. Cost

**No run reported a cost figure.** Today's four shards carry `built`, `processed`, `scanned` and
`flagged` — no cost field anywhere, and no PR body states one. Item **36**, 29th day: one field in
the wave's routine-log shard would close it.

The only spend signal available is shape, not money: the wave ran **serially through every pipeline
stage** (a cloud session has no `Workflow` tool), re-fetched all 23 source links, and read two
primary printings plus a 1947 Dutch first edition in full.
