{"date":"2026-07-30","routine":"daily-merge","outcome":"pr","processed":4,"note":"Ran LATE and by hand — the scheduled 07:00 local pass did not fire (diagnosed in #270). Cleared the whole stranded backlog: #266 wave-d20260730 (5 records), #267 reports/2026-07-30 (egress-failure log), #268 review/2026-07-30 (no-op log), plus #271 (the 240-page reuse-line fix the r28 wave reported). Left alone correctly: #272 ops/merge-authority-to-actions = HUMAN prefix, and it is the operator's in-progress replacement for this very pass. FINDING — merge-gate.js and GitHub disagree about staleness: the gate returned MERGE 'green, in scope, up to date' for #266 while GitHub refused it as 'not up to date with the base branch'. The gate had computed its verdict against a base that main had already moved past (#270 landed between). A gate that says MERGE on a branch GitHub will refuse is the one failure mode this pass cannot absorb, because it reads as 'nothing to do'. SECOND — GitHub left mergeable=UNKNOWN on #267/#268 indefinitely (6 polls over 2.5min); the gate reports that as WAIT, so a pass that trusts only the gate stalls forever on it. I proceeded on the observable fact that both were behind. THIRD — merge-gate.js hit a 403 rate limit mid-pass; it reports that as 'cannot list PRs' and returns no verdicts, which is indistinguishable from a quiet board."}
